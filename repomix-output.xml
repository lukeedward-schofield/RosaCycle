This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
backend/
  app/
    auth/
      __init__.py
      controller.py
      repository.py
      routes.py
      schema.py
      service.py
      user_model.py
      utils.py
    map/
      __init__.py
      controller.py
      model.py
      repository.py
      routes.py
      schema.py
      service.py
    profile/
      __init__.py
      controller.py
      repository.py
      routes.py
      schema.py
      service.py
    scan/
      __init__.py
      controller.py
      routes.py
      service.py
    services/
      auth_service.py
    shared/
      database/
        __init__.py
      models/
        __init__.py
        enums.py
      notification/
        __init__.py
        controller.py
        model.py
        repository.py
        routes.py
        schema.py
        service.py
      rating/
        __init__.py
        controller.py
        model.py
        repository.py
        routes.py
        schema.py
        service.py
      utils/
        __init__.py
        errors.py
        file_storage.py
        mixins.py
        pagination.py
        rate_limit.py
    trades/
      offer/
        __init__.py
        controller.py
        model.py
        repository.py
        routes.py
        schema.py
        service.py
      __init__.py
      controller.py
      model.py
      repository.py
      routes.py
      schema.py
      service.py
    __init__.py
    app.py
    config.py
  docs/
    API.md
  migrations/
    versions/
      41b0a18a356f_initial_schema.py
      66982ff562fa_check_user_schema.py
    alembic.ini
    env.py
    README
    script.py.mako
  .env.example
  .flaskenv
  .gitignore
  .gitkeep
  .python-version
  Procfile
  repomix-output.xml
  requirements.txt
  run.py
  seed.py
frontend/
  public/
    favicon.svg
  src/
    assets/
      logo.svg
    features/
      auth/
        screens/
          SignInScreen.jsx
          SignUpScreen.jsx
        AuthContext.jsx
      map/
        screens/
          MapScreen.jsx
          ResourceSpotConfirmScreen.jsx
          ResourceSpotScreen.jsx
          SpotReportedScreen.jsx
        geo.js
      profile/
        screens/
          EditProfileScreen.jsx
      scan/
        components/
          AIDetectedForm.jsx
          CameraViewfinder.jsx
        screens/
          ScanConfirmScreen.jsx
          ScanScreen.jsx
      trades/
        components/
          cards/
            TradeCard.jsx
          trade/
            EcoImpactBox.jsx
            FilterChipRow.jsx
            MessageThread.jsx
        screens/
          EditListingScreen.jsx
          MessageThreadScreen.jsx
          OfferSentScreen.jsx
          RateTraderScreen.jsx
          TradeCreatedScreen.jsx
          TradeDetailScreen.jsx
          TradesScreen.jsx
          ViewOfferScreen.jsx
    shared/
      components/
        common/
          ConfirmationScreen.jsx
          FloatingActionButton.jsx
          Logo.jsx
          MaterialTag.jsx
          PasswordInput.jsx
          PrimaryButton.jsx
          SearchBar.jsx
          SecondaryButton.jsx
          StarRating.jsx
          StatusPill.jsx
        layout/
          BottomNav.jsx
          Header.jsx
          UserMenu.jsx
      lib/
        pendingCapture.js
        storageKeys.js
      services/
        api.js
      utils/
        constants.js
        tradeFormat.js
    App.jsx
    index.css
    main.jsx
  .env.example
  .gitignore
  .gitkeep
  fix-imports.cjs
  index.html
  package.json
  postcss.config.js
  repomix-output.xml
  tailwind.config.js
  vite.config.js
backend.rar
README.md
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="backend/migrations/versions/66982ff562fa_check_user_schema.py">
"""
check user schema

Revision ID: 66982ff562fa
Revises: 41b0a18a356f
Create Date: 2026-08-05 13:29:50.301447

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

# revision identifiers, used by Alembic.
revision = '66982ff562fa'
down_revision = '41b0a18a356f'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('users', schema=None) as batch_op:
        batch_op.add_column(sa.Column('info_updated_at', sa.DateTime(timezone=True), nullable=True))
        batch_op.add_column(sa.Column('password_updated_at', sa.DateTime(timezone=True), nullable=True))
        batch_op.drop_column('profile_updated_at')

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('users', schema=None) as batch_op:
        batch_op.add_column(sa.Column('profile_updated_at', postgresql.TIMESTAMP(timezone=True), autoincrement=False, nullable=True))
        batch_op.drop_column('password_updated_at')
        batch_op.drop_column('info_updated_at')

    # ### end Alembic commands ###
</file>

<file path="frontend/repomix-output.xml">
This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
public/
  favicon.svg
src/
  assets/
    logo.svg
  features/
    auth/
      screens/
        SignInScreen.jsx
        SignUpScreen.jsx
      AuthContext.jsx
    map/
      screens/
        MapScreen.jsx
        ResourceSpotConfirmScreen.jsx
        ResourceSpotScreen.jsx
        SpotReportedScreen.jsx
      geo.js
    profile/
      screens/
        EditProfileScreen.jsx
    scan/
      components/
        AIDetectedForm.jsx
        CameraViewfinder.jsx
      screens/
        ScanConfirmScreen.jsx
        ScanScreen.jsx
    trades/
      components/
        cards/
          TradeCard.jsx
        trade/
          EcoImpactBox.jsx
          FilterChipRow.jsx
          MessageThread.jsx
      screens/
        EditListingScreen.jsx
        MessageThreadScreen.jsx
        OfferSentScreen.jsx
        RateTraderScreen.jsx
        TradeCreatedScreen.jsx
        TradeDetailScreen.jsx
        TradesScreen.jsx
        ViewOfferScreen.jsx
  shared/
    components/
      common/
        ConfirmationScreen.jsx
        FloatingActionButton.jsx
        Logo.jsx
        MaterialTag.jsx
        PasswordInput.jsx
        PrimaryButton.jsx
        SearchBar.jsx
        SecondaryButton.jsx
        StarRating.jsx
        StatusPill.jsx
      layout/
        BottomNav.jsx
        Header.jsx
        UserMenu.jsx
    lib/
      pendingCapture.js
      storageKeys.js
    services/
      api.js
    utils/
      constants.js
      tradeFormat.js
  App.jsx
  index.css
  main.jsx
.env.example
.gitignore
.gitkeep
fix-imports.cjs
index.html
package.json
postcss.config.js
tailwind.config.js
vite.config.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="public/favicon.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="2000" zoomAndPan="magnify" viewBox="0 0 1500 1499.999933" height="2000" preserveAspectRatio="xMidYMid meet" version="1.0"><defs><clipPath id="fb435d5862"><path d="M 977 371 L 1298 371 L 1298 628 L 977 628 Z M 977 371 " clip-rule="nonzero"/></clipPath><clipPath id="bd2365c669"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath><clipPath id="8a36694c69"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath></defs><path stroke-linecap="butt" transform="matrix(0.627627, -0.41059, 0.41059, 0.627627, 850.581296, 942.983635)" fill="none" stroke-linejoin="miter" d="M 27.738967 153.335882 C 120.789709 -8.442483 298.543162 -8.443382 560.987755 153.33184 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.324044, 0.676384, -0.676384, -0.324044, 1395.097425, 847.756731)" fill="none" stroke-linejoin="miter" d="M 25.672548 211.246672 C 203.554284 -27.749642 381.442508 -27.748337 559.323325 211.245693 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.742472, 0.105999, -0.105999, 0.742472, 345.349897, 817.95039)" fill="none" stroke-linejoin="miter" d="M 43.567852 24.529848 C 400.107518 657.734999 738.125941 657.738308 1057.622384 24.534619 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.406135, -0.630519, 0.630519, 0.406135, 183.309934, 719.938622)" fill="none" stroke-linejoin="miter" d="M 39.353683 265.381789 C 264.409727 -21.796159 535.300314 -21.794026 852.019803 265.379429 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.72096, 0.20668, -0.20668, 0.72096, 747.727769, 161.278749)" fill="none" stroke-linejoin="miter" d="M 25.450087 157.050118 C 266.832937 14.317423 428.639468 14.316418 510.866109 157.053544 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><g clip-path="url(#fb435d5862)"><g clip-path="url(#bd2365c669)"><g clip-path="url(#8a36694c69)"><path fill="#245f28" d="M 1262 379.851562 L 1262.277344 380 C 1263.257812 380.527344 1264.21875 381.082031 1265.171875 381.65625 C 1266.121094 382.230469 1267.058594 382.832031 1267.980469 383.453125 C 1268.902344 384.074219 1269.808594 384.71875 1270.699219 385.382812 C 1271.589844 386.050781 1272.460938 386.738281 1273.316406 387.445312 C 1274.175781 388.15625 1275.011719 388.886719 1275.832031 389.636719 C 1276.652344 390.386719 1277.453125 391.15625 1278.238281 391.945312 C 1279.019531 392.734375 1279.78125 393.542969 1280.527344 394.367188 C 1281.269531 395.195312 1281.992188 396.039062 1282.691406 396.902344 C 1283.394531 397.765625 1284.074219 398.644531 1284.734375 399.539062 C 1285.390625 400.4375 1286.027344 401.347656 1286.640625 402.273438 C 1287.253906 403.199219 1287.84375 404.140625 1288.414062 405.097656 C 1288.980469 406.054688 1289.523438 407.023438 1290.042969 408.007812 C 1290.5625 408.988281 1291.058594 409.984375 1291.53125 410.992188 C 1292 411.996094 1292.445312 413.015625 1292.867188 414.042969 C 1293.289062 415.074219 1293.683594 416.113281 1294.054688 417.160156 C 1294.425781 418.207031 1294.769531 419.265625 1295.085938 420.332031 C 1295.40625 421.394531 1295.695312 422.46875 1295.960938 423.546875 C 1296.226562 424.625 1296.464844 425.710938 1296.679688 426.804688 C 1296.890625 427.894531 1297.078125 428.992188 1297.234375 430.089844 C 1297.394531 431.191406 1297.523438 432.292969 1297.628906 433.402344 C 1297.734375 434.507812 1297.808594 435.617188 1297.859375 436.726562 C 1297.910156 437.839844 1297.933594 438.949219 1297.929688 440.0625 C 1297.921875 441.171875 1297.890625 442.285156 1297.832031 443.394531 C 1297.773438 444.503906 1297.6875 445.613281 1297.574219 446.71875 C 1297.460938 447.824219 1297.320312 448.925781 1297.152344 450.023438 C 1296.984375 451.125 1296.789062 452.21875 1296.566406 453.308594 C 1296.34375 454.394531 1296.097656 455.480469 1295.824219 456.558594 C 1295.546875 457.632812 1295.246094 458.703125 1294.921875 459.765625 C 1294.59375 460.828125 1294.238281 461.882812 1293.859375 462.929688 C 1293.480469 463.972656 1293.078125 465.007812 1292.648438 466.035156 C 1292.21875 467.058594 1291.765625 468.074219 1291.285156 469.074219 C 1290.804688 470.078125 1290.300781 471.070312 1289.773438 472.046875 L 1224.914062 592.132812 C 1224.386719 593.109375 1223.835938 594.074219 1223.261719 595.027344 C 1222.683594 595.976562 1222.085938 596.914062 1221.464844 597.835938 C 1220.84375 598.757812 1220.199219 599.664062 1219.53125 600.550781 C 1218.867188 601.441406 1218.179688 602.316406 1217.46875 603.171875 C 1216.761719 604.027344 1216.03125 604.867188 1215.28125 605.6875 C 1214.53125 606.507812 1213.761719 607.308594 1212.972656 608.089844 C 1212.183594 608.875 1211.375 609.636719 1210.546875 610.378906 C 1209.722656 611.125 1208.875 611.84375 1208.015625 612.546875 C 1207.152344 613.25 1206.273438 613.929688 1205.375 614.585938 C 1204.480469 615.246094 1203.570312 615.882812 1202.644531 616.496094 C 1201.714844 617.109375 1200.773438 617.699219 1199.816406 618.265625 C 1198.863281 618.832031 1197.894531 619.378906 1196.910156 619.898438 C 1195.929688 620.417969 1194.933594 620.914062 1193.925781 621.382812 C 1192.917969 621.855469 1191.902344 622.300781 1190.871094 622.722656 C 1189.84375 623.144531 1188.804688 623.539062 1187.757812 623.910156 C 1186.707031 624.277344 1185.652344 624.621094 1184.585938 624.941406 C 1183.519531 625.257812 1182.449219 625.550781 1181.371094 625.816406 C 1180.289062 626.082031 1179.203125 626.320312 1178.113281 626.53125 C 1177.023438 626.746094 1175.925781 626.929688 1174.824219 627.089844 C 1173.726562 627.246094 1172.621094 627.378906 1171.515625 627.484375 C 1170.410156 627.585938 1169.300781 627.664062 1168.1875 627.714844 C 1167.078125 627.765625 1165.96875 627.785156 1164.855469 627.78125 C 1163.746094 627.777344 1162.632812 627.746094 1161.523438 627.6875 C 1160.414062 627.628906 1159.304688 627.539062 1158.199219 627.425781 C 1157.09375 627.3125 1155.992188 627.171875 1154.890625 627.003906 C 1153.792969 626.835938 1152.699219 626.644531 1151.609375 626.421875 C 1150.519531 626.199219 1149.4375 625.953125 1148.359375 625.675781 C 1147.28125 625.402344 1146.210938 625.101562 1145.148438 624.773438 C 1144.085938 624.449219 1143.035156 624.09375 1141.988281 623.714844 C 1140.945312 623.335938 1139.910156 622.933594 1138.882812 622.503906 C 1137.859375 622.074219 1136.84375 621.617188 1135.839844 621.140625 C 1134.839844 620.660156 1133.847656 620.15625 1132.871094 619.628906 L 1012.738281 554.785156 C 1011.761719 554.257812 1010.796875 553.703125 1009.84375 553.128906 C 1008.890625 552.554688 1007.957031 551.957031 1007.035156 551.335938 C 1006.113281 550.714844 1005.207031 550.070312 1004.316406 549.402344 C 1003.425781 548.738281 1002.550781 548.050781 1001.695312 547.339844 C 1000.835938 546.632812 1000 545.902344 999.179688 545.152344 C 998.359375 544.402344 997.554688 543.632812 996.773438 542.84375 C 995.988281 542.054688 995.226562 541.246094 994.484375 540.421875 C 993.738281 539.59375 993.015625 538.75 992.316406 537.886719 C 991.613281 537.023438 990.933594 536.144531 990.273438 535.25 C 989.617188 534.355469 988.980469 533.441406 988.367188 532.515625 C 987.75 531.589844 987.160156 530.648438 986.59375 529.691406 C 986.027344 528.734375 985.480469 527.765625 984.960938 526.785156 C 984.441406 525.800781 983.945312 524.804688 983.476562 523.796875 C 983.003906 522.792969 982.558594 521.773438 982.136719 520.746094 C 981.714844 519.714844 981.320312 518.675781 980.949219 517.628906 C 980.578125 516.582031 980.234375 515.523438 979.917969 514.457031 C 979.597656 513.394531 979.308594 512.320312 979.042969 511.242188 C 978.777344 510.160156 978.539062 509.074219 978.324219 507.984375 C 978.113281 506.894531 977.925781 505.796875 977.769531 504.695312 C 977.609375 503.597656 977.480469 502.492188 977.375 501.386719 C 977.269531 500.277344 977.195312 499.167969 977.144531 498.058594 C 977.09375 496.949219 977.074219 495.835938 977.078125 494.726562 C 977.082031 493.613281 977.113281 492.503906 977.171875 491.390625 C 977.234375 490.28125 977.320312 489.171875 977.433594 488.066406 C 977.546875 486.960938 977.6875 485.859375 977.855469 484.761719 C 978.023438 483.660156 978.21875 482.566406 978.441406 481.476562 C 978.664062 480.386719 978.910156 479.304688 979.1875 478.226562 C 979.460938 477.148438 979.761719 476.082031 980.089844 475.019531 C 980.417969 473.957031 980.773438 472.902344 981.152344 471.855469 C 981.53125 470.8125 981.933594 469.777344 982.363281 468.75 C 982.796875 467.726562 983.25 466.710938 983.730469 465.707031 C 984.210938 464.707031 984.714844 463.714844 985.242188 462.738281 L 985.394531 462.457031 C 985.921875 461.480469 986.472656 460.515625 987.050781 459.5625 C 987.625 458.613281 988.222656 457.675781 988.84375 456.753906 C 989.464844 455.832031 990.109375 454.925781 990.773438 454.035156 C 991.441406 453.144531 992.128906 452.269531 992.835938 451.414062 C 993.546875 450.558594 994.273438 449.71875 995.023438 448.898438 C 995.773438 448.078125 996.542969 447.273438 997.332031 446.492188 C 998.121094 445.710938 998.929688 444.945312 999.757812 444.203125 C 1000.585938 443.460938 1001.429688 442.738281 1002.289062 442.035156 C 1003.152344 441.332031 1004.03125 440.652344 1004.929688 439.992188 C 1005.824219 439.335938 1006.734375 438.699219 1007.660156 438.085938 C 1008.589844 437.472656 1009.53125 436.878906 1010.488281 436.3125 C 1011.441406 435.746094 1012.410156 435.203125 1013.394531 434.679688 C 1014.378906 434.160156 1015.371094 433.664062 1016.378906 433.195312 C 1017.386719 432.722656 1018.402344 432.277344 1019.433594 431.855469 C 1020.460938 431.433594 1021.5 431.039062 1022.550781 430.667969 C 1023.597656 430.300781 1024.65625 429.953125 1025.71875 429.636719 C 1026.785156 429.320312 1027.859375 429.027344 1028.9375 428.761719 C 1030.015625 428.496094 1031.101562 428.257812 1032.195312 428.042969 C 1033.285156 427.832031 1034.382812 427.648438 1035.480469 427.488281 C 1036.582031 427.332031 1037.6875 427.199219 1038.792969 427.09375 C 1039.898438 426.992188 1041.007812 426.914062 1042.117188 426.863281 C 1043.230469 426.8125 1044.339844 426.792969 1045.453125 426.796875 C 1046.566406 426.800781 1047.675781 426.832031 1048.785156 426.894531 C 1049.894531 426.953125 1051.003906 427.039062 1052.109375 427.152344 C 1053.214844 427.265625 1054.320312 427.40625 1055.417969 427.574219 C 1056.515625 427.746094 1057.609375 427.9375 1058.699219 428.160156 C 1059.789062 428.382812 1060.875 428.632812 1061.949219 428.90625 C 1063.027344 429.179688 1064.097656 429.484375 1065.160156 429.808594 C 1066.222656 430.136719 1067.277344 430.492188 1068.320312 430.871094 C 1069.367188 431.25 1070.402344 431.65625 1071.425781 432.085938 C 1072.453125 432.515625 1073.464844 432.96875 1074.46875 433.449219 C 1075.472656 433.929688 1076.460938 434.433594 1077.441406 434.964844 L 1137.492188 467.390625 L 1169.953125 407.34375 C 1170.480469 406.367188 1171.03125 405.402344 1171.609375 404.453125 C 1172.183594 403.5 1172.78125 402.5625 1173.402344 401.640625 C 1174.023438 400.71875 1174.667969 399.8125 1175.335938 398.921875 C 1176 398.035156 1176.6875 397.160156 1177.394531 396.304688 C 1178.105469 395.445312 1178.835938 394.609375 1179.585938 393.789062 C 1180.335938 392.96875 1181.105469 392.164062 1181.894531 391.382812 C 1182.683594 390.601562 1183.492188 389.835938 1184.316406 389.09375 C 1185.144531 388.351562 1185.988281 387.628906 1186.851562 386.925781 C 1187.714844 386.222656 1188.59375 385.542969 1189.488281 384.886719 C 1190.382812 384.226562 1191.296875 383.589844 1192.222656 382.976562 C 1193.148438 382.363281 1194.089844 381.773438 1195.046875 381.207031 C 1196.003906 380.636719 1196.972656 380.09375 1197.957031 379.574219 C 1198.9375 379.054688 1199.933594 378.558594 1200.941406 378.085938 C 1201.945312 377.617188 1202.964844 377.171875 1203.992188 376.75 C 1205.023438 376.328125 1206.0625 375.933594 1207.109375 375.5625 C 1208.15625 375.191406 1209.214844 374.847656 1210.28125 374.53125 C 1211.34375 374.210938 1212.417969 373.921875 1213.496094 373.65625 C 1214.578125 373.390625 1215.660156 373.148438 1216.753906 372.9375 C 1217.84375 372.726562 1218.941406 372.539062 1220.039062 372.382812 C 1221.140625 372.222656 1222.246094 372.09375 1223.351562 371.988281 C 1224.457031 371.882812 1225.566406 371.808594 1226.679688 371.757812 C 1227.789062 371.707031 1228.898438 371.683594 1230.011719 371.691406 C 1231.125 371.695312 1232.234375 371.726562 1233.34375 371.785156 C 1234.453125 371.84375 1235.5625 371.933594 1236.667969 372.046875 C 1237.773438 372.160156 1238.875 372.300781 1239.976562 372.46875 C 1241.074219 372.636719 1242.167969 372.832031 1243.257812 373.050781 C 1244.347656 373.273438 1245.429688 373.523438 1246.507812 373.796875 C 1247.585938 374.070312 1248.65625 374.371094 1249.71875 374.699219 C 1250.78125 375.027344 1251.835938 375.378906 1252.878906 375.761719 C 1253.925781 376.140625 1254.960938 376.542969 1255.984375 376.972656 C 1257.011719 377.402344 1258.023438 377.859375 1259.027344 378.339844 C 1260.03125 378.816406 1261.019531 379.320312 1262 379.851562 Z M 1262 379.851562 " fill-opacity="1" fill-rule="nonzero"/></g></g></g><path stroke-linecap="butt" transform="matrix(0.403919, -0.631941, 0.631941, 0.403919, 104.893904, 729.104149)" fill="none" stroke-linejoin="miter" d="M 40.50488 300.474597 C 282.249287 -33.492864 589.421014 -33.490352 962.021645 300.47494 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.734847, 0.149999, -0.149999, 0.734847, 304.884588, 831.580108)" fill="none" stroke-linejoin="miter" d="M 41.766746 27.491179 C 472.054803 681.225638 813.229103 681.225807 1065.289646 27.491684 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.713054, 0.232494, -0.232494, 0.713054, 743.751953, 84.389709)" fill="none" stroke-linejoin="miter" d="M 27.552117 192.796126 C 315.880744 2.40229 514.553475 2.404029 623.562127 192.793052 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.479248, -0.576907, 0.576907, 0.479248, 975.623445, 1084.867279)" fill="none" stroke-linejoin="miter" d="M 10.923997 43.992439 C 74.518874 60.091943 159.21204 60.093361 265.000168 43.992687 " stroke="#245f28" stroke-width="89" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 329.956147, 817.015974)" fill="none" stroke-linejoin="miter" d="M 0.000357227 50.000231 C 37.480224 49.999953 88.464376 49.999514 152.947912 49.997153 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 331.706899, 815.218854)" fill="none" stroke-linejoin="miter" d="M 0.000389071 49.999177 C 37.480256 49.998899 88.464408 49.998461 152.946183 50.001001 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.747763, -0.0578889, 0.0578889, 0.747763, 722.333707, 226.603261)" fill="none" stroke-linejoin="miter" d="M 8.805162 73.553045 C 184.314962 42.151956 282.885164 42.150737 304.510575 73.548986 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/></svg>
</file>

<file path="src/assets/logo.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="2000" zoomAndPan="magnify" viewBox="0 0 1500 1499.999933" height="2000" preserveAspectRatio="xMidYMid meet" version="1.0"><defs><clipPath id="fb435d5862"><path d="M 977 371 L 1298 371 L 1298 628 L 977 628 Z M 977 371 " clip-rule="nonzero"/></clipPath><clipPath id="bd2365c669"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath><clipPath id="8a36694c69"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath></defs><path stroke-linecap="butt" transform="matrix(0.627627, -0.41059, 0.41059, 0.627627, 850.581296, 942.983635)" fill="none" stroke-linejoin="miter" d="M 27.738967 153.335882 C 120.789709 -8.442483 298.543162 -8.443382 560.987755 153.33184 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.324044, 0.676384, -0.676384, -0.324044, 1395.097425, 847.756731)" fill="none" stroke-linejoin="miter" d="M 25.672548 211.246672 C 203.554284 -27.749642 381.442508 -27.748337 559.323325 211.245693 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.742472, 0.105999, -0.105999, 0.742472, 345.349897, 817.95039)" fill="none" stroke-linejoin="miter" d="M 43.567852 24.529848 C 400.107518 657.734999 738.125941 657.738308 1057.622384 24.534619 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.406135, -0.630519, 0.630519, 0.406135, 183.309934, 719.938622)" fill="none" stroke-linejoin="miter" d="M 39.353683 265.381789 C 264.409727 -21.796159 535.300314 -21.794026 852.019803 265.379429 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.72096, 0.20668, -0.20668, 0.72096, 747.727769, 161.278749)" fill="none" stroke-linejoin="miter" d="M 25.450087 157.050118 C 266.832937 14.317423 428.639468 14.316418 510.866109 157.053544 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><g clip-path="url(#fb435d5862)"><g clip-path="url(#bd2365c669)"><g clip-path="url(#8a36694c69)"><path fill="#245f28" d="M 1262 379.851562 L 1262.277344 380 C 1263.257812 380.527344 1264.21875 381.082031 1265.171875 381.65625 C 1266.121094 382.230469 1267.058594 382.832031 1267.980469 383.453125 C 1268.902344 384.074219 1269.808594 384.71875 1270.699219 385.382812 C 1271.589844 386.050781 1272.460938 386.738281 1273.316406 387.445312 C 1274.175781 388.15625 1275.011719 388.886719 1275.832031 389.636719 C 1276.652344 390.386719 1277.453125 391.15625 1278.238281 391.945312 C 1279.019531 392.734375 1279.78125 393.542969 1280.527344 394.367188 C 1281.269531 395.195312 1281.992188 396.039062 1282.691406 396.902344 C 1283.394531 397.765625 1284.074219 398.644531 1284.734375 399.539062 C 1285.390625 400.4375 1286.027344 401.347656 1286.640625 402.273438 C 1287.253906 403.199219 1287.84375 404.140625 1288.414062 405.097656 C 1288.980469 406.054688 1289.523438 407.023438 1290.042969 408.007812 C 1290.5625 408.988281 1291.058594 409.984375 1291.53125 410.992188 C 1292 411.996094 1292.445312 413.015625 1292.867188 414.042969 C 1293.289062 415.074219 1293.683594 416.113281 1294.054688 417.160156 C 1294.425781 418.207031 1294.769531 419.265625 1295.085938 420.332031 C 1295.40625 421.394531 1295.695312 422.46875 1295.960938 423.546875 C 1296.226562 424.625 1296.464844 425.710938 1296.679688 426.804688 C 1296.890625 427.894531 1297.078125 428.992188 1297.234375 430.089844 C 1297.394531 431.191406 1297.523438 432.292969 1297.628906 433.402344 C 1297.734375 434.507812 1297.808594 435.617188 1297.859375 436.726562 C 1297.910156 437.839844 1297.933594 438.949219 1297.929688 440.0625 C 1297.921875 441.171875 1297.890625 442.285156 1297.832031 443.394531 C 1297.773438 444.503906 1297.6875 445.613281 1297.574219 446.71875 C 1297.460938 447.824219 1297.320312 448.925781 1297.152344 450.023438 C 1296.984375 451.125 1296.789062 452.21875 1296.566406 453.308594 C 1296.34375 454.394531 1296.097656 455.480469 1295.824219 456.558594 C 1295.546875 457.632812 1295.246094 458.703125 1294.921875 459.765625 C 1294.59375 460.828125 1294.238281 461.882812 1293.859375 462.929688 C 1293.480469 463.972656 1293.078125 465.007812 1292.648438 466.035156 C 1292.21875 467.058594 1291.765625 468.074219 1291.285156 469.074219 C 1290.804688 470.078125 1290.300781 471.070312 1289.773438 472.046875 L 1224.914062 592.132812 C 1224.386719 593.109375 1223.835938 594.074219 1223.261719 595.027344 C 1222.683594 595.976562 1222.085938 596.914062 1221.464844 597.835938 C 1220.84375 598.757812 1220.199219 599.664062 1219.53125 600.550781 C 1218.867188 601.441406 1218.179688 602.316406 1217.46875 603.171875 C 1216.761719 604.027344 1216.03125 604.867188 1215.28125 605.6875 C 1214.53125 606.507812 1213.761719 607.308594 1212.972656 608.089844 C 1212.183594 608.875 1211.375 609.636719 1210.546875 610.378906 C 1209.722656 611.125 1208.875 611.84375 1208.015625 612.546875 C 1207.152344 613.25 1206.273438 613.929688 1205.375 614.585938 C 1204.480469 615.246094 1203.570312 615.882812 1202.644531 616.496094 C 1201.714844 617.109375 1200.773438 617.699219 1199.816406 618.265625 C 1198.863281 618.832031 1197.894531 619.378906 1196.910156 619.898438 C 1195.929688 620.417969 1194.933594 620.914062 1193.925781 621.382812 C 1192.917969 621.855469 1191.902344 622.300781 1190.871094 622.722656 C 1189.84375 623.144531 1188.804688 623.539062 1187.757812 623.910156 C 1186.707031 624.277344 1185.652344 624.621094 1184.585938 624.941406 C 1183.519531 625.257812 1182.449219 625.550781 1181.371094 625.816406 C 1180.289062 626.082031 1179.203125 626.320312 1178.113281 626.53125 C 1177.023438 626.746094 1175.925781 626.929688 1174.824219 627.089844 C 1173.726562 627.246094 1172.621094 627.378906 1171.515625 627.484375 C 1170.410156 627.585938 1169.300781 627.664062 1168.1875 627.714844 C 1167.078125 627.765625 1165.96875 627.785156 1164.855469 627.78125 C 1163.746094 627.777344 1162.632812 627.746094 1161.523438 627.6875 C 1160.414062 627.628906 1159.304688 627.539062 1158.199219 627.425781 C 1157.09375 627.3125 1155.992188 627.171875 1154.890625 627.003906 C 1153.792969 626.835938 1152.699219 626.644531 1151.609375 626.421875 C 1150.519531 626.199219 1149.4375 625.953125 1148.359375 625.675781 C 1147.28125 625.402344 1146.210938 625.101562 1145.148438 624.773438 C 1144.085938 624.449219 1143.035156 624.09375 1141.988281 623.714844 C 1140.945312 623.335938 1139.910156 622.933594 1138.882812 622.503906 C 1137.859375 622.074219 1136.84375 621.617188 1135.839844 621.140625 C 1134.839844 620.660156 1133.847656 620.15625 1132.871094 619.628906 L 1012.738281 554.785156 C 1011.761719 554.257812 1010.796875 553.703125 1009.84375 553.128906 C 1008.890625 552.554688 1007.957031 551.957031 1007.035156 551.335938 C 1006.113281 550.714844 1005.207031 550.070312 1004.316406 549.402344 C 1003.425781 548.738281 1002.550781 548.050781 1001.695312 547.339844 C 1000.835938 546.632812 1000 545.902344 999.179688 545.152344 C 998.359375 544.402344 997.554688 543.632812 996.773438 542.84375 C 995.988281 542.054688 995.226562 541.246094 994.484375 540.421875 C 993.738281 539.59375 993.015625 538.75 992.316406 537.886719 C 991.613281 537.023438 990.933594 536.144531 990.273438 535.25 C 989.617188 534.355469 988.980469 533.441406 988.367188 532.515625 C 987.75 531.589844 987.160156 530.648438 986.59375 529.691406 C 986.027344 528.734375 985.480469 527.765625 984.960938 526.785156 C 984.441406 525.800781 983.945312 524.804688 983.476562 523.796875 C 983.003906 522.792969 982.558594 521.773438 982.136719 520.746094 C 981.714844 519.714844 981.320312 518.675781 980.949219 517.628906 C 980.578125 516.582031 980.234375 515.523438 979.917969 514.457031 C 979.597656 513.394531 979.308594 512.320312 979.042969 511.242188 C 978.777344 510.160156 978.539062 509.074219 978.324219 507.984375 C 978.113281 506.894531 977.925781 505.796875 977.769531 504.695312 C 977.609375 503.597656 977.480469 502.492188 977.375 501.386719 C 977.269531 500.277344 977.195312 499.167969 977.144531 498.058594 C 977.09375 496.949219 977.074219 495.835938 977.078125 494.726562 C 977.082031 493.613281 977.113281 492.503906 977.171875 491.390625 C 977.234375 490.28125 977.320312 489.171875 977.433594 488.066406 C 977.546875 486.960938 977.6875 485.859375 977.855469 484.761719 C 978.023438 483.660156 978.21875 482.566406 978.441406 481.476562 C 978.664062 480.386719 978.910156 479.304688 979.1875 478.226562 C 979.460938 477.148438 979.761719 476.082031 980.089844 475.019531 C 980.417969 473.957031 980.773438 472.902344 981.152344 471.855469 C 981.53125 470.8125 981.933594 469.777344 982.363281 468.75 C 982.796875 467.726562 983.25 466.710938 983.730469 465.707031 C 984.210938 464.707031 984.714844 463.714844 985.242188 462.738281 L 985.394531 462.457031 C 985.921875 461.480469 986.472656 460.515625 987.050781 459.5625 C 987.625 458.613281 988.222656 457.675781 988.84375 456.753906 C 989.464844 455.832031 990.109375 454.925781 990.773438 454.035156 C 991.441406 453.144531 992.128906 452.269531 992.835938 451.414062 C 993.546875 450.558594 994.273438 449.71875 995.023438 448.898438 C 995.773438 448.078125 996.542969 447.273438 997.332031 446.492188 C 998.121094 445.710938 998.929688 444.945312 999.757812 444.203125 C 1000.585938 443.460938 1001.429688 442.738281 1002.289062 442.035156 C 1003.152344 441.332031 1004.03125 440.652344 1004.929688 439.992188 C 1005.824219 439.335938 1006.734375 438.699219 1007.660156 438.085938 C 1008.589844 437.472656 1009.53125 436.878906 1010.488281 436.3125 C 1011.441406 435.746094 1012.410156 435.203125 1013.394531 434.679688 C 1014.378906 434.160156 1015.371094 433.664062 1016.378906 433.195312 C 1017.386719 432.722656 1018.402344 432.277344 1019.433594 431.855469 C 1020.460938 431.433594 1021.5 431.039062 1022.550781 430.667969 C 1023.597656 430.300781 1024.65625 429.953125 1025.71875 429.636719 C 1026.785156 429.320312 1027.859375 429.027344 1028.9375 428.761719 C 1030.015625 428.496094 1031.101562 428.257812 1032.195312 428.042969 C 1033.285156 427.832031 1034.382812 427.648438 1035.480469 427.488281 C 1036.582031 427.332031 1037.6875 427.199219 1038.792969 427.09375 C 1039.898438 426.992188 1041.007812 426.914062 1042.117188 426.863281 C 1043.230469 426.8125 1044.339844 426.792969 1045.453125 426.796875 C 1046.566406 426.800781 1047.675781 426.832031 1048.785156 426.894531 C 1049.894531 426.953125 1051.003906 427.039062 1052.109375 427.152344 C 1053.214844 427.265625 1054.320312 427.40625 1055.417969 427.574219 C 1056.515625 427.746094 1057.609375 427.9375 1058.699219 428.160156 C 1059.789062 428.382812 1060.875 428.632812 1061.949219 428.90625 C 1063.027344 429.179688 1064.097656 429.484375 1065.160156 429.808594 C 1066.222656 430.136719 1067.277344 430.492188 1068.320312 430.871094 C 1069.367188 431.25 1070.402344 431.65625 1071.425781 432.085938 C 1072.453125 432.515625 1073.464844 432.96875 1074.46875 433.449219 C 1075.472656 433.929688 1076.460938 434.433594 1077.441406 434.964844 L 1137.492188 467.390625 L 1169.953125 407.34375 C 1170.480469 406.367188 1171.03125 405.402344 1171.609375 404.453125 C 1172.183594 403.5 1172.78125 402.5625 1173.402344 401.640625 C 1174.023438 400.71875 1174.667969 399.8125 1175.335938 398.921875 C 1176 398.035156 1176.6875 397.160156 1177.394531 396.304688 C 1178.105469 395.445312 1178.835938 394.609375 1179.585938 393.789062 C 1180.335938 392.96875 1181.105469 392.164062 1181.894531 391.382812 C 1182.683594 390.601562 1183.492188 389.835938 1184.316406 389.09375 C 1185.144531 388.351562 1185.988281 387.628906 1186.851562 386.925781 C 1187.714844 386.222656 1188.59375 385.542969 1189.488281 384.886719 C 1190.382812 384.226562 1191.296875 383.589844 1192.222656 382.976562 C 1193.148438 382.363281 1194.089844 381.773438 1195.046875 381.207031 C 1196.003906 380.636719 1196.972656 380.09375 1197.957031 379.574219 C 1198.9375 379.054688 1199.933594 378.558594 1200.941406 378.085938 C 1201.945312 377.617188 1202.964844 377.171875 1203.992188 376.75 C 1205.023438 376.328125 1206.0625 375.933594 1207.109375 375.5625 C 1208.15625 375.191406 1209.214844 374.847656 1210.28125 374.53125 C 1211.34375 374.210938 1212.417969 373.921875 1213.496094 373.65625 C 1214.578125 373.390625 1215.660156 373.148438 1216.753906 372.9375 C 1217.84375 372.726562 1218.941406 372.539062 1220.039062 372.382812 C 1221.140625 372.222656 1222.246094 372.09375 1223.351562 371.988281 C 1224.457031 371.882812 1225.566406 371.808594 1226.679688 371.757812 C 1227.789062 371.707031 1228.898438 371.683594 1230.011719 371.691406 C 1231.125 371.695312 1232.234375 371.726562 1233.34375 371.785156 C 1234.453125 371.84375 1235.5625 371.933594 1236.667969 372.046875 C 1237.773438 372.160156 1238.875 372.300781 1239.976562 372.46875 C 1241.074219 372.636719 1242.167969 372.832031 1243.257812 373.050781 C 1244.347656 373.273438 1245.429688 373.523438 1246.507812 373.796875 C 1247.585938 374.070312 1248.65625 374.371094 1249.71875 374.699219 C 1250.78125 375.027344 1251.835938 375.378906 1252.878906 375.761719 C 1253.925781 376.140625 1254.960938 376.542969 1255.984375 376.972656 C 1257.011719 377.402344 1258.023438 377.859375 1259.027344 378.339844 C 1260.03125 378.816406 1261.019531 379.320312 1262 379.851562 Z M 1262 379.851562 " fill-opacity="1" fill-rule="nonzero"/></g></g></g><path stroke-linecap="butt" transform="matrix(0.403919, -0.631941, 0.631941, 0.403919, 104.893904, 729.104149)" fill="none" stroke-linejoin="miter" d="M 40.50488 300.474597 C 282.249287 -33.492864 589.421014 -33.490352 962.021645 300.47494 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.734847, 0.149999, -0.149999, 0.734847, 304.884588, 831.580108)" fill="none" stroke-linejoin="miter" d="M 41.766746 27.491179 C 472.054803 681.225638 813.229103 681.225807 1065.289646 27.491684 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.713054, 0.232494, -0.232494, 0.713054, 743.751953, 84.389709)" fill="none" stroke-linejoin="miter" d="M 27.552117 192.796126 C 315.880744 2.40229 514.553475 2.404029 623.562127 192.793052 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.479248, -0.576907, 0.576907, 0.479248, 975.623445, 1084.867279)" fill="none" stroke-linejoin="miter" d="M 10.923997 43.992439 C 74.518874 60.091943 159.21204 60.093361 265.000168 43.992687 " stroke="#245f28" stroke-width="89" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 329.956147, 817.015974)" fill="none" stroke-linejoin="miter" d="M 0.000357227 50.000231 C 37.480224 49.999953 88.464376 49.999514 152.947912 49.997153 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 331.706899, 815.218854)" fill="none" stroke-linejoin="miter" d="M 0.000389071 49.999177 C 37.480256 49.998899 88.464408 49.998461 152.946183 50.001001 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.747763, -0.0578889, 0.0578889, 0.747763, 722.333707, 226.603261)" fill="none" stroke-linejoin="miter" d="M 8.805162 73.553045 C 184.314962 42.151956 282.885164 42.150737 304.510575 73.548986 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/></svg>
</file>

<file path="src/features/auth/screens/SignInScreen.jsx">
import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import Logo from '@/shared/components/common/Logo';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import PasswordInput from '@/shared/components/common/PasswordInput';
import { loginUser } from '@/shared/services/api';
import { useAuth } from '@/features/auth/AuthContext';

export default function SignInScreen() {
  const navigate = useNavigate();
  const { login } = useAuth();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const canSubmit = /\S+@\S+\.\S+/.test(email) && password.length > 0;

  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!canSubmit || loading) return;
    setLoading(true);
    setError('');
    try {
      const { user, token } = await loginUser({ email: email.trim(), password });
      login(user, token);
      navigate('/trades', { replace: true });
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex flex-col justify-center px-6 py-10">
      <div className="flex justify-center mb-8">
        <Logo size="lg" iconOnly />
      </div>

      <h1 className="text-2xl font-bold text-gray-900 text-center">Welcome back</h1>
      <p className="text-sm text-gray-500 text-center mt-1 mb-8">Sign in to continue.</p>

      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label className="text-sm font-medium text-gray-700 mb-1 block">Email</label>
          <input
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="you@example.com"
            className="w-full px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none"
          />
        </div>

        <div>
          <label className="text-sm font-medium text-gray-700 mb-1 block">Password</label>
          <PasswordInput
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="••••••••"
          />
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton type="submit" disabled={!canSubmit || loading}>
          {loading ? 'Signing in...' : 'Sign In'}
        </PrimaryButton>
      </form>

      <p className="text-center text-sm text-gray-500 mt-6">
        Don&apos;t have an account?{' '}
        <Link to="/signup" className="text-brand-600 font-semibold">
          Sign up
        </Link>
      </p>
    </div>
  );
}
</file>

<file path="src/features/auth/screens/SignUpScreen.jsx">
import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import Logo from '@/shared/components/common/Logo';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import PasswordInput from '@/shared/components/common/PasswordInput';
import { registerUser } from '@/shared/services/api';
import { useAuth } from '@/features/auth/AuthContext';

const inputClass =
  'w-full px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none';
const labelClass = 'text-sm font-medium text-gray-700 mb-1 block';

export default function SignUpScreen() {
  const navigate = useNavigate();
  const { login } = useAuth();
  const [firstName, setFirstName] = useState('');
  const [lastName, setLastName] = useState('');
  const [username, setUsername] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const validationMessage = (() => {
    if (!firstName.trim() || !lastName.trim()) return 'Enter your first and last name.';
    if (username.trim().length <= 2) return 'Username must be at least 3 characters.';
    if (!/\S+@\S+\.\S+/.test(email)) return 'Enter a valid email address.';
    if (password.length < 6) return 'Password must be at least 6 characters.';
    if (password !== confirmPassword) return "Passwords don't match.";
    return '';
  })();
  const canSubmit = validationMessage === '';
  const touched = [firstName, lastName, username, email, password, confirmPassword].some(
    (v) => v.length > 0
  );

  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!canSubmit || loading) return;
    setLoading(true);
    setError('');
    try {
      const { user, token } = await registerUser({
        firstName: firstName.trim(),
        lastName: lastName.trim(),
        username: username.trim(),
        email: email.trim(),
        password,
      });
      login(user, token);
      navigate('/trades', { replace: true });
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex flex-col justify-center px-6 py-10">
      <div className="flex justify-center mb-8">
        <Logo size="lg" iconOnly />
      </div>

      <h1 className="text-2xl font-bold text-gray-900 text-center">Create your account</h1>
      <p className="text-sm text-gray-500 text-center mt-1 mb-8">
        Join RosaCycle and start trading recyclables.
      </p>

      <form onSubmit={handleSubmit} className="space-y-4">
        <div className="grid grid-cols-2 gap-3">
          <div>
            <label className={labelClass}>First name</label>
            <input
              className={inputClass}
              value={firstName}
              onChange={(e) => setFirstName(e.target.value)}
              placeholder="Juan"
            />
          </div>
          <div>
            <label className={labelClass}>Last name</label>
            <input
              className={inputClass}
              value={lastName}
              onChange={(e) => setLastName(e.target.value)}
              placeholder="Dela Cruz"
            />
          </div>
        </div>

        <div>
          <label className={labelClass}>Username</label>
          <input
            className={inputClass}
            value={username}
            onChange={(e) => setUsername(e.target.value)}
            placeholder="juandelacruz"
          />
        </div>

        <div>
          <label className={labelClass}>Email</label>
          <input
            type="email"
            className={inputClass}
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="you@example.com"
          />
        </div>

        <div>
          <label className={labelClass}>Password</label>
          <PasswordInput
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="At least 6 characters"
          />
        </div>

        <div>
          <label className={labelClass}>Confirm password</label>
          <PasswordInput
            value={confirmPassword}
            onChange={(e) => setConfirmPassword(e.target.value)}
            placeholder="Re-enter your password"
          />
        </div>

        {touched && validationMessage && (
          <p className="text-xs text-gray-500">{validationMessage}</p>
        )}
        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton type="submit" disabled={!canSubmit || loading}>
          {loading ? 'Creating account...' : 'Sign Up'}
        </PrimaryButton>
      </form>

      <p className="text-center text-sm text-gray-500 mt-6">
        Already have an account?{' '}
        <Link to="/signin" className="text-brand-600 font-semibold">
          Sign in
        </Link>
      </p>
    </div>
  );
}
</file>

<file path="src/features/auth/AuthContext.jsx">
import { createContext, useContext, useEffect, useState } from 'react';
import { logoutUser, updateUserProfile } from '@/shared/services/api';
import { USER_STORAGE_KEY, TOKEN_STORAGE_KEY } from '@/shared/lib/storageKeys';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(() => {
    const raw = localStorage.getItem(USER_STORAGE_KEY);
    return raw ? JSON.parse(raw) : null;
  });
  const [token, setToken] = useState(() => localStorage.getItem(TOKEN_STORAGE_KEY));

  useEffect(() => {
    if (user) localStorage.setItem(USER_STORAGE_KEY, JSON.stringify(user));
    else localStorage.removeItem(USER_STORAGE_KEY);
  }, [user]);

  useEffect(() => {
    if (token) localStorage.setItem(TOKEN_STORAGE_KEY, token);
    else localStorage.removeItem(TOKEN_STORAGE_KEY);
  }, [token]);

  // The backend has no refresh-token flow — a 401 means the token is gone/expired.
  // api.js can't reach this context directly (plain functions, not hooks), so it
  // dispatches this event instead; we react to it here by force-signing-out.
  useEffect(() => {
    const handleUnauthorized = () => {
      setUser(null);
      setToken(null);
    };
    window.addEventListener('rosacycle:unauthorized', handleUnauthorized);
    return () => window.removeEventListener('rosacycle:unauthorized', handleUnauthorized);
  }, []);

  const login = (userData, authToken) => {
    setUser(userData);
    setToken(authToken);
  };

  const logout = async () => {
    await logoutUser();
    setUser(null);
    setToken(null);
  };

  const updateUser = async (updates, currentPassword) => {
    const updated = await updateUserProfile(updates, currentPassword);
    setUser(updated);
    return updated;
  };

  return (
    <AuthContext.Provider value={{ user, isAuthenticated: !!user && !!token, login, logout, updateUser }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error('useAuth must be used within AuthProvider');
  return ctx;
}
</file>

<file path="src/features/map/screens/MapScreen.jsx">
import { useEffect, useRef, useState } from 'react';
import { MapPin, ChevronDown } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import MaterialTag from '@/shared/components/common/MaterialTag';
import { fetchResourceSpots, markResourceSpotCollected, addResourceSpotPhoto } from '@/shared/services/api';
import { getDistanceKm } from '@/features/map/geo';

export default function MapScreen() {
  const [selectedId, setSelectedId] = useState(null);
  const [spots, setSpots] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [busyId, setBusyId] = useState(null);
  const fileInputRef = useRef(null);
  const photoTargetId = useRef(null);

  const loadSpots = () => {
    setLoading(true);
    setError('');
    fetchResourceSpots()
      .then(setSpots)
      .catch((err) => setError(err.message || 'Could not load resource spots.'))
      .finally(() => setLoading(false));
  };

  useEffect(loadSpots, []);

  const toggleSite = (id) => setSelectedId((prev) => (prev === id ? null : id));

  const handleMarkCollected = async (id) => {
    if (busyId) return;
    setBusyId(id);
    try {
      await markResourceSpotCollected(id);
      loadSpots();
    } catch (err) {
      setError(err.message || 'Could not mark this spot collected.');
    } finally {
      setBusyId(null);
    }
  };

  const handleAddPhotoClick = (id) => {
    photoTargetId.current = id;
    fileInputRef.current?.click();
  };

  const handlePhotoFileChange = async (e) => {
    const file = e.target.files?.[0];
    const id = photoTargetId.current;
    e.target.value = '';
    if (!file || !id) return;
    setBusyId(id);
    try {
      await addResourceSpotPhoto(id, file);
      loadSpots();
    } catch (err) {
      setError(err.message || 'Could not add photo.');
    } finally {
      setBusyId(null);
    }
  };

  return (
    <div className="pb-24">
      <Header />

      <input
        ref={fileInputRef}
        type="file"
        accept="image/*"
        onChange={handlePhotoFileChange}
        className="hidden"
      />

      {/* Map container placeholder — no real coordinates from the backend, pins
          are laid out by index instead of lat/long. */}
      <div className="relative w-full h-56 bg-gray-200 flex items-center justify-center">
        <p className="text-sm text-gray-400">Map view</p>
        {spots.map((site, i) => (
          <button
            key={site.id}
            onClick={() => toggleSite(site.id)}
            className="absolute"
            style={{ left: `${25 + i * 22}%`, top: `${35 + (i % 2) * 20}%` }}
          >
            <MapPin
              size={28}
              className={selectedId === site.id ? 'text-brand-700 fill-brand-200' : 'text-brand-600 fill-brand-100'}
            />
          </button>
        ))}
      </div>

      <div className="px-4 pt-4 space-y-3">
        <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide">Nearby reported sites</p>

        {loading && <p className="text-center text-sm text-gray-400 py-8">Loading...</p>}
        {!loading && error && <p className="text-center text-sm text-red-500 py-4">{error}</p>}
        {!loading && !error && spots.length === 0 && (
          <p className="text-center text-sm text-gray-400 py-8">No resource spots reported yet.</p>
        )}

        {!loading &&
          spots.map((site) => {
            const expanded = selectedId === site.id;
            const distanceKm = getDistanceKm(site.location);
            const isBusy = busyId === site.id;
            return (
              <div
                key={site.id}
                className={`bg-white border rounded-xl overflow-hidden ${
                  expanded ? 'border-brand-500' : 'border-gray-100'
                }`}
              >
                <button
                  onClick={() => toggleSite(site.id)}
                  className="w-full flex items-center justify-between px-4 py-3 text-left"
                >
                  <div>
                    <p className="font-semibold text-gray-900 text-sm">{site.name}</p>
                    {distanceKm != null && <p className="text-xs text-gray-400 mt-0.5">{distanceKm} km away</p>}
                  </div>
                  <div className="flex items-center gap-2">
                    <MaterialTag material={site.material} />
                    <ChevronDown
                      size={16}
                      className={`text-gray-400 transition-transform ${expanded ? 'rotate-180' : ''}`}
                    />
                  </div>
                </button>

                {expanded && (
                  <div className="px-4 pb-4 pt-3 border-t border-gray-100 space-y-2">
                    <div className="aspect-[4/3] rounded-lg bg-gray-100 border border-gray-200 overflow-hidden">
                      {site.image && (
                        <img src={site.image} alt={site.name} className="w-full h-full object-cover" />
                      )}
                    </div>
                    <div className="flex gap-6 text-sm pt-2">
                      {site.weightKg != null && (
                        <span><span className="font-semibold">{site.weightKg}kg</span> weight</span>
                      )}
                      {site.quantity != null && (
                        <span><span className="font-semibold">{site.quantity}</span> quantity</span>
                      )}
                    </div>
                    {site.location && <p className="text-sm text-gray-500">📍 {site.location}</p>}
                    {site.description && (
                      <p className="text-sm text-gray-600 italic">&ldquo;{site.description}&rdquo;</p>
                    )}
                    {site.permissionNote && (
                      <p className="text-xs text-gray-400">Access notes: {site.permissionNote}</p>
                    )}

                    <div className="flex gap-2 pt-2">
                      <button
                        onClick={() => handleAddPhotoClick(site.id)}
                        disabled={isBusy}
                        className="flex-1 py-2.5 rounded-lg bg-gray-100 text-gray-700 text-xs font-semibold disabled:opacity-50"
                      >
                        Add photo
                      </button>
                      <button
                        onClick={() => handleMarkCollected(site.id)}
                        disabled={isBusy}
                        className="flex-1 py-2.5 rounded-lg bg-brand-600 text-white text-xs font-semibold disabled:opacity-50"
                      >
                        {isBusy ? 'Working...' : 'Mark Collected'}
                      </button>
                    </div>
                  </div>
                )}
              </div>
            );
          })}
      </div>

      <BottomNav />
    </div>
  );
}
</file>

<file path="src/features/map/screens/ResourceSpotConfirmScreen.jsx">
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import { SPOT_MATERIAL_OPTIONS } from '@/shared/utils/constants';
import { reportResourceSpot } from '@/shared/services/api';
import { getPendingCapture, clearPendingCapture } from '@/shared/lib/pendingCapture';

const inputClass =
  'w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand-500';
const labelClass = 'text-xs font-semibold text-gray-500 mb-1.5 block';

export default function ResourceSpotConfirmScreen() {
  const navigate = useNavigate();
  const [name, setName] = useState('');
  const [material, setMaterial] = useState('');
  const [weightKg, setWeightKg] = useState('');
  const [quantity, setQuantity] = useState('');
  const [description, setDescription] = useState('');
  const [locationText, setLocationText] = useState('');
  const [permissionNote, setPermissionNote] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState('');

  const imageFile = getPendingCapture();
  const previewUrl = imageFile ? URL.createObjectURL(imageFile) : null;

  const canSubmit = name.trim().length > 0 && material && locationText.trim().length > 0;

  const handleSubmit = async () => {
    if (!canSubmit || submitting) return;
    setSubmitting(true);
    setError('');
    try {
      const saved = await reportResourceSpot(
        {
          name: name.trim(),
          material,
          weightKg: Number(weightKg) || undefined,
          quantity: Number(quantity) || undefined,
          description: description.trim(),
          locationText: locationText.trim(),
          permissionNote: permissionNote.trim(),
        },
        imageFile
      );
      clearPendingCapture();
      navigate('/camera/reported', { state: { spot: saved } });
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Report Spot" />

      <div className="aspect-[4/3] bg-gray-200 flex items-center justify-center text-gray-400 text-sm overflow-hidden">
        {previewUrl ? (
          <img src={previewUrl} alt="Captured spot" className="w-full h-full object-cover" />
        ) : (
          'No photo captured'
        )}
      </div>

      <div className="p-5 space-y-6">
        <div>
          <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
            AI-Detected (editable)
          </p>
          <div className="space-y-3">
            <div>
              <label className={labelClass}>Material spotted</label>
              <div className="grid grid-cols-3 gap-2">
                {SPOT_MATERIAL_OPTIONS.map((opt) => (
                  <button
                    key={opt}
                    type="button"
                    onClick={() => setMaterial(opt)}
                    className={`py-2 rounded-lg text-xs font-medium ${
                      material === opt ? 'bg-brand-600 text-white' : 'bg-gray-100 text-gray-600'
                    }`}
                  >
                    {opt}
                  </button>
                ))}
              </div>
            </div>
            <div>
              <label className={labelClass}>Estimated weight (kg)</label>
              <input
                type="number"
                className={inputClass}
                value={weightKg}
                onChange={(e) => setWeightKg(e.target.value)}
                placeholder="e.g. 4.0"
              />
            </div>
            <div>
              <label className={labelClass}>Estimated quantity</label>
              <input
                type="number"
                className={inputClass}
                value={quantity}
                onChange={(e) => setQuantity(e.target.value)}
                placeholder="e.g. 3"
              />
            </div>
          </div>
        </div>

        <div>
          <label className={labelClass}>Spot name</label>
          <input
            className={inputClass}
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="e.g. Overgrown Lot"
          />
        </div>

        <div>
          <label className={labelClass}>Description (optional)</label>
          <textarea
            className={`${inputClass} min-h-[90px] resize-none`}
            value={description}
            onChange={(e) => setDescription(e.target.value)}
            placeholder="What did you find here?"
          />
        </div>

        <div>
          <label className={labelClass}>Location</label>
          <input
            className={inputClass}
            value={locationText}
            onChange={(e) => setLocationText(e.target.value)}
            placeholder="e.g. Tagapo, Santa Rosa, Laguna"
          />
        </div>

        <div>
          <label className={labelClass}>Permission / access notes (optional)</label>
          <textarea
            className={`${inputClass} min-h-[70px] resize-none`}
            value={permissionNote}
            onChange={(e) => setPermissionNote(e.target.value)}
            placeholder="e.g. Public lot, no permission needed / Ask caretaker first"
          />
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton onClick={handleSubmit} disabled={!canSubmit || submitting}>
          {submitting ? 'Reporting...' : 'Report Spot'}
        </PrimaryButton>
      </div>
    </div>
  );
}
</file>

<file path="src/features/map/screens/ResourceSpotScreen.jsx">
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import CameraViewfinder from '@/features/scan/components/CameraViewfinder';
import { setPendingCapture } from '@/shared/lib/pendingCapture';

/**
 * Bottom-nav "Camera" tab. Captures a photo of a resource spot (a location
 * with recyclable/reusable material worth flagging) and reports it as a
 * pin on the Map — this never creates a Trade. Trade item scanning lives
 * at /trades/scan instead (see ScanScreen.jsx).
 */
export default function ResourceSpotScreen() {
  const navigate = useNavigate();
  const [previewUrl, setPreviewUrl] = useState(null);

  const handleFileSelected = (file) => {
    setPendingCapture(file);
    setPreviewUrl(URL.createObjectURL(file));
  };

  const goToConfirm = () => {
    navigate('/camera/confirm');
  };

  return (
    <div className="h-screen flex flex-col overflow-hidden">
      <Header title="Resource Spot" showBell={false} />
      <div className="flex-1 relative min-h-0 pb-16">
        <CameraViewfinder
          previewImage={previewUrl}
          onCapture={goToConfirm}
          onFileSelected={handleFileSelected}
          onFlip={() => {}}
        />
        {previewUrl && (
          <div className="absolute bottom-0 left-0 right-0 px-4 py-3 bg-white border-t border-gray-100">
            <p className="text-sm text-gray-500 text-center">Photo captured. Tap shutter again to continue</p>
          </div>
        )}
      </div>
      <BottomNav />
    </div>
  );
}
</file>

<file path="src/features/map/screens/SpotReportedScreen.jsx">
import { useLocation, useNavigate } from 'react-router-dom';
import ConfirmationScreen from '@/shared/components/common/ConfirmationScreen';

export default function SpotReportedScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const spot = location.state?.spot;

  return (
    <ConfirmationScreen
      heading="Spot Reported!"
      subtext="Thanks for helping the community find this resource. It'll now show up as a pin on the map."
      summaryCard={
        spot && (
          <div>
            <p className="font-bold text-gray-900">{spot.name}</p>
            <p className="text-sm text-gray-500 mt-1">
              {spot.material}
              {spot.weightKg != null && ` • ${spot.weightKg}kg`}
              {spot.quantity != null && ` • ${spot.quantity} qty`}
            </p>
            {spot.description && <p className="text-sm text-gray-500 mt-1">{spot.description}</p>}
            {spot.permissionNote && (
              <p className="text-xs text-gray-400 mt-2">Access notes: {spot.permissionNote}</p>
            )}
          </div>
        )
      }
      primaryAction={{ label: 'View on Map', onClick: () => navigate('/map') }}
      secondaryAction={{ label: 'Report Another Spot', onClick: () => navigate('/camera') }}
    />
  );
}
</file>

<file path="src/features/map/geo.js">
// Mock data scope: Santa Rosa, Laguna only. The mock current user is based in
// Sinalhan, so every other barangay's distance is computed relative to that —
// single source of truth instead of hand-picked distanceKm numbers that can
// drift out of sync with the location text (that mismatch was a real bug once
// already, see mockTrackTrades history).
//
// Distances are straight-line approximations for demo purposes, not routed.
const BARANGAY_DISTANCE_FROM_USER_KM = {
  Sinalhan: 0,
  Tagapo: 1.1,
  Ibaba: 1.3,
  'Market Area': 1.6,
  Kanluran: 1.8,
  Malusak: 2.0,
  Pooc: 2.1,
  Labas: 2.4,
  Aplaya: 2.7,
  Dila: 3.0,
  Dita: 3.2,
  Caingin: 3.5,
  'Santo Domingo': 3.8,
  Macabling: 4.0,
  Malitlit: 4.6,
  'Pulong Santa Cruz': 5.2,
  Balibago: 5.5,
  'Don Jose': 6.0,
};

export const SANTA_ROSA_BARANGAYS = Object.keys(BARANGAY_DISTANCE_FROM_USER_KM);

// `location` is a full string like "Balibago, Santa Rosa, Laguna" — match it
// against the known barangay names rather than requiring an exact key.
export function getDistanceKm(location) {
  if (!location) return null;
  const barangay = SANTA_ROSA_BARANGAYS.find((b) => location.includes(b));
  return barangay ? BARANGAY_DISTANCE_FROM_USER_KM[barangay] : null;
}
</file>

<file path="src/features/profile/screens/EditProfileScreen.jsx">
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { LogOut } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import PasswordInput from '@/shared/components/common/PasswordInput';
import { useAuth } from '@/features/auth/AuthContext';

const inputClass =
  'w-full px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none';
const labelClass = 'text-sm font-medium text-gray-700 mb-1 block';

export default function EditProfileScreen() {
  const navigate = useNavigate();
  const { user, updateUser, logout } = useAuth();
  const [firstName, setFirstName] = useState(user?.firstName || '');
  const [lastName, setLastName] = useState(user?.lastName || '');
  const [username, setUsername] = useState(user?.username || '');
  const [email, setEmail] = useState(user?.email || '');
  const [currentPassword, setCurrentPassword] = useState('');
  const [password, setPassword] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');

  const changingPassword = currentPassword.length > 0 || password.length > 0 || confirmPassword.length > 0;

  const hasChanges =
    firstName.trim() !== (user?.firstName || '') ||
    lastName.trim() !== (user?.lastName || '') ||
    username.trim() !== (user?.username || '') ||
    email.trim() !== (user?.email || '') ||
    changingPassword;

  const canSave =
    hasChanges &&
    firstName.trim().length > 0 &&
    lastName.trim().length > 0 &&
    username.trim().length > 2 &&
    /\S+@\S+\.\S+/.test(email) &&
    (!changingPassword ||
      (currentPassword.length > 0 && password.length >= 6 && password === confirmPassword));

  const handleSave = async () => {
    if (!canSave || saving) return;
    setSaving(true);
    setError('');
    try {
      const updates = {
        firstName: firstName.trim(),
        lastName: lastName.trim(),
        username: username.trim(),
        email: email.trim(),
      };
      if (changingPassword) updates.password = password;
      await updateUser(updates, changingPassword ? currentPassword : undefined);
      navigate(-1);
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setSaving(false);
    }
  };

  const handleLogout = async () => {
    await logout();
    navigate('/signin', { replace: true });
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Edit Profile" />

      <div className="p-5 space-y-4">
        <fieldset className="space-y-4">
          <div className="grid grid-cols-2 gap-3">
            <div>
              <label className={labelClass}>First name</label>
              <input className={inputClass} value={firstName} onChange={(e) => setFirstName(e.target.value)} />
            </div>
            <div>
              <label className={labelClass}>Last name</label>
              <input className={inputClass} value={lastName} onChange={(e) => setLastName(e.target.value)} />
            </div>
          </div>

          <div>
            <label className={labelClass}>Username</label>
            <input className={inputClass} value={username} onChange={(e) => setUsername(e.target.value)} />
          </div>

          <div>
            <label className={labelClass}>Email</label>
            <input
              type="email"
              className={inputClass}
              value={email}
              onChange={(e) => setEmail(e.target.value)}
            />
          </div>

          <div className="border-t border-gray-100 pt-4 space-y-4">
            <p className="text-sm font-semibold text-gray-900">Change Password</p>

            <div>
              <label className={labelClass}>Current password</label>
              <PasswordInput
                value={currentPassword}
                onChange={(e) => setCurrentPassword(e.target.value)}
                placeholder="Required to set a new password"
              />
            </div>

            <div>
              <label className={labelClass}>New password</label>
              <PasswordInput
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="At least 6 characters"
              />
            </div>

            <div>
              <label className={labelClass}>Confirm new password</label>
              <PasswordInput
                value={confirmPassword}
                onChange={(e) => setConfirmPassword(e.target.value)}
                placeholder="Re-enter your new password"
              />
              {confirmPassword.length > 0 && password !== confirmPassword && (
                <p className="text-xs text-red-500 mt-1">Passwords don&apos;t match.</p>
              )}
            </div>
          </div>
        </fieldset>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton onClick={handleSave} disabled={!canSave || saving}>
          {saving ? 'Saving...' : 'Save Changes'}
        </PrimaryButton>

        <button
          onClick={handleLogout}
          className="w-full flex items-center justify-center gap-2 py-3.5 rounded-xl bg-red-500 text-white font-semibold active:scale-[0.98] transition-transform hover:bg-red-600"
        >
          <LogOut size={16} />
          Log Out
        </button>
      </div>
    </div>
  );
}
</file>

<file path="src/features/scan/components/AIDetectedForm.jsx">
/**
 * Editable form shown after AI scans a photo.
 *
 * context="posting" -> shows Caption + Location + Desired pickup location +
 *                       Trade-for fields (docs: "User details" step under
 *                       Posting a Trade). Caption is a listing description,
 *                       so it only applies here — you're creating a new
 *                       public listing.
 * context="bidding"  -> just the AI-detected item fields above (Category,
 *                       Material, weight, Item name) — no caption, no
 *                       location, no message field. This is an offer on
 *                       someone else's existing trade; the trade owner
 *                       reviews the item itself, nothing else to fill in.
 *
 * All AI-generated values are pre-filled but editable, per docs:
 * "AI scans the photo and outputs the required item details" -> "User can edit details"
 */
export default function AIDetectedForm({ context = 'posting', values, onChange }) {
  const set = (key) => (e) => onChange?.({ ...values, [key]: e.target.value });

  const inputClass =
    'w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand-500';
  const labelClass = 'text-xs font-semibold text-gray-500 mb-1.5 block';

  return (
    <div className="space-y-5">
      <div>
        <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
          AI-Detected (editable)
        </p>
        <div className="space-y-3">
          <div>
            <label className={labelClass}>Category</label>
            <input className={inputClass} value={values.category || ''} onChange={set('category')} placeholder="e.g. Metal" />
          </div>
          <div>
            <label className={labelClass}>Material</label>
            <input className={inputClass} value={values.material || ''} onChange={set('material')} placeholder="e.g. Steel scrap" />
          </div>
          <div>
            <label className={labelClass}>Estimated weight (kg)</label>
            <input
              type="number"
              className={inputClass}
              value={values.weightKg || ''}
              onChange={set('weightKg')}
              placeholder="e.g. 4.5"
            />
          </div>
          {values.confidence != null && (
            <p className="text-xs text-gray-400">AI confidence: {values.confidence}%</p>
          )}
        </div>
      </div>

      <div>
        <label className={labelClass}>Item name</label>
        <input className={inputClass} value={values.itemName || ''} onChange={set('itemName')} placeholder="e.g. Old Woods" />
      </div>

      {context === 'posting' && (
        <div className="border-t border-gray-100 pt-5 space-y-3">
          <div>
            <label className={labelClass}>Quantity</label>
            <input
              type="number"
              min="1"
              className={inputClass}
              value={values.quantity || ''}
              onChange={set('quantity')}
              placeholder="e.g. 1"
            />
          </div>
          <div>
            <label className={labelClass}>Caption</label>
            <textarea
              className={`${inputClass} min-h-[80px] resize-none`}
              value={values.description || ''}
              onChange={set('description')}
              placeholder="Describe the item's condition, e.g. &quot;Flattened boxes, dry and clean.&quot;"
            />
          </div>

          <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide">Location</p>
          <div>
            <label className={labelClass}>Current location</label>
            <input
              className={inputClass}
              value={values.currentLocation || ''}
              onChange={set('currentLocation')}
              placeholder="Auto-filled from GPS"
            />
          </div>
          <div>
            <label className={labelClass}>Desired pickup location</label>
            <input
              className={inputClass}
              value={values.pickupLocation || ''}
              onChange={set('pickupLocation')}
              placeholder="Where should traders pick this up?"
            />
          </div>
          <div>
            <label className={labelClass}>Trade for</label>
            <div className="grid grid-cols-3 gap-2 mb-2">
              {[
                { key: 'specific', label: 'Specific item' },
                { key: 'nothing', label: 'Nothing' },
                { key: 'negotiating', label: 'Negotiable' },
              ].map((opt) => (
                <button
                  key={opt.key}
                  type="button"
                  onClick={() => onChange?.({ ...values, tradingForType: opt.key })}
                  className={`py-2 rounded-lg text-xs font-medium ${
                    values.tradingForType === opt.key
                      ? 'bg-brand-600 text-white'
                      : 'bg-gray-100 text-gray-600'
                  }`}
                >
                  {opt.label}
                </button>
              ))}
            </div>
            {values.tradingForType === 'specific' && (
              <input
                className={inputClass}
                value={values.tradingForValue || ''}
                onChange={set('tradingForValue')}
                placeholder="e.g. Paper or old magazines"
              />
            )}
          </div>
        </div>
      )}
    </div>
  );
}
</file>

<file path="src/features/scan/components/CameraViewfinder.jsx">
import { useRef } from 'react';
import { Upload, Circle, RefreshCw } from 'lucide-react';

/**
 * Camera viewfinder shell. There's no live camera feed — capture is a plain
 * <input type="file" accept="image/*"> (mobile browsers show a native
 * camera-or-gallery chooser for this). `onCapture` is only called once a
 * photo has already been selected — it means "continue," not "take photo."
 * `boundingBox` is a placeholder prop: { x, y, width, height } in percent
 * (0-100), drawn as an overlay rectangle, for a future AI-detection box.
 */
export default function CameraViewfinder({ previewImage, boundingBox, onCapture, onFileSelected, onFlip }) {
  const fileInputRef = useRef(null);

  const openPicker = () => fileInputRef.current?.click();

  const handleFileChange = (e) => {
    const file = e.target.files?.[0];
    if (file) onFileSelected(file);
    e.target.value = ''; // allow re-selecting the same file again later
  };

  const handleShutterClick = () => {
    if (previewImage) onCapture();
    else openPicker();
  };

  return (
    <div className="relative w-full h-full bg-black overflow-hidden">
      <input
        ref={fileInputRef}
        type="file"
        accept="image/*"
        onChange={handleFileChange}
        className="hidden"
      />

      {previewImage ? (
        <img src={previewImage} alt="Camera preview" className="w-full h-full object-cover" />
      ) : (
        <div className="w-full h-full flex items-center justify-center text-gray-500 text-sm">
          Camera preview
        </div>
      )}

      {boundingBox && (
        <div
          className="absolute border-2 border-sky-400 rounded-md"
          style={{
            left: `${boundingBox.x}%`,
            top: `${boundingBox.y}%`,
            width: `${boundingBox.width}%`,
            height: `${boundingBox.height}%`,
          }}
        />
      )}

      <div className="absolute bottom-0 left-0 right-0 flex items-center justify-between px-8 py-6 bg-gradient-to-t from-black/60 to-transparent">
        <button onClick={openPicker} aria-label="Upload from gallery" className="text-white active:scale-90 transition-transform">
          <Upload size={26} />
        </button>
        <button onClick={handleShutterClick} aria-label="Capture" className="active:scale-90 transition-transform">
          <Circle size={64} className="text-white" strokeWidth={2.5} />
        </button>
        <button onClick={onFlip} aria-label="Flip camera" className="w-11 h-11 rounded-full bg-black/40 flex items-center justify-center text-white active:scale-90 transition-transform">
          <RefreshCw size={20} />
        </button>
      </div>
    </div>
  );
}
</file>

<file path="src/features/scan/screens/ScanConfirmScreen.jsx">
import { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import AIDetectedForm from '@/features/scan/components/AIDetectedForm';
import { createTrade, sendOffer } from '@/shared/services/api';
import { getPendingCapture, clearPendingCapture } from '@/shared/lib/pendingCapture';

const BLANK_VALUES = {
  category: '',
  material: '',
  weightKg: '',
  itemName: '',
  quantity: '',
  description: '',
  currentLocation: '',
  pickupLocation: '',
  tradingForType: 'negotiating',
  tradingForValue: '',
};

export default function ScanConfirmScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const context = location.state?.context || 'posting';
  const tradeId = location.state?.tradeId;

  const [values, setValues] = useState(BLANK_VALUES);
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState('');

  const imageFile = getPendingCapture();
  const previewUrl = imageFile ? URL.createObjectURL(imageFile) : null;

  const canSubmit =
    context === 'posting'
      ? Boolean(
          values.itemName?.trim() &&
            values.category?.trim() &&
            values.material?.trim() &&
            values.quantity &&
            values.currentLocation?.trim()
        )
      : Boolean(values.itemName?.trim() && values.category?.trim() && values.material?.trim());

  const handlePrimaryAction = async () => {
    if (!canSubmit || submitting) return;
    setSubmitting(true);
    setError('');
    try {
      if (context === 'posting') {
        const trade = await createTrade(
          {
            itemName: values.itemName,
            category: values.category,
            material: values.material,
            description: values.description,
            weightKg: values.weightKg,
            quantity: values.quantity,
            locationText: values.currentLocation,
            pickupLocationText: values.pickupLocation,
            tradingForType: values.tradingForType,
            tradingForValue: values.tradingForType === 'specific' ? values.tradingForValue : undefined,
          },
          imageFile
        );
        clearPendingCapture();
        navigate('/trades/scan/created', { state: { trade } });
      } else {
        const offer = await sendOffer(
          tradeId,
          {
            itemName: values.itemName,
            category: values.category,
            material: values.material,
            weightKg: values.weightKg,
            description: values.description,
          },
          imageFile
        );
        clearPendingCapture();
        navigate(`/trades/${tradeId}/offer-sent`, { state: { offer } });
      }
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Edit Details" />

      <div className="aspect-[4/3] bg-gray-200 flex items-center justify-center text-gray-400 text-sm overflow-hidden">
        {previewUrl ? (
          <img src={previewUrl} alt="Captured item" className="w-full h-full object-cover" />
        ) : (
          'No photo captured'
        )}
      </div>

      <div className="p-5 space-y-6">
        <AIDetectedForm context={context} values={values} onChange={setValues} />

        {error && <p className="text-sm text-red-500">{error}</p>}

        <div className="space-y-3">
          <PrimaryButton onClick={handlePrimaryAction} disabled={!canSubmit || submitting}>
            {submitting
              ? 'Submitting...'
              : context === 'posting'
                ? 'Create Trade'
                : 'Send Offer'}
          </PrimaryButton>
          <button onClick={() => navigate(-1)} className="w-full text-center text-sm text-gray-500 font-medium">
            Retake Photo
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/features/scan/screens/ScanScreen.jsx">
import { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import CameraViewfinder from '@/features/scan/components/CameraViewfinder';
import { setPendingCapture } from '@/shared/lib/pendingCapture';

/**
 * AI scan step for a Trade item — reached only from within the Trades flow
 * (post a new item, or send an offer on someone else's item). This is
 * distinct from ResourceSpotScreen (the bottom-nav Camera tab), which
 * reports a location, not an item, and never creates a Trade.
 */
export default function ScanScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const context = location.state?.context || 'posting'; // 'posting' | 'bidding'
  const tradeId = location.state?.tradeId;

  const [previewUrl, setPreviewUrl] = useState(null);

  const handleFileSelected = (file) => {
    setPendingCapture(file);
    setPreviewUrl(URL.createObjectURL(file));
  };

  const goToConfirm = () => {
    navigate('/trades/scan/confirm', { state: { context, tradeId } });
  };

  return (
    <div className="h-screen flex flex-col overflow-hidden">
      <Header title={context === 'posting' ? 'Post an Item' : 'Create Offer'} showBell={false} />
      <div className="flex-1 relative min-h-0 pb-16">
        <CameraViewfinder
          previewImage={previewUrl}
          onCapture={goToConfirm}
          onFileSelected={handleFileSelected}
          onFlip={() => {}}
        />
        {previewUrl && (
          <div className="absolute bottom-0 left-0 right-0 px-4 py-3 bg-white border-t border-gray-100">
            <p className="text-sm text-gray-500 text-center">Photo captured. Tap shutter again to continue</p>
          </div>
        )}
      </div>
      <BottomNav />
    </div>
  );
}
</file>

<file path="src/features/trades/components/cards/TradeCard.jsx">
import MaterialTag from '@/shared/components/common/MaterialTag';
import StatusPill from '@/shared/components/common/StatusPill';
import { getTradeStatus } from '@/shared/utils/tradeFormat';

/**
 * Two visual variants:
 * - "browse" (default): used in the Browse tab list — shows distance + poster name
 * - "track": used in the My Trades tab — minimal glance info only
 *   (name, material, weight, status); full detail (quantity, description,
 *   trading-for) lives in TradeDetailScreen behind "View Details".
 */
export default function TradeCard({ trade, variant = 'browse', onClick }) {
  const { image, name, material, distanceKm, posterName, weightKg } = trade;

  return (
    <button
      onClick={onClick}
      className="w-full text-left bg-white rounded-2xl overflow-hidden shadow-sm border border-gray-200"
    >
      <div className="relative aspect-[4/3] bg-gray-100 border-b border-gray-200">
        {image && <img src={image} alt={name} className="w-full h-full object-cover" />}
        {material && (
          <div className="absolute top-3 right-3">
            <MaterialTag material={material} />
          </div>
        )}
      </div>

      <div className="p-4">
        <div className="flex items-start justify-between gap-2">
          <h3 className="font-bold text-gray-900">{name}</h3>
          {weightKg != null && (
            <span className="font-semibold text-gray-900 shrink-0">{weightKg}kg</span>
          )}
        </div>

        {variant === 'browse' ? (
          <div className="flex items-center justify-between mt-1">
            <p className="text-sm text-gray-500">
              {distanceKm != null && `${distanceKm}km`}
              {distanceKm != null && posterName && ' • '}
              {posterName}
            </p>
          </div>
        ) : (
          <div className="mt-2">
            <StatusPill status={getTradeStatus(trade)} />
          </div>
        )}
      </div>
    </button>
  );
}
</file>

<file path="src/features/trades/components/trade/EcoImpactBox.jsx">
import { Leaf } from 'lucide-react';

export default function EcoImpactBox({ weightKg }) {
  if (!weightKg) return null;
  // The message always mirrors the item's own weight exactly — no estimate
  // or multiplier — and simply doesn't render at all when there's no weight.
  const pollutionKg = Number(weightKg);

  return (
    <div className="flex items-start gap-3 bg-green-200 rounded-2xl p-4">
      <div className="shrink-0 w-11 h-11 rounded-full bg-green-100 flex items-center justify-center relative">
        <Leaf size={22} className="absolute text-green-300 rotate-[20deg] translate-x-1" />
        <Leaf size={19} className="relative text-green-600 -translate-x-0.5 translate-y-0.5" />
      </div>
      <div>
        <p className="font-semibold text-gray-900 text-sm">Eco-Impact</p>
        <p className="text-sm text-gray-800 mt-0.5">
          Trading this material prevents ~{pollutionKg}kg of pollution from new production.
        </p>
      </div>
    </div>
  );
}
</file>

<file path="src/features/trades/components/trade/FilterChipRow.jsx">
export default function FilterChipRow({
  options = ['All Items', 'Wood', 'Metal', 'Plastic', 'Fabric', 'Paper', 'E-waste', 'Mixed'],
  active,
  onSelect,
}) {
  return (
    <div className="flex gap-2 overflow-x-auto no-scrollbar px-4 -mx-4 pb-1">
      {options.map((opt) => {
        const isActive = opt === active;
        return (
          <button
            key={opt}
            onClick={() => onSelect?.(opt)}
            className={`shrink-0 px-4 py-2 rounded-full text-sm font-medium whitespace-nowrap transition-colors ${
              isActive ? 'bg-brand-600 text-white' : 'bg-gray-100 text-gray-700'
            }`}
          >
            {opt}
          </button>
        );
      })}
    </div>
  );
}
</file>

<file path="src/features/trades/components/trade/MessageThread.jsx">
import { useState } from 'react';
import { Send } from 'lucide-react';

/**
 * Simple message thread — UI shell only.
 * Real-time send/receive logic belongs to Integration Lead ("Future chat integration").
 * This component just needs `messages` (array of { id, text, fromMe }) and an onSend callback.
 */
export default function MessageThread({ messages = [], onSend }) {
  const [draft, setDraft] = useState('');

  const handleSend = () => {
    if (!draft.trim()) return;
    onSend?.(draft.trim());
    setDraft('');
  };

  return (
    <div className="flex flex-col h-full">
      <div className="flex-1 overflow-y-auto px-4 py-4 space-y-3">
        {messages.map((m) => (
          <div key={m.id} className={`flex ${m.fromMe ? 'justify-end' : 'justify-start'}`}>
            <div
              className={`max-w-[75%] px-4 py-2.5 rounded-2xl text-sm ${
                m.fromMe ? 'bg-brand-600 text-white rounded-br-sm' : 'bg-gray-100 text-gray-800 rounded-bl-sm'
              }`}
            >
              {m.text}
            </div>
          </div>
        ))}
        {messages.length === 0 && (
          <p className="text-center text-sm text-gray-400 mt-10">Say hello to start the conversation.</p>
        )}
      </div>

      <div className="flex items-center gap-2 px-4 py-3 border-t border-gray-100 bg-white sticky bottom-0">
        <input
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          onKeyDown={(e) => e.key === 'Enter' && handleSend()}
          placeholder="Message"
          className="flex-1 bg-gray-100 rounded-full px-4 py-2.5 text-sm outline-none"
        />
        <button
          onClick={handleSend}
          className="w-10 h-10 rounded-full bg-brand-600 text-white flex items-center justify-center shrink-0 active:scale-95 transition-transform"
          aria-label="Send"
        >
          <Send size={17} />
        </button>
      </div>
    </div>
  );
}
</file>

<file path="src/features/trades/screens/EditListingScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import { fetchTradeById, updateTrade } from '@/shared/services/api';

const inputClass =
  'w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand-500';
const labelClass = 'text-xs font-semibold text-gray-500 mb-1.5 block';

const TRADE_FOR_OPTIONS = [
  { key: 'specific', label: 'Specific item' },
  { key: 'nothing', label: 'Nothing' },
  { key: 'negotiating', label: 'Negotiable' },
];

export default function EditListingScreen() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [trade, setTrade] = useState(null);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');

  const [name, setName] = useState('');
  const [description, setDescription] = useState('');
  const [weightKg, setWeightKg] = useState('');
  const [quantity, setQuantity] = useState('');
  const [tradingForType, setTradingForType] = useState('negotiating');
  const [tradingForValue, setTradingForValue] = useState('');

  useEffect(() => {
    fetchTradeById(id)
      .then((data) => {
        setTrade(data);
        setName(data.name || '');
        setDescription(data.description || '');
        setWeightKg(data.weightKg ?? '');
        setQuantity(data.quantity ?? '');
        setTradingForType(data.tradingFor?.type || 'negotiating');
        setTradingForValue(data.tradingFor?.value || '');
      })
      .catch(() => setTrade(null))
      .finally(() => setLoading(false));
  }, [id]);

  if (loading) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Edit Listing" />
        <p className="text-center text-sm text-gray-400 py-16">Loading...</p>
      </div>
    );
  }

  if (!trade) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Edit Listing" />
        <p className="text-center text-sm text-gray-400 py-16">Listing not found.</p>
      </div>
    );
  }

  const canSave = name.trim().length > 0 && Number(weightKg) > 0 && Number(quantity) > 0 && !saving;

  const handleSave = async () => {
    if (!canSave) return;
    setSaving(true);
    setError('');
    try {
      await updateTrade(trade.id, {
        itemName: name.trim(),
        description: description.trim(),
        weightKg: Number(weightKg),
        quantity: Number(quantity),
        tradingForType,
        tradingForValue: tradingForType === 'specific' ? tradingForValue.trim() : undefined,
      });
      navigate(`/trades/${trade.id}?owner=1`, { replace: true });
    } catch (err) {
      setError(err.message || 'Could not save changes.');
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Edit Listing" />

      <div className="p-5 space-y-5">
        <div>
          <label className={labelClass}>Item name</label>
          <input className={inputClass} value={name} onChange={(e) => setName(e.target.value)} />
        </div>

        <div>
          <label className={labelClass}>Caption</label>
          <textarea
            className={`${inputClass} min-h-[80px] resize-none`}
            value={description}
            onChange={(e) => setDescription(e.target.value)}
          />
        </div>

        <div className="grid grid-cols-2 gap-3">
          <div>
            <label className={labelClass}>Weight (kg)</label>
            <input
              type="number"
              className={inputClass}
              value={weightKg}
              onChange={(e) => setWeightKg(e.target.value)}
            />
          </div>
          <div>
            <label className={labelClass}>Quantity</label>
            <input
              type="number"
              className={inputClass}
              value={quantity}
              onChange={(e) => setQuantity(e.target.value)}
            />
          </div>
        </div>

        <div>
          <label className={labelClass}>Trade for</label>
          <div className="grid grid-cols-3 gap-2 mb-2">
            {TRADE_FOR_OPTIONS.map((opt) => (
              <button
                key={opt.key}
                type="button"
                onClick={() => setTradingForType(opt.key)}
                className={`py-2 rounded-lg text-xs font-medium ${
                  tradingForType === opt.key ? 'bg-brand-600 text-white' : 'bg-gray-100 text-gray-600'
                }`}
              >
                {opt.label}
              </button>
            ))}
          </div>
          {tradingForType === 'specific' && (
            <input
              className={inputClass}
              value={tradingForValue}
              onChange={(e) => setTradingForValue(e.target.value)}
              placeholder="e.g. Paper or old magazines"
            />
          )}
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton onClick={handleSave} disabled={!canSave}>
          {saving ? 'Saving...' : 'Save Changes'}
        </PrimaryButton>
      </div>
    </div>
  );
}
</file>

<file path="src/features/trades/screens/MessageThreadScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import MessageThread from '@/features/trades/components/trade/MessageThread';
import { fetchTradeById } from '@/shared/services/api';

export default function MessageThreadScreen() {
  const navigate = useNavigate();
  const { id } = useParams();
  const [trade, setTrade] = useState(null);

  useEffect(() => {
    fetchTradeById(id).then(setTrade).catch(() => setTrade(null));
  }, [id]);

  // Placeholder local state — real-time sync is Integration Lead's job.
  const [messages, setMessages] = useState([
    { id: 1, text: 'Hi! Is this still available?', fromMe: false },
    { id: 2, text: 'Yes, still open for trade!', fromMe: true },
  ]);

  const handleSend = (text) => {
    setMessages((prev) => [...prev, { id: prev.length + 1, text, fromMe: true }]);
  };

  return (
    <div className="flex flex-col h-screen">
      <Header onBack={() => navigate(-1)} title={trade?.posterName || 'Conversation'} showBell={false} />
      <div className="flex-1 overflow-hidden">
        <MessageThread messages={messages} onSend={handleSend} />
      </div>
    </div>
  );
}
</file>

<file path="src/features/trades/screens/OfferSentScreen.jsx">
import { useEffect, useState } from 'react';
import { useLocation, useNavigate, useParams } from 'react-router-dom';
import ConfirmationScreen from '@/shared/components/common/ConfirmationScreen';
import StatusPill from '@/shared/components/common/StatusPill';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';
import { fetchTradeById } from '@/shared/services/api';

export default function OfferSentScreen() {
  const navigate = useNavigate();
  const { id } = useParams();
  const location = useLocation();
  const offer = location.state?.offer;
  const [trade, setTrade] = useState(null);

  useEffect(() => {
    fetchTradeById(id).then(setTrade).catch(() => setTrade(null));
  }, [id]);

  return (
    <ConfirmationScreen
      heading="Offer Sent!"
      subtext={`${trade?.posterName || 'The poster'} will be notified about your offer. You'll get an update here once they respond.`}
      summaryCard={
        <div>
          <p className="text-xs text-gray-400 font-semibold uppercase mb-3">Your Offer</p>
          <div className="flex items-center gap-3">
            <div className="w-16 h-16 rounded-xl bg-gray-100 border border-gray-200 shrink-0 overflow-hidden">
              {offer?.image && (
                <img src={offer.image} alt={offer.itemName} className="w-full h-full object-cover" />
              )}
            </div>
            <div className="min-w-0">
              <p className="font-bold text-gray-900 truncate">{offer?.itemName || offer?.material || 'Your item'}</p>
              {offer?.weightKg && <p className="text-sm text-gray-500 mt-0.5">{offer.weightKg}kg weight</p>}
            </div>
          </div>
          {offer?.description && (
            <p className="text-sm text-gray-500 italic mt-3">&ldquo;{offer.description}&rdquo;</p>
          )}
          {offer?.weightKg && (
            <div className="mt-3">
              <EcoImpactBox weightKg={Number(offer.weightKg)} />
            </div>
          )}
          <div className="border-t border-gray-100 mt-3 pt-3 flex items-center justify-between">
            <span className="text-sm text-gray-400">Status</span>
            <StatusPill status="Pending" />
          </div>
        </div>
      }
      primaryAction={{
        label: 'View Offer History',
        onClick: () => navigate('/trades?tab=offers'),
      }}
      secondaryAction={{
        label: 'Back to Browse',
        onClick: () => navigate('/trades?tab=browse'),
      }}
    />
  );
}
</file>

<file path="src/features/trades/screens/RateTraderScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import StarRating from '@/shared/components/common/StarRating';
import { fetchTradeById, submitRating } from '@/shared/services/api';

export default function RateTraderScreen() {
  const navigate = useNavigate();
  const { tradeId } = useParams();
  const [trade, setTrade] = useState(null);
  const [rating, setRating] = useState(0);
  const [comment, setComment] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState('');

  useEffect(() => {
    fetchTradeById(tradeId).then(setTrade).catch(() => setTrade(null));
  }, [tradeId]);

  const handleSubmit = async () => {
    if (rating === 0 || submitting) return;
    setSubmitting(true);
    setError('');
    try {
      await submitRating(tradeId, rating, comment.trim() || undefined);
      navigate('/trades');
    } catch (err) {
      setError(err.message || 'Could not submit rating.');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Rate Trader" showBell={false} />

      <div className="px-5 pt-6 space-y-6 text-center">
        <h1 className="text-xl font-bold text-gray-900">
          Rate your trade with {trade?.posterName || 'the trader'}
        </h1>

        <div className="bg-white border border-gray-100 rounded-2xl p-4 flex items-center gap-3 text-left">
          <div className="w-14 h-14 rounded-lg bg-gray-100 shrink-0" />
          <div>
            <p className="font-semibold text-gray-900">{trade?.name || 'Trade item'}</p>
            <p className="text-xs text-gray-400">Completed trade</p>
          </div>
        </div>

        <StarRating value={rating} onChange={setRating} />

        <textarea
          value={comment}
          onChange={(e) => setComment(e.target.value)}
          placeholder="Leave a note about this trade (optional)"
          className="w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none min-h-[90px] resize-none text-left"
        />

        {error && <p className="text-sm text-red-500 text-left">{error}</p>}

        <div className="space-y-3 pt-2">
          <PrimaryButton disabled={rating === 0 || submitting} onClick={handleSubmit}>
            {submitting ? 'Submitting...' : 'Submit Rating'}
          </PrimaryButton>
          <button onClick={() => navigate('/trades')} className="text-sm text-gray-500 font-medium">
            Skip for now
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/features/trades/screens/TradeCreatedScreen.jsx">
import { useLocation, useNavigate } from 'react-router-dom';
import ConfirmationScreen from '@/shared/components/common/ConfirmationScreen';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';

const TRADING_FOR_LABELS = {
  specific: (tf) => tf.value || 'a specific item',
  nothing: () => 'Nothing (free item)',
  negotiating: () => 'Open for negotiating',
};

export default function TradeCreatedScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const trade = location.state?.trade;

  return (
    <ConfirmationScreen
      heading="Trade Created!"
      subtext="Your item is now live. Interested traders can view your item picture, details, and send you an offer."
      summaryCard={
        trade && (
          <div>
            <p className="font-bold text-gray-900">{trade.name || trade.material}</p>
            <p className="text-sm text-gray-500 mt-1">{trade.weightKg}kg • {trade.category}</p>
            <p className="text-sm text-gray-500 mt-1">
              Trade for:{' '}
              {(TRADING_FOR_LABELS[trade.tradingFor?.type] || TRADING_FOR_LABELS.negotiating)(trade.tradingFor || {})}
            </p>
            {trade.description && (
              <p className="text-sm text-gray-500 italic mt-2">&ldquo;{trade.description}&rdquo;</p>
            )}
            <div className="mt-3">
              <EcoImpactBox weightKg={Number(trade.weightKg)} />
            </div>
          </div>
        )
      }
      primaryAction={{
        label: 'View in My Trades',
        onClick: () => navigate('/trades?tab=mine'),
      }}
      secondaryAction={{
        label: 'Back to Browse',
        onClick: () => navigate('/trades?tab=browse'),
      }}
    />
  );
}
</file>

<file path="src/features/trades/screens/TradeDetailScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams, useSearchParams } from 'react-router-dom';
import { MessageCircle, MoreVertical } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import MaterialTag from '@/shared/components/common/MaterialTag';
import StatusPill from '@/shared/components/common/StatusPill';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';
import { formatTradingFor, getTradeStatus } from '@/shared/utils/tradeFormat';
import { fetchTradeById, fetchOffersForTrade, acceptOffer, declineOffer } from '@/shared/services/api';

export default function TradeDetailScreen() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  // TradesScreen links to My Trades items with ?owner=1 — derive isOwner from
  // that instead of a prop, since no route ever actually passed one in.
  const isOwner = searchParams.get('owner') === '1';

  const [trade, setTrade] = useState(null);
  const [offers, setOffers] = useState([]);
  const [loading, setLoading] = useState(true);
  const [notFound, setNotFound] = useState(false);
  const [error, setError] = useState('');
  const [decidingOfferId, setDecidingOfferId] = useState(null);

  const loadTrade = () => {
    setLoading(true);
    setNotFound(false);
    setError('');
    const requests = [fetchTradeById(id)];
    if (isOwner) requests.push(fetchOffersForTrade(id));

    Promise.all(requests)
      .then(([tradeData, offersData]) => {
        setTrade(tradeData);
        if (isOwner) setOffers(offersData || []);
      })
      .catch((err) => {
        if (err.message?.toLowerCase().includes('not found')) setNotFound(true);
        else setError(err.message || 'Could not load this trade.');
      })
      .finally(() => setLoading(false));
  };

  useEffect(() => {
    loadTrade();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [id, isOwner]);

  const decide = async (offerId, action) => {
    if (decidingOfferId) return;
    setDecidingOfferId(offerId);
    setError('');
    try {
      if (action === 'accept') await acceptOffer(offerId);
      else await declineOffer(offerId);
      loadTrade();
    } catch (err) {
      setError(err.message || 'Could not update this offer.');
    } finally {
      setDecidingOfferId(null);
    }
  };

  if (loading) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Items" />
        <p className="text-center text-sm text-gray-400 py-16">Loading...</p>
      </div>
    );
  }

  if (notFound || !trade) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Items" />
        <p className="text-center text-sm text-gray-400 py-16">This trade could not be found.</p>
      </div>
    );
  }

  const itemInfo = (
    <>
      <div className="h-44 bg-gray-100 border-b border-gray-200">
        {trade.image && <img src={trade.image} alt={trade.name} className="w-full h-full object-cover" />}
        <div className="absolute top-3 right-3">
          <MaterialTag material={trade.material} />
        </div>
      </div>

      <div className="p-5 space-y-5">
        <div>
          <div className="flex items-start justify-between gap-2">
            <h1 className="text-2xl font-bold text-gray-900">{trade.name}</h1>
            {isOwner && <StatusPill status={getTradeStatus(trade)} />}
          </div>
          <p className="text-sm text-gray-500 mt-1">
            Posted by {trade.posterName}
            {trade.distanceKm != null && ` • ${trade.distanceKm}km`}
          </p>
          {trade.location && <p className="text-sm text-gray-500 mt-0.5">📍 {trade.location}</p>}
        </div>

        <div className="flex items-center justify-between text-sm">
          <div className="flex gap-6">
            <span><span className="font-semibold">{trade.weightKg}kg</span> weight</span>
            <span><span className="font-semibold">{trade.quantity}</span> quantity</span>
          </div>
          {isOwner && (
            <button
              onClick={() => navigate(`/trades/${trade.id}/edit`)}
              aria-label="Edit Listing"
              className="p-1 -mr-1 text-gray-400 active:scale-95 transition-transform"
            >
              <MoreVertical size={20} />
            </button>
          )}
        </div>

        {trade.description && (
          <p className="text-sm text-gray-600 italic">&ldquo;{trade.description}&rdquo;</p>
        )}

        <EcoImpactBox weightKg={trade.weightKg} />

        <div>
          <p className="text-sm text-gray-400 mb-1">Trade for:</p>
          <p className="font-semibold text-gray-900">{formatTradingFor(trade.tradingFor)}</p>
        </div>

        {/* Per docs: "user accepts offer > a chat is started" — for the owner,
            each accepted offer below already has its own "Open Chat" button,
            so this generic shortcut is just for the non-owner side. */}
        {trade.offerAccepted && !isOwner && (
          <button
            onClick={() => navigate(`/trades/${trade.id}/messages`)}
            className="flex items-center gap-2 text-brand-600 font-semibold text-sm"
          >
            <MessageCircle size={18} />
            Message
          </button>
        )}

        {!isOwner && (
          <div className="space-y-3 pt-2">
            {trade.tradingFor?.type === 'nothing' ? (
              // Free item — nothing to negotiate, so skip the offer flow entirely
              // and go straight to messaging the poster to arrange pickup.
              <PrimaryButton onClick={() => navigate(`/trades/${trade.id}/messages`)}>Message</PrimaryButton>
            ) : trade.status !== 'open' ? (
              <p className="text-center text-sm text-gray-400 py-2">
                This trade is no longer accepting offers.
              </p>
            ) : (
              <PrimaryButton
                onClick={() => navigate('/trades/scan', { state: { context: 'bidding', tradeId: trade.id } })}
              >
                Create Offer
              </PrimaryButton>
            )}
          </div>
        )}
      </div>
    </>
  );

  // Non-owner: no Received Offers section, so the page just scrolls normally.
  if (!isOwner) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Items" />
        <div className="relative">{itemInfo}</div>
      </div>
    );
  }

  // Owner: item info stays fixed in place; only Received Offers scrolls.
  return (
    <div className="h-screen flex flex-col overflow-hidden">
      <Header onBack={() => navigate(-1)} title="Items" />
      <div className="shrink-0 relative">{itemInfo}</div>

      <div className="flex-1 overflow-y-auto min-h-0 border-t border-gray-100 px-5 py-5">
        <h2 className="font-bold text-gray-900 mb-3">Received Offers</h2>
        {error && <p className="text-sm text-red-500 mb-3">{error}</p>}
        {offers.length === 0 ? (
          <p className="text-center text-sm text-gray-400 py-8">No offers received yet.</p>
        ) : (
          <div className="space-y-3">
            {offers.map((offer) => (
              <div key={offer.id} className="bg-white border border-gray-200 rounded-2xl p-4 space-y-3">
                <div className="flex items-start justify-between gap-2">
                  <div>
                    <p className="font-bold text-gray-900">{offer.offererName}</p>
                    <p className="text-xs text-gray-400">{new Date(offer.createdAt).toLocaleDateString()}</p>
                  </div>
                  <StatusPill status={offer.status} />
                </div>

                <div className="flex items-center gap-3 bg-gray-50 rounded-xl px-3 py-2">
                  <div className="w-12 h-12 rounded-lg bg-gray-100 border border-gray-200 shrink-0 overflow-hidden">
                    {offer.image && (
                      <img src={offer.image} alt={offer.itemName} className="w-full h-full object-cover" />
                    )}
                  </div>
                  <div className="flex-1 min-w-0">
                    <p className="text-sm font-semibold text-gray-900 truncate">{offer.itemName}</p>
                    <p className="text-xs text-gray-500">{offer.weightKg}kg</p>
                  </div>
                  <MaterialTag material={offer.material} />
                </div>

                {offer.status === 'pending' && (
                  <div className="space-y-2 pt-1">
                    <PrimaryButton
                      onClick={() => decide(offer.id, 'accept')}
                      disabled={decidingOfferId === offer.id}
                    >
                      {decidingOfferId === offer.id ? 'Accepting...' : 'Accept Offer'}
                    </PrimaryButton>
                    <button
                      onClick={() => decide(offer.id, 'decline')}
                      disabled={decidingOfferId === offer.id}
                      className="w-full text-center text-sm text-gray-500 font-medium disabled:opacity-50"
                    >
                      Decline
                    </button>
                  </div>
                )}

                {offer.status === 'accepted' && (
                  <PrimaryButton onClick={() => navigate(`/trades/${trade.id}/messages`)}>
                    <span className="flex items-center justify-center gap-2">
                      <MessageCircle size={18} />
                      Open Chat
                    </span>
                  </PrimaryButton>
                )}
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/features/trades/screens/TradesScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { ChevronRight, MessageCircle } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import SearchBar from '@/shared/components/common/SearchBar';
import FloatingActionButton from '@/shared/components/common/FloatingActionButton';
import FilterChipRow from '@/features/trades/components/trade/FilterChipRow';
import TradeCard from '@/features/trades/components/cards/TradeCard';
import StatusPill from '@/shared/components/common/StatusPill';
import { fetchBrowseTrades, fetchMyTrades, fetchMyOffers } from '@/shared/services/api';

const SUB_TABS = [
  { key: 'browse', label: 'Browse' },
  { key: 'mine', label: 'My Trades' },
  { key: 'offers', label: 'Offer History' },
];

export default function TradesScreen() {
  const navigate = useNavigate();
  // Tab state lives in the URL (not local state) so the browser Back button
  // returns to the tab you were actually on, instead of always resetting to
  // Browse — switching tabs uses replace so it doesn't spam browser history.
  const [searchParams, setSearchParams] = useSearchParams();
  const rawTab = searchParams.get('tab');
  const subTab = rawTab === 'mine' || rawTab === 'offers' ? rawTab : 'browse';
  const [search, setSearch] = useState('');
  const [filter, setFilter] = useState('All Items');

  const [browseTrades, setBrowseTrades] = useState([]);
  const [myTrades, setMyTrades] = useState([]);
  const [offerHistory, setOfferHistory] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setError('');
    Promise.all([fetchBrowseTrades(), fetchMyTrades(), fetchMyOffers()])
      .then(([browse, mine, offers]) => {
        if (cancelled) return;
        setBrowseTrades(browse);
        setMyTrades(mine);
        setOfferHistory(offers);
      })
      .catch((err) => {
        if (!cancelled) setError(err.message || 'Could not load trades.');
      })
      .finally(() => {
        if (!cancelled) setLoading(false);
      });
    return () => {
      cancelled = true;
    };
  }, []);

  const setSubTab = (tab) => {
    const next = new URLSearchParams(searchParams);
    next.set('tab', tab);
    setSearchParams(next, { replace: true });
  };

  const goToCreateTrade = () => navigate('/trades/scan', { state: { context: 'posting' } });

  const filteredBrowse = browseTrades.filter((t) => {
    const matchesFilter = filter === 'All Items' || t.material === filter;
    const matchesSearch = t.name.toLowerCase().includes(search.toLowerCase());
    return matchesFilter && matchesSearch;
  });

  return (
    <div className="pb-40">
      <Header />

      {/* Segmented tabs: Browse (all available trades), My Trades (own posts), Offer History (offers you sent) */}
      <div className="px-4 pt-4">
        <div className="flex bg-gray-100 rounded-xl p-1">
          {SUB_TABS.map((t) => (
            <button
              key={t.key}
              onClick={() => setSubTab(t.key)}
              className={`flex-1 py-2 rounded-lg text-xs font-semibold transition-colors ${
                subTab === t.key ? 'bg-white text-gray-900 shadow-sm' : 'text-gray-500'
              }`}
            >
              {t.label}
            </button>
          ))}
        </div>
      </div>

      {loading && <p className="text-center text-sm text-gray-400 py-10">Loading...</p>}
      {!loading && error && <p className="text-center text-sm text-red-500 py-10">{error}</p>}

      {!loading && !error && subTab === 'browse' && (
        <div className="px-4 pt-4 space-y-4">
          <SearchBar value={search} onChange={setSearch} />
          <FilterChipRow active={filter} onSelect={setFilter} />

          <div className="flex items-center justify-between text-sm">
            <span className="text-gray-500">{filteredBrowse.length} items nearby</span>
            <button className="text-gray-500 font-medium">Sort: Nearest ▾</button>
          </div>

          <div className="space-y-4">
            {filteredBrowse.map((trade) => (
              <TradeCard key={trade.id} trade={trade} onClick={() => navigate(`/trades/${trade.id}`)} />
            ))}
            {filteredBrowse.length === 0 && (
              <p className="text-center text-sm text-gray-400 py-10">No items match your search.</p>
            )}
          </div>
        </div>
      )}

      {!loading && !error && subTab === 'mine' && (
        <div className="px-4 pt-4 space-y-4">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-xl font-bold text-gray-900">My Trades</h1>
              <p className="text-sm text-gray-500 mt-1 max-w-[280px]">
                Manage your active contributions and review your ecological trade history.
              </p>
            </div>
            <span className="shrink-0 px-3 py-1 rounded-full bg-brand-100 text-brand-700 text-xs font-semibold">
              {myTrades.length} Active
            </span>
          </div>

          {myTrades.length > 0 ? (
            <div className="space-y-4">
              {myTrades.map((trade) => (
                <div key={trade.id} className="space-y-2">
                  <TradeCard trade={trade} variant="track" onClick={() => navigate(`/trades/${trade.id}?owner=1`)} />
                  <button
                    onClick={() => navigate(`/trades/${trade.id}?owner=1`)}
                    className="w-full py-3 rounded-xl bg-gray-100 text-gray-700 font-semibold text-sm"
                  >
                    View Details
                  </button>
                </div>
              ))}
            </div>
          ) : (
            <div className="text-center py-16">
              <p className="text-gray-400 text-sm mb-4">No active trades yet.</p>
              <button onClick={goToCreateTrade} className="text-brand-600 font-semibold text-sm">
                Post your first item →
              </button>
            </div>
          )}
        </div>
      )}

      {!loading && !error && subTab === 'offers' && (
        <div className="px-4 pt-4 space-y-4">
          <div>
            <h1 className="text-xl font-bold text-gray-900">Offer History</h1>
            <p className="text-sm text-gray-500 mt-1">
              Tap an offer to view what you sent.{' '}
              {offerHistory.some((o) => o.status === 'accepted') && 'Accepted offers open the chat.'}
            </p>
          </div>

          {offerHistory.length > 0 ? (
            <div className="space-y-2">
              {offerHistory.map((offer) => {
                const accepted = offer.status === 'accepted';
                return (
                  <button
                    key={offer.id}
                    onClick={() => navigate(`/trades/${offer.tradeId}/my-offer`)}
                    className="w-full flex items-center justify-between bg-white border border-gray-100 rounded-xl px-4 py-3 active:scale-[0.98] transition-transform"
                  >
                    <span className="text-sm font-medium text-gray-700">{offer.tradeName}</span>
                    <span className="flex items-center gap-2">
                      {accepted ? (
                        <>
                          <span className="text-xs font-semibold text-brand-600">Accepted</span>
                          <MessageCircle size={16} className="text-brand-600" />
                        </>
                      ) : (
                        <>
                          <StatusPill status={offer.status} />
                          <ChevronRight size={16} className="text-gray-300" />
                        </>
                      )}
                    </span>
                  </button>
                );
              })}
            </div>
          ) : (
            <p className="text-center text-sm text-gray-400 py-10">You haven&apos;t sent any offers yet.</p>
          )}
        </div>
      )}

      <FloatingActionButton label="Post a trade" onClick={goToCreateTrade} />
      <BottomNav />
    </div>
  );
}
</file>

<file path="src/features/trades/screens/ViewOfferScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { MessageCircle } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import MaterialTag from '@/shared/components/common/MaterialTag';
import StatusPill from '@/shared/components/common/StatusPill';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';
import { fetchTradeById, fetchMyOffers } from '@/shared/services/api';

/**
 * Read-only record of an offer you already sent — reached from Offer History.
 * Not the create-offer flow: you already offered, this just shows what and
 * lets you jump to the chat once it's been accepted.
 */
export default function ViewOfferScreen() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [trade, setTrade] = useState(null);
  const [offer, setOffer] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    Promise.all([fetchTradeById(id).catch(() => null), fetchMyOffers()])
      .then(([tradeData, offers]) => {
        setTrade(tradeData);
        setOffer(offers.find((o) => o.tradeId === id) || null);
      })
      .finally(() => setLoading(false));
  }, [id]);

  if (loading) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Your Offer" />
        <p className="text-center text-sm text-gray-400 py-16">Loading...</p>
      </div>
    );
  }

  if (!offer) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Your Offer" />
        <p className="text-center text-sm text-gray-400 py-16">Offer not found.</p>
      </div>
    );
  }

  const accepted = offer.status === 'accepted';

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Your Offer" />

      <div className="p-5 space-y-5">
        <div>
          <p className="text-xs text-gray-400 font-semibold uppercase mb-1">Offer on</p>
          <p className="font-bold text-gray-900">{offer.tradeName}</p>
          {trade?.posterName && <p className="text-sm text-gray-500 mt-0.5">Posted by {trade.posterName}</p>}
        </div>

        <div className="border-t border-gray-100 pt-5">
          <p className="text-xs text-gray-400 font-semibold uppercase mb-3">You offered</p>
          <div className="flex items-center gap-3">
            <div className="w-16 h-16 rounded-xl bg-gray-100 border border-gray-200 shrink-0 overflow-hidden">
              {offer.image && (
                <img src={offer.image} alt={offer.itemName} className="w-full h-full object-cover" />
              )}
            </div>
            <div className="min-w-0 flex-1">
              <p className="font-bold text-gray-900 truncate">{offer.itemName}</p>
              <div className="flex items-center gap-2 mt-1">
                {offer.weightKg && <span className="text-sm text-gray-500">{offer.weightKg}kg</span>}
                {offer.material && <MaterialTag material={offer.material} />}
              </div>
            </div>
          </div>
          {offer.description && (
            <p className="text-sm text-gray-500 italic mt-3">&ldquo;{offer.description}&rdquo;</p>
          )}
          {offer.weightKg && (
            <div className="mt-3">
              <EcoImpactBox weightKg={Number(offer.weightKg)} />
            </div>
          )}
        </div>

        <div className="border-t border-gray-100 pt-5 flex items-center justify-between">
          <span className="text-sm text-gray-400">Status</span>
          <StatusPill status={offer.status} />
        </div>

        {accepted ? (
          <PrimaryButton onClick={() => navigate(`/trades/${offer.tradeId}/messages`)}>
            <span className="flex items-center justify-center gap-2">
              <MessageCircle size={18} />
              Open Chat
            </span>
          </PrimaryButton>
        ) : (
          <p className="text-center text-sm text-gray-400">
            Waiting for {trade?.posterName || 'the poster'} to respond.
          </p>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/shared/components/common/ConfirmationScreen.jsx">
import { Check } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';

/**
 * Shared layout used by: Trade Created, Offer Sent (and could be reused
 * for other "success" moments later, e.g. a future report-submitted screen).
 *
 * summaryCard: optional ReactNode rendered inside a bordered card
 * primaryAction: { label, onClick }
 * secondaryAction: { label, onClick }
 */
export default function ConfirmationScreen({
  heading,
  subtext,
  summaryCard,
  primaryAction,
  secondaryAction,
}) {
  return (
    <div className="pb-28">
      <Header showBell={false} />
      <div className="px-5 pt-10 flex flex-col items-center text-center">
        <div className="w-20 h-20 rounded-full bg-brand-100 flex items-center justify-center mb-5">
          <div className="w-14 h-14 rounded-full bg-brand-600 flex items-center justify-center">
            <Check size={28} className="text-white" strokeWidth={3} />
          </div>
        </div>
        <h1 className="text-2xl font-bold text-gray-900 mb-2">{heading}</h1>
        {subtext && <p className="text-sm text-gray-500 max-w-xs">{subtext}</p>}

        {summaryCard && (
          <div className="w-full mt-8 border border-gray-200 rounded-2xl p-4 text-left">
            {summaryCard}
          </div>
        )}

        <div className="w-full mt-8 space-y-3">
          {primaryAction && (
            <PrimaryButton onClick={primaryAction.onClick}>{primaryAction.label}</PrimaryButton>
          )}
          {secondaryAction && (
            <button onClick={secondaryAction.onClick} className="text-sm text-gray-500 font-medium">
              {secondaryAction.label}
            </button>
          )}
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/shared/components/common/FloatingActionButton.jsx">
import { Plus } from 'lucide-react';

export default function FloatingActionButton({ onClick, label = 'Add' }) {
  return (
    <button
      onClick={onClick}
      aria-label={label}
      className="fixed z-30 bottom-24 w-14 h-14 rounded-full bg-brand-600 text-white shadow-lg flex items-center justify-center active:scale-95 transition-transform hover:bg-brand-700"
      style={{ right: 'max(1.25rem, calc(50% - 240px + 1.25rem))' }}
    >
      <Plus size={26} />
    </button>
  );
}
</file>

<file path="src/shared/components/common/Logo.jsx">
import logoIcon from '@/assets/logo.svg';

/**
 * Brand lockup. `iconOnly` shows just the mark (used on auth screens);
 * default shows "RosaCycle" text + mark together (used in the app header).
 */
const SIZES = {
  sm: { icon: 26, text: 'text-lg' },
  md: { icon: 34, text: 'text-2xl' },
  lg: { icon: 64, text: 'text-4xl' },
};

export default function Logo({ size = 'md', iconOnly = false, className = '' }) {
  const { icon, text } = SIZES[size] || SIZES.md;

  if (iconOnly) {
    return <img src={logoIcon} alt="RosaCycle" width={icon} height={icon} className={className} />;
  }

  return (
    <div className={`flex items-center gap-1 ${className}`}>
      <span className={`font-extrabold tracking-tight ${text}`} style={{ color: '#245f28' }}>
        RosaCycle
      </span>
      <img src={logoIcon} alt="" width={icon} height={icon} className="shrink-0" />
    </div>
  );
}
</file>

<file path="src/shared/components/common/MaterialTag.jsx">
const COLORS = {
  wood: 'bg-amber-100 text-amber-800',
  metal: 'bg-slate-200 text-slate-700',
  plastic: 'bg-blue-100 text-blue-700',
  fabric: 'bg-pink-100 text-pink-700',
  paper: 'bg-yellow-100 text-yellow-800',
  'e-waste': 'bg-indigo-100 text-indigo-700',
  mixed: 'bg-brand-100 text-brand-700',
  organic: 'bg-brand-100 text-brand-700',
};

export default function MaterialTag({ material, className = '' }) {
  const key = (material || '').toLowerCase();
  const style = COLORS[key] || 'bg-gray-100 text-gray-700';
  return (
    <span className={`inline-block px-3 py-1 rounded-full text-xs font-medium capitalize ${style} ${className}`}>
      {material}
    </span>
  );
}
</file>

<file path="src/shared/components/common/PasswordInput.jsx">
import { useState } from 'react';
import { Eye, EyeOff } from 'lucide-react';

/**
 * Password field with a show/hide toggle. Hidden by default (closed eye).
 */
export default function PasswordInput({ value, onChange, placeholder, className = '' }) {
  const [visible, setVisible] = useState(false);

  return (
    <div className="relative">
      <input
        type={visible ? 'text' : 'password'}
        value={value}
        onChange={onChange}
        placeholder={placeholder}
        className={`w-full pr-11 px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none ${className}`}
      />
      <button
        type="button"
        onClick={() => setVisible((v) => !v)}
        aria-label={visible ? 'Hide password' : 'Show password'}
        className="absolute right-3.5 top-1/2 -translate-y-1/2 text-gray-400 active:scale-90 transition-transform"
      >
        {visible ? <Eye size={19} /> : <EyeOff size={19} />}
      </button>
    </div>
  );
}
</file>

<file path="src/shared/components/common/PrimaryButton.jsx">
export default function PrimaryButton({ children, onClick, disabled = false, type = 'button', className = '' }) {
  return (
    <button
      type={type}
      onClick={onClick}
      disabled={disabled}
      className={`w-full py-3.5 rounded-xl font-semibold text-white transition-colors active:scale-[0.98] ${
        disabled ? 'bg-gray-300 cursor-not-allowed' : 'bg-brand-600 hover:bg-brand-700'
      } ${className}`}
    >
      {children}
    </button>
  );
}
</file>

<file path="src/shared/components/common/SearchBar.jsx">
import { Search } from 'lucide-react';

export default function SearchBar({ value, onChange, placeholder = 'Search materials' }) {
  return (
    <div className="flex items-center gap-2 bg-gray-200/70 rounded-xl px-4 py-3">
      <Search size={18} className="text-gray-500 shrink-0" />
      <input
        type="text"
        value={value}
        onChange={(e) => onChange?.(e.target.value)}
        placeholder={placeholder}
        className="bg-transparent outline-none text-sm w-full placeholder:text-gray-500"
      />
    </div>
  );
}
</file>

<file path="src/shared/components/common/SecondaryButton.jsx">
export default function SecondaryButton({ children, onClick, className = '' }) {
  return (
    <button
      type="button"
      onClick={onClick}
      className={`w-full py-3.5 rounded-xl font-semibold text-gray-700 bg-gray-100 hover:bg-gray-200 transition-colors active:scale-[0.98] ${className}`}
    >
      {children}
    </button>
  );
}
</file>

<file path="src/shared/components/common/StarRating.jsx">
import { Star } from 'lucide-react';

export default function StarRating({ value = 0, onChange }) {
  return (
    <div className="flex items-center justify-center gap-2">
      {[1, 2, 3, 4, 5].map((n) => (
        <button
          key={n}
          type="button"
          onClick={() => onChange?.(n)}
          aria-label={`${n} star`}
          className="active:scale-90 transition-transform"
        >
          <Star
            size={36}
            className={n <= value ? 'fill-yellow-400 text-yellow-400' : 'text-gray-300'}
          />
        </button>
      ))}
    </div>
  );
}
</file>

<file path="src/shared/components/common/StatusPill.jsx">
const STYLES = {
  pending: 'bg-yellow-100 text-yellow-800',
  accepted: 'bg-brand-100 text-brand-700',
  declined: 'bg-red-100 text-red-700',
};

export default function StatusPill({ status }) {
  const key = (status || '').toLowerCase();
  return (
    <span className={`px-3 py-1 rounded-full text-xs font-semibold capitalize ${STYLES[key] || 'bg-gray-100 text-gray-700'}`}>
      {status}
    </span>
  );
}
</file>

<file path="src/shared/components/layout/BottomNav.jsx">
import { Recycle, Camera, MapPin } from 'lucide-react';
import { useNavigate, useLocation } from 'react-router-dom';

/**
 * FINAL structure per team decision (confirmed by Luke/Franz/Albert):
 * 3 tabs — Trades (Browse + My Trades as internal sub-tabs), Camera, Map.
 * Dashboard was dropped. "Rate Trader" remains a one-off screen reached only
 * after a completed trade — intentionally NOT in this bottom nav.
 */
const TABS = [
  { key: 'trades', label: 'Trades', icon: Recycle, path: '/trades' },
  { key: 'camera', label: 'Camera', icon: Camera, path: '/camera' },
  { key: 'map', label: 'Map', icon: MapPin, path: '/map' },
];

export default function BottomNav() {
  const navigate = useNavigate();
  const location = useLocation();

  return (
    <nav className="fixed bottom-0 left-1/2 -translate-x-1/2 w-full max-w-[480px] bg-white border-t border-gray-100 flex items-stretch z-30">
      {TABS.map(({ key, label, icon: Icon, path }) => {
        const isActive = location.pathname.startsWith(path);
        return (
          <button
            key={key}
            onClick={() => navigate(path)}
            className="flex-1 flex flex-col items-center justify-center gap-1 py-2.5"
          >
            <span
              className={`flex items-center justify-center rounded-full px-3 py-1 transition-colors ${
                isActive ? 'bg-brand-600 text-white' : 'text-gray-500'
              }`}
            >
              <Icon size={20} />
            </span>
            <span className={`text-[11px] ${isActive ? 'text-brand-600 font-semibold' : 'text-gray-500'}`}>
              {label}
            </span>
          </button>
        );
      })}
    </nav>
  );
}
</file>

<file path="src/shared/components/layout/Header.jsx">
import { ArrowLeft, Bell } from 'lucide-react';
import Logo from '@/shared/components/common/Logo';
import UserMenu from '@/shared/components/layout/UserMenu';

/**
 * Shared header used across all screens.
 * - Default: shows "RosaCycle" logo + notification bell + user avatar (rightmost)
 * - With `onBack`: shows a back arrow + optional title instead of the logo
 */
export default function Header({ onBack, title, showBell = true }) {
  return (
    <header className="flex items-center justify-between px-4 py-3 bg-white border-b border-gray-100 sticky top-0 z-20">
      <div className="flex items-center gap-2">
        {onBack ? (
          <button
            onClick={onBack}
            aria-label="Go back"
            className="p-1 -ml-1 text-gray-700 active:scale-95 transition-transform"
          >
            <ArrowLeft size={22} />
          </button>
        ) : null}
        {title ? <span className="font-semibold text-gray-900">{title}</span> : <Logo size="sm" />}
      </div>

      <div className="flex items-center gap-3">
        {showBell && (
          <button aria-label="Notifications" className="p-1 text-gray-700 active:scale-95 transition-transform">
            <Bell size={22} />
          </button>
        )}
        <UserMenu />
      </div>
    </header>
  );
}
</file>

<file path="src/shared/components/layout/UserMenu.jsx">
import { useState, useRef, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { LogOut } from 'lucide-react';
import { useAuth } from '@/features/auth/AuthContext';

export default function UserMenu() {
  const { user, logout } = useAuth();
  const navigate = useNavigate();
  const [open, setOpen] = useState(false);
  const menuRef = useRef(null);

  useEffect(() => {
    if (!open) return;
    const handleClickOutside = (e) => {
      if (menuRef.current && !menuRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener('mousedown', handleClickOutside);
    return () => document.removeEventListener('mousedown', handleClickOutside);
  }, [open]);

  const handleLogout = async () => {
    setOpen(false);
    await logout();
    navigate('/signin', { replace: true });
  };

  const handleEditProfile = () => {
    setOpen(false);
    navigate('/profile/edit');
  };

  const displayName = [user?.firstName, user?.lastName].filter(Boolean).join(' ') || user?.username;
  const initial = user?.firstName?.[0]?.toUpperCase() || user?.username?.[0]?.toUpperCase() || '?';

  return (
    <div className="relative" ref={menuRef}>
      <button
        onClick={() => setOpen((v) => !v)}
        aria-label="User profile"
        aria-expanded={open}
        className="w-8 h-8 rounded-full bg-brand-600 text-white text-sm font-semibold flex items-center justify-center active:scale-95 transition-transform"
      >
        {initial}
      </button>

      {open && (
        <div className="absolute right-0 top-10 w-48 bg-white rounded-xl shadow-lg border border-gray-100 py-2 z-30">
          <button
            onClick={handleEditProfile}
            className="w-full text-left px-4 py-2 border-b border-gray-100 hover:bg-gray-50"
          >
            <p className="text-sm font-semibold text-gray-900 truncate">{displayName || 'RosaCycle User'}</p>
            <p className="text-xs text-gray-500 truncate">{user?.email}</p>
          </button>
          <button
            onClick={handleLogout}
            className="w-full flex items-center gap-2 px-4 py-2.5 text-sm text-red-500 font-medium hover:bg-gray-50"
          >
            <LogOut size={16} />
            Log Out
          </button>
        </div>
      )}
    </div>
  );
}
</file>

<file path="src/shared/lib/pendingCapture.js">
// Plain in-memory singleton for passing a captured File between screens.
// Not router state: a File riding through navigate(path, { state }) is
// unusual, under-documented browser behavior — this sidesteps it entirely.
// Only one capture flow is ever active at a time, so a module-level
// singleton is enough.
let pendingFile = null;

export function setPendingCapture(file) {
  pendingFile = file;
}

export function getPendingCapture() {
  return pendingFile;
}

export function clearPendingCapture() {
  pendingFile = null;
}
</file>

<file path="src/shared/lib/storageKeys.js">
// Shared localStorage keys — used by both AuthContext (React state sync) and
// api.js (plain functions, can't reach React context) so they never drift.
export const USER_STORAGE_KEY = 'rosacycle_user';
export const TOKEN_STORAGE_KEY = 'rosacycle_token';
</file>

<file path="src/shared/services/api.js">
/**
 * API layer — talks to the real RosaCycle backend (see backend/docs/API.md
 * for the full reference). Screens should only import from here, never call
 * fetch() directly and never import from data/mock*.js.
 */
import { TOKEN_STORAGE_KEY } from '@/shared/lib/storageKeys';

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000';

function getToken() {
  return localStorage.getItem(TOKEN_STORAGE_KEY);
}

function buildFormData(fields = {}, imageFile) {
  const form = new FormData();
  Object.entries(fields).forEach(([key, value]) => {
    if (value === undefined || value === null || value === '') return;
    form.append(key, value);
  });
  if (imageFile) form.append('image', imageFile);
  return form;
}

async function apiFetch(path, { method = 'GET', json, form, query } = {}) {
  let url = `${API_BASE_URL}${path}`;
  if (query) {
    const qs = new URLSearchParams(
      Object.entries(query).filter(([, v]) => v !== undefined && v !== null && v !== '')
    ).toString();
    if (qs) url += `?${qs}`;
  }

  const headers = {};
  const token = getToken();
  if (token) headers['Authorization'] = `Bearer ${token}`;

  const init = { method, headers };
  if (form) {
    init.body = form; // browser sets multipart Content-Type + boundary itself
  } else if (json !== undefined) {
    headers['Content-Type'] = 'application/json';
    init.body = JSON.stringify(json);
  }

  const res = await fetch(url, init);

  if (res.status === 401) {
    window.dispatchEvent(new CustomEvent('rosacycle:unauthorized'));
  }

  if (!res.ok) {
    let message = `Request failed (${res.status}).`;
    try {
      const body = await res.json();
      if (body?.error) message = body.error;
    } catch {
      // non-JSON error body — fall back to the generic message
    }
    throw new Error(message);
  }

  if (res.status === 204) return null;
  return res.json();
}

// --- Auth ---------------------------------------------------------------

export async function registerUser({ firstName, lastName, username, email, password }) {
  return apiFetch('/auth/register', {
    method: 'POST',
    json: { firstName, lastName, username, email, password },
  });
}

export async function loginUser({ email, password }) {
  return apiFetch('/auth/login', { method: 'POST', json: { email, password } });
}

export async function logoutUser() {
  try {
    await apiFetch('/auth/logout', { method: 'POST' });
  } catch {
    // logout is a stateless no-op server-side — never block local sign-out on it
  }
  return true;
}

export async function fetchCurrentUser() {
  return apiFetch('/users/me');
}

export async function updateUserProfile(updates, currentPassword) {
  const form = buildFormData({
    firstName: updates.firstName,
    lastName: updates.lastName,
    username: updates.username,
    email: updates.email,
    password: updates.password,
    currentPassword,
  });
  if (updates.imageFile) form.set('image', updates.imageFile);
  return apiFetch('/users/me', { method: 'PATCH', form });
}

// --- Trades ---------------------------------------------------------------

export async function fetchBrowseTrades({ category, location } = {}) {
  return apiFetch('/trades', { query: { category, location } });
}

export async function fetchMyTrades() {
  return apiFetch('/trades/mine');
}

export async function fetchTradeById(id) {
  return apiFetch(`/trades/${id}`);
}

export async function createTrade(fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch('/trades', { method: 'POST', form });
}

export async function updateTrade(id, fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch(`/trades/${id}`, { method: 'PATCH', form });
}

// --- Offers ---------------------------------------------------------------

export async function sendOffer(tradeId, fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch(`/trades/${tradeId}/offers`, { method: 'POST', form });
}

export async function fetchOffersForTrade(tradeId) {
  return apiFetch(`/trades/${tradeId}/offers`);
}

export async function acceptOffer(offerId) {
  return apiFetch(`/offers/${offerId}/accept`, { method: 'POST' });
}

export async function declineOffer(offerId) {
  return apiFetch(`/offers/${offerId}/decline`, { method: 'POST' });
}

export async function fetchMyOffers() {
  return apiFetch('/offers/mine');
}

export async function fetchReceivedOffers() {
  return apiFetch('/offers/received');
}

// --- Resource Spots ---------------------------------------------------------------

export async function fetchResourceSpots() {
  return apiFetch('/resource-spots');
}

export async function fetchResourceSpotById(id) {
  return apiFetch(`/resource-spots/${id}`);
}

export async function reportResourceSpot(fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch('/resource-spots', { method: 'POST', form });
}

export async function addResourceSpotPhoto(id, imageFile) {
  const form = buildFormData({}, imageFile);
  return apiFetch(`/resource-spots/${id}/photos`, { method: 'POST', form });
}

export async function markResourceSpotCollected(id) {
  return apiFetch(`/resource-spots/${id}/collected`, { method: 'POST' });
}

// --- Notifications ---------------------------------------------------------------

export async function fetchNotifications() {
  return apiFetch('/notifications');
}

export async function fetchUnreadNotificationCount() {
  return apiFetch('/notifications/unread-count');
}

export async function markNotificationRead(id) {
  return apiFetch(`/notifications/${id}/read`, { method: 'PATCH' });
}

// --- Ratings ---------------------------------------------------------------

export async function submitRating(tradeId, score, comment) {
  return apiFetch(`/trades/${tradeId}/ratings`, { method: 'POST', json: { score, comment } });
}

export async function fetchUserRatings(userId) {
  return apiFetch(`/users/${userId}/ratings`);
}
</file>

<file path="src/shared/utils/constants.js">
export const SPOT_MATERIAL_OPTIONS = [
  'Wood',
  'Metal',
  'Plastic',
  'Fabric',
  'Paper',
  'E-waste',
  'Mixed',
  'Organic',
];
</file>

<file path="src/shared/utils/tradeFormat.js">
// Human-readable label for the tradingFor field per doc spec (x / nothing / open for negotiating)
export function formatTradingFor(tradingFor) {
  if (!tradingFor) return 'Open to offers';
  switch (tradingFor.type) {
    case 'specific':
      return tradingFor.value;
    case 'nothing':
      return 'Nothing (free item)';
    case 'negotiating':
      return 'Open for negotiating';
    default:
      return 'Open to offers';
  }
}

// Derives a trade's lifecycle status: Pending (open, whether or not it has an
// offer yet awaiting your decision) -> Accepted (offer accepted, chat unlocked).
export function getTradeStatus(trade) {
  return trade.offerAccepted ? 'Accepted' : 'Pending';
}
</file>

<file path="src/App.jsx">
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { AuthProvider, useAuth } from '@/features/auth/AuthContext';

import SignInScreen from '@/features/auth/screens/SignInScreen';
import SignUpScreen from '@/features/auth/screens/SignUpScreen';
import EditProfileScreen from '@/features/profile/screens/EditProfileScreen';
import TradesScreen from '@/features/trades/screens/TradesScreen';
import TradeDetailScreen from '@/features/trades/screens/TradeDetailScreen';
import EditListingScreen from '@/features/trades/screens/EditListingScreen';
import ViewOfferScreen from '@/features/trades/screens/ViewOfferScreen';
import ScanScreen from '@/features/scan/screens/ScanScreen';
import ScanConfirmScreen from '@/features/scan/screens/ScanConfirmScreen';
import TradeCreatedScreen from '@/features/trades/screens/TradeCreatedScreen';
import OfferSentScreen from '@/features/trades/screens/OfferSentScreen';
import MessageThreadScreen from '@/features/trades/screens/MessageThreadScreen';
import ResourceSpotScreen from '@/features/map/screens/ResourceSpotScreen';
import ResourceSpotConfirmScreen from '@/features/map/screens/ResourceSpotConfirmScreen';
import SpotReportedScreen from '@/features/map/screens/SpotReportedScreen';
import MapScreen from '@/features/map/screens/MapScreen';
import RateTraderScreen from '@/features/trades/screens/RateTraderScreen';

// Gate for screens that require a signed-in user; bounces to /signin otherwise.
function RequireAuth({ children }) {
  const { isAuthenticated } = useAuth();
  return isAuthenticated ? children : <Navigate to="/signin" replace />;
}

/**
 * FINAL nav structure (confirmed by team): 3 bottom tabs.
 * - /trades  -> Trades (Browse + My Trades internal sub-tabs)
 * - /camera  -> Camera / Resource Spot report (pins the Map, NOT a Trade)
 * - /map     -> Map (view-only pinned resource spot reports)
 *
 * Item scanning for a Trade (post or offer) is a *separate* flow at
 * /trades/scan — only reached from within Trades, never from the bottom nav.
 * Rate Trader is intentionally NOT a tab — reached only after a completed trade.
 */
export default function App() {
  return (
    <AuthProvider>
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Navigate to="/trades" replace />} />

          {/* Auth (email + password) */}
          <Route path="/signin" element={<SignInScreen />} />
          <Route path="/signup" element={<SignUpScreen />} />

          <Route path="/profile/edit" element={<RequireAuth><EditProfileScreen /></RequireAuth>} />

          {/* Trades (Browse + My Trades) */}
          <Route path="/trades" element={<RequireAuth><TradesScreen /></RequireAuth>} />
          {/* AI item scan, for posting a new trade or sending an offer on an existing one */}
          <Route path="/trades/scan" element={<RequireAuth><ScanScreen /></RequireAuth>} />
          <Route path="/trades/scan/confirm" element={<RequireAuth><ScanConfirmScreen /></RequireAuth>} />
          <Route path="/trades/scan/created" element={<RequireAuth><TradeCreatedScreen /></RequireAuth>} />
          <Route path="/trades/:id" element={<RequireAuth><TradeDetailScreen /></RequireAuth>} />
          <Route path="/trades/:id/edit" element={<RequireAuth><EditListingScreen /></RequireAuth>} />
          <Route path="/trades/:id/my-offer" element={<RequireAuth><ViewOfferScreen /></RequireAuth>} />
          <Route path="/trades/:id/offer-sent" element={<RequireAuth><OfferSentScreen /></RequireAuth>} />
          <Route path="/trades/:id/messages" element={<RequireAuth><MessageThreadScreen /></RequireAuth>} />

          {/* Camera tab: report a Resource Spot, pins the Map — not a Trade */}
          <Route path="/camera" element={<RequireAuth><ResourceSpotScreen /></RequireAuth>} />
          <Route path="/camera/confirm" element={<RequireAuth><ResourceSpotConfirmScreen /></RequireAuth>} />
          <Route path="/camera/reported" element={<RequireAuth><SpotReportedScreen /></RequireAuth>} />

          {/* Map (view-only pinned resource spot reports) */}
          <Route path="/map" element={<RequireAuth><MapScreen /></RequireAuth>} />

          {/* Rate Trader (one-off, reached after a completed trade) */}
          <Route path="/rate/:tradeId" element={<RequireAuth><RateTraderScreen /></RequireAuth>} />

          <Route path="*" element={<Navigate to="/trades" replace />} />
        </Routes>
      </BrowserRouter>
    </AuthProvider>
  );
}
</file>

<file path="src/index.css">
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-gray-50 text-gray-900;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

/* Mobile-first: cap width on larger screens to simulate phone frame */
#root {
  max-width: 480px;
  margin: 0 auto;
  min-height: 100vh;
  background: white;
  position: relative;
}
</file>

<file path="src/main.jsx">
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
</file>

<file path=".env.example">
VITE_API_BASE_URL=http://localhost:5000
</file>

<file path=".gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
</file>

<file path=".gitkeep">

</file>

<file path="fix-imports.cjs">
const fs = require('fs');
const path = require('path');

const SRC = path.join(__dirname, 'src');

const MAP = {
  AuthContext: 'features/auth/AuthContext',
  SignInScreen: 'features/auth/screens/SignInScreen',
  SignUpScreen: 'features/auth/screens/SignUpScreen',
  EditProfileScreen: 'features/profile/screens/EditProfileScreen',
  TradesScreen: 'features/trades/screens/TradesScreen',
  TradeDetailScreen: 'features/trades/screens/TradeDetailScreen',
  EditListingScreen: 'features/trades/screens/EditListingScreen',
  ViewOfferScreen: 'features/trades/screens/ViewOfferScreen',
  TradeCreatedScreen: 'features/trades/screens/TradeCreatedScreen',
  OfferSentScreen: 'features/trades/screens/OfferSentScreen',
  MessageThreadScreen: 'features/trades/screens/MessageThreadScreen',
  RateTraderScreen: 'features/trades/screens/RateTraderScreen',
  TradeCard: 'features/trades/components/cards/TradeCard',
  EcoImpactBox: 'features/trades/components/trade/EcoImpactBox',
  FilterChipRow: 'features/trades/components/trade/FilterChipRow',
  MessageThread: 'features/trades/components/trade/MessageThread',
  ScanScreen: 'features/scan/screens/ScanScreen',
  ScanConfirmScreen: 'features/scan/screens/ScanConfirmScreen',
  AIDetectedForm: 'features/scan/components/AIDetectedForm',
  CameraViewfinder: 'features/scan/components/CameraViewfinder',
  MapScreen: 'features/map/screens/MapScreen',
  ResourceSpotScreen: 'features/map/screens/ResourceSpotScreen',
  ResourceSpotConfirmScreen: 'features/map/screens/ResourceSpotConfirmScreen',
  SpotReportedScreen: 'features/map/screens/SpotReportedScreen',
  geo: 'features/map/geo',
  Header: 'shared/components/layout/Header',
  BottomNav: 'shared/components/layout/BottomNav',
  UserMenu: 'shared/components/layout/UserMenu',
  ConfirmationScreen: 'shared/components/common/ConfirmationScreen',
  FloatingActionButton: 'shared/components/common/FloatingActionButton',
  Logo: 'shared/components/common/Logo',
  MaterialTag: 'shared/components/common/MaterialTag',
  PasswordInput: 'shared/components/common/PasswordInput',
  PrimaryButton: 'shared/components/common/PrimaryButton',
  SearchBar: 'shared/components/common/SearchBar',
  SecondaryButton: 'shared/components/common/SecondaryButton',
  StarRating: 'shared/components/common/StarRating',
  StatusPill: 'shared/components/common/StatusPill',
  api: 'shared/services/api',
  storageKeys: 'shared/lib/storageKeys',
  pendingCapture: 'shared/lib/pendingCapture',
  constants: 'shared/utils/constants',
  tradeFormat: 'shared/utils/tradeFormat',
  'logo.svg': 'assets/logo.svg',
};

function walk(dir, out = []) {
  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
    const full = path.join(dir, entry.name);
    if (entry.isDirectory()) walk(full, out);
    else if (/\.(jsx?|tsx?)$/.test(entry.name)) out.push(full);
  }
  return out;
}

const files = walk(SRC);
let totalChanges = 0;

for (const file of files) {
  let content = fs.readFileSync(file, 'utf8');
  let changed = false;

  content = content.replace(
    /from\s+['"](\.\.?\/[^'"]+)['"]/g,
    (match, importPath) => {
      const base = path.basename(importPath);
      const key = MAP[base] !== undefined ? base : base.replace(/\.(jsx?|tsx?)$/, '');
      if (MAP[key] !== undefined) {
        changed = true;
        totalChanges++;
        return `from '@/${MAP[key]}'`;
      }
      return match;
    }
  );

  if (changed) {
    fs.writeFileSync(file, content, 'utf8');
    console.log('Updated:', path.relative(SRC, file));
  }
}

console.log(`\nDone. ${totalChanges} import(s) rewritten across ${files.length} files.`);
</file>

<file path="index.html">
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RosaCycle</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
</file>

<file path="package.json">
{
  "name": "rosacycle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.4",
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.23",
    "tailwindcss": "^3.4.19",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "lucide-react": "^1.27.0",
    "react-router-dom": "^7.18.1"
  }
}
</file>

<file path="postcss.config.js">
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
</file>

<file path="tailwind.config.js">
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          50: '#eefbf2',
          100: '#d6f5e0',
          500: '#22a559',
          600: '#1fa34c',
          700: '#188040',
        },
      },
    },
  },
  plugins: [],
}
</file>

<file path="vite.config.js">
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
});
</file>

</files>
</file>

<file path="backend/app/auth/__init__.py">

</file>

<file path="backend/app/auth/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_user(user):
    return {
        "id": user.id,
        "username": user.username,
        "firstName": user.first_name,
        "lastName": user.last_name,
        "email": user.email,
        "profileImage": build_media_url(user.profile_image_path),
        "role": user.role.value,
        "createdAt": user.created_at.isoformat(),
    }
</file>

<file path="backend/app/auth/utils.py">
from functools import wraps

from flask import g
from flask_jwt_extended import get_jwt_identity, verify_jwt_in_request
from werkzeug.security import check_password_hash, generate_password_hash

from app.shared.utils.errors import ApiError


def hash_password(raw_password):
    return generate_password_hash(raw_password)


def verify_password(raw_password, password_hash):
    return check_password_hash(password_hash, raw_password)


def require_auth(fn):
    @wraps(fn)
    def wrapper(*args, **kwargs):
        verify_jwt_in_request()
        from app.profile.repository import get_user_by_id

        user = get_user_by_id(get_jwt_identity())
        if user is None:
            raise ApiError("User not found.", 401)
        g.current_user = user
        return fn(*args, **kwargs)

    return wrapper
</file>

<file path="backend/app/map/__init__.py">

</file>

<file path="backend/app/map/controller.py">
from flask import g, request

from app.map.schema import serialize_resource_spot
from app.map.service import (
    add_photo_update,
    get_spot,
    list_active,
    mark_collected as mark_collected_service,
    report_spot,
)


def list_spots():
    spots = list_active()
    return [serialize_resource_spot(s) for s in spots], 200


def get_spot_by_id(spot_id):
    spot = get_spot(spot_id)
    return serialize_resource_spot(spot), 200


def create_spot():
    spot = report_spot(
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_resource_spot(spot), 201


def update_spot_photo(spot_id):
    spot = add_photo_update(spot_id, image_file=request.files.get("image"))
    return serialize_resource_spot(spot), 200


def mark_collected(spot_id):
    spot = mark_collected_service(spot_id)
    return serialize_resource_spot(spot), 200
</file>

<file path="backend/app/map/model.py">
from datetime import timedelta

from app.shared.database import db
from app.shared.models.enums import ResourceSpotStatus
from app.shared.utils.mixins import utcnow, uuid_pk_column


class ResourceSpot(db.Model):
    __tablename__ = "resource_spots"

    id = uuid_pk_column()
    reporter_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    name = db.Column(db.String(120), nullable=False)
    material = db.Column(db.String(120), nullable=False, index=True)
    weight_kg = db.Column(db.Numeric(8, 2), nullable=True)
    quantity = db.Column(db.Integer, nullable=True)
    description = db.Column(db.Text, nullable=True)

    location_text = db.Column(db.String(255), nullable=False, index=True)
    permission_note = db.Column(db.String(255), nullable=True)

    image_path = db.Column(db.String(255), nullable=True)

    status = db.Column(db.Enum(ResourceSpotStatus), nullable=False, default=ResourceSpotStatus.ACTIVE, index=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    expires_at = db.Column(db.DateTime(timezone=True), nullable=False)
    collected_at = db.Column(db.DateTime(timezone=True), nullable=True)

    reporter = db.relationship("User", back_populates="resource_spots")

    @staticmethod
    def default_expiry(days):
        return utcnow() + timedelta(days=days)
</file>

<file path="backend/app/map/repository.py">
from app.shared.database import db
from app.shared.models.enums import ResourceSpotStatus
from app.map.model import ResourceSpot
from app.shared.utils.mixins import utcnow


def get_spot_by_id(spot_id):
    return db.session.get(ResourceSpot, spot_id)


def list_active():
    return (
        ResourceSpot.query.filter(
            ResourceSpot.status == ResourceSpotStatus.ACTIVE,
            ResourceSpot.expires_at > utcnow(),
        )
        .order_by(ResourceSpot.created_at.desc())
        .all()
    )


def create_spot(spot):
    db.session.add(spot)
    db.session.commit()
    return spot


def save_spot(spot):
    db.session.commit()
    return spot
</file>

<file path="backend/app/map/routes.py">
from flask import Blueprint

from app.map import controller as resource_spot_controller
from app.auth.utils import require_auth

resource_spot_bp = Blueprint("resource_spot", __name__, url_prefix="/resource-spots")


@resource_spot_bp.get("")
@require_auth
def list_spots():
    return resource_spot_controller.list_spots()


@resource_spot_bp.get("/<spot_id>")
@require_auth
def get_spot(spot_id):
    return resource_spot_controller.get_spot_by_id(spot_id)


@resource_spot_bp.post("")
@require_auth
def create_spot():
    return resource_spot_controller.create_spot()


@resource_spot_bp.post("/<spot_id>/photos")
@require_auth
def update_spot_photo(spot_id):
    return resource_spot_controller.update_spot_photo(spot_id)


@resource_spot_bp.post("/<spot_id>/collected")
@require_auth
def mark_collected(spot_id):
    return resource_spot_controller.mark_collected(spot_id)
</file>

<file path="backend/app/map/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_resource_spot(spot):
    return {
        "id": spot.id,
        "name": spot.name,
        "material": spot.material,
        "weightKg": float(spot.weight_kg) if spot.weight_kg is not None else None,
        "quantity": spot.quantity,
        "description": spot.description,
        "location": spot.location_text,
        "permissionNote": spot.permission_note,
        "image": build_media_url(spot.image_path),
        "status": spot.status.value,
        "reporterId": spot.reporter_id,
        "reporterName": spot.reporter.first_name,
        "createdAt": spot.created_at.isoformat(),
        "expiresAt": spot.expires_at.isoformat(),
    }
</file>

<file path="backend/app/map/service.py">
from flask import current_app

from app.shared.models.enums import ResourceSpotStatus
from app.map.model import ResourceSpot
from app.map.repository import (
    create_spot,
    get_spot_by_id,
    list_active as repo_list_active,
    save_spot,
)
from app.shared.utils.errors import ConflictError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import utcnow

REQUIRED_FIELDS = ("name", "material", "locationText")


def report_spot(reporter_id, *, fields, image_file):
    missing = [f for f in REQUIRED_FIELDS if not fields.get(f)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    spot = ResourceSpot(
        reporter_id=reporter_id,
        name=fields["name"],
        material=fields["material"],
        weight_kg=fields.get("weightKg"),
        quantity=fields.get("quantity"),
        description=fields.get("description"),
        location_text=fields["locationText"],
        permission_note=fields.get("permissionNote"),
        status=ResourceSpotStatus.ACTIVE,
        expires_at=ResourceSpot.default_expiry(current_app.config["RESOURCE_SPOT_EXPIRY_DAYS"]),
    )
    if image_file is not None:
        spot.image_path = save_image(image_file, "resource_spots")

    return create_spot(spot)


def get_spot(spot_id):
    spot = get_spot_by_id(spot_id)
    if spot is None:
        raise NotFoundError("Resource spot not found.")
    return spot


def list_active():
    return repo_list_active()


def add_photo_update(spot_id, *, image_file):
    spot = get_spot(spot_id)
    if spot.status != ResourceSpotStatus.ACTIVE:
        raise ConflictError("This resource spot is no longer active.")
    if image_file is None:
        raise ValidationError("An updated photo is required.")

    spot.image_path = save_image(image_file, "resource_spots")
    return save_spot(spot)


def mark_collected(spot_id):
    spot = get_spot(spot_id)
    if spot.status == ResourceSpotStatus.COLLECTED:
        raise ConflictError("This resource spot is already marked collected.")

    spot.status = ResourceSpotStatus.COLLECTED
    spot.collected_at = utcnow()
    return save_spot(spot)
</file>

<file path="backend/app/profile/__init__.py">

</file>

<file path="backend/app/profile/controller.py">
from flask import g, request

from app.profile.schema import serialize_user
from app.profile.service import update_profile


def get_me():
    return serialize_user(g.current_user), 200


def update_me():
    form = request.form
    updates = {}
    for form_key, field in (
        ("firstName", "first_name"),
        ("lastName", "last_name"),
        ("username", "username"),
        ("email", "email"),
        ("password", "password"),
    ):
        if form_key in form and form[form_key]:
            updates[field] = form[form_key]

    current_password = form.get("currentPassword")
    image_file = request.files.get("image")

    user = update_profile(
        g.current_user.id,
        updates=updates,
        current_password=current_password,
        image_file=image_file,
    )
    return serialize_user(user), 200
</file>

<file path="backend/app/profile/routes.py">
from flask import Blueprint

from app.profile import controller as user_controller
from app.auth.utils import require_auth

user_bp = Blueprint("user", __name__, url_prefix="/users")


@user_bp.get("/me")
@require_auth
def get_me():
    return user_controller.get_me()


@user_bp.patch("/me")
@require_auth
def update_me():
    return user_controller.update_me()
</file>

<file path="backend/app/profile/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_user(user):
    return {
        "id": user.id,
        "username": user.username,
        "firstName": user.first_name,
        "lastName": user.last_name,
        "email": user.email,
        "profileImage": build_media_url(user.profile_image_path),
        "role": user.role.value,
        "createdAt": user.created_at.isoformat(),
    }
</file>

<file path="backend/app/scan/__init__.py">

</file>

<file path="backend/app/scan/controller.py">
from flask import request

from app.scan.service import assess_resource_spot_photo, assess_trade_photo
from app.shared.utils.errors import ValidationError


def assess_trade():
    image_file = request.files.get("image")
    if image_file is None:
        raise ValidationError("An image file is required.")
    return assess_trade_photo(image_file), 200


def assess_resource_spot():
    image_file = request.files.get("image")
    if image_file is None:
        raise ValidationError("An image file is required.")
    return assess_resource_spot_photo(image_file), 200
</file>

<file path="backend/app/scan/routes.py">
from flask import Blueprint

from app.scan import controller as ai_controller
from app.auth.utils import require_auth

ai_bp = Blueprint("ai", __name__, url_prefix="/ai")


@ai_bp.post("/assess-trade-photo")
@require_auth
def assess_trade():
    return ai_controller.assess_trade()


@ai_bp.post("/assess-resource-spot-photo")
@require_auth
def assess_resource_spot():
    return ai_controller.assess_resource_spot()
</file>

<file path="backend/app/scan/service.py">
import json

from flask import current_app

TRADE_ASSESSMENT_PROMPT = """You are assisting a circular-economy trading app. Look at the photo of an item
someone wants to trade and respond with ONLY a JSON object (no markdown) with these keys:
"itemName" (short string), "category" (one of: Wood, Metal, Plastic, Fabric, Paper, E-waste, Mixed, Organic),
"material" (short free-text description, e.g. "Steel scrap"), "weightKg" (number, your best estimate),
"confidence" (integer 0-100, your confidence in this assessment)."""

RESOURCE_SPOT_ASSESSMENT_PROMPT = """You are assisting a community resource-recovery app. Look at the photo of a
location containing discarded but reusable materials and respond with ONLY a JSON object (no markdown) with these
keys: "material" (one of: Wood, Metal, Plastic, Fabric, Paper, E-waste, Mixed, Organic), "weightKg" (number, your
best estimate of total material weight), "quantity" (integer, your best estimate of item count),
"confidence" (integer 0-100, your confidence in this assessment)."""

EMPTY_TRADE_RESULT = {
    "itemName": "",
    "category": "",
    "material": "",
    "weightKg": None,
    "confidence": 0,
}

EMPTY_SPOT_RESULT = {
    "material": "",
    "weightKg": None,
    "quantity": None,
    "confidence": 0,
}


def _call_gemini(image_file, prompt):
    import google.generativeai as genai

    api_key = current_app.config.get("GEMINI_API_KEY")
    if not api_key:
        raise RuntimeError("GEMINI_API_KEY is not configured.")

    genai.configure(api_key=api_key)
    model = genai.GenerativeModel("models/gemini-flash-latest")

    image_bytes = image_file.read()
    mime_type = image_file.mimetype or "image/jpeg"

    response = model.generate_content(
        [prompt, {"mime_type": mime_type, "data": image_bytes}],
        generation_config={"response_mime_type": "application/json"},
    )
    return json.loads(response.text)


def assess_trade_photo(image_file):
    """Returns a suggestion dict for a trade item photo. Never raises — on any
    failure (missing key, timeout, bad response) it returns a zero-confidence
    empty suggestion so the user can always fall through to manual entry."""
    try:
        result = _call_gemini(image_file, TRADE_ASSESSMENT_PROMPT)
        return {**EMPTY_TRADE_RESULT, **result}
    except Exception:
        current_app.logger.exception("Gemini trade assessment failed")
        return dict(EMPTY_TRADE_RESULT)


def assess_resource_spot_photo(image_file):
    try:
        result = _call_gemini(image_file, RESOURCE_SPOT_ASSESSMENT_PROMPT)
        return {**EMPTY_SPOT_RESULT, **result}
    except Exception:
        current_app.logger.exception("Gemini resource spot assessment failed")
        return dict(EMPTY_SPOT_RESULT)
</file>

<file path="backend/app/shared/database/__init__.py">
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate

db = SQLAlchemy()
migrate = Migrate()
</file>

<file path="backend/app/shared/models/enums.py">
import enum


class UserRole(str, enum.Enum):
    USER = "user"
    ADMIN = "admin"


class TradeStatus(str, enum.Enum):
    OPEN = "open"
    RESERVED = "reserved"
    COMPLETED = "completed"


class OfferStatus(str, enum.Enum):
    PENDING = "pending"
    ACCEPTED = "accepted"
    DECLINED = "declined"


class ResourceSpotStatus(str, enum.Enum):
    ACTIVE = "active"
    COLLECTED = "collected"


class TradingForType(str, enum.Enum):
    SPECIFIC = "specific"
    NOTHING = "nothing"
    NEGOTIATING = "negotiating"


class NotificationType(str, enum.Enum):
    OFFER_RECEIVED = "offer_received"
    OFFER_ACCEPTED = "offer_accepted"
    OFFER_DECLINED = "offer_declined"
</file>

<file path="backend/app/shared/notification/__init__.py">

</file>

<file path="backend/app/shared/notification/controller.py">
from flask import g

from app.shared.notification.schema import serialize_notification
from app.shared.notification.service import list_for_user, mark_read, unread_count


def list_notifications():
    notifications = list_for_user(g.current_user.id)
    return [serialize_notification(n) for n in notifications], 200


def unread_notification_count():
    return {"count": unread_count(g.current_user.id)}, 200


def mark_notification_read(notification_id):
    notification = mark_read(notification_id, g.current_user.id)
    return serialize_notification(notification), 200
</file>

<file path="backend/app/shared/notification/model.py">
from app.shared.database import db
from app.shared.models.enums import NotificationType
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Notification(db.Model):
    __tablename__ = "notifications"

    id = uuid_pk_column()
    recipient_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    type = db.Column(db.Enum(NotificationType), nullable=False)
    title = db.Column(db.String(120), nullable=False)
    body = db.Column(db.String(255), nullable=False)

    related_trade_id = db.Column(db.String(36), db.ForeignKey("trades.id"), nullable=True)
    related_offer_id = db.Column(db.String(36), db.ForeignKey("offers.id"), nullable=True)

    is_read = db.Column(db.Boolean, nullable=False, default=False, index=True)
    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)

    recipient = db.relationship("User", back_populates="notifications")
</file>

<file path="backend/app/shared/notification/repository.py">
from app.shared.database import db
from app.shared.notification.model import Notification


def create_notification(notification):
    db.session.add(notification)
    db.session.commit()
    return notification


def list_for_user(user_id):
    return Notification.query.filter_by(recipient_id=user_id).order_by(Notification.created_at.desc()).all()


def count_unread(user_id):
    return Notification.query.filter_by(recipient_id=user_id, is_read=False).count()


def get_notification_by_id(notification_id):
    return db.session.get(Notification, notification_id)


def save_notification(notification):
    db.session.commit()
    return notification
</file>

<file path="backend/app/shared/notification/routes.py">
from flask import Blueprint

from app.shared.notification import controller as notification_controller
from app.auth.utils import require_auth

notification_bp = Blueprint("notification", __name__, url_prefix="/notifications")


@notification_bp.get("")
@require_auth
def list_notifications():
    return notification_controller.list_notifications()


@notification_bp.get("/unread-count")
@require_auth
def unread_notification_count():
    return notification_controller.unread_notification_count()


@notification_bp.patch("/<notification_id>/read")
@require_auth
def mark_notification_read(notification_id):
    return notification_controller.mark_notification_read(notification_id)
</file>

<file path="backend/app/shared/notification/schema.py">
def serialize_notification(notification):
    return {
        "id": notification.id,
        "type": notification.type.value,
        "title": notification.title,
        "body": notification.body,
        "relatedTradeId": notification.related_trade_id,
        "relatedOfferId": notification.related_offer_id,
        "isRead": notification.is_read,
        "createdAt": notification.created_at.isoformat(),
    }
</file>

<file path="backend/app/shared/notification/service.py">
from app.shared.notification.model import Notification
from app.shared.notification.repository import (
    count_unread,
    create_notification as create_notification_row,
    get_notification_by_id,
    list_for_user as repo_list_for_user,
    save_notification,
)
from app.shared.utils.errors import ForbiddenError, NotFoundError


def notify(*, recipient_id, type, title, body, trade_id=None, offer_id=None):
    notification = Notification(
        recipient_id=recipient_id,
        type=type,
        title=title,
        body=body,
        related_trade_id=trade_id,
        related_offer_id=offer_id,
    )
    return create_notification_row(notification)


def list_for_user(user_id):
    return repo_list_for_user(user_id)


def unread_count(user_id):
    return count_unread(user_id)


def mark_read(notification_id, user_id):
    notification = get_notification_by_id(notification_id)
    if notification is None:
        raise NotFoundError("Notification not found.")
    if notification.recipient_id != user_id:
        raise ForbiddenError("You cannot modify another user's notification.")
    notification.is_read = True
    return save_notification(notification)
</file>

<file path="backend/app/shared/rating/__init__.py">

</file>

<file path="backend/app/shared/rating/controller.py">
from flask import g, request

from app.shared.rating.schema import serialize_rating
from app.shared.rating.service import list_for_user, rate_trade
from app.shared.utils.errors import ValidationError


def create_rating(trade_id):
    body = request.get_json(silent=True) or {}
    if "score" not in body:
        raise ValidationError("score is required.")

    rating = rate_trade(trade_id, g.current_user.id, score=body["score"], comment=body.get("comment"))
    return serialize_rating(rating), 201


def ratings_for_user(user_id):
    result = list_for_user(user_id)
    return {
        "ratings": [serialize_rating(r) for r in result["ratings"]],
        "average": result["average"],
    }, 200
</file>

<file path="backend/app/shared/rating/model.py">
from app.shared.database import db
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Rating(db.Model):
    __tablename__ = "ratings"
    __table_args__ = (
        db.UniqueConstraint("trade_id", "rater_id", name="uq_rating_trade_rater"),
        db.CheckConstraint("score >= 1 AND score <= 5", name="ck_rating_score_range"),
    )

    id = uuid_pk_column()
    trade_id = db.Column(db.String(36), db.ForeignKey("trades.id"), nullable=False, index=True)
    rater_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)
    rated_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    score = db.Column(db.Integer, nullable=False)
    comment = db.Column(db.String(500), nullable=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
</file>

<file path="backend/app/shared/rating/repository.py">
from sqlalchemy.exc import IntegrityError

from app.shared.database import db
from app.shared.rating.model import Rating
from app.shared.utils.errors import ConflictError


def list_for_user(user_id):
    return Rating.query.filter_by(rated_id=user_id).order_by(Rating.created_at.desc()).all()


def average_for_user(user_id):
    result = db.session.query(db.func.avg(Rating.score)).filter(Rating.rated_id == user_id).scalar()
    return float(result) if result is not None else None


def create_rating(rating):
    db.session.add(rating)
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        raise ConflictError("You have already rated this trade.")
    return rating
</file>

<file path="backend/app/shared/rating/routes.py">
from flask import Blueprint

from app.shared.rating import controller as rating_controller
from app.auth.utils import require_auth

rating_bp = Blueprint("rating", __name__)


@rating_bp.post("/trades/<trade_id>/ratings")
@require_auth
def create_rating(trade_id):
    return rating_controller.create_rating(trade_id)


@rating_bp.get("/users/<user_id>/ratings")
@require_auth
def ratings_for_user(user_id):
    return rating_controller.ratings_for_user(user_id)
</file>

<file path="backend/app/shared/rating/schema.py">
def serialize_rating(rating):
    return {
        "id": rating.id,
        "tradeId": rating.trade_id,
        "raterId": rating.rater_id,
        "ratedId": rating.rated_id,
        "score": rating.score,
        "comment": rating.comment,
        "createdAt": rating.created_at.isoformat(),
    }
</file>

<file path="backend/app/shared/rating/service.py">
from app.shared.models.enums import TradeStatus
from app.shared.rating.model import Rating
from app.trades.offer.repository import get_accepted_offer
from app.shared.rating.repository import average_for_user, create_rating, list_for_user as repo_list_for_user
from app.trades.repository import get_trade_by_id
from app.shared.utils.errors import ForbiddenError, NotFoundError, ValidationError


def _other_party(trade, rater_id):
    accepted_offer = get_accepted_offer(trade.id)
    if accepted_offer is None:
        raise ForbiddenError("This trade has no accepted offer to rate.")

    if rater_id == trade.owner_id:
        return accepted_offer.offerer_id
    if rater_id == accepted_offer.offerer_id:
        return trade.owner_id
    raise ForbiddenError("Only the two parties of a trade may rate each other.")


def rate_trade(trade_id, rater_id, *, score, comment=None):
    trade = get_trade_by_id(trade_id)
    if trade is None:
        raise NotFoundError("Trade not found.")
    if trade.status != TradeStatus.COMPLETED:
        raise ForbiddenError("You can only rate completed trades.")
    if not isinstance(score, int) or score < 1 or score > 5:
        raise ValidationError("score must be an integer between 1 and 5.")

    rated_id = _other_party(trade, rater_id)

    rating = Rating(trade_id=trade.id, rater_id=rater_id, rated_id=rated_id, score=score, comment=comment)
    return create_rating(rating)


def list_for_user(user_id):
    return {
        "ratings": repo_list_for_user(user_id),
        "average": average_for_user(user_id),
    }
</file>

<file path="backend/app/shared/utils/__init__.py">

</file>

<file path="backend/app/shared/utils/errors.py">
class ApiError(Exception):
    status_code = 400

    def __init__(self, message, status_code=None):
        super().__init__(message)
        self.message = message
        if status_code is not None:
            self.status_code = status_code


class NotFoundError(ApiError):
    status_code = 404


class ConflictError(ApiError):
    status_code = 409


class ForbiddenError(ApiError):
    status_code = 403


class ValidationError(ApiError):
    status_code = 422


def register_error_handlers(app):
    @app.errorhandler(ApiError)
    def handle_api_error(err):
        return {"error": err.message}, err.status_code

    @app.errorhandler(404)
    def handle_404(err):
        return {"error": "Not found."}, 404

    @app.errorhandler(500)
    def handle_500(err):
        app.logger.exception(err)
        return {"error": "Internal server error."}, 500
</file>

<file path="backend/app/shared/utils/file_storage.py">
import io
import os
import uuid

from flask import current_app
from PIL import Image
from werkzeug.utils import secure_filename

ALLOWED_EXTENSIONS = {"jpg", "jpeg", "png", "webp"}


def _extension(filename):
    if "." not in filename:
        return None
    return filename.rsplit(".", 1)[1].lower()


def save_image(file_storage, subfolder):
    """Validates, strips EXIF, and saves an uploaded image under UPLOAD_ROOT/subfolder.

    Returns the relative path to store on the model (e.g. "trades/<uuid>.jpg").
    """
    filename = secure_filename(file_storage.filename or "")
    ext = _extension(filename)
    if ext not in ALLOWED_EXTENSIONS:
        from app.shared.utils.errors import ValidationError

        raise ValidationError("Unsupported image type. Use JPEG, PNG, or WEBP.")

    image = Image.open(file_storage.stream)
    image = image.convert("RGB") if ext in ("jpg", "jpeg") else image.copy()

    # Strip EXIF (including GPS tags) by re-saving pixel data only.
    clean = Image.new(image.mode, image.size)
    clean.putdata(list(image.getdata()))

    upload_root = current_app.config["UPLOAD_ROOT"]
    target_dir = os.path.join(upload_root, subfolder)
    os.makedirs(target_dir, exist_ok=True)

    new_filename = f"{uuid.uuid4()}.{ext}"
    target_path = os.path.join(target_dir, new_filename)

    buffer = io.BytesIO()
    save_format = "JPEG" if ext in ("jpg", "jpeg") else ext.upper()
    clean.save(buffer, format=save_format)
    with open(target_path, "wb") as f:
        f.write(buffer.getvalue())

    return f"{subfolder}/{new_filename}"


def build_media_url(relative_path):
    if not relative_path:
        return None
    base = current_app.config["PUBLIC_BASE_URL"].rstrip("/")
    return f"{base}/media/{relative_path}"
</file>

<file path="backend/app/shared/utils/mixins.py">
import uuid
from datetime import datetime, timezone

from app.shared.database import db


def utcnow():
    return datetime.now(timezone.utc)


def ensure_aware(dt):
    """SQLite drops tzinfo on round-trip even for DateTime(timezone=True) columns,
    while Postgres preserves it. Normalize so comparisons work on either backend."""
    if dt is not None and dt.tzinfo is None:
        return dt.replace(tzinfo=timezone.utc)
    return dt


def uuid_pk_column():
    return db.Column(db.String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
</file>

<file path="backend/app/shared/utils/pagination.py">
from flask import request

DEFAULT_PAGE_SIZE = 20
MAX_PAGE_SIZE = 100


def paginate_query(query):
    page = request.args.get("page", 1, type=int)
    page_size = request.args.get("page_size", DEFAULT_PAGE_SIZE, type=int)
    page = max(page, 1)
    page_size = min(max(page_size, 1), MAX_PAGE_SIZE)

    total = query.count()
    items = query.offset((page - 1) * page_size).limit(page_size).all()

    return {
        "items": items,
        "page": page,
        "page_size": page_size,
        "total": total,
    }
</file>

<file path="backend/app/shared/utils/rate_limit.py">
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

limiter = Limiter(key_func=get_remote_address, default_limits=[], storage_uri="memory://")
</file>

<file path="backend/app/trades/offer/__init__.py">

</file>

<file path="backend/app/trades/offer/controller.py">
from flask import g, request

from app.trades.offer.schema import serialize_offer
from app.trades.offer.service import (
    accept_offer as accept_offer_service,
    decline_offer as decline_offer_service,
    list_by_trade,
    list_received,
    list_sent,
    send_offer as send_offer_service,
)


def send_offer(trade_id):
    offer = send_offer_service(
        trade_id,
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_offer(offer), 201


def offers_for_trade(trade_id):
    offers = list_by_trade(trade_id)
    return [serialize_offer(o) for o in offers], 200


def accept_offer(offer_id):
    offer = accept_offer_service(offer_id, g.current_user.id)
    return serialize_offer(offer), 200


def decline_offer(offer_id):
    offer = decline_offer_service(offer_id, g.current_user.id)
    return serialize_offer(offer), 200


def my_sent_offers():
    offers = list_sent(g.current_user.id)
    return [serialize_offer(o) for o in offers], 200


def my_received_offers():
    offers = list_received(g.current_user.id)
    return [serialize_offer(o) for o in offers], 200
</file>

<file path="backend/app/trades/offer/model.py">
from app.shared.database import db
from app.shared.models.enums import OfferStatus
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Offer(db.Model):
    __tablename__ = "offers"
    __table_args__ = (
        db.Index(
            "ix_offers_one_pending_per_trade",
            "trade_id",
            unique=True,
            postgresql_where=db.text("status = 'PENDING'"),
            sqlite_where=db.text("status = 'PENDING'"),
        ),
    )

    id = uuid_pk_column()
    trade_id = db.Column(db.String(36), db.ForeignKey("trades.id"), nullable=False, index=True)
    offerer_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    item_name = db.Column(db.String(120), nullable=False)
    category = db.Column(db.String(50), nullable=False)
    material = db.Column(db.String(120), nullable=False)
    weight_kg = db.Column(db.Numeric(8, 2), nullable=True)
    description = db.Column(db.Text, nullable=True)
    image_path = db.Column(db.String(255), nullable=True)

    status = db.Column(db.Enum(OfferStatus), nullable=False, default=OfferStatus.PENDING, index=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    decided_at = db.Column(db.DateTime(timezone=True), nullable=True)

    trade = db.relationship("Trade", back_populates="offers")
    offerer = db.relationship("User", back_populates="offers")
</file>

<file path="backend/app/trades/offer/repository.py">
from sqlalchemy.exc import IntegrityError

from app.shared.database import db
from app.trades.offer.model import Offer
from app.trades.model import Trade
from app.shared.utils.errors import ConflictError


def get_offer_by_id(offer_id):
    return db.session.get(Offer, offer_id)


def list_by_trade(trade_id):
    return Offer.query.filter_by(trade_id=trade_id).order_by(Offer.created_at.desc()).all()


def get_accepted_offer(trade_id):
    from app.shared.models.enums import OfferStatus

    return Offer.query.filter_by(trade_id=trade_id, status=OfferStatus.ACCEPTED).first()


def list_sent(offerer_id):
    return Offer.query.filter_by(offerer_id=offerer_id).order_by(Offer.created_at.desc()).all()


def list_received(owner_id):
    return (
        Offer.query.join(Trade, Offer.trade_id == Trade.id)
        .filter(Trade.owner_id == owner_id)
        .order_by(Offer.created_at.desc())
        .all()
    )


def add_offer(offer):
    db.session.add(offer)
    return offer


def save(*_objects):
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        raise ConflictError("This trade already has an active offer.")
</file>

<file path="backend/app/trades/offer/routes.py">
from flask import Blueprint

from app.trades.offer import controller as offer_controller
from app.auth.utils import require_auth

offer_bp = Blueprint("offer", __name__)


@offer_bp.post("/trades/<trade_id>/offers")
@require_auth
def send_offer(trade_id):
    return offer_controller.send_offer(trade_id)


@offer_bp.get("/trades/<trade_id>/offers")
@require_auth
def offers_for_trade(trade_id):
    return offer_controller.offers_for_trade(trade_id)


@offer_bp.post("/offers/<offer_id>/accept")
@require_auth
def accept_offer(offer_id):
    return offer_controller.accept_offer(offer_id)


@offer_bp.post("/offers/<offer_id>/decline")
@require_auth
def decline_offer(offer_id):
    return offer_controller.decline_offer(offer_id)


@offer_bp.get("/offers/mine")
@require_auth
def my_sent_offers():
    return offer_controller.my_sent_offers()


@offer_bp.get("/offers/received")
@require_auth
def my_received_offers():
    return offer_controller.my_received_offers()
</file>

<file path="backend/app/trades/offer/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_offer(offer):
    return {
        "id": offer.id,
        "tradeId": offer.trade_id,
        "tradeName": offer.trade.item_name,
        "offererId": offer.offerer_id,
        "offererName": offer.offerer.first_name,
        "itemName": offer.item_name,
        "category": offer.category,
        "material": offer.material,
        "weightKg": float(offer.weight_kg) if offer.weight_kg is not None else None,
        "description": offer.description,
        "image": build_media_url(offer.image_path),
        "status": offer.status.value,
        "createdAt": offer.created_at.isoformat(),
        "decidedAt": offer.decided_at.isoformat() if offer.decided_at else None,
    }
</file>

<file path="backend/app/trades/offer/service.py">
from app.shared.models.enums import NotificationType, OfferStatus, TradeStatus
from app.trades.offer.model import Offer
from app.trades.offer.repository import (
    add_offer,
    get_offer_by_id,
    list_by_trade as repo_list_by_trade,
    list_received as repo_list_received,
    list_sent as repo_list_sent,
    save,
)
from app.trades.repository import get_trade_by_id
from app.shared.notification.service import notify
from app.shared.utils.errors import ConflictError, ForbiddenError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import utcnow

REQUIRED_OFFER_FIELDS = ("itemName", "category", "material")


def send_offer(trade_id, offerer_id, *, fields, image_file):
    trade = get_trade_by_id(trade_id)
    if trade is None:
        raise NotFoundError("Trade not found.")
    if trade.owner_id == offerer_id:
        raise ForbiddenError("You cannot send an offer on your own trade.")
    if trade.status != TradeStatus.OPEN:
        raise ConflictError("This trade is no longer accepting offers.")

    missing = [f for f in REQUIRED_OFFER_FIELDS if not fields.get(f)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    offer = Offer(
        trade_id=trade.id,
        offerer_id=offerer_id,
        item_name=fields["itemName"],
        category=fields["category"],
        material=fields["material"],
        weight_kg=fields.get("weightKg"),
        description=fields.get("description"),
        status=OfferStatus.PENDING,
    )
    if image_file is not None:
        offer.image_path = save_image(image_file, "offers")

    add_offer(offer)
    trade.status = TradeStatus.RESERVED
    save(offer, trade)

    notify(
        recipient_id=trade.owner_id,
        type=NotificationType.OFFER_RECEIVED,
        title="New offer received",
        body=f"You received an offer on \"{trade.item_name}\".",
        trade_id=trade.id,
        offer_id=offer.id,
    )
    return offer


def _get_offer_for_decision(offer_id, caller_id):
    offer = get_offer_by_id(offer_id)
    if offer is None:
        raise NotFoundError("Offer not found.")
    if offer.trade.owner_id != caller_id:
        raise ForbiddenError("Only the trade owner can decide on this offer.")
    if offer.status != OfferStatus.PENDING:
        raise ConflictError("This offer has already been decided.")
    return offer


def accept_offer(offer_id, caller_id):
    offer = _get_offer_for_decision(offer_id, caller_id)
    offer.status = OfferStatus.ACCEPTED
    offer.decided_at = utcnow()
    offer.trade.status = TradeStatus.COMPLETED
    save(offer, offer.trade)

    notify(
        recipient_id=offer.offerer_id,
        type=NotificationType.OFFER_ACCEPTED,
        title="Offer accepted",
        body=f"Your offer on \"{offer.trade.item_name}\" was accepted!",
        trade_id=offer.trade_id,
        offer_id=offer.id,
    )
    return offer


def decline_offer(offer_id, caller_id):
    offer = _get_offer_for_decision(offer_id, caller_id)
    offer.status = OfferStatus.DECLINED
    offer.decided_at = utcnow()
    offer.trade.status = TradeStatus.OPEN
    save(offer, offer.trade)

    notify(
        recipient_id=offer.offerer_id,
        type=NotificationType.OFFER_DECLINED,
        title="Offer declined",
        body=f"Your offer on \"{offer.trade.item_name}\" was declined.",
        trade_id=offer.trade_id,
        offer_id=offer.id,
    )
    return offer


def list_by_trade(trade_id):
    return repo_list_by_trade(trade_id)


def list_sent(offerer_id):
    return repo_list_sent(offerer_id)


def list_received(owner_id):
    return repo_list_received(owner_id)
</file>

<file path="backend/app/trades/__init__.py">

</file>

<file path="backend/app/trades/controller.py">
from flask import g, request

from app.trades.schema import serialize_trade
from app.trades.service import (
    create_trade as create_trade_service,
    get_trade as get_trade_service,
    list_browse,
    list_mine,
    update_trade as update_trade_service,
)


def browse_trades():
    category = request.args.get("category")
    location = request.args.get("location")
    trades = list_browse(g.current_user.id, category=category, location=location)
    return [serialize_trade(t) for t in trades], 200


def my_trades():
    trades = list_mine(g.current_user.id)
    return [serialize_trade(t) for t in trades], 200


def get_trade(trade_id):
    trade = get_trade_service(trade_id)
    return serialize_trade(trade), 200


def create_trade():
    trade = create_trade_service(
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_trade(trade), 201


def update_trade(trade_id):
    trade = update_trade_service(
        trade_id,
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_trade(trade), 200
</file>

<file path="backend/app/trades/model.py">
from app.shared.database import db
from app.shared.models.enums import TradeStatus, TradingForType
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Trade(db.Model):
    __tablename__ = "trades"

    id = uuid_pk_column()
    owner_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    item_name = db.Column(db.String(120), nullable=False)
    category = db.Column(db.String(50), nullable=False, index=True)
    material = db.Column(db.String(120), nullable=False)
    description = db.Column(db.Text, nullable=True)
    weight_kg = db.Column(db.Numeric(8, 2), nullable=True)
    quantity = db.Column(db.Integer, nullable=False, default=1)

    location_text = db.Column(db.String(255), nullable=False, index=True)
    pickup_location_text = db.Column(db.String(255), nullable=True)

    trading_for_type = db.Column(db.Enum(TradingForType), nullable=False, default=TradingForType.NEGOTIATING)
    trading_for_value = db.Column(db.String(255), nullable=True)

    image_path = db.Column(db.String(255), nullable=True)

    status = db.Column(db.Enum(TradeStatus), nullable=False, default=TradeStatus.OPEN, index=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    updated_at = db.Column(db.DateTime(timezone=True), default=utcnow, onupdate=utcnow, nullable=False)

    owner = db.relationship("User", back_populates="trades")
    offers = db.relationship("Offer", back_populates="trade", lazy="dynamic", cascade="all, delete-orphan")

    @property
    def has_offers(self):
        return self.offers.count() > 0

    @property
    def offer_accepted(self):
        from app.shared.models.enums import OfferStatus

        return self.offers.filter_by(status=OfferStatus.ACCEPTED).first() is not None
</file>

<file path="backend/app/trades/repository.py">
from app.shared.database import db
from app.trades.model import Trade


def get_trade_by_id(trade_id):
    return db.session.get(Trade, trade_id)


def list_browse(*, exclude_owner_id=None, category=None, location=None):
    query = Trade.query
    if exclude_owner_id is not None:
        query = query.filter(Trade.owner_id != exclude_owner_id)
    if category:
        query = query.filter(db.func.lower(Trade.category) == category.lower())
    if location:
        query = query.filter(Trade.location_text.ilike(f"%{location}%"))
    return query.order_by(Trade.created_at.desc())


def list_mine(owner_id):
    return Trade.query.filter_by(owner_id=owner_id).order_by(Trade.created_at.desc())


def create_trade(trade):
    db.session.add(trade)
    db.session.commit()
    return trade


def save_trade(trade):
    db.session.commit()
    return trade
</file>

<file path="backend/app/trades/routes.py">
from flask import Blueprint

from app.trades import controller as trade_controller
from app.auth.utils import require_auth

trade_bp = Blueprint("trade", __name__, url_prefix="/trades")


@trade_bp.get("")
@require_auth
def browse_trades():
    return trade_controller.browse_trades()


@trade_bp.get("/mine")
@require_auth
def my_trades():
    return trade_controller.my_trades()


@trade_bp.get("/<trade_id>")
@require_auth
def get_trade(trade_id):
    return trade_controller.get_trade(trade_id)


@trade_bp.post("")
@require_auth
def create_trade():
    return trade_controller.create_trade()


@trade_bp.patch("/<trade_id>")
@require_auth
def update_trade(trade_id):
    return trade_controller.update_trade(trade_id)
</file>

<file path="backend/app/trades/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_trade(trade):
    return {
        "id": trade.id,
        "name": trade.item_name,
        "category": trade.category,
        "material": trade.material,
        "image": build_media_url(trade.image_path),
        "location": trade.location_text,
        "pickupLocation": trade.pickup_location_text,
        "posterName": trade.owner.first_name,
        "posterId": trade.owner_id,
        "weightKg": float(trade.weight_kg) if trade.weight_kg is not None else None,
        "quantity": trade.quantity,
        "tradingFor": {
            "type": trade.trading_for_type.value,
            "value": trade.trading_for_value,
        },
        "description": trade.description,
        "status": trade.status.value,
        "hasOffers": trade.has_offers,
        "offerAccepted": trade.offer_accepted,
        "createdAt": trade.created_at.isoformat(),
    }
</file>

<file path="backend/app/trades/service.py">
from app.shared.models.enums import TradeStatus, TradingForType
from app.trades.model import Trade
from app.trades.repository import (
    create_trade as create_trade_row,
    get_trade_by_id,
    list_browse as repo_list_browse,
    list_mine as repo_list_mine,
    save_trade,
)
from app.shared.utils.errors import ForbiddenError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image

VALID_TRADING_FOR_TYPES = {t.value for t in TradingForType}


def create_trade(owner_id, *, fields, image_file):
    trading_for_type = fields.get("tradingForType")
    if trading_for_type not in VALID_TRADING_FOR_TYPES:
        raise ValidationError("tradingForType must be one of: specific, nothing, negotiating.")

    required = ("itemName", "category", "material", "locationText", "quantity")
    missing = [f for f in required if not fields.get(f)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    trade = Trade(
        owner_id=owner_id,
        item_name=fields["itemName"],
        category=fields["category"],
        material=fields["material"],
        description=fields.get("description"),
        weight_kg=fields.get("weightKg"),
        quantity=int(fields["quantity"]),
        location_text=fields["locationText"],
        pickup_location_text=fields.get("pickupLocationText"),
        trading_for_type=TradingForType(trading_for_type),
        trading_for_value=fields.get("tradingForValue"),
        status=TradeStatus.OPEN,
    )
    if image_file is not None:
        trade.image_path = save_image(image_file, "trades")

    return create_trade_row(trade)


def get_trade(trade_id):
    trade = get_trade_by_id(trade_id)
    if trade is None:
        raise NotFoundError("Trade not found.")
    return trade


def update_trade(trade_id, owner_id, *, fields, image_file):
    trade = get_trade(trade_id)
    if trade.owner_id != owner_id:
        raise ForbiddenError("Only the trade owner can edit this trade.")

    for form_field, attr in (
        ("itemName", "item_name"),
        ("category", "category"),
        ("material", "material"),
        ("description", "description"),
        ("weightKg", "weight_kg"),
        ("locationText", "location_text"),
        ("pickupLocationText", "pickup_location_text"),
        ("tradingForValue", "trading_for_value"),
    ):
        if form_field in fields:
            setattr(trade, attr, fields[form_field])

    if "quantity" in fields:
        trade.quantity = int(fields["quantity"])
    if "tradingForType" in fields:
        if fields["tradingForType"] not in VALID_TRADING_FOR_TYPES:
            raise ValidationError("tradingForType must be one of: specific, nothing, negotiating.")
        trade.trading_for_type = TradingForType(fields["tradingForType"])
    if image_file is not None:
        trade.image_path = save_image(image_file, "trades")

    return save_trade(trade)


def list_browse(current_user_id, *, category=None, location=None):
    return repo_list_browse(exclude_owner_id=current_user_id, category=category, location=location).all()


def list_mine(owner_id):
    return repo_list_mine(owner_id).all()
</file>

<file path="backend/.flaskenv">
FLASK_APP=run.py
 FLASK_ENV=development
</file>

<file path="backend/.gitkeep">

</file>

<file path="backend/repomix-output.xml">
This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
app/
  auth/
    __init__.py
    controller.py
    repository.py
    routes.py
    schema.py
    service.py
    user_model.py
    utils.py
  map/
    __init__.py
    controller.py
    model.py
    repository.py
    routes.py
    schema.py
    service.py
  profile/
    __init__.py
    controller.py
    model.py
    repository.py
    routes.py
    schema.py
    service.py
  scan/
    __init__.py
    controller.py
    routes.py
    service.py
  services/
    auth_service.py
  shared/
    database/
      __init__.py
    models/
      __init__.py
      enums.py
    notification/
      __init__.py
      controller.py
      model.py
      repository.py
      routes.py
      schema.py
      service.py
    rating/
      __init__.py
      controller.py
      model.py
      repository.py
      routes.py
      schema.py
      service.py
    utils/
      __init__.py
      errors.py
      file_storage.py
      mixins.py
      pagination.py
      rate_limit.py
  trades/
    offer/
      __init__.py
      controller.py
      model.py
      repository.py
      routes.py
      schema.py
      service.py
    __init__.py
    controller.py
    model.py
    repository.py
    routes.py
    schema.py
    service.py
  __init__.py
  app.py
  config.py
docs/
  API.md
migrations/
  versions/
    41b0a18a356f_initial_schema.py
  alembic.ini
  env.py
  README
  script.py.mako
.env.example
.flaskenv
.gitignore
.gitkeep
.python-version
Procfile
requirements.txt
run.py
seed.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="app/auth/__init__.py">

</file>

<file path="app/auth/controller.py">
from flask import request
from flask_jwt_extended import create_access_token

from app.auth.schema import serialize_user
from app.auth.service import authenticate_user, register_user
from app.services.auth_service import google_login_or_register
from app.utils.errors import ValidationError


def register():
    
    body = request.get_json(silent=True) or {}
    required = ("username", "firstName", "lastName", "email", "password")
    missing = [field for field in required if not body.get(field)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    user = register_user(
        username=body["username"],
        first_name=body["firstName"],
        last_name=body["lastName"],
        email=body["email"],
        password=body["password"],
    )
    token = create_access_token(identity=user.id)
    return {"token": token, "user": serialize_user(user)}, 201


def login():
    body = request.get_json(silent=True) or {}
    if not body.get("email") or not body.get("password"):
        raise ValidationError("Email and password are required.")

    user = authenticate_user(email=body["email"], password=body["password"])
    token = create_access_token(identity=user.id)
    return {"token": token, "user": serialize_user(user)}, 200


def logout():
    return {"success": True}, 200

def google_login():
    body = request.get_json(silent=True) or {}
    token = body.get("token")  # Frontend sends Google ID Token here
    if not token:
        raise ValidationError("Google token is required.")

    user = google_login_or_register(token)
    access_token = create_access_token(identity=str(user.id))
    
    return {"token": access_token, "user": serialize_user(user)}, 200
</file>

<file path="app/auth/repository.py">
from app.database import db
from app.auth.user_model import User


def get_user_by_id(user_id):
    return db.session.get(User, user_id)


def get_user_by_email(email):
    return User.query.filter(db.func.lower(User.email) == email.lower()).first()


def get_user_by_username(username):
    return User.query.filter(db.func.lower(User.username) == username.lower()).first()


def create_user(user):
    db.session.add(user)
    db.session.commit()
    return user


def save_user(user):
    db.session.commit()
    return user
</file>

<file path="app/auth/routes.py">
from flask import Blueprint
from app.auth import controller

auth_bp = Blueprint("auth", __name__, url_prefix="/auth")

@auth_bp.post("/register")
def register():
    return controller.register()

@auth_bp.post("/login")
def login():
    return controller.login()

@auth_bp.post("/logout")
def logout():
    return controller.logout()

@auth_bp.post("/google")
def google_login():
    return controller.google_login()
</file>

<file path="app/auth/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_user(user):
    return {
        "id": user.id,
        "username": user.username,
        "firstName": user.first_name,
        "lastName": user.last_name,
        "email": user.email,
        "profileImage": build_media_url(user.profile_image_path),
        "role": user.role.value,
        "createdAt": user.created_at.isoformat(),
    }
</file>

<file path="app/auth/service.py">
from datetime import timedelta

from flask import current_app

from app.auth.user_model import User
from app.auth.repository import (
    create_user,
    get_user_by_email,
    get_user_by_id,
    get_user_by_username,
    save_user,
)
from app.auth.utils import hash_password, verify_password
from app.shared.utils.errors import ConflictError, ForbiddenError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import ensure_aware, utcnow


def register_user(*, username, first_name, last_name, email, password):
    if get_user_by_email(email) is not None:
        raise ConflictError("An account with this email already exists.")
    if get_user_by_username(username) is not None:
        raise ConflictError("An account with this username already exists.")

    user = User(
        username=username,
        first_name=first_name,
        last_name=last_name,
        email=email,
        password_hash=hash_password(password),
    )
    return create_user(user)


def authenticate_user(*, email, password):
    user = get_user_by_email(email)
    if user is None or not verify_password(password, user.password_hash):
        raise ValidationError("Invalid email or password.", status_code=401)
    return user


def get_profile(user_id):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")
    return user


def update_profile(user_id, *, updates, current_password, image_file):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")

    wants_sensitive_change = any(k in updates for k in ("password", "email", "username")) or image_file is not None
    if wants_sensitive_change and user.profile_updated_at is not None:
        cooldown = timedelta(days=current_app.config["PROFILE_EDIT_COOLDOWN_DAYS"])
        next_allowed_at = ensure_aware(user.profile_updated_at) + cooldown
        if utcnow() < next_allowed_at:
            days_left = (next_allowed_at - utcnow()).days + 1
            raise ConflictError(f"You can update your profile again in {days_left} day(s).")

    if "password" in updates or "email" in updates or "username" in updates:
        if not current_password or not verify_password(current_password, user.password_hash):
            raise ForbiddenError("Current password is incorrect.")

    if "first_name" in updates:
        user.first_name = updates["first_name"]
    if "last_name" in updates:
        user.last_name = updates["last_name"]
    if "username" in updates:
        existing = get_user_by_username(updates["username"])
        if existing is not None and existing.id != user.id:
            raise ConflictError("An account with this username already exists.")
        user.username = updates["username"]
    if "email" in updates:
        existing = get_user_by_email(updates["email"])
        if existing is not None and existing.id != user.id:
            raise ConflictError("An account with this email already exists.")
        user.email = updates["email"]
    if "password" in updates:
        user.password_hash = hash_password(updates["password"])

    if image_file is not None:
        user.profile_image_path = save_image(image_file, "profiles")

    if wants_sensitive_change:
        user.profile_updated_at = utcnow()

    return save_user(user)
</file>

<file path="app/auth/user_model.py">
from app.shared.database import db
from app.shared.models.enums import UserRole
from app.shared.utils.mixins import utcnow, uuid_pk_column


class User(db.Model):
    __tablename__ = "users"

    id = uuid_pk_column()
    username = db.Column(db.String(50), unique=True, nullable=False, index=True)
    first_name = db.Column(db.String(50), nullable=False)
    last_name = db.Column(db.String(50), nullable=False)
    email = db.Column(db.String(255), unique=True, nullable=False, index=True)
    password_hash = db.Column(db.String(255), nullable=False)
    profile_image_path = db.Column(db.String(255), nullable=True)
    role = db.Column(db.Enum(UserRole), default=UserRole.USER, nullable=False)
    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    profile_updated_at = db.Column(db.DateTime(timezone=True), nullable=True)

    trades = db.relationship("Trade", back_populates="owner", lazy="dynamic")
    offers = db.relationship("Offer", back_populates="offerer", lazy="dynamic")
    resource_spots = db.relationship("ResourceSpot", back_populates="reporter", lazy="dynamic")
    notifications = db.relationship("Notification", back_populates="recipient", lazy="dynamic")
</file>

<file path="app/auth/utils.py">
from functools import wraps

from flask import g
from flask_jwt_extended import get_jwt_identity, verify_jwt_in_request
from werkzeug.security import check_password_hash, generate_password_hash

from app.shared.utils.errors import ApiError


def hash_password(raw_password):
    return generate_password_hash(raw_password)


def verify_password(raw_password, password_hash):
    return check_password_hash(password_hash, raw_password)


def require_auth(fn):
    @wraps(fn)
    def wrapper(*args, **kwargs):
        verify_jwt_in_request()
        from app.profile.repository import get_user_by_id

        user = get_user_by_id(get_jwt_identity())
        if user is None:
            raise ApiError("User not found.", 401)
        g.current_user = user
        return fn(*args, **kwargs)

    return wrapper
</file>

<file path="app/map/__init__.py">

</file>

<file path="app/map/controller.py">
from flask import g, request

from app.map.schema import serialize_resource_spot
from app.map.service import (
    add_photo_update,
    get_spot,
    list_active,
    mark_collected as mark_collected_service,
    report_spot,
)


def list_spots():
    spots = list_active()
    return [serialize_resource_spot(s) for s in spots], 200


def get_spot_by_id(spot_id):
    spot = get_spot(spot_id)
    return serialize_resource_spot(spot), 200


def create_spot():
    spot = report_spot(
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_resource_spot(spot), 201


def update_spot_photo(spot_id):
    spot = add_photo_update(spot_id, image_file=request.files.get("image"))
    return serialize_resource_spot(spot), 200


def mark_collected(spot_id):
    spot = mark_collected_service(spot_id)
    return serialize_resource_spot(spot), 200
</file>

<file path="app/map/model.py">
from datetime import timedelta

from app.shared.database import db
from app.shared.models.enums import ResourceSpotStatus
from app.shared.utils.mixins import utcnow, uuid_pk_column


class ResourceSpot(db.Model):
    __tablename__ = "resource_spots"

    id = uuid_pk_column()
    reporter_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    name = db.Column(db.String(120), nullable=False)
    material = db.Column(db.String(120), nullable=False, index=True)
    weight_kg = db.Column(db.Numeric(8, 2), nullable=True)
    quantity = db.Column(db.Integer, nullable=True)
    description = db.Column(db.Text, nullable=True)

    location_text = db.Column(db.String(255), nullable=False, index=True)
    permission_note = db.Column(db.String(255), nullable=True)

    image_path = db.Column(db.String(255), nullable=True)

    status = db.Column(db.Enum(ResourceSpotStatus), nullable=False, default=ResourceSpotStatus.ACTIVE, index=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    expires_at = db.Column(db.DateTime(timezone=True), nullable=False)
    collected_at = db.Column(db.DateTime(timezone=True), nullable=True)

    reporter = db.relationship("User", back_populates="resource_spots")

    @staticmethod
    def default_expiry(days):
        return utcnow() + timedelta(days=days)
</file>

<file path="app/map/repository.py">
from app.shared.database import db
from app.shared.models.enums import ResourceSpotStatus
from app.map.model import ResourceSpot
from app.shared.utils.mixins import utcnow


def get_spot_by_id(spot_id):
    return db.session.get(ResourceSpot, spot_id)


def list_active():
    return (
        ResourceSpot.query.filter(
            ResourceSpot.status == ResourceSpotStatus.ACTIVE,
            ResourceSpot.expires_at > utcnow(),
        )
        .order_by(ResourceSpot.created_at.desc())
        .all()
    )


def create_spot(spot):
    db.session.add(spot)
    db.session.commit()
    return spot


def save_spot(spot):
    db.session.commit()
    return spot
</file>

<file path="app/map/routes.py">
from flask import Blueprint

from app.map import controller as resource_spot_controller
from app.auth.utils import require_auth

resource_spot_bp = Blueprint("resource_spot", __name__, url_prefix="/resource-spots")


@resource_spot_bp.get("")
@require_auth
def list_spots():
    return resource_spot_controller.list_spots()


@resource_spot_bp.get("/<spot_id>")
@require_auth
def get_spot(spot_id):
    return resource_spot_controller.get_spot_by_id(spot_id)


@resource_spot_bp.post("")
@require_auth
def create_spot():
    return resource_spot_controller.create_spot()


@resource_spot_bp.post("/<spot_id>/photos")
@require_auth
def update_spot_photo(spot_id):
    return resource_spot_controller.update_spot_photo(spot_id)


@resource_spot_bp.post("/<spot_id>/collected")
@require_auth
def mark_collected(spot_id):
    return resource_spot_controller.mark_collected(spot_id)
</file>

<file path="app/map/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_resource_spot(spot):
    return {
        "id": spot.id,
        "name": spot.name,
        "material": spot.material,
        "weightKg": float(spot.weight_kg) if spot.weight_kg is not None else None,
        "quantity": spot.quantity,
        "description": spot.description,
        "location": spot.location_text,
        "permissionNote": spot.permission_note,
        "image": build_media_url(spot.image_path),
        "status": spot.status.value,
        "reporterId": spot.reporter_id,
        "reporterName": spot.reporter.first_name,
        "createdAt": spot.created_at.isoformat(),
        "expiresAt": spot.expires_at.isoformat(),
    }
</file>

<file path="app/map/service.py">
from flask import current_app

from app.shared.models.enums import ResourceSpotStatus
from app.map.model import ResourceSpot
from app.map.repository import (
    create_spot,
    get_spot_by_id,
    list_active as repo_list_active,
    save_spot,
)
from app.shared.utils.errors import ConflictError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import utcnow

REQUIRED_FIELDS = ("name", "material", "locationText")


def report_spot(reporter_id, *, fields, image_file):
    missing = [f for f in REQUIRED_FIELDS if not fields.get(f)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    spot = ResourceSpot(
        reporter_id=reporter_id,
        name=fields["name"],
        material=fields["material"],
        weight_kg=fields.get("weightKg"),
        quantity=fields.get("quantity"),
        description=fields.get("description"),
        location_text=fields["locationText"],
        permission_note=fields.get("permissionNote"),
        status=ResourceSpotStatus.ACTIVE,
        expires_at=ResourceSpot.default_expiry(current_app.config["RESOURCE_SPOT_EXPIRY_DAYS"]),
    )
    if image_file is not None:
        spot.image_path = save_image(image_file, "resource_spots")

    return create_spot(spot)


def get_spot(spot_id):
    spot = get_spot_by_id(spot_id)
    if spot is None:
        raise NotFoundError("Resource spot not found.")
    return spot


def list_active():
    return repo_list_active()


def add_photo_update(spot_id, *, image_file):
    spot = get_spot(spot_id)
    if spot.status != ResourceSpotStatus.ACTIVE:
        raise ConflictError("This resource spot is no longer active.")
    if image_file is None:
        raise ValidationError("An updated photo is required.")

    spot.image_path = save_image(image_file, "resource_spots")
    return save_spot(spot)


def mark_collected(spot_id):
    spot = get_spot(spot_id)
    if spot.status == ResourceSpotStatus.COLLECTED:
        raise ConflictError("This resource spot is already marked collected.")

    spot.status = ResourceSpotStatus.COLLECTED
    spot.collected_at = utcnow()
    return save_spot(spot)
</file>

<file path="app/profile/__init__.py">

</file>

<file path="app/profile/controller.py">
from flask import g, request

from app.profile.schema import serialize_user
from app.profile.service import update_profile


def get_me():
    return serialize_user(g.current_user), 200


def update_me():
    form = request.form
    updates = {}
    for form_key, field in (
        ("firstName", "first_name"),
        ("lastName", "last_name"),
        ("username", "username"),
        ("email", "email"),
        ("password", "password"),
    ):
        if form_key in form and form[form_key]:
            updates[field] = form[form_key]

    current_password = form.get("currentPassword")
    image_file = request.files.get("image")

    user = update_profile(
        g.current_user.id,
        updates=updates,
        current_password=current_password,
        image_file=image_file,
    )
    return serialize_user(user), 200
</file>

<file path="app/profile/model.py">
from app.shared.database import db
from app.shared.models.enums import UserRole
from app.shared.utils.mixins import utcnow, uuid_pk_column

class User(db.Model):
    __tablename__ = "users"

    id = uuid_pk_column()
    username = db.Column(db.String(50), unique=True, nullable=False, index=True)
    first_name = db.Column(db.String(50), nullable=False)
    last_name = db.Column(db.String(50), nullable=False)
    email = db.Column(db.String(255), unique=True, nullable=False, index=True)
    password_hash = db.Column(db.String(255), nullable=True)  # <-- Changed to True for OAuth users
    google_id = db.Column(db.String(255), unique=True, nullable=True, index=True)  # <-- Added
    profile_image_path = db.Column(db.String(255), nullable=True)
    role = db.Column(db.Enum(UserRole), default=UserRole.USER, nullable=False)
    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    profile_updated_at = db.Column(db.DateTime(timezone=True), nullable=True)
    
    trades = db.relationship("Trade", back_populates="owner", lazy="dynamic")
    offers = db.relationship("Offer", back_populates="offerer", lazy="dynamic")
    resource_spots = db.relationship("ResourceSpot", back_populates="reporter", lazy="dynamic")
    notifications = db.relationship("Notification", back_populates="recipient", lazy="dynamic")
</file>

<file path="app/profile/repository.py">
from app.shared.database import db
from app.profile.model import User


def get_user_by_id(user_id):
    return db.session.get(User, user_id)


def get_user_by_email(email):
    return User.query.filter(db.func.lower(User.email) == email.lower()).first()


def get_user_by_username(username):
    return User.query.filter(db.func.lower(User.username) == username.lower()).first()


def create_user(user):
    db.session.add(user)
    db.session.commit()
    return user


def save_user(user):
    db.session.commit()
    return user
</file>

<file path="app/profile/routes.py">
from flask import Blueprint

from app.profile import controller as user_controller
from app.auth.utils import require_auth

user_bp = Blueprint("user", __name__, url_prefix="/users")


@user_bp.get("/me")
@require_auth
def get_me():
    return user_controller.get_me()


@user_bp.patch("/me")
@require_auth
def update_me():
    return user_controller.update_me()
</file>

<file path="app/profile/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_user(user):
    return {
        "id": user.id,
        "username": user.username,
        "firstName": user.first_name,
        "lastName": user.last_name,
        "email": user.email,
        "profileImage": build_media_url(user.profile_image_path),
        "role": user.role.value,
        "createdAt": user.created_at.isoformat(),
    }
</file>

<file path="app/profile/service.py">
from datetime import timedelta

from flask import current_app

from app.profile.model import User
from app.profile.repository import (
    create_user,
    get_user_by_email,
    get_user_by_id,
    get_user_by_username,
    save_user,
)
from app.auth.utils import hash_password, verify_password
from app.shared.utils.errors import ConflictError, ForbiddenError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import ensure_aware, utcnow


def register_user(*, username, first_name, last_name, email, password):
    if get_user_by_email(email) is not None:
        raise ConflictError("An account with this email already exists.")
    if get_user_by_username(username) is not None:
        raise ConflictError("An account with this username already exists.")

    user = User(
        username=username,
        first_name=first_name,
        last_name=last_name,
        email=email,
        password_hash=hash_password(password),
    )
    return create_user(user)


def authenticate_user(*, email, password):
    user = get_user_by_email(email)
    if user is None or not verify_password(password, user.password_hash):
        raise ValidationError("Invalid email or password.", status_code=401)
    return user


def get_profile(user_id):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")
    return user


def update_profile(user_id, *, updates, current_password, image_file):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")

    wants_sensitive_change = any(k in updates for k in ("password", "email", "username")) or image_file is not None
    if wants_sensitive_change and user.profile_updated_at is not None:
        cooldown = timedelta(days=current_app.config["PROFILE_EDIT_COOLDOWN_DAYS"])
        next_allowed_at = ensure_aware(user.profile_updated_at) + cooldown
        if utcnow() < next_allowed_at:
            days_left = (next_allowed_at - utcnow()).days + 1
            raise ConflictError(f"You can update your profile again in {days_left} day(s).")

    if "password" in updates or "email" in updates or "username" in updates:
        if not current_password or not verify_password(current_password, user.password_hash):
            raise ForbiddenError("Current password is incorrect.")

    if "first_name" in updates:
        user.first_name = updates["first_name"]
    if "last_name" in updates:
        user.last_name = updates["last_name"]
    if "username" in updates:
        existing = get_user_by_username(updates["username"])
        if existing is not None and existing.id != user.id:
            raise ConflictError("An account with this username already exists.")
        user.username = updates["username"]
    if "email" in updates:
        existing = get_user_by_email(updates["email"])
        if existing is not None and existing.id != user.id:
            raise ConflictError("An account with this email already exists.")
        user.email = updates["email"]
    if "password" in updates:
        user.password_hash = hash_password(updates["password"])

    if image_file is not None:
        user.profile_image_path = save_image(image_file, "profiles")

    if wants_sensitive_change:
        user.profile_updated_at = utcnow()

    return save_user(user)
</file>

<file path="app/scan/__init__.py">

</file>

<file path="app/scan/controller.py">
from flask import request

from app.scan.service import assess_resource_spot_photo, assess_trade_photo
from app.shared.utils.errors import ValidationError


def assess_trade():
    image_file = request.files.get("image")
    if image_file is None:
        raise ValidationError("An image file is required.")
    return assess_trade_photo(image_file), 200


def assess_resource_spot():
    image_file = request.files.get("image")
    if image_file is None:
        raise ValidationError("An image file is required.")
    return assess_resource_spot_photo(image_file), 200
</file>

<file path="app/scan/routes.py">
from flask import Blueprint

from app.scan import controller as ai_controller
from app.auth.utils import require_auth

ai_bp = Blueprint("ai", __name__, url_prefix="/ai")


@ai_bp.post("/assess-trade-photo")
@require_auth
def assess_trade():
    return ai_controller.assess_trade()


@ai_bp.post("/assess-resource-spot-photo")
@require_auth
def assess_resource_spot():
    return ai_controller.assess_resource_spot()
</file>

<file path="app/scan/service.py">
import json

from flask import current_app

TRADE_ASSESSMENT_PROMPT = """You are assisting a circular-economy trading app. Look at the photo of an item
someone wants to trade and respond with ONLY a JSON object (no markdown) with these keys:
"itemName" (short string), "category" (one of: Wood, Metal, Plastic, Fabric, Paper, E-waste, Mixed, Organic),
"material" (short free-text description, e.g. "Steel scrap"), "weightKg" (number, your best estimate),
"confidence" (integer 0-100, your confidence in this assessment)."""

RESOURCE_SPOT_ASSESSMENT_PROMPT = """You are assisting a community resource-recovery app. Look at the photo of a
location containing discarded but reusable materials and respond with ONLY a JSON object (no markdown) with these
keys: "material" (one of: Wood, Metal, Plastic, Fabric, Paper, E-waste, Mixed, Organic), "weightKg" (number, your
best estimate of total material weight), "quantity" (integer, your best estimate of item count),
"confidence" (integer 0-100, your confidence in this assessment)."""

EMPTY_TRADE_RESULT = {
    "itemName": "",
    "category": "",
    "material": "",
    "weightKg": None,
    "confidence": 0,
}

EMPTY_SPOT_RESULT = {
    "material": "",
    "weightKg": None,
    "quantity": None,
    "confidence": 0,
}


def _call_gemini(image_file, prompt):
    import google.generativeai as genai

    api_key = current_app.config.get("GEMINI_API_KEY")
    if not api_key:
        raise RuntimeError("GEMINI_API_KEY is not configured.")

    genai.configure(api_key=api_key)
    model = genai.GenerativeModel("models/gemini-flash-latest")

    image_bytes = image_file.read()
    mime_type = image_file.mimetype or "image/jpeg"

    response = model.generate_content(
        [prompt, {"mime_type": mime_type, "data": image_bytes}],
        generation_config={"response_mime_type": "application/json"},
    )
    return json.loads(response.text)


def assess_trade_photo(image_file):
    """Returns a suggestion dict for a trade item photo. Never raises — on any
    failure (missing key, timeout, bad response) it returns a zero-confidence
    empty suggestion so the user can always fall through to manual entry."""
    try:
        result = _call_gemini(image_file, TRADE_ASSESSMENT_PROMPT)
        return {**EMPTY_TRADE_RESULT, **result}
    except Exception:
        current_app.logger.exception("Gemini trade assessment failed")
        return dict(EMPTY_TRADE_RESULT)


def assess_resource_spot_photo(image_file):
    try:
        result = _call_gemini(image_file, RESOURCE_SPOT_ASSESSMENT_PROMPT)
        return {**EMPTY_SPOT_RESULT, **result}
    except Exception:
        current_app.logger.exception("Gemini resource spot assessment failed")
        return dict(EMPTY_SPOT_RESULT)
</file>

<file path="app/services/auth_service.py">
# backend/app/services/auth_service.py
from flask import current_app
from google.oauth2 import id_token
from google.auth.transport import requests

from app.database import db
from app.models.user import User
from app.repositories.user_repository import get_user_by_email # or user_service
from app.utils.errors import ValidationError

def verify_google_token(token: str) -> dict:
    """Verifies the ID token sent from frontend with Google's servers."""
    try:
        client_id = current_app.config.get("GOOGLE_CLIENT_ID")
        # If client_id is empty in dev, pass None or set up your Google Console Client ID
        id_info = id_token.verify_oauth2_token(
            token, 
            requests.Request(), 
            audience=client_id if client_id else None
        )
        return id_info
    except Exception as e:
        raise ValidationError(f"Invalid Google token: {str(e)}")

def google_login_or_register(id_token_str: str) -> User:
    """Authenticates a user via Google ID Token; creates a profile if new."""
    payload = verify_google_token(id_token_str)
    
    email = payload.get("email")
    google_id = payload.get("sub")
    first_name = payload.get("given_name", "Google")
    last_name = payload.get("family_name", "User")
    picture = payload.get("picture")

    if not email:
        raise ValidationError("Google account must have an email address.")

    # 1. Check if user exists by google_id or email
    user = User.query.filter((User.google_id == google_id) | (User.email == email)).first()

    if user:
        # Link google_id if not already linked
        if not user.google_id:
            user.google_id = google_id
            db.session.commit()
        return user

    # 2. Create new user if first-time login
    # Generate a unique base username from email
    base_username = email.split("@")[0]
    username = base_username
    counter = 1
    while User.query.filter_by(username=username).first():
        username = f"{base_username}{counter}"
        counter += 1

    new_user = User(
        username=username,
        first_name=first_name,
        last_name=last_name,
        email=email,
        google_id=google_id,
        profile_image_path=picture
    )
    db.session.add(new_user)
    db.session.commit()

    return new_user
</file>

<file path="app/shared/database/__init__.py">
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate

db = SQLAlchemy()
migrate = Migrate()
</file>

<file path="app/shared/models/__init__.py">
from app.profile.model import User  # noqa: F401
from app.trades.model import Trade  # noqa: F401
from app.trades.offer.model import Offer  # noqa: F401
from app.map.model import ResourceSpot  # noqa: F401
from app.shared.notification.model import Notification  # noqa: F401
from app.shared.rating.model import Rating  # noqa: F401
</file>

<file path="app/shared/models/enums.py">
import enum


class UserRole(str, enum.Enum):
    USER = "user"
    ADMIN = "admin"


class TradeStatus(str, enum.Enum):
    OPEN = "open"
    RESERVED = "reserved"
    COMPLETED = "completed"


class OfferStatus(str, enum.Enum):
    PENDING = "pending"
    ACCEPTED = "accepted"
    DECLINED = "declined"


class ResourceSpotStatus(str, enum.Enum):
    ACTIVE = "active"
    COLLECTED = "collected"


class TradingForType(str, enum.Enum):
    SPECIFIC = "specific"
    NOTHING = "nothing"
    NEGOTIATING = "negotiating"


class NotificationType(str, enum.Enum):
    OFFER_RECEIVED = "offer_received"
    OFFER_ACCEPTED = "offer_accepted"
    OFFER_DECLINED = "offer_declined"
</file>

<file path="app/shared/notification/__init__.py">

</file>

<file path="app/shared/notification/controller.py">
from flask import g

from app.shared.notification.schema import serialize_notification
from app.shared.notification.service import list_for_user, mark_read, unread_count


def list_notifications():
    notifications = list_for_user(g.current_user.id)
    return [serialize_notification(n) for n in notifications], 200


def unread_notification_count():
    return {"count": unread_count(g.current_user.id)}, 200


def mark_notification_read(notification_id):
    notification = mark_read(notification_id, g.current_user.id)
    return serialize_notification(notification), 200
</file>

<file path="app/shared/notification/model.py">
from app.shared.database import db
from app.shared.models.enums import NotificationType
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Notification(db.Model):
    __tablename__ = "notifications"

    id = uuid_pk_column()
    recipient_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    type = db.Column(db.Enum(NotificationType), nullable=False)
    title = db.Column(db.String(120), nullable=False)
    body = db.Column(db.String(255), nullable=False)

    related_trade_id = db.Column(db.String(36), db.ForeignKey("trades.id"), nullable=True)
    related_offer_id = db.Column(db.String(36), db.ForeignKey("offers.id"), nullable=True)

    is_read = db.Column(db.Boolean, nullable=False, default=False, index=True)
    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)

    recipient = db.relationship("User", back_populates="notifications")
</file>

<file path="app/shared/notification/repository.py">
from app.shared.database import db
from app.shared.notification.model import Notification


def create_notification(notification):
    db.session.add(notification)
    db.session.commit()
    return notification


def list_for_user(user_id):
    return Notification.query.filter_by(recipient_id=user_id).order_by(Notification.created_at.desc()).all()


def count_unread(user_id):
    return Notification.query.filter_by(recipient_id=user_id, is_read=False).count()


def get_notification_by_id(notification_id):
    return db.session.get(Notification, notification_id)


def save_notification(notification):
    db.session.commit()
    return notification
</file>

<file path="app/shared/notification/routes.py">
from flask import Blueprint

from app.shared.notification import controller as notification_controller
from app.auth.utils import require_auth

notification_bp = Blueprint("notification", __name__, url_prefix="/notifications")


@notification_bp.get("")
@require_auth
def list_notifications():
    return notification_controller.list_notifications()


@notification_bp.get("/unread-count")
@require_auth
def unread_notification_count():
    return notification_controller.unread_notification_count()


@notification_bp.patch("/<notification_id>/read")
@require_auth
def mark_notification_read(notification_id):
    return notification_controller.mark_notification_read(notification_id)
</file>

<file path="app/shared/notification/schema.py">
def serialize_notification(notification):
    return {
        "id": notification.id,
        "type": notification.type.value,
        "title": notification.title,
        "body": notification.body,
        "relatedTradeId": notification.related_trade_id,
        "relatedOfferId": notification.related_offer_id,
        "isRead": notification.is_read,
        "createdAt": notification.created_at.isoformat(),
    }
</file>

<file path="app/shared/notification/service.py">
from app.shared.notification.model import Notification
from app.shared.notification.repository import (
    count_unread,
    create_notification as create_notification_row,
    get_notification_by_id,
    list_for_user as repo_list_for_user,
    save_notification,
)
from app.shared.utils.errors import ForbiddenError, NotFoundError


def notify(*, recipient_id, type, title, body, trade_id=None, offer_id=None):
    notification = Notification(
        recipient_id=recipient_id,
        type=type,
        title=title,
        body=body,
        related_trade_id=trade_id,
        related_offer_id=offer_id,
    )
    return create_notification_row(notification)


def list_for_user(user_id):
    return repo_list_for_user(user_id)


def unread_count(user_id):
    return count_unread(user_id)


def mark_read(notification_id, user_id):
    notification = get_notification_by_id(notification_id)
    if notification is None:
        raise NotFoundError("Notification not found.")
    if notification.recipient_id != user_id:
        raise ForbiddenError("You cannot modify another user's notification.")
    notification.is_read = True
    return save_notification(notification)
</file>

<file path="app/shared/rating/__init__.py">

</file>

<file path="app/shared/rating/controller.py">
from flask import g, request

from app.shared.rating.schema import serialize_rating
from app.shared.rating.service import list_for_user, rate_trade
from app.shared.utils.errors import ValidationError


def create_rating(trade_id):
    body = request.get_json(silent=True) or {}
    if "score" not in body:
        raise ValidationError("score is required.")

    rating = rate_trade(trade_id, g.current_user.id, score=body["score"], comment=body.get("comment"))
    return serialize_rating(rating), 201


def ratings_for_user(user_id):
    result = list_for_user(user_id)
    return {
        "ratings": [serialize_rating(r) for r in result["ratings"]],
        "average": result["average"],
    }, 200
</file>

<file path="app/shared/rating/model.py">
from app.shared.database import db
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Rating(db.Model):
    __tablename__ = "ratings"
    __table_args__ = (
        db.UniqueConstraint("trade_id", "rater_id", name="uq_rating_trade_rater"),
        db.CheckConstraint("score >= 1 AND score <= 5", name="ck_rating_score_range"),
    )

    id = uuid_pk_column()
    trade_id = db.Column(db.String(36), db.ForeignKey("trades.id"), nullable=False, index=True)
    rater_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)
    rated_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    score = db.Column(db.Integer, nullable=False)
    comment = db.Column(db.String(500), nullable=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
</file>

<file path="app/shared/rating/repository.py">
from sqlalchemy.exc import IntegrityError

from app.shared.database import db
from app.shared.rating.model import Rating
from app.shared.utils.errors import ConflictError


def list_for_user(user_id):
    return Rating.query.filter_by(rated_id=user_id).order_by(Rating.created_at.desc()).all()


def average_for_user(user_id):
    result = db.session.query(db.func.avg(Rating.score)).filter(Rating.rated_id == user_id).scalar()
    return float(result) if result is not None else None


def create_rating(rating):
    db.session.add(rating)
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        raise ConflictError("You have already rated this trade.")
    return rating
</file>

<file path="app/shared/rating/routes.py">
from flask import Blueprint

from app.shared.rating import controller as rating_controller
from app.auth.utils import require_auth

rating_bp = Blueprint("rating", __name__)


@rating_bp.post("/trades/<trade_id>/ratings")
@require_auth
def create_rating(trade_id):
    return rating_controller.create_rating(trade_id)


@rating_bp.get("/users/<user_id>/ratings")
@require_auth
def ratings_for_user(user_id):
    return rating_controller.ratings_for_user(user_id)
</file>

<file path="app/shared/rating/schema.py">
def serialize_rating(rating):
    return {
        "id": rating.id,
        "tradeId": rating.trade_id,
        "raterId": rating.rater_id,
        "ratedId": rating.rated_id,
        "score": rating.score,
        "comment": rating.comment,
        "createdAt": rating.created_at.isoformat(),
    }
</file>

<file path="app/shared/rating/service.py">
from app.shared.models.enums import TradeStatus
from app.shared.rating.model import Rating
from app.trades.offer.repository import get_accepted_offer
from app.shared.rating.repository import average_for_user, create_rating, list_for_user as repo_list_for_user
from app.trades.repository import get_trade_by_id
from app.shared.utils.errors import ForbiddenError, NotFoundError, ValidationError


def _other_party(trade, rater_id):
    accepted_offer = get_accepted_offer(trade.id)
    if accepted_offer is None:
        raise ForbiddenError("This trade has no accepted offer to rate.")

    if rater_id == trade.owner_id:
        return accepted_offer.offerer_id
    if rater_id == accepted_offer.offerer_id:
        return trade.owner_id
    raise ForbiddenError("Only the two parties of a trade may rate each other.")


def rate_trade(trade_id, rater_id, *, score, comment=None):
    trade = get_trade_by_id(trade_id)
    if trade is None:
        raise NotFoundError("Trade not found.")
    if trade.status != TradeStatus.COMPLETED:
        raise ForbiddenError("You can only rate completed trades.")
    if not isinstance(score, int) or score < 1 or score > 5:
        raise ValidationError("score must be an integer between 1 and 5.")

    rated_id = _other_party(trade, rater_id)

    rating = Rating(trade_id=trade.id, rater_id=rater_id, rated_id=rated_id, score=score, comment=comment)
    return create_rating(rating)


def list_for_user(user_id):
    return {
        "ratings": repo_list_for_user(user_id),
        "average": average_for_user(user_id),
    }
</file>

<file path="app/shared/utils/__init__.py">

</file>

<file path="app/shared/utils/errors.py">
class ApiError(Exception):
    status_code = 400

    def __init__(self, message, status_code=None):
        super().__init__(message)
        self.message = message
        if status_code is not None:
            self.status_code = status_code


class NotFoundError(ApiError):
    status_code = 404


class ConflictError(ApiError):
    status_code = 409


class ForbiddenError(ApiError):
    status_code = 403


class ValidationError(ApiError):
    status_code = 422


def register_error_handlers(app):
    @app.errorhandler(ApiError)
    def handle_api_error(err):
        return {"error": err.message}, err.status_code

    @app.errorhandler(404)
    def handle_404(err):
        return {"error": "Not found."}, 404

    @app.errorhandler(500)
    def handle_500(err):
        app.logger.exception(err)
        return {"error": "Internal server error."}, 500
</file>

<file path="app/shared/utils/file_storage.py">
import io
import os
import uuid

from flask import current_app
from PIL import Image
from werkzeug.utils import secure_filename

ALLOWED_EXTENSIONS = {"jpg", "jpeg", "png", "webp"}


def _extension(filename):
    if "." not in filename:
        return None
    return filename.rsplit(".", 1)[1].lower()


def save_image(file_storage, subfolder):
    """Validates, strips EXIF, and saves an uploaded image under UPLOAD_ROOT/subfolder.

    Returns the relative path to store on the model (e.g. "trades/<uuid>.jpg").
    """
    filename = secure_filename(file_storage.filename or "")
    ext = _extension(filename)
    if ext not in ALLOWED_EXTENSIONS:
        from app.shared.utils.errors import ValidationError

        raise ValidationError("Unsupported image type. Use JPEG, PNG, or WEBP.")

    image = Image.open(file_storage.stream)
    image = image.convert("RGB") if ext in ("jpg", "jpeg") else image.copy()

    # Strip EXIF (including GPS tags) by re-saving pixel data only.
    clean = Image.new(image.mode, image.size)
    clean.putdata(list(image.getdata()))

    upload_root = current_app.config["UPLOAD_ROOT"]
    target_dir = os.path.join(upload_root, subfolder)
    os.makedirs(target_dir, exist_ok=True)

    new_filename = f"{uuid.uuid4()}.{ext}"
    target_path = os.path.join(target_dir, new_filename)

    buffer = io.BytesIO()
    save_format = "JPEG" if ext in ("jpg", "jpeg") else ext.upper()
    clean.save(buffer, format=save_format)
    with open(target_path, "wb") as f:
        f.write(buffer.getvalue())

    return f"{subfolder}/{new_filename}"


def build_media_url(relative_path):
    if not relative_path:
        return None
    base = current_app.config["PUBLIC_BASE_URL"].rstrip("/")
    return f"{base}/media/{relative_path}"
</file>

<file path="app/shared/utils/mixins.py">
import uuid
from datetime import datetime, timezone

from app.shared.database import db


def utcnow():
    return datetime.now(timezone.utc)


def ensure_aware(dt):
    """SQLite drops tzinfo on round-trip even for DateTime(timezone=True) columns,
    while Postgres preserves it. Normalize so comparisons work on either backend."""
    if dt is not None and dt.tzinfo is None:
        return dt.replace(tzinfo=timezone.utc)
    return dt


def uuid_pk_column():
    return db.Column(db.String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
</file>

<file path="app/shared/utils/pagination.py">
from flask import request

DEFAULT_PAGE_SIZE = 20
MAX_PAGE_SIZE = 100


def paginate_query(query):
    page = request.args.get("page", 1, type=int)
    page_size = request.args.get("page_size", DEFAULT_PAGE_SIZE, type=int)
    page = max(page, 1)
    page_size = min(max(page_size, 1), MAX_PAGE_SIZE)

    total = query.count()
    items = query.offset((page - 1) * page_size).limit(page_size).all()

    return {
        "items": items,
        "page": page,
        "page_size": page_size,
        "total": total,
    }
</file>

<file path="app/shared/utils/rate_limit.py">
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

limiter = Limiter(key_func=get_remote_address, default_limits=[], storage_uri="memory://")
</file>

<file path="app/trades/offer/__init__.py">

</file>

<file path="app/trades/offer/controller.py">
from flask import g, request

from app.trades.offer.schema import serialize_offer
from app.trades.offer.service import (
    accept_offer as accept_offer_service,
    decline_offer as decline_offer_service,
    list_by_trade,
    list_received,
    list_sent,
    send_offer as send_offer_service,
)


def send_offer(trade_id):
    offer = send_offer_service(
        trade_id,
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_offer(offer), 201


def offers_for_trade(trade_id):
    offers = list_by_trade(trade_id)
    return [serialize_offer(o) for o in offers], 200


def accept_offer(offer_id):
    offer = accept_offer_service(offer_id, g.current_user.id)
    return serialize_offer(offer), 200


def decline_offer(offer_id):
    offer = decline_offer_service(offer_id, g.current_user.id)
    return serialize_offer(offer), 200


def my_sent_offers():
    offers = list_sent(g.current_user.id)
    return [serialize_offer(o) for o in offers], 200


def my_received_offers():
    offers = list_received(g.current_user.id)
    return [serialize_offer(o) for o in offers], 200
</file>

<file path="app/trades/offer/model.py">
from app.shared.database import db
from app.shared.models.enums import OfferStatus
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Offer(db.Model):
    __tablename__ = "offers"
    __table_args__ = (
        db.Index(
            "ix_offers_one_pending_per_trade",
            "trade_id",
            unique=True,
            postgresql_where=db.text("status = 'PENDING'"),
            sqlite_where=db.text("status = 'PENDING'"),
        ),
    )

    id = uuid_pk_column()
    trade_id = db.Column(db.String(36), db.ForeignKey("trades.id"), nullable=False, index=True)
    offerer_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    item_name = db.Column(db.String(120), nullable=False)
    category = db.Column(db.String(50), nullable=False)
    material = db.Column(db.String(120), nullable=False)
    weight_kg = db.Column(db.Numeric(8, 2), nullable=True)
    description = db.Column(db.Text, nullable=True)
    image_path = db.Column(db.String(255), nullable=True)

    status = db.Column(db.Enum(OfferStatus), nullable=False, default=OfferStatus.PENDING, index=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    decided_at = db.Column(db.DateTime(timezone=True), nullable=True)

    trade = db.relationship("Trade", back_populates="offers")
    offerer = db.relationship("User", back_populates="offers")
</file>

<file path="app/trades/offer/repository.py">
from sqlalchemy.exc import IntegrityError

from app.shared.database import db
from app.trades.offer.model import Offer
from app.trades.model import Trade
from app.shared.utils.errors import ConflictError


def get_offer_by_id(offer_id):
    return db.session.get(Offer, offer_id)


def list_by_trade(trade_id):
    return Offer.query.filter_by(trade_id=trade_id).order_by(Offer.created_at.desc()).all()


def get_accepted_offer(trade_id):
    from app.shared.models.enums import OfferStatus

    return Offer.query.filter_by(trade_id=trade_id, status=OfferStatus.ACCEPTED).first()


def list_sent(offerer_id):
    return Offer.query.filter_by(offerer_id=offerer_id).order_by(Offer.created_at.desc()).all()


def list_received(owner_id):
    return (
        Offer.query.join(Trade, Offer.trade_id == Trade.id)
        .filter(Trade.owner_id == owner_id)
        .order_by(Offer.created_at.desc())
        .all()
    )


def add_offer(offer):
    db.session.add(offer)
    return offer


def save(*_objects):
    try:
        db.session.commit()
    except IntegrityError:
        db.session.rollback()
        raise ConflictError("This trade already has an active offer.")
</file>

<file path="app/trades/offer/routes.py">
from flask import Blueprint

from app.trades.offer import controller as offer_controller
from app.auth.utils import require_auth

offer_bp = Blueprint("offer", __name__)


@offer_bp.post("/trades/<trade_id>/offers")
@require_auth
def send_offer(trade_id):
    return offer_controller.send_offer(trade_id)


@offer_bp.get("/trades/<trade_id>/offers")
@require_auth
def offers_for_trade(trade_id):
    return offer_controller.offers_for_trade(trade_id)


@offer_bp.post("/offers/<offer_id>/accept")
@require_auth
def accept_offer(offer_id):
    return offer_controller.accept_offer(offer_id)


@offer_bp.post("/offers/<offer_id>/decline")
@require_auth
def decline_offer(offer_id):
    return offer_controller.decline_offer(offer_id)


@offer_bp.get("/offers/mine")
@require_auth
def my_sent_offers():
    return offer_controller.my_sent_offers()


@offer_bp.get("/offers/received")
@require_auth
def my_received_offers():
    return offer_controller.my_received_offers()
</file>

<file path="app/trades/offer/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_offer(offer):
    return {
        "id": offer.id,
        "tradeId": offer.trade_id,
        "tradeName": offer.trade.item_name,
        "offererId": offer.offerer_id,
        "offererName": offer.offerer.first_name,
        "itemName": offer.item_name,
        "category": offer.category,
        "material": offer.material,
        "weightKg": float(offer.weight_kg) if offer.weight_kg is not None else None,
        "description": offer.description,
        "image": build_media_url(offer.image_path),
        "status": offer.status.value,
        "createdAt": offer.created_at.isoformat(),
        "decidedAt": offer.decided_at.isoformat() if offer.decided_at else None,
    }
</file>

<file path="app/trades/offer/service.py">
from app.shared.models.enums import NotificationType, OfferStatus, TradeStatus
from app.trades.offer.model import Offer
from app.trades.offer.repository import (
    add_offer,
    get_offer_by_id,
    list_by_trade as repo_list_by_trade,
    list_received as repo_list_received,
    list_sent as repo_list_sent,
    save,
)
from app.trades.repository import get_trade_by_id
from app.shared.notification.service import notify
from app.shared.utils.errors import ConflictError, ForbiddenError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import utcnow

REQUIRED_OFFER_FIELDS = ("itemName", "category", "material")


def send_offer(trade_id, offerer_id, *, fields, image_file):
    trade = get_trade_by_id(trade_id)
    if trade is None:
        raise NotFoundError("Trade not found.")
    if trade.owner_id == offerer_id:
        raise ForbiddenError("You cannot send an offer on your own trade.")
    if trade.status != TradeStatus.OPEN:
        raise ConflictError("This trade is no longer accepting offers.")

    missing = [f for f in REQUIRED_OFFER_FIELDS if not fields.get(f)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    offer = Offer(
        trade_id=trade.id,
        offerer_id=offerer_id,
        item_name=fields["itemName"],
        category=fields["category"],
        material=fields["material"],
        weight_kg=fields.get("weightKg"),
        description=fields.get("description"),
        status=OfferStatus.PENDING,
    )
    if image_file is not None:
        offer.image_path = save_image(image_file, "offers")

    add_offer(offer)
    trade.status = TradeStatus.RESERVED
    save(offer, trade)

    notify(
        recipient_id=trade.owner_id,
        type=NotificationType.OFFER_RECEIVED,
        title="New offer received",
        body=f"You received an offer on \"{trade.item_name}\".",
        trade_id=trade.id,
        offer_id=offer.id,
    )
    return offer


def _get_offer_for_decision(offer_id, caller_id):
    offer = get_offer_by_id(offer_id)
    if offer is None:
        raise NotFoundError("Offer not found.")
    if offer.trade.owner_id != caller_id:
        raise ForbiddenError("Only the trade owner can decide on this offer.")
    if offer.status != OfferStatus.PENDING:
        raise ConflictError("This offer has already been decided.")
    return offer


def accept_offer(offer_id, caller_id):
    offer = _get_offer_for_decision(offer_id, caller_id)
    offer.status = OfferStatus.ACCEPTED
    offer.decided_at = utcnow()
    offer.trade.status = TradeStatus.COMPLETED
    save(offer, offer.trade)

    notify(
        recipient_id=offer.offerer_id,
        type=NotificationType.OFFER_ACCEPTED,
        title="Offer accepted",
        body=f"Your offer on \"{offer.trade.item_name}\" was accepted!",
        trade_id=offer.trade_id,
        offer_id=offer.id,
    )
    return offer


def decline_offer(offer_id, caller_id):
    offer = _get_offer_for_decision(offer_id, caller_id)
    offer.status = OfferStatus.DECLINED
    offer.decided_at = utcnow()
    offer.trade.status = TradeStatus.OPEN
    save(offer, offer.trade)

    notify(
        recipient_id=offer.offerer_id,
        type=NotificationType.OFFER_DECLINED,
        title="Offer declined",
        body=f"Your offer on \"{offer.trade.item_name}\" was declined.",
        trade_id=offer.trade_id,
        offer_id=offer.id,
    )
    return offer


def list_by_trade(trade_id):
    return repo_list_by_trade(trade_id)


def list_sent(offerer_id):
    return repo_list_sent(offerer_id)


def list_received(owner_id):
    return repo_list_received(owner_id)
</file>

<file path="app/trades/__init__.py">

</file>

<file path="app/trades/controller.py">
from flask import g, request

from app.trades.schema import serialize_trade
from app.trades.service import (
    create_trade as create_trade_service,
    get_trade as get_trade_service,
    list_browse,
    list_mine,
    update_trade as update_trade_service,
)


def browse_trades():
    category = request.args.get("category")
    location = request.args.get("location")
    trades = list_browse(g.current_user.id, category=category, location=location)
    return [serialize_trade(t) for t in trades], 200


def my_trades():
    trades = list_mine(g.current_user.id)
    return [serialize_trade(t) for t in trades], 200


def get_trade(trade_id):
    trade = get_trade_service(trade_id)
    return serialize_trade(trade), 200


def create_trade():
    trade = create_trade_service(
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_trade(trade), 201


def update_trade(trade_id):
    trade = update_trade_service(
        trade_id,
        g.current_user.id,
        fields=request.form,
        image_file=request.files.get("image"),
    )
    return serialize_trade(trade), 200
</file>

<file path="app/trades/model.py">
from app.shared.database import db
from app.shared.models.enums import TradeStatus, TradingForType
from app.shared.utils.mixins import utcnow, uuid_pk_column


class Trade(db.Model):
    __tablename__ = "trades"

    id = uuid_pk_column()
    owner_id = db.Column(db.String(36), db.ForeignKey("users.id"), nullable=False, index=True)

    item_name = db.Column(db.String(120), nullable=False)
    category = db.Column(db.String(50), nullable=False, index=True)
    material = db.Column(db.String(120), nullable=False)
    description = db.Column(db.Text, nullable=True)
    weight_kg = db.Column(db.Numeric(8, 2), nullable=True)
    quantity = db.Column(db.Integer, nullable=False, default=1)

    location_text = db.Column(db.String(255), nullable=False, index=True)
    pickup_location_text = db.Column(db.String(255), nullable=True)

    trading_for_type = db.Column(db.Enum(TradingForType), nullable=False, default=TradingForType.NEGOTIATING)
    trading_for_value = db.Column(db.String(255), nullable=True)

    image_path = db.Column(db.String(255), nullable=True)

    status = db.Column(db.Enum(TradeStatus), nullable=False, default=TradeStatus.OPEN, index=True)

    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    updated_at = db.Column(db.DateTime(timezone=True), default=utcnow, onupdate=utcnow, nullable=False)

    owner = db.relationship("User", back_populates="trades")
    offers = db.relationship("Offer", back_populates="trade", lazy="dynamic", cascade="all, delete-orphan")

    @property
    def has_offers(self):
        return self.offers.count() > 0

    @property
    def offer_accepted(self):
        from app.shared.models.enums import OfferStatus

        return self.offers.filter_by(status=OfferStatus.ACCEPTED).first() is not None
</file>

<file path="app/trades/repository.py">
from app.shared.database import db
from app.trades.model import Trade


def get_trade_by_id(trade_id):
    return db.session.get(Trade, trade_id)


def list_browse(*, exclude_owner_id=None, category=None, location=None):
    query = Trade.query
    if exclude_owner_id is not None:
        query = query.filter(Trade.owner_id != exclude_owner_id)
    if category:
        query = query.filter(db.func.lower(Trade.category) == category.lower())
    if location:
        query = query.filter(Trade.location_text.ilike(f"%{location}%"))
    return query.order_by(Trade.created_at.desc())


def list_mine(owner_id):
    return Trade.query.filter_by(owner_id=owner_id).order_by(Trade.created_at.desc())


def create_trade(trade):
    db.session.add(trade)
    db.session.commit()
    return trade


def save_trade(trade):
    db.session.commit()
    return trade
</file>

<file path="app/trades/routes.py">
from flask import Blueprint

from app.trades import controller as trade_controller
from app.auth.utils import require_auth

trade_bp = Blueprint("trade", __name__, url_prefix="/trades")


@trade_bp.get("")
@require_auth
def browse_trades():
    return trade_controller.browse_trades()


@trade_bp.get("/mine")
@require_auth
def my_trades():
    return trade_controller.my_trades()


@trade_bp.get("/<trade_id>")
@require_auth
def get_trade(trade_id):
    return trade_controller.get_trade(trade_id)


@trade_bp.post("")
@require_auth
def create_trade():
    return trade_controller.create_trade()


@trade_bp.patch("/<trade_id>")
@require_auth
def update_trade(trade_id):
    return trade_controller.update_trade(trade_id)
</file>

<file path="app/trades/schema.py">
from app.shared.utils.file_storage import build_media_url


def serialize_trade(trade):
    return {
        "id": trade.id,
        "name": trade.item_name,
        "category": trade.category,
        "material": trade.material,
        "image": build_media_url(trade.image_path),
        "location": trade.location_text,
        "pickupLocation": trade.pickup_location_text,
        "posterName": trade.owner.first_name,
        "posterId": trade.owner_id,
        "weightKg": float(trade.weight_kg) if trade.weight_kg is not None else None,
        "quantity": trade.quantity,
        "tradingFor": {
            "type": trade.trading_for_type.value,
            "value": trade.trading_for_value,
        },
        "description": trade.description,
        "status": trade.status.value,
        "hasOffers": trade.has_offers,
        "offerAccepted": trade.offer_accepted,
        "createdAt": trade.created_at.isoformat(),
    }
</file>

<file path="app/trades/service.py">
from app.shared.models.enums import TradeStatus, TradingForType
from app.trades.model import Trade
from app.trades.repository import (
    create_trade as create_trade_row,
    get_trade_by_id,
    list_browse as repo_list_browse,
    list_mine as repo_list_mine,
    save_trade,
)
from app.shared.utils.errors import ForbiddenError, NotFoundError, ValidationError
from app.shared.utils.file_storage import save_image

VALID_TRADING_FOR_TYPES = {t.value for t in TradingForType}


def create_trade(owner_id, *, fields, image_file):
    trading_for_type = fields.get("tradingForType")
    if trading_for_type not in VALID_TRADING_FOR_TYPES:
        raise ValidationError("tradingForType must be one of: specific, nothing, negotiating.")

    required = ("itemName", "category", "material", "locationText", "quantity")
    missing = [f for f in required if not fields.get(f)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    trade = Trade(
        owner_id=owner_id,
        item_name=fields["itemName"],
        category=fields["category"],
        material=fields["material"],
        description=fields.get("description"),
        weight_kg=fields.get("weightKg"),
        quantity=int(fields["quantity"]),
        location_text=fields["locationText"],
        pickup_location_text=fields.get("pickupLocationText"),
        trading_for_type=TradingForType(trading_for_type),
        trading_for_value=fields.get("tradingForValue"),
        status=TradeStatus.OPEN,
    )
    if image_file is not None:
        trade.image_path = save_image(image_file, "trades")

    return create_trade_row(trade)


def get_trade(trade_id):
    trade = get_trade_by_id(trade_id)
    if trade is None:
        raise NotFoundError("Trade not found.")
    return trade


def update_trade(trade_id, owner_id, *, fields, image_file):
    trade = get_trade(trade_id)
    if trade.owner_id != owner_id:
        raise ForbiddenError("Only the trade owner can edit this trade.")

    for form_field, attr in (
        ("itemName", "item_name"),
        ("category", "category"),
        ("material", "material"),
        ("description", "description"),
        ("weightKg", "weight_kg"),
        ("locationText", "location_text"),
        ("pickupLocationText", "pickup_location_text"),
        ("tradingForValue", "trading_for_value"),
    ):
        if form_field in fields:
            setattr(trade, attr, fields[form_field])

    if "quantity" in fields:
        trade.quantity = int(fields["quantity"])
    if "tradingForType" in fields:
        if fields["tradingForType"] not in VALID_TRADING_FOR_TYPES:
            raise ValidationError("tradingForType must be one of: specific, nothing, negotiating.")
        trade.trading_for_type = TradingForType(fields["tradingForType"])
    if image_file is not None:
        trade.image_path = save_image(image_file, "trades")

    return save_trade(trade)


def list_browse(current_user_id, *, category=None, location=None):
    return repo_list_browse(exclude_owner_id=current_user_id, category=category, location=location).all()


def list_mine(owner_id):
    return repo_list_mine(owner_id).all()
</file>

<file path="app/__init__.py">

</file>

<file path="app/app.py">
import os

from flask import Flask, current_app, send_from_directory
from flask_cors import CORS
from flask_jwt_extended import JWTManager

from app.config import Config
from app.shared.database import db, migrate
from app.shared.utils.errors import register_error_handlers
from app.shared.utils.rate_limit import limiter


def create_app(config_object=Config):
    app = Flask(__name__, instance_relative_config=True)
    app.config.from_object(config_object)

    db.init_app(app)
    migrate.init_app(app, db)

    with app.app_context():
        from app.shared import models  # noqa: F401  (registers models with SQLAlchemy metadata)
    JWTManager(app)
    CORS(app, origins=app.config["CORS_ORIGINS"], supports_credentials=True)
    limiter.init_app(app)

    register_error_handlers(app)
    _register_blueprints(app)

    os.makedirs(app.config["UPLOAD_ROOT"], exist_ok=True)

    @app.get("/")
    def index():
        return {
            "service": "RosaCycle API",
            "status": "ok",
            "docs": "backend/docs/API.md",
            "health": "/health",
        }

    @app.get("/health")
    def health():
        return {"status": "ok"}

    @app.get("/media/<path:filename>")
    def media(filename):
        return send_from_directory(current_app.config["UPLOAD_ROOT"], filename)

    return app


def _register_blueprints(app):
    from app.auth.routes import auth_bp
    from app.profile.routes import user_bp
    from app.trades.routes import trade_bp
    from app.trades.offer.routes import offer_bp
    from app.map.routes import resource_spot_bp
    from app.shared.notification.routes import notification_bp
    from app.shared.rating.routes import rating_bp
    from app.scan.routes import ai_bp

    app.register_blueprint(auth_bp)
    app.register_blueprint(user_bp)
    app.register_blueprint(trade_bp)
    app.register_blueprint(offer_bp)
    app.register_blueprint(resource_spot_bp)
    app.register_blueprint(notification_bp)
    app.register_blueprint(rating_bp)
    app.register_blueprint(ai_bp)
</file>

<file path="docs/API.md">
# RosaCycle Backend API Reference

A complete reference for every endpoint currently implemented. Written for whoever wires the frontend up to this backend — read this instead of the Flask source.

## Base URL

Local dev: `http://localhost:5000`
Deployed: whatever Railway domain the project owner gives you (ask them — the CLI session that built this backend was deliberately unlinked from Railway so the deployed project could be handed off cleanly).

## Auth

All endpoints require a JWT **except**: `POST /auth/register`, `POST /auth/login`, `GET /health`, `GET /`, `GET /media/<path>`.

Flow: call `POST /auth/register` or `POST /auth/login` → both return `{"token": "...", "user": {...}}` → send that token on every subsequent request as:

```
Authorization: Bearer <token>
```

There is no refresh-token flow and no server-side session/blocklist — tokens are stateless JWTs with no built-in expiry handling beyond whatever `flask-jwt-extended`'s defaults are. `POST /auth/logout` is a no-op that just returns `{"success": true}` — actually discarding the token client-side (e.g. clearing local storage) is what "logs out" the user.

## Error format

Every error (validation, not-found, forbidden, conflict, or unexpected) returns:

```json
{ "error": "Human-readable message." }
```

with an appropriate HTTP status code:

| Status | Meaning | Example cause |
|---|---|---|
| 400 | Validation error | Missing required field |
| 401 | Auth failure | Missing/invalid JWT, wrong login credentials |
| 403 | Forbidden | Acting on a resource you don't own (e.g. editing someone else's trade) |
| 404 | Not found | Bad ID in a URL |
| 409 | Conflict | e.g. sending an offer on a trade that's already reserved, re-deciding an already-decided offer |
| 422 | Unprocessable | Reserved for stricter validation, currently used interchangeably with 400 in a couple of spots |
| 500 | Server error | Unexpected/unhandled exception |

## Request formats — read this before wiring forms

Most endpoints take a plain **JSON body** (`Content-Type: application/json`). Exactly **three** take **`multipart/form-data`** instead, because they accept an image file alongside text fields:

- `POST /trades` (create trade)
- `POST /trades/:id/offers` (send offer)
- `POST /resource-spots` (report resource spot)
- (also `PATCH /trades/:id`, `PATCH /users/me`, `POST /resource-spots/:id/photos` — anything that can attach/replace an image)

For those, send a `FormData` body with each field as a form field (not nested JSON) and the image under the key `image`. Don't set `Content-Type` manually for `FormData` — let the browser set the multipart boundary.

## Enum values — exact casing the API returns

These are lowercase (Python enum `.value`), not capitalized. Map to whatever display labels you want on the frontend — don't guess the casing.

- **Trade status**: `open` | `reserved` | `completed`
- **Offer status**: `pending` | `accepted` | `declined`
- **Resource Spot status**: `active` | `collected`
- **Trading-for type**: `specific` | `nothing` | `negotiating`
- **User role**: `user` | `admin`
- **Notification type**: `offer_received` | `offer_accepted` | `offer_declined`

## Images

Any `image` field in a response is either `null` or a full URL (already resolved against `PUBLIC_BASE_URL`) — e.g. `"https://.../media/trades/<uuid>.jpg"`. Just use it directly as an `<img src>`; don't try to construct the URL yourself.

---

## Auth

### `POST /auth/register`
Public. JSON body:
```json
{ "username": "luke", "firstName": "Luke", "lastName": "S", "email": "luke@example.com", "password": "password123" }
```
→ `201`
```json
{ "token": "...", "user": { "id": "...", "username": "luke", "firstName": "Luke", "lastName": "S", "email": "luke@example.com", "profileImage": null, "role": "user", "createdAt": "2026-07-28T12:01:44+00:00" } }
```
`409` if email or username is already taken.

### `POST /auth/login`
Public. JSON body: `{ "email": "...", "password": "..." }` → `200` same shape as register. `401` on wrong credentials.

### `POST /auth/logout`
Auth required. No body. → `200 { "success": true }`. Doesn't invalidate the token server-side.

---

## User

### `GET /users/me`
Auth required. → `200`, the current user (same shape as the register/login `user` object).

### `PATCH /users/me`
Auth required. **Multipart** form fields, all optional: `firstName`, `lastName`, `username`, `email`, `password`, `currentPassword` (required if changing `username`/`email`/`password`), file field `image` for a new profile photo.

Business rule: changing username/email/password/photo is rate-limited to once every `PROFILE_EDIT_COOLDOWN_DAYS` (default 7) — a second sensitive change within that window returns `409` with a "you can update again in N day(s)" message.

→ `200`, updated user object. `403` if `currentPassword` is missing/wrong when required.

---

## Trade

Trade object shape (returned by all trade endpoints):
```json
{
  "id": "...", "name": "Old Woods", "category": "Wood", "material": "Reclaimed lumber",
  "image": null, "location": "Balibago, Santa Rosa, Laguna", "pickupLocation": null,
  "posterName": "Luke", "posterId": "...", "weightKg": 4.5, "quantity": 1,
  "tradingFor": { "type": "negotiating", "value": null },
  "description": "...", "status": "open", "hasOffers": false, "offerAccepted": false,
  "createdAt": "2026-07-28T12:01:44+00:00"
}
```
Note: `hasOffers`/`offerAccepted` are computed from the trade's offers at read time — they're not stored fields, always trust what the API returns rather than caching/deriving them yourself.

### `GET /trades`
Auth required. Query params (both optional): `?category=Wood`, `?location=Santa+Rosa`. Returns all **other users'** open/reserved/completed trades (excludes your own) — this is the Browse view. → `200`, array of Trade.

### `GET /trades/mine`
Auth required. → `200`, array of your own trades (Track Trades / My Trades view).

### `GET /trades/:id`
Auth required. → `200`, single Trade. `404` if not found.

### `POST /trades`
Auth required. **Multipart.** Required fields: `itemName`, `category`, `material`, `locationText`, `quantity`, `tradingForType` (one of `specific`/`nothing`/`negotiating`). Optional: `description`, `weightKg`, `pickupLocationText`, `tradingForValue` (required in practice if `tradingForType` is `specific`), file `image`. → `201`, the created Trade.

### `PATCH /trades/:id`
Auth required, **owner only** (`403` otherwise). **Multipart.** Any of the same fields as create, all optional — only sent fields get updated. → `200`, updated Trade.

---

## Offer

Offer object shape:
```json
{
  "id": "...", "tradeId": "...", "tradeName": "Old Woods",
  "offererId": "...", "offererName": "Aero",
  "itemName": "Old Bond Paper", "category": "Paper", "material": "Clean sheets",
  "weightKg": 2.0, "description": "...", "image": null,
  "status": "pending", "createdAt": "...", "decidedAt": null
}
```

### `POST /trades/:id/offers`
Auth required. **Multipart.** Required: `itemName`, `category`, `material`. Optional: `weightKg`, `description`, file `image`.

Business rules enforced: you can't offer on your own trade (`403`); the trade must be `open` (`409` if already `reserved`/`completed` — **a trade can only have one active/pending offer at a time**, this is the core invariant). On success the trade flips to `reserved`. → `201`, the created Offer.

### `GET /trades/:id/offers`
Auth required. → `200`, array of Offers on that trade (there will only ever be at most one `pending` at a time, but past `declined` ones stay in history).

### `POST /offers/:id/accept`
Auth required, must be the **trade owner** (`403` otherwise). Offer must be `pending` (`409` if already decided). On success: offer → `accepted`, trade → `completed`, offerer gets an `offer_accepted` notification. → `200`, updated Offer.

### `POST /offers/:id/decline`
Same auth/ownership rules as accept. On success: offer → `declined`, **trade reopens to `open`** (so a new offer can be sent), offerer gets an `offer_declined` notification. → `200`, updated Offer.

### `GET /offers/mine`
Auth required. → `200`, array of offers **you sent** (on other people's trades).

### `GET /offers/received`
Auth required. → `200`, array of offers **received** on your own trades.

---

## Resource Spot

Resource Spot object shape:
```json
{
  "id": "...", "name": "Garbage Pile", "material": "Plastic",
  "weightKg": 6.5, "quantity": 12, "description": "...",
  "location": "Tagapo, Santa Rosa, Laguna", "permissionNote": "Public area, no permission needed.",
  "image": null, "status": "active", "reporterId": "...", "reporterName": "Luke",
  "createdAt": "...", "expiresAt": "..."
}
```
No lat/lng — location is free text only (MVP scope decision). Map UI needs to render pins some other way (e.g. geocode `location` client-side, or just list them) — this is a known frontend gap, not something the API provides.

### `GET /resource-spots`
Auth required. → `200`, array of currently **active and unexpired** spots only (expired/collected ones are filtered out server-side — no client-side filtering needed).

### `GET /resource-spots/:id`
Auth required. → `200`, single spot (includes expired/collected ones too, unlike the list endpoint). `404` if not found.

### `POST /resource-spots`
Auth required. **Multipart.** Required: `name`, `material`, `locationText`. Optional: `weightKg`, `quantity`, `description`, `permissionNote`, file `image`. → `201`, created spot. `expiresAt` is set automatically (server-configured number of days out).

### `POST /resource-spots/:id/photos`
Auth required — **any authenticated user**, not just the reporter (per spec: "community members may submit a new photo after collecting materials"). **Multipart**, required file `image`. `409` if the spot is already `collected`. → `200`, updated spot with the new photo.

### `POST /resource-spots/:id/collected`
Auth required — again any authenticated user, not owner-restricted. No body. `409` if already collected. → `200`, updated spot with `status: "collected"` (this is when it should disappear from a map view — check `GET /resource-spots` again, it'll be filtered out).

---

## Notification

Notification object shape:
```json
{
  "id": "...", "type": "offer_received", "title": "New offer received",
  "body": "You received an offer on \"Old Woods\".",
  "relatedTradeId": "...", "relatedOfferId": "...",
  "isRead": false, "createdAt": "..."
}
```

### `GET /notifications`
Auth required. → `200`, all your notifications, newest first.

### `GET /notifications/unread-count`
Auth required. → `200 { "count": 3 }` — for a bell-icon badge.

### `PATCH /notifications/:id/read`
Auth required, must be the recipient (`403` otherwise). No body. → `200`, updated notification with `isRead: true`.

---

## Rating

Only usable on **completed** trades, and only by the two actual parties (the trade owner and the offerer whose offer was accepted) rating each other — one rating per direction per trade.

### `POST /trades/:id/ratings`
Auth required. JSON body: `{ "score": 5, "comment": "Great trade!" }` (`score` 1–5 required, `comment` optional). `403` if the trade isn't completed, you're not a party to it, or there's no accepted offer to rate. `409` if you've already rated this trade. → `201`, created rating.

### `GET /users/:id/ratings`
Auth required. → `200 { "ratings": [...], "average": 4.5 }` — `average` is `null` if the user has no ratings yet.

---

## AI (Gemini photo assessment)

Stateless — nothing is persisted by these calls. Always returns `200`, never errors out to the client (falls back to an empty/zero-confidence suggestion if Gemini fails or `GEMINI_API_KEY` isn't set) — always let the user fall through to manual entry regardless of what comes back.

### `POST /ai/assess-trade-photo`
Auth required. **Multipart**, required file `image`. → `200`:
```json
{ "itemName": "", "category": "", "material": "Steel scrap", "weightKg": 4.5, "confidence": 87 }
```

### `POST /ai/assess-resource-spot-photo`
Auth required. **Multipart**, required file `image`. → `200`:
```json
{ "material": "Plastic", "weightKg": 4.0, "quantity": 3, "confidence": 82 }
```

---

## Misc

### `GET /health`
Public. → `200 { "status": "ok" }`.

### `GET /`
Public. → `200`, basic service info — useful for confirming the deployed URL is actually this API before you start integrating.

### `GET /media/<path:filename>`
Public. Serves uploaded images. You should never need to construct this URL manually — every `image` field in every response is already the full resolved URL.
</file>

<file path="migrations/versions/41b0a18a356f_initial_schema.py">
"""Initial schema

Revision ID: 41b0a18a356f
Revises: 
Create Date: 2026-07-28 18:14:10.120158

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = '41b0a18a356f'
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table('users',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('username', sa.String(length=50), nullable=False),
    sa.Column('first_name', sa.String(length=50), nullable=False),
    sa.Column('last_name', sa.String(length=50), nullable=False),
    sa.Column('email', sa.String(length=255), nullable=False),
    sa.Column('password_hash', sa.String(length=255), nullable=False),
    sa.Column('profile_image_path', sa.String(length=255), nullable=True),
    sa.Column('role', sa.Enum('USER', 'ADMIN', name='userrole'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('profile_updated_at', sa.DateTime(timezone=True), nullable=True),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('users', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_users_email'), ['email'], unique=True)
        batch_op.create_index(batch_op.f('ix_users_username'), ['username'], unique=True)

    op.create_table('resource_spots',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('reporter_id', sa.String(length=36), nullable=False),
    sa.Column('name', sa.String(length=120), nullable=False),
    sa.Column('material', sa.String(length=120), nullable=False),
    sa.Column('weight_kg', sa.Numeric(precision=8, scale=2), nullable=True),
    sa.Column('quantity', sa.Integer(), nullable=True),
    sa.Column('description', sa.Text(), nullable=True),
    sa.Column('location_text', sa.String(length=255), nullable=False),
    sa.Column('permission_note', sa.String(length=255), nullable=True),
    sa.Column('image_path', sa.String(length=255), nullable=True),
    sa.Column('status', sa.Enum('ACTIVE', 'COLLECTED', name='resourcespotstatus'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('expires_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('collected_at', sa.DateTime(timezone=True), nullable=True),
    sa.ForeignKeyConstraint(['reporter_id'], ['users.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('resource_spots', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_resource_spots_location_text'), ['location_text'], unique=False)
        batch_op.create_index(batch_op.f('ix_resource_spots_material'), ['material'], unique=False)
        batch_op.create_index(batch_op.f('ix_resource_spots_reporter_id'), ['reporter_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_resource_spots_status'), ['status'], unique=False)

    op.create_table('trades',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('owner_id', sa.String(length=36), nullable=False),
    sa.Column('item_name', sa.String(length=120), nullable=False),
    sa.Column('category', sa.String(length=50), nullable=False),
    sa.Column('material', sa.String(length=120), nullable=False),
    sa.Column('description', sa.Text(), nullable=True),
    sa.Column('weight_kg', sa.Numeric(precision=8, scale=2), nullable=True),
    sa.Column('quantity', sa.Integer(), nullable=False),
    sa.Column('location_text', sa.String(length=255), nullable=False),
    sa.Column('pickup_location_text', sa.String(length=255), nullable=True),
    sa.Column('trading_for_type', sa.Enum('SPECIFIC', 'NOTHING', 'NEGOTIATING', name='tradingfortype'), nullable=False),
    sa.Column('trading_for_value', sa.String(length=255), nullable=True),
    sa.Column('image_path', sa.String(length=255), nullable=True),
    sa.Column('status', sa.Enum('OPEN', 'RESERVED', 'COMPLETED', name='tradestatus'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('updated_at', sa.DateTime(timezone=True), nullable=False),
    sa.ForeignKeyConstraint(['owner_id'], ['users.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('trades', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_trades_category'), ['category'], unique=False)
        batch_op.create_index(batch_op.f('ix_trades_location_text'), ['location_text'], unique=False)
        batch_op.create_index(batch_op.f('ix_trades_owner_id'), ['owner_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_trades_status'), ['status'], unique=False)

    op.create_table('offers',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('trade_id', sa.String(length=36), nullable=False),
    sa.Column('offerer_id', sa.String(length=36), nullable=False),
    sa.Column('item_name', sa.String(length=120), nullable=False),
    sa.Column('category', sa.String(length=50), nullable=False),
    sa.Column('material', sa.String(length=120), nullable=False),
    sa.Column('weight_kg', sa.Numeric(precision=8, scale=2), nullable=True),
    sa.Column('description', sa.Text(), nullable=True),
    sa.Column('image_path', sa.String(length=255), nullable=True),
    sa.Column('status', sa.Enum('PENDING', 'ACCEPTED', 'DECLINED', name='offerstatus'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('decided_at', sa.DateTime(timezone=True), nullable=True),
    sa.ForeignKeyConstraint(['offerer_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['trade_id'], ['trades.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('offers', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_offers_offerer_id'), ['offerer_id'], unique=False)
        batch_op.create_index('ix_offers_one_pending_per_trade', ['trade_id'], unique=True, postgresql_where=sa.text("status = 'PENDING'"), sqlite_where=sa.text("status = 'PENDING'"))
        batch_op.create_index(batch_op.f('ix_offers_status'), ['status'], unique=False)
        batch_op.create_index(batch_op.f('ix_offers_trade_id'), ['trade_id'], unique=False)

    op.create_table('ratings',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('trade_id', sa.String(length=36), nullable=False),
    sa.Column('rater_id', sa.String(length=36), nullable=False),
    sa.Column('rated_id', sa.String(length=36), nullable=False),
    sa.Column('score', sa.Integer(), nullable=False),
    sa.Column('comment', sa.String(length=500), nullable=True),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.CheckConstraint('score >= 1 AND score <= 5', name='ck_rating_score_range'),
    sa.ForeignKeyConstraint(['rated_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['rater_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['trade_id'], ['trades.id'], ),
    sa.PrimaryKeyConstraint('id'),
    sa.UniqueConstraint('trade_id', 'rater_id', name='uq_rating_trade_rater')
    )
    with op.batch_alter_table('ratings', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_ratings_rated_id'), ['rated_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_ratings_rater_id'), ['rater_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_ratings_trade_id'), ['trade_id'], unique=False)

    op.create_table('notifications',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('recipient_id', sa.String(length=36), nullable=False),
    sa.Column('type', sa.Enum('OFFER_RECEIVED', 'OFFER_ACCEPTED', 'OFFER_DECLINED', name='notificationtype'), nullable=False),
    sa.Column('title', sa.String(length=120), nullable=False),
    sa.Column('body', sa.String(length=255), nullable=False),
    sa.Column('related_trade_id', sa.String(length=36), nullable=True),
    sa.Column('related_offer_id', sa.String(length=36), nullable=True),
    sa.Column('is_read', sa.Boolean(), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.ForeignKeyConstraint(['recipient_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['related_offer_id'], ['offers.id'], ),
    sa.ForeignKeyConstraint(['related_trade_id'], ['trades.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('notifications', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_notifications_is_read'), ['is_read'], unique=False)
        batch_op.create_index(batch_op.f('ix_notifications_recipient_id'), ['recipient_id'], unique=False)

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('notifications', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_notifications_recipient_id'))
        batch_op.drop_index(batch_op.f('ix_notifications_is_read'))

    op.drop_table('notifications')
    with op.batch_alter_table('ratings', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_ratings_trade_id'))
        batch_op.drop_index(batch_op.f('ix_ratings_rater_id'))
        batch_op.drop_index(batch_op.f('ix_ratings_rated_id'))

    op.drop_table('ratings')
    with op.batch_alter_table('offers', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_offers_trade_id'))
        batch_op.drop_index(batch_op.f('ix_offers_status'))
        batch_op.drop_index('ix_offers_one_pending_per_trade', postgresql_where=sa.text("status = 'PENDING'"), sqlite_where=sa.text("status = 'PENDING'"))
        batch_op.drop_index(batch_op.f('ix_offers_offerer_id'))

    op.drop_table('offers')
    with op.batch_alter_table('trades', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_trades_status'))
        batch_op.drop_index(batch_op.f('ix_trades_owner_id'))
        batch_op.drop_index(batch_op.f('ix_trades_location_text'))
        batch_op.drop_index(batch_op.f('ix_trades_category'))

    op.drop_table('trades')
    with op.batch_alter_table('resource_spots', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_resource_spots_status'))
        batch_op.drop_index(batch_op.f('ix_resource_spots_reporter_id'))
        batch_op.drop_index(batch_op.f('ix_resource_spots_material'))
        batch_op.drop_index(batch_op.f('ix_resource_spots_location_text'))

    op.drop_table('resource_spots')
    with op.batch_alter_table('users', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_users_username'))
        batch_op.drop_index(batch_op.f('ix_users_email'))

    op.drop_table('users')
    # ### end Alembic commands ###
</file>

<file path="migrations/alembic.ini">
# A generic, single database configuration.

[alembic]
# template used to generate migration files
# file_template = %%(rev)s_%%(slug)s

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false


# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic,flask_migrate

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[logger_flask_migrate]
level = INFO
handlers =
qualname = flask_migrate

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
</file>

<file path="migrations/env.py">
import logging
from logging.config import fileConfig

from flask import current_app

from alembic import context

# this is the Alembic Config object, which provides
# access to the values within the .ini file in use.
config = context.config

# Interpret the config file for Python logging.
# This line sets up loggers basically.
fileConfig(config.config_file_name)
logger = logging.getLogger('alembic.env')


def get_engine():
    try:
        # this works with Flask-SQLAlchemy<3 and Alchemical
        return current_app.extensions['migrate'].db.get_engine()
    except (TypeError, AttributeError):
        # this works with Flask-SQLAlchemy>=3
        return current_app.extensions['migrate'].db.engine


def get_engine_url():
    try:
        return get_engine().url.render_as_string(hide_password=False).replace(
            '%', '%%')
    except AttributeError:
        return str(get_engine().url).replace('%', '%%')


# add your model's MetaData object here
# for 'autogenerate' support
# from myapp import mymodel
# target_metadata = mymodel.Base.metadata
config.set_main_option('sqlalchemy.url', get_engine_url())
target_db = current_app.extensions['migrate'].db

# other values from the config, defined by the needs of env.py,
# can be acquired:
# my_important_option = config.get_main_option("my_important_option")
# ... etc.


def get_metadata():
    if hasattr(target_db, 'metadatas'):
        return target_db.metadatas[None]
    return target_db.metadata


def run_migrations_offline():
    """Run migrations in 'offline' mode.

    This configures the context with just a URL
    and not an Engine, though an Engine is acceptable
    here as well.  By skipping the Engine creation
    we don't even need a DBAPI to be available.

    Calls to context.execute() here emit the given string to the
    script output.

    """
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url, target_metadata=get_metadata(), literal_binds=True
    )

    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online():
    """Run migrations in 'online' mode.

    In this scenario we need to create an Engine
    and associate a connection with the context.

    """

    # this callback is used to prevent an auto-migration from being generated
    # when there are no changes to the schema
    # reference: http://alembic.zzzcomputing.com/en/latest/cookbook.html
    def process_revision_directives(context, revision, directives):
        if getattr(config.cmd_opts, 'autogenerate', False):
            script = directives[0]
            if script.upgrade_ops.is_empty():
                directives[:] = []
                logger.info('No changes in schema detected.')

    conf_args = current_app.extensions['migrate'].configure_args
    if conf_args.get("process_revision_directives") is None:
        conf_args["process_revision_directives"] = process_revision_directives

    connectable = get_engine()

    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=get_metadata(),
            **conf_args
        )

        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
</file>

<file path="migrations/README">
Single-database configuration for Flask.
</file>

<file path="migrations/script.py.mako">
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

"""
from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

# revision identifiers, used by Alembic.
revision = ${repr(up_revision)}
down_revision = ${repr(down_revision)}
branch_labels = ${repr(branch_labels)}
depends_on = ${repr(depends_on)}


def upgrade():
    ${upgrades if upgrades else "pass"}


def downgrade():
    ${downgrades if downgrades else "pass"}
</file>

<file path=".flaskenv">
FLASK_APP=run.py
 FLASK_ENV=development
</file>

<file path=".gitignore">
venv/
__pycache__/
*.pyc
.env
instance/
*.db
</file>

<file path=".gitkeep">

</file>

<file path=".python-version">
3.13
</file>

<file path="Procfile">
release: flask db upgrade
web: gunicorn -w 1 -b 0.0.0.0:$PORT run:app
</file>

<file path="requirements.txt">
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Cors==5.0.0
Flask-Limiter==3.9.2
psycopg2-binary==2.9.10
python-dotenv==1.0.1
Pillow==11.0.0
google-generativeai==0.8.3
google-auth==2.38.0
gunicorn==23.0.0
</file>

<file path="run.py">
from dotenv import load_dotenv

load_dotenv()

from app.app import create_app  # noqa: E402

app = create_app()

if __name__ == "__main__":
    app.run(debug=True)
</file>

<file path="seed.py">
"""Populates the database with demo data for local development/demos.

Usage: python seed.py
Safe to re-run: skips seeding if any users already exist.
"""
from dotenv import load_dotenv

load_dotenv()

from app.app import create_app  # noqa: E402
from app.database import db  # noqa: E402
from app.models.enums import ResourceSpotStatus, TradeStatus, TradingForType  # noqa: E402
from app.models.resource_spot import ResourceSpot  # noqa: E402
from app.models.trade import Trade  # noqa: E402
from app.models.user import User  # noqa: E402
from app.utils.auth import hash_password  # noqa: E402

DEMO_USERS = [
    {"username": "luke", "first_name": "Luke", "last_name": "S", "email": "luke@example.com"},
    {"username": "aero", "first_name": "Aero", "last_name": "T", "email": "aero@example.com"},
    {"username": "franz", "first_name": "Franz", "last_name": "M", "email": "franz@example.com"},
]
DEMO_PASSWORD = "password123"

DEMO_TRADES = [
    {
        "item_name": "Old Woods", "category": "Wood", "material": "Reclaimed lumber",
        "description": "Still good, open for swap: paper or old magazines.",
        "weight_kg": 4.5, "quantity": 1, "location_text": "Balibago, Santa Rosa, Laguna",
        "trading_for_type": TradingForType.SPECIFIC, "trading_for_value": "Paper or old magazines",
    },
    {
        "item_name": "Scrap Metal Pile", "category": "Metal", "material": "Assorted steel scraps",
        "description": "From a home renovation project.",
        "weight_kg": 6.2, "quantity": 1, "location_text": "Dita, Santa Rosa, Laguna",
        "trading_for_type": TradingForType.NEGOTIATING, "trading_for_value": None,
    },
    {
        "item_name": "Bulk Cardboard", "category": "Mixed", "material": "Flattened boxes",
        "description": "Dry and clean.",
        "weight_kg": 15.0, "quantity": 9, "location_text": "Pulong Santa Cruz, Santa Rosa, Laguna",
        "trading_for_type": TradingForType.NOTHING, "trading_for_value": None,
    },
]

DEMO_SPOTS = [
    {
        "name": "Garbage Pile", "material": "Plastic",
        "description": "Pile of plastic bottles and containers left near the sari-sari store.",
        "weight_kg": 6.5, "quantity": 12, "location_text": "Tagapo, Santa Rosa, Laguna",
        "permission_note": "Public area, no permission needed.",
    },
    {
        "name": "Overgrown Lot", "material": "Mixed",
        "description": "Vacant lot with scattered scrap metal and cardboard.",
        "weight_kg": 15.0, "quantity": 5, "location_text": "Market Area, Santa Rosa, Laguna",
        "permission_note": "Ask barangay caretaker first.",
    },
]


def seed():
    app = create_app()
    with app.app_context():
        if User.query.first() is not None:
            print("Database already has users — skipping seed.")
            return

        users = []
        for u in DEMO_USERS:
            user = User(password_hash=hash_password(DEMO_PASSWORD), **u)
            db.session.add(user)
            users.append(user)
        db.session.flush()

        for i, t in enumerate(DEMO_TRADES):
            trade = Trade(owner_id=users[i % len(users)].id, status=TradeStatus.OPEN, **t)
            db.session.add(trade)

        for i, s in enumerate(DEMO_SPOTS):
            spot = ResourceSpot(
                reporter_id=users[i % len(users)].id,
                status=ResourceSpotStatus.ACTIVE,
                expires_at=ResourceSpot.default_expiry(app.config["RESOURCE_SPOT_EXPIRY_DAYS"]),
                **s,
            )
            db.session.add(spot)

        db.session.commit()
        print(f"Seeded {len(users)} users, {len(DEMO_TRADES)} trades, {len(DEMO_SPOTS)} resource spots.")
        print(f"Demo login: any of {[u['email'] for u in DEMO_USERS]} / password: {DEMO_PASSWORD}")


if __name__ == "__main__":
    seed()
</file>

</files>
</file>

<file path="frontend/public/favicon.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="2000" zoomAndPan="magnify" viewBox="0 0 1500 1499.999933" height="2000" preserveAspectRatio="xMidYMid meet" version="1.0"><defs><clipPath id="fb435d5862"><path d="M 977 371 L 1298 371 L 1298 628 L 977 628 Z M 977 371 " clip-rule="nonzero"/></clipPath><clipPath id="bd2365c669"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath><clipPath id="8a36694c69"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath></defs><path stroke-linecap="butt" transform="matrix(0.627627, -0.41059, 0.41059, 0.627627, 850.581296, 942.983635)" fill="none" stroke-linejoin="miter" d="M 27.738967 153.335882 C 120.789709 -8.442483 298.543162 -8.443382 560.987755 153.33184 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.324044, 0.676384, -0.676384, -0.324044, 1395.097425, 847.756731)" fill="none" stroke-linejoin="miter" d="M 25.672548 211.246672 C 203.554284 -27.749642 381.442508 -27.748337 559.323325 211.245693 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.742472, 0.105999, -0.105999, 0.742472, 345.349897, 817.95039)" fill="none" stroke-linejoin="miter" d="M 43.567852 24.529848 C 400.107518 657.734999 738.125941 657.738308 1057.622384 24.534619 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.406135, -0.630519, 0.630519, 0.406135, 183.309934, 719.938622)" fill="none" stroke-linejoin="miter" d="M 39.353683 265.381789 C 264.409727 -21.796159 535.300314 -21.794026 852.019803 265.379429 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.72096, 0.20668, -0.20668, 0.72096, 747.727769, 161.278749)" fill="none" stroke-linejoin="miter" d="M 25.450087 157.050118 C 266.832937 14.317423 428.639468 14.316418 510.866109 157.053544 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><g clip-path="url(#fb435d5862)"><g clip-path="url(#bd2365c669)"><g clip-path="url(#8a36694c69)"><path fill="#245f28" d="M 1262 379.851562 L 1262.277344 380 C 1263.257812 380.527344 1264.21875 381.082031 1265.171875 381.65625 C 1266.121094 382.230469 1267.058594 382.832031 1267.980469 383.453125 C 1268.902344 384.074219 1269.808594 384.71875 1270.699219 385.382812 C 1271.589844 386.050781 1272.460938 386.738281 1273.316406 387.445312 C 1274.175781 388.15625 1275.011719 388.886719 1275.832031 389.636719 C 1276.652344 390.386719 1277.453125 391.15625 1278.238281 391.945312 C 1279.019531 392.734375 1279.78125 393.542969 1280.527344 394.367188 C 1281.269531 395.195312 1281.992188 396.039062 1282.691406 396.902344 C 1283.394531 397.765625 1284.074219 398.644531 1284.734375 399.539062 C 1285.390625 400.4375 1286.027344 401.347656 1286.640625 402.273438 C 1287.253906 403.199219 1287.84375 404.140625 1288.414062 405.097656 C 1288.980469 406.054688 1289.523438 407.023438 1290.042969 408.007812 C 1290.5625 408.988281 1291.058594 409.984375 1291.53125 410.992188 C 1292 411.996094 1292.445312 413.015625 1292.867188 414.042969 C 1293.289062 415.074219 1293.683594 416.113281 1294.054688 417.160156 C 1294.425781 418.207031 1294.769531 419.265625 1295.085938 420.332031 C 1295.40625 421.394531 1295.695312 422.46875 1295.960938 423.546875 C 1296.226562 424.625 1296.464844 425.710938 1296.679688 426.804688 C 1296.890625 427.894531 1297.078125 428.992188 1297.234375 430.089844 C 1297.394531 431.191406 1297.523438 432.292969 1297.628906 433.402344 C 1297.734375 434.507812 1297.808594 435.617188 1297.859375 436.726562 C 1297.910156 437.839844 1297.933594 438.949219 1297.929688 440.0625 C 1297.921875 441.171875 1297.890625 442.285156 1297.832031 443.394531 C 1297.773438 444.503906 1297.6875 445.613281 1297.574219 446.71875 C 1297.460938 447.824219 1297.320312 448.925781 1297.152344 450.023438 C 1296.984375 451.125 1296.789062 452.21875 1296.566406 453.308594 C 1296.34375 454.394531 1296.097656 455.480469 1295.824219 456.558594 C 1295.546875 457.632812 1295.246094 458.703125 1294.921875 459.765625 C 1294.59375 460.828125 1294.238281 461.882812 1293.859375 462.929688 C 1293.480469 463.972656 1293.078125 465.007812 1292.648438 466.035156 C 1292.21875 467.058594 1291.765625 468.074219 1291.285156 469.074219 C 1290.804688 470.078125 1290.300781 471.070312 1289.773438 472.046875 L 1224.914062 592.132812 C 1224.386719 593.109375 1223.835938 594.074219 1223.261719 595.027344 C 1222.683594 595.976562 1222.085938 596.914062 1221.464844 597.835938 C 1220.84375 598.757812 1220.199219 599.664062 1219.53125 600.550781 C 1218.867188 601.441406 1218.179688 602.316406 1217.46875 603.171875 C 1216.761719 604.027344 1216.03125 604.867188 1215.28125 605.6875 C 1214.53125 606.507812 1213.761719 607.308594 1212.972656 608.089844 C 1212.183594 608.875 1211.375 609.636719 1210.546875 610.378906 C 1209.722656 611.125 1208.875 611.84375 1208.015625 612.546875 C 1207.152344 613.25 1206.273438 613.929688 1205.375 614.585938 C 1204.480469 615.246094 1203.570312 615.882812 1202.644531 616.496094 C 1201.714844 617.109375 1200.773438 617.699219 1199.816406 618.265625 C 1198.863281 618.832031 1197.894531 619.378906 1196.910156 619.898438 C 1195.929688 620.417969 1194.933594 620.914062 1193.925781 621.382812 C 1192.917969 621.855469 1191.902344 622.300781 1190.871094 622.722656 C 1189.84375 623.144531 1188.804688 623.539062 1187.757812 623.910156 C 1186.707031 624.277344 1185.652344 624.621094 1184.585938 624.941406 C 1183.519531 625.257812 1182.449219 625.550781 1181.371094 625.816406 C 1180.289062 626.082031 1179.203125 626.320312 1178.113281 626.53125 C 1177.023438 626.746094 1175.925781 626.929688 1174.824219 627.089844 C 1173.726562 627.246094 1172.621094 627.378906 1171.515625 627.484375 C 1170.410156 627.585938 1169.300781 627.664062 1168.1875 627.714844 C 1167.078125 627.765625 1165.96875 627.785156 1164.855469 627.78125 C 1163.746094 627.777344 1162.632812 627.746094 1161.523438 627.6875 C 1160.414062 627.628906 1159.304688 627.539062 1158.199219 627.425781 C 1157.09375 627.3125 1155.992188 627.171875 1154.890625 627.003906 C 1153.792969 626.835938 1152.699219 626.644531 1151.609375 626.421875 C 1150.519531 626.199219 1149.4375 625.953125 1148.359375 625.675781 C 1147.28125 625.402344 1146.210938 625.101562 1145.148438 624.773438 C 1144.085938 624.449219 1143.035156 624.09375 1141.988281 623.714844 C 1140.945312 623.335938 1139.910156 622.933594 1138.882812 622.503906 C 1137.859375 622.074219 1136.84375 621.617188 1135.839844 621.140625 C 1134.839844 620.660156 1133.847656 620.15625 1132.871094 619.628906 L 1012.738281 554.785156 C 1011.761719 554.257812 1010.796875 553.703125 1009.84375 553.128906 C 1008.890625 552.554688 1007.957031 551.957031 1007.035156 551.335938 C 1006.113281 550.714844 1005.207031 550.070312 1004.316406 549.402344 C 1003.425781 548.738281 1002.550781 548.050781 1001.695312 547.339844 C 1000.835938 546.632812 1000 545.902344 999.179688 545.152344 C 998.359375 544.402344 997.554688 543.632812 996.773438 542.84375 C 995.988281 542.054688 995.226562 541.246094 994.484375 540.421875 C 993.738281 539.59375 993.015625 538.75 992.316406 537.886719 C 991.613281 537.023438 990.933594 536.144531 990.273438 535.25 C 989.617188 534.355469 988.980469 533.441406 988.367188 532.515625 C 987.75 531.589844 987.160156 530.648438 986.59375 529.691406 C 986.027344 528.734375 985.480469 527.765625 984.960938 526.785156 C 984.441406 525.800781 983.945312 524.804688 983.476562 523.796875 C 983.003906 522.792969 982.558594 521.773438 982.136719 520.746094 C 981.714844 519.714844 981.320312 518.675781 980.949219 517.628906 C 980.578125 516.582031 980.234375 515.523438 979.917969 514.457031 C 979.597656 513.394531 979.308594 512.320312 979.042969 511.242188 C 978.777344 510.160156 978.539062 509.074219 978.324219 507.984375 C 978.113281 506.894531 977.925781 505.796875 977.769531 504.695312 C 977.609375 503.597656 977.480469 502.492188 977.375 501.386719 C 977.269531 500.277344 977.195312 499.167969 977.144531 498.058594 C 977.09375 496.949219 977.074219 495.835938 977.078125 494.726562 C 977.082031 493.613281 977.113281 492.503906 977.171875 491.390625 C 977.234375 490.28125 977.320312 489.171875 977.433594 488.066406 C 977.546875 486.960938 977.6875 485.859375 977.855469 484.761719 C 978.023438 483.660156 978.21875 482.566406 978.441406 481.476562 C 978.664062 480.386719 978.910156 479.304688 979.1875 478.226562 C 979.460938 477.148438 979.761719 476.082031 980.089844 475.019531 C 980.417969 473.957031 980.773438 472.902344 981.152344 471.855469 C 981.53125 470.8125 981.933594 469.777344 982.363281 468.75 C 982.796875 467.726562 983.25 466.710938 983.730469 465.707031 C 984.210938 464.707031 984.714844 463.714844 985.242188 462.738281 L 985.394531 462.457031 C 985.921875 461.480469 986.472656 460.515625 987.050781 459.5625 C 987.625 458.613281 988.222656 457.675781 988.84375 456.753906 C 989.464844 455.832031 990.109375 454.925781 990.773438 454.035156 C 991.441406 453.144531 992.128906 452.269531 992.835938 451.414062 C 993.546875 450.558594 994.273438 449.71875 995.023438 448.898438 C 995.773438 448.078125 996.542969 447.273438 997.332031 446.492188 C 998.121094 445.710938 998.929688 444.945312 999.757812 444.203125 C 1000.585938 443.460938 1001.429688 442.738281 1002.289062 442.035156 C 1003.152344 441.332031 1004.03125 440.652344 1004.929688 439.992188 C 1005.824219 439.335938 1006.734375 438.699219 1007.660156 438.085938 C 1008.589844 437.472656 1009.53125 436.878906 1010.488281 436.3125 C 1011.441406 435.746094 1012.410156 435.203125 1013.394531 434.679688 C 1014.378906 434.160156 1015.371094 433.664062 1016.378906 433.195312 C 1017.386719 432.722656 1018.402344 432.277344 1019.433594 431.855469 C 1020.460938 431.433594 1021.5 431.039062 1022.550781 430.667969 C 1023.597656 430.300781 1024.65625 429.953125 1025.71875 429.636719 C 1026.785156 429.320312 1027.859375 429.027344 1028.9375 428.761719 C 1030.015625 428.496094 1031.101562 428.257812 1032.195312 428.042969 C 1033.285156 427.832031 1034.382812 427.648438 1035.480469 427.488281 C 1036.582031 427.332031 1037.6875 427.199219 1038.792969 427.09375 C 1039.898438 426.992188 1041.007812 426.914062 1042.117188 426.863281 C 1043.230469 426.8125 1044.339844 426.792969 1045.453125 426.796875 C 1046.566406 426.800781 1047.675781 426.832031 1048.785156 426.894531 C 1049.894531 426.953125 1051.003906 427.039062 1052.109375 427.152344 C 1053.214844 427.265625 1054.320312 427.40625 1055.417969 427.574219 C 1056.515625 427.746094 1057.609375 427.9375 1058.699219 428.160156 C 1059.789062 428.382812 1060.875 428.632812 1061.949219 428.90625 C 1063.027344 429.179688 1064.097656 429.484375 1065.160156 429.808594 C 1066.222656 430.136719 1067.277344 430.492188 1068.320312 430.871094 C 1069.367188 431.25 1070.402344 431.65625 1071.425781 432.085938 C 1072.453125 432.515625 1073.464844 432.96875 1074.46875 433.449219 C 1075.472656 433.929688 1076.460938 434.433594 1077.441406 434.964844 L 1137.492188 467.390625 L 1169.953125 407.34375 C 1170.480469 406.367188 1171.03125 405.402344 1171.609375 404.453125 C 1172.183594 403.5 1172.78125 402.5625 1173.402344 401.640625 C 1174.023438 400.71875 1174.667969 399.8125 1175.335938 398.921875 C 1176 398.035156 1176.6875 397.160156 1177.394531 396.304688 C 1178.105469 395.445312 1178.835938 394.609375 1179.585938 393.789062 C 1180.335938 392.96875 1181.105469 392.164062 1181.894531 391.382812 C 1182.683594 390.601562 1183.492188 389.835938 1184.316406 389.09375 C 1185.144531 388.351562 1185.988281 387.628906 1186.851562 386.925781 C 1187.714844 386.222656 1188.59375 385.542969 1189.488281 384.886719 C 1190.382812 384.226562 1191.296875 383.589844 1192.222656 382.976562 C 1193.148438 382.363281 1194.089844 381.773438 1195.046875 381.207031 C 1196.003906 380.636719 1196.972656 380.09375 1197.957031 379.574219 C 1198.9375 379.054688 1199.933594 378.558594 1200.941406 378.085938 C 1201.945312 377.617188 1202.964844 377.171875 1203.992188 376.75 C 1205.023438 376.328125 1206.0625 375.933594 1207.109375 375.5625 C 1208.15625 375.191406 1209.214844 374.847656 1210.28125 374.53125 C 1211.34375 374.210938 1212.417969 373.921875 1213.496094 373.65625 C 1214.578125 373.390625 1215.660156 373.148438 1216.753906 372.9375 C 1217.84375 372.726562 1218.941406 372.539062 1220.039062 372.382812 C 1221.140625 372.222656 1222.246094 372.09375 1223.351562 371.988281 C 1224.457031 371.882812 1225.566406 371.808594 1226.679688 371.757812 C 1227.789062 371.707031 1228.898438 371.683594 1230.011719 371.691406 C 1231.125 371.695312 1232.234375 371.726562 1233.34375 371.785156 C 1234.453125 371.84375 1235.5625 371.933594 1236.667969 372.046875 C 1237.773438 372.160156 1238.875 372.300781 1239.976562 372.46875 C 1241.074219 372.636719 1242.167969 372.832031 1243.257812 373.050781 C 1244.347656 373.273438 1245.429688 373.523438 1246.507812 373.796875 C 1247.585938 374.070312 1248.65625 374.371094 1249.71875 374.699219 C 1250.78125 375.027344 1251.835938 375.378906 1252.878906 375.761719 C 1253.925781 376.140625 1254.960938 376.542969 1255.984375 376.972656 C 1257.011719 377.402344 1258.023438 377.859375 1259.027344 378.339844 C 1260.03125 378.816406 1261.019531 379.320312 1262 379.851562 Z M 1262 379.851562 " fill-opacity="1" fill-rule="nonzero"/></g></g></g><path stroke-linecap="butt" transform="matrix(0.403919, -0.631941, 0.631941, 0.403919, 104.893904, 729.104149)" fill="none" stroke-linejoin="miter" d="M 40.50488 300.474597 C 282.249287 -33.492864 589.421014 -33.490352 962.021645 300.47494 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.734847, 0.149999, -0.149999, 0.734847, 304.884588, 831.580108)" fill="none" stroke-linejoin="miter" d="M 41.766746 27.491179 C 472.054803 681.225638 813.229103 681.225807 1065.289646 27.491684 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.713054, 0.232494, -0.232494, 0.713054, 743.751953, 84.389709)" fill="none" stroke-linejoin="miter" d="M 27.552117 192.796126 C 315.880744 2.40229 514.553475 2.404029 623.562127 192.793052 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.479248, -0.576907, 0.576907, 0.479248, 975.623445, 1084.867279)" fill="none" stroke-linejoin="miter" d="M 10.923997 43.992439 C 74.518874 60.091943 159.21204 60.093361 265.000168 43.992687 " stroke="#245f28" stroke-width="89" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 329.956147, 817.015974)" fill="none" stroke-linejoin="miter" d="M 0.000357227 50.000231 C 37.480224 49.999953 88.464376 49.999514 152.947912 49.997153 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 331.706899, 815.218854)" fill="none" stroke-linejoin="miter" d="M 0.000389071 49.999177 C 37.480256 49.998899 88.464408 49.998461 152.946183 50.001001 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.747763, -0.0578889, 0.0578889, 0.747763, 722.333707, 226.603261)" fill="none" stroke-linejoin="miter" d="M 8.805162 73.553045 C 184.314962 42.151956 282.885164 42.150737 304.510575 73.548986 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/></svg>
</file>

<file path="frontend/src/assets/logo.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="2000" zoomAndPan="magnify" viewBox="0 0 1500 1499.999933" height="2000" preserveAspectRatio="xMidYMid meet" version="1.0"><defs><clipPath id="fb435d5862"><path d="M 977 371 L 1298 371 L 1298 628 L 977 628 Z M 977 371 " clip-rule="nonzero"/></clipPath><clipPath id="bd2365c669"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath><clipPath id="8a36694c69"><path d="M 1350.941406 582.09375 L 1028.277344 678.472656 L 959.800781 449.234375 L 1282.464844 352.851562 Z M 1350.941406 582.09375 " clip-rule="nonzero"/></clipPath></defs><path stroke-linecap="butt" transform="matrix(0.627627, -0.41059, 0.41059, 0.627627, 850.581296, 942.983635)" fill="none" stroke-linejoin="miter" d="M 27.738967 153.335882 C 120.789709 -8.442483 298.543162 -8.443382 560.987755 153.33184 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.324044, 0.676384, -0.676384, -0.324044, 1395.097425, 847.756731)" fill="none" stroke-linejoin="miter" d="M 25.672548 211.246672 C 203.554284 -27.749642 381.442508 -27.748337 559.323325 211.245693 " stroke="#245f28" stroke-width="64" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.742472, 0.105999, -0.105999, 0.742472, 345.349897, 817.95039)" fill="none" stroke-linejoin="miter" d="M 43.567852 24.529848 C 400.107518 657.734999 738.125941 657.738308 1057.622384 24.534619 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.406135, -0.630519, 0.630519, 0.406135, 183.309934, 719.938622)" fill="none" stroke-linejoin="miter" d="M 39.353683 265.381789 C 264.409727 -21.796159 535.300314 -21.794026 852.019803 265.379429 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.72096, 0.20668, -0.20668, 0.72096, 747.727769, 161.278749)" fill="none" stroke-linejoin="miter" d="M 25.450087 157.050118 C 266.832937 14.317423 428.639468 14.316418 510.866109 157.053544 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><g clip-path="url(#fb435d5862)"><g clip-path="url(#bd2365c669)"><g clip-path="url(#8a36694c69)"><path fill="#245f28" d="M 1262 379.851562 L 1262.277344 380 C 1263.257812 380.527344 1264.21875 381.082031 1265.171875 381.65625 C 1266.121094 382.230469 1267.058594 382.832031 1267.980469 383.453125 C 1268.902344 384.074219 1269.808594 384.71875 1270.699219 385.382812 C 1271.589844 386.050781 1272.460938 386.738281 1273.316406 387.445312 C 1274.175781 388.15625 1275.011719 388.886719 1275.832031 389.636719 C 1276.652344 390.386719 1277.453125 391.15625 1278.238281 391.945312 C 1279.019531 392.734375 1279.78125 393.542969 1280.527344 394.367188 C 1281.269531 395.195312 1281.992188 396.039062 1282.691406 396.902344 C 1283.394531 397.765625 1284.074219 398.644531 1284.734375 399.539062 C 1285.390625 400.4375 1286.027344 401.347656 1286.640625 402.273438 C 1287.253906 403.199219 1287.84375 404.140625 1288.414062 405.097656 C 1288.980469 406.054688 1289.523438 407.023438 1290.042969 408.007812 C 1290.5625 408.988281 1291.058594 409.984375 1291.53125 410.992188 C 1292 411.996094 1292.445312 413.015625 1292.867188 414.042969 C 1293.289062 415.074219 1293.683594 416.113281 1294.054688 417.160156 C 1294.425781 418.207031 1294.769531 419.265625 1295.085938 420.332031 C 1295.40625 421.394531 1295.695312 422.46875 1295.960938 423.546875 C 1296.226562 424.625 1296.464844 425.710938 1296.679688 426.804688 C 1296.890625 427.894531 1297.078125 428.992188 1297.234375 430.089844 C 1297.394531 431.191406 1297.523438 432.292969 1297.628906 433.402344 C 1297.734375 434.507812 1297.808594 435.617188 1297.859375 436.726562 C 1297.910156 437.839844 1297.933594 438.949219 1297.929688 440.0625 C 1297.921875 441.171875 1297.890625 442.285156 1297.832031 443.394531 C 1297.773438 444.503906 1297.6875 445.613281 1297.574219 446.71875 C 1297.460938 447.824219 1297.320312 448.925781 1297.152344 450.023438 C 1296.984375 451.125 1296.789062 452.21875 1296.566406 453.308594 C 1296.34375 454.394531 1296.097656 455.480469 1295.824219 456.558594 C 1295.546875 457.632812 1295.246094 458.703125 1294.921875 459.765625 C 1294.59375 460.828125 1294.238281 461.882812 1293.859375 462.929688 C 1293.480469 463.972656 1293.078125 465.007812 1292.648438 466.035156 C 1292.21875 467.058594 1291.765625 468.074219 1291.285156 469.074219 C 1290.804688 470.078125 1290.300781 471.070312 1289.773438 472.046875 L 1224.914062 592.132812 C 1224.386719 593.109375 1223.835938 594.074219 1223.261719 595.027344 C 1222.683594 595.976562 1222.085938 596.914062 1221.464844 597.835938 C 1220.84375 598.757812 1220.199219 599.664062 1219.53125 600.550781 C 1218.867188 601.441406 1218.179688 602.316406 1217.46875 603.171875 C 1216.761719 604.027344 1216.03125 604.867188 1215.28125 605.6875 C 1214.53125 606.507812 1213.761719 607.308594 1212.972656 608.089844 C 1212.183594 608.875 1211.375 609.636719 1210.546875 610.378906 C 1209.722656 611.125 1208.875 611.84375 1208.015625 612.546875 C 1207.152344 613.25 1206.273438 613.929688 1205.375 614.585938 C 1204.480469 615.246094 1203.570312 615.882812 1202.644531 616.496094 C 1201.714844 617.109375 1200.773438 617.699219 1199.816406 618.265625 C 1198.863281 618.832031 1197.894531 619.378906 1196.910156 619.898438 C 1195.929688 620.417969 1194.933594 620.914062 1193.925781 621.382812 C 1192.917969 621.855469 1191.902344 622.300781 1190.871094 622.722656 C 1189.84375 623.144531 1188.804688 623.539062 1187.757812 623.910156 C 1186.707031 624.277344 1185.652344 624.621094 1184.585938 624.941406 C 1183.519531 625.257812 1182.449219 625.550781 1181.371094 625.816406 C 1180.289062 626.082031 1179.203125 626.320312 1178.113281 626.53125 C 1177.023438 626.746094 1175.925781 626.929688 1174.824219 627.089844 C 1173.726562 627.246094 1172.621094 627.378906 1171.515625 627.484375 C 1170.410156 627.585938 1169.300781 627.664062 1168.1875 627.714844 C 1167.078125 627.765625 1165.96875 627.785156 1164.855469 627.78125 C 1163.746094 627.777344 1162.632812 627.746094 1161.523438 627.6875 C 1160.414062 627.628906 1159.304688 627.539062 1158.199219 627.425781 C 1157.09375 627.3125 1155.992188 627.171875 1154.890625 627.003906 C 1153.792969 626.835938 1152.699219 626.644531 1151.609375 626.421875 C 1150.519531 626.199219 1149.4375 625.953125 1148.359375 625.675781 C 1147.28125 625.402344 1146.210938 625.101562 1145.148438 624.773438 C 1144.085938 624.449219 1143.035156 624.09375 1141.988281 623.714844 C 1140.945312 623.335938 1139.910156 622.933594 1138.882812 622.503906 C 1137.859375 622.074219 1136.84375 621.617188 1135.839844 621.140625 C 1134.839844 620.660156 1133.847656 620.15625 1132.871094 619.628906 L 1012.738281 554.785156 C 1011.761719 554.257812 1010.796875 553.703125 1009.84375 553.128906 C 1008.890625 552.554688 1007.957031 551.957031 1007.035156 551.335938 C 1006.113281 550.714844 1005.207031 550.070312 1004.316406 549.402344 C 1003.425781 548.738281 1002.550781 548.050781 1001.695312 547.339844 C 1000.835938 546.632812 1000 545.902344 999.179688 545.152344 C 998.359375 544.402344 997.554688 543.632812 996.773438 542.84375 C 995.988281 542.054688 995.226562 541.246094 994.484375 540.421875 C 993.738281 539.59375 993.015625 538.75 992.316406 537.886719 C 991.613281 537.023438 990.933594 536.144531 990.273438 535.25 C 989.617188 534.355469 988.980469 533.441406 988.367188 532.515625 C 987.75 531.589844 987.160156 530.648438 986.59375 529.691406 C 986.027344 528.734375 985.480469 527.765625 984.960938 526.785156 C 984.441406 525.800781 983.945312 524.804688 983.476562 523.796875 C 983.003906 522.792969 982.558594 521.773438 982.136719 520.746094 C 981.714844 519.714844 981.320312 518.675781 980.949219 517.628906 C 980.578125 516.582031 980.234375 515.523438 979.917969 514.457031 C 979.597656 513.394531 979.308594 512.320312 979.042969 511.242188 C 978.777344 510.160156 978.539062 509.074219 978.324219 507.984375 C 978.113281 506.894531 977.925781 505.796875 977.769531 504.695312 C 977.609375 503.597656 977.480469 502.492188 977.375 501.386719 C 977.269531 500.277344 977.195312 499.167969 977.144531 498.058594 C 977.09375 496.949219 977.074219 495.835938 977.078125 494.726562 C 977.082031 493.613281 977.113281 492.503906 977.171875 491.390625 C 977.234375 490.28125 977.320312 489.171875 977.433594 488.066406 C 977.546875 486.960938 977.6875 485.859375 977.855469 484.761719 C 978.023438 483.660156 978.21875 482.566406 978.441406 481.476562 C 978.664062 480.386719 978.910156 479.304688 979.1875 478.226562 C 979.460938 477.148438 979.761719 476.082031 980.089844 475.019531 C 980.417969 473.957031 980.773438 472.902344 981.152344 471.855469 C 981.53125 470.8125 981.933594 469.777344 982.363281 468.75 C 982.796875 467.726562 983.25 466.710938 983.730469 465.707031 C 984.210938 464.707031 984.714844 463.714844 985.242188 462.738281 L 985.394531 462.457031 C 985.921875 461.480469 986.472656 460.515625 987.050781 459.5625 C 987.625 458.613281 988.222656 457.675781 988.84375 456.753906 C 989.464844 455.832031 990.109375 454.925781 990.773438 454.035156 C 991.441406 453.144531 992.128906 452.269531 992.835938 451.414062 C 993.546875 450.558594 994.273438 449.71875 995.023438 448.898438 C 995.773438 448.078125 996.542969 447.273438 997.332031 446.492188 C 998.121094 445.710938 998.929688 444.945312 999.757812 444.203125 C 1000.585938 443.460938 1001.429688 442.738281 1002.289062 442.035156 C 1003.152344 441.332031 1004.03125 440.652344 1004.929688 439.992188 C 1005.824219 439.335938 1006.734375 438.699219 1007.660156 438.085938 C 1008.589844 437.472656 1009.53125 436.878906 1010.488281 436.3125 C 1011.441406 435.746094 1012.410156 435.203125 1013.394531 434.679688 C 1014.378906 434.160156 1015.371094 433.664062 1016.378906 433.195312 C 1017.386719 432.722656 1018.402344 432.277344 1019.433594 431.855469 C 1020.460938 431.433594 1021.5 431.039062 1022.550781 430.667969 C 1023.597656 430.300781 1024.65625 429.953125 1025.71875 429.636719 C 1026.785156 429.320312 1027.859375 429.027344 1028.9375 428.761719 C 1030.015625 428.496094 1031.101562 428.257812 1032.195312 428.042969 C 1033.285156 427.832031 1034.382812 427.648438 1035.480469 427.488281 C 1036.582031 427.332031 1037.6875 427.199219 1038.792969 427.09375 C 1039.898438 426.992188 1041.007812 426.914062 1042.117188 426.863281 C 1043.230469 426.8125 1044.339844 426.792969 1045.453125 426.796875 C 1046.566406 426.800781 1047.675781 426.832031 1048.785156 426.894531 C 1049.894531 426.953125 1051.003906 427.039062 1052.109375 427.152344 C 1053.214844 427.265625 1054.320312 427.40625 1055.417969 427.574219 C 1056.515625 427.746094 1057.609375 427.9375 1058.699219 428.160156 C 1059.789062 428.382812 1060.875 428.632812 1061.949219 428.90625 C 1063.027344 429.179688 1064.097656 429.484375 1065.160156 429.808594 C 1066.222656 430.136719 1067.277344 430.492188 1068.320312 430.871094 C 1069.367188 431.25 1070.402344 431.65625 1071.425781 432.085938 C 1072.453125 432.515625 1073.464844 432.96875 1074.46875 433.449219 C 1075.472656 433.929688 1076.460938 434.433594 1077.441406 434.964844 L 1137.492188 467.390625 L 1169.953125 407.34375 C 1170.480469 406.367188 1171.03125 405.402344 1171.609375 404.453125 C 1172.183594 403.5 1172.78125 402.5625 1173.402344 401.640625 C 1174.023438 400.71875 1174.667969 399.8125 1175.335938 398.921875 C 1176 398.035156 1176.6875 397.160156 1177.394531 396.304688 C 1178.105469 395.445312 1178.835938 394.609375 1179.585938 393.789062 C 1180.335938 392.96875 1181.105469 392.164062 1181.894531 391.382812 C 1182.683594 390.601562 1183.492188 389.835938 1184.316406 389.09375 C 1185.144531 388.351562 1185.988281 387.628906 1186.851562 386.925781 C 1187.714844 386.222656 1188.59375 385.542969 1189.488281 384.886719 C 1190.382812 384.226562 1191.296875 383.589844 1192.222656 382.976562 C 1193.148438 382.363281 1194.089844 381.773438 1195.046875 381.207031 C 1196.003906 380.636719 1196.972656 380.09375 1197.957031 379.574219 C 1198.9375 379.054688 1199.933594 378.558594 1200.941406 378.085938 C 1201.945312 377.617188 1202.964844 377.171875 1203.992188 376.75 C 1205.023438 376.328125 1206.0625 375.933594 1207.109375 375.5625 C 1208.15625 375.191406 1209.214844 374.847656 1210.28125 374.53125 C 1211.34375 374.210938 1212.417969 373.921875 1213.496094 373.65625 C 1214.578125 373.390625 1215.660156 373.148438 1216.753906 372.9375 C 1217.84375 372.726562 1218.941406 372.539062 1220.039062 372.382812 C 1221.140625 372.222656 1222.246094 372.09375 1223.351562 371.988281 C 1224.457031 371.882812 1225.566406 371.808594 1226.679688 371.757812 C 1227.789062 371.707031 1228.898438 371.683594 1230.011719 371.691406 C 1231.125 371.695312 1232.234375 371.726562 1233.34375 371.785156 C 1234.453125 371.84375 1235.5625 371.933594 1236.667969 372.046875 C 1237.773438 372.160156 1238.875 372.300781 1239.976562 372.46875 C 1241.074219 372.636719 1242.167969 372.832031 1243.257812 373.050781 C 1244.347656 373.273438 1245.429688 373.523438 1246.507812 373.796875 C 1247.585938 374.070312 1248.65625 374.371094 1249.71875 374.699219 C 1250.78125 375.027344 1251.835938 375.378906 1252.878906 375.761719 C 1253.925781 376.140625 1254.960938 376.542969 1255.984375 376.972656 C 1257.011719 377.402344 1258.023438 377.859375 1259.027344 378.339844 C 1260.03125 378.816406 1261.019531 379.320312 1262 379.851562 Z M 1262 379.851562 " fill-opacity="1" fill-rule="nonzero"/></g></g></g><path stroke-linecap="butt" transform="matrix(0.403919, -0.631941, 0.631941, 0.403919, 104.893904, 729.104149)" fill="none" stroke-linejoin="miter" d="M 40.50488 300.474597 C 282.249287 -33.492864 589.421014 -33.490352 962.021645 300.47494 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.734847, 0.149999, -0.149999, 0.734847, 304.884588, 831.580108)" fill="none" stroke-linejoin="miter" d="M 41.766746 27.491179 C 472.054803 681.225638 813.229103 681.225807 1065.289646 27.491684 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.713054, 0.232494, -0.232494, 0.713054, 743.751953, 84.389709)" fill="none" stroke-linejoin="miter" d="M 27.552117 192.796126 C 315.880744 2.40229 514.553475 2.404029 623.562127 192.793052 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.479248, -0.576907, 0.576907, 0.479248, 975.623445, 1084.867279)" fill="none" stroke-linejoin="miter" d="M 10.923997 43.992439 C 74.518874 60.091943 159.21204 60.093361 265.000168 43.992687 " stroke="#245f28" stroke-width="89" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 329.956147, 817.015974)" fill="none" stroke-linejoin="miter" d="M 0.000357227 50.000231 C 37.480224 49.999953 88.464376 49.999514 152.947912 49.997153 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(-0.253673, -0.705797, 0.705797, -0.253673, 331.706899, 815.218854)" fill="none" stroke-linejoin="miter" d="M 0.000389071 49.999177 C 37.480256 49.998899 88.464408 49.998461 152.946183 50.001001 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/><path stroke-linecap="butt" transform="matrix(0.747763, -0.0578889, 0.0578889, 0.747763, 722.333707, 226.603261)" fill="none" stroke-linejoin="miter" d="M 8.805162 73.553045 C 184.314962 42.151956 282.885164 42.150737 304.510575 73.548986 " stroke="#245f28" stroke-width="100" stroke-opacity="1" stroke-miterlimit="4"/></svg>
</file>

<file path="frontend/src/index.css">
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-gray-50 text-gray-900;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

/* Mobile-first: cap width on larger screens to simulate phone frame */
#root {
  max-width: 480px;
  margin: 0 auto;
  min-height: 100vh;
  background: white;
  position: relative;
}
</file>

<file path="frontend/.gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
</file>

<file path="frontend/.gitkeep">

</file>

<file path="frontend/index.html">
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RosaCycle</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
</file>

<file path="frontend/postcss.config.js">
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
</file>

<file path="frontend/tailwind.config.js">
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          50: '#eefbf2',
          100: '#d6f5e0',
          500: '#22a559',
          600: '#1fa34c',
          700: '#188040',
        },
      },
    },
  },
  plugins: [],
}
</file>

<file path="backend/app/auth/repository.py">
from app.shared.database import db
from app.auth.user_model import User


def get_user_by_id(user_id):
    return db.session.get(User, user_id)


def get_user_by_email(email):
    return User.query.filter(db.func.lower(User.email) == email.lower()).first()


def get_user_by_username(username):
    return User.query.filter(db.func.lower(User.username) == username.lower()).first()


def create_user(user):
    db.session.add(user)
    db.session.commit()
    return user


def save_user(user):
    db.session.commit()
    return user
</file>

<file path="backend/app/auth/routes.py">
from flask import Blueprint
from app.auth import controller

auth_bp = Blueprint("auth", __name__, url_prefix="/auth")

@auth_bp.post("/register")
def register():
    return controller.register()

@auth_bp.post("/login")
def login():
    return controller.login()

@auth_bp.post("/logout")
def logout():
    return controller.logout()

@auth_bp.post("/google")
def google_login():
    return controller.google_login()
</file>

<file path="backend/app/auth/service.py">
from datetime import timedelta

from flask import current_app

from app.auth.user_model import User
from app.auth.repository import (
    create_user,
    get_user_by_email,
    get_user_by_id,
    get_user_by_username,
    save_user,
)
from app.auth.utils import hash_password, verify_password
from app.shared.utils.errors import (
    ConflictError,
    ForbiddenError,
    NotFoundError,
    ValidationError,
)
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import ensure_aware, utcnow


def register_user(*, username, first_name, last_name, email, password):
    if get_user_by_email(email) is not None:
        raise ConflictError("An account with this email already exists.")
    if get_user_by_username(username) is not None:
        raise ConflictError("An account with this username already exists.")

    user = User(
        username=username,
        first_name=first_name,
        last_name=last_name,
        email=email,
        password_hash=hash_password(password),
    )
    return create_user(user)


def authenticate_user(*, email, password):
    user = get_user_by_email(email)
    if user is None or not verify_password(password, user.password_hash):
        raise ValidationError("Invalid email or password.", status_code=401)
    return user


def get_profile(user_id):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")
    return user
</file>

<file path="backend/app/auth/user_model.py">
from app.shared.database import db
from app.shared.models.enums import UserRole
from app.shared.utils.mixins import utcnow, uuid_pk_column


class User(db.Model):
    __tablename__ = "users"

    id = uuid_pk_column()
    username = db.Column(db.String(50), unique=True, nullable=False, index=True)
    first_name = db.Column(db.String(50), nullable=False)
    last_name = db.Column(db.String(50), nullable=False)
    email = db.Column(db.String(255), unique=True, nullable=False, index=True)
    password_hash = db.Column(db.String(255), nullable=False)
    profile_image_path = db.Column(db.String(255), nullable=True)
    role = db.Column(db.Enum(UserRole), default=UserRole.USER, nullable=False)
    created_at = db.Column(db.DateTime(timezone=True), default=utcnow, nullable=False)
    info_updated_at = db.Column(db.DateTime(timezone=True), nullable=True)
    password_updated_at = db.Column(db.DateTime(timezone=True), nullable=True)

    trades = db.relationship("Trade", back_populates="owner", lazy="dynamic")
    offers = db.relationship("Offer", back_populates="offerer", lazy="dynamic")
    resource_spots = db.relationship(
        "ResourceSpot", back_populates="reporter", lazy="dynamic"
    )
    notifications = db.relationship(
        "Notification", back_populates="recipient", lazy="dynamic"
    )
</file>

<file path="backend/app/profile/repository.py">
from app.shared.database import db
from app.auth.user_model import User


def get_user_by_id(user_id):
    return db.session.get(User, user_id)


def get_user_by_email(email):
    return User.query.filter(db.func.lower(User.email) == email.lower()).first()


def get_user_by_username(username):
    return User.query.filter(db.func.lower(User.username) == username.lower()).first()


def create_user(user):
    db.session.add(user)
    db.session.commit()
    return user


def save_user(user):
    db.session.commit()
    return user
</file>

<file path="backend/app/profile/service.py">
from datetime import timedelta

from flask import current_app

from app.auth.user_model import User
from app.profile.repository import (
    create_user,
    get_user_by_email,
    get_user_by_id,
    get_user_by_username,
    save_user,
)
from app.auth.utils import hash_password, verify_password
from app.shared.utils.errors import (
    ConflictError,
    ForbiddenError,
    NotFoundError,
    ValidationError,
)
from app.shared.utils.file_storage import save_image
from app.shared.utils.mixins import ensure_aware, utcnow


def register_user(*, username, first_name, last_name, email, password):
    if get_user_by_email(email) is not None:
        raise ConflictError("An account with this email already exists.")
    if get_user_by_username(username) is not None:
        raise ConflictError("An account with this username already exists.")

    user = User(
        username=username,
        first_name=first_name,
        last_name=last_name,
        email=email,
        password_hash=hash_password(password),
    )
    return create_user(user)


def authenticate_user(*, email, password):
    user = get_user_by_email(email)
    if user is None or not verify_password(password, user.password_hash):
        raise ValidationError("Invalid email or password.", status_code=401)
    return user


def get_profile(user_id):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")
    return user


def update_profile(user_id, *, updates, current_password, image_file):
    user = get_user_by_id(user_id)
    if user is None:
        raise NotFoundError("User not found.")

    info_fields = {"first_name", "last_name", "username", "email"}
    changing_info = any(k in updates for k in info_fields)
    changing_password = "password" in updates

    cooldown = timedelta(days=current_app.config["PROFILE_EDIT_COOLDOWN_DAYS"])

    if changing_info and user.info_updated_at is not None:
        next_allowed_at = ensure_aware(user.info_updated_at) + cooldown
        if utcnow() < next_allowed_at:
            days_left = (next_allowed_at - utcnow()).days + 1
            raise ConflictError(
                f"You can update your profile info again in {days_left} day(s)."
            )

    if changing_password and user.password_updated_at is not None:
        next_allowed_at = ensure_aware(user.password_updated_at) + cooldown
        if utcnow() < next_allowed_at:
            days_left = (next_allowed_at - utcnow()).days + 1
            raise ConflictError(
                f"You can change your password again in {days_left} day(s)."
            )

    if "password" in updates or "email" in updates or "username" in updates:
        if not current_password or not verify_password(
            current_password, user.password_hash
        ):
            raise ForbiddenError("Current password is incorrect.")

    if "first_name" in updates:
        user.first_name = updates["first_name"]
    if "last_name" in updates:
        user.last_name = updates["last_name"]
    if "username" in updates:
        existing = get_user_by_username(updates["username"])
        if existing is not None and existing.id != user.id:
            raise ConflictError("An account with this username already exists.")
        user.username = updates["username"]
    if "email" in updates:
        existing = get_user_by_email(updates["email"])
        if existing is not None and existing.id != user.id:
            raise ConflictError("An account with this email already exists.")
        user.email = updates["email"]
    if "password" in updates:
        user.password_hash = hash_password(updates["password"])

    if image_file is not None:
        user.profile_image_path = save_image(image_file, "profiles")

    if changing_info:
        user.info_updated_at = utcnow()
    if changing_password:
        user.password_updated_at = utcnow()

    return save_user(user)
</file>

<file path="backend/app/services/auth_service.py">
# backend/app/services/auth_service.py
from flask import current_app
from google.oauth2 import id_token
from google.auth.transport import requests

from app.shared.database import db
from app.auth.user_model import User
from app.shared.utils.errors import ValidationError


def verify_google_token(token: str) -> dict:
    """Verifies the ID token sent from frontend with Google's servers."""
    try:
        client_id = current_app.config.get("GOOGLE_CLIENT_ID")
        # If client_id is empty in dev, pass None or set up your Google Console Client ID
        id_info = id_token.verify_oauth2_token(
            token, requests.Request(), audience=client_id if client_id else None
        )
        return id_info
    except Exception as e:
        raise ValidationError(f"Invalid Google token: {str(e)}")


def google_login_or_register(id_token_str: str) -> User:
    """Authenticates a user via Google ID Token; creates a profile if new."""
    payload = verify_google_token(id_token_str)

    email = payload.get("email")
    google_id = payload.get("sub")
    first_name = payload.get("given_name", "Google")
    last_name = payload.get("family_name", "User")
    picture = payload.get("picture")

    if not email:
        raise ValidationError("Google account must have an email address.")

    # 1. Check if user exists by google_id or email
    user = User.query.filter(
        (User.google_id == google_id) | (User.email == email)
    ).first()

    if user:
        # Link google_id if not already linked
        if not user.google_id:
            user.google_id = google_id
            db.session.commit()
        return user

    # 2. Create new user if first-time login
    # Generate a unique base username from email
    base_username = email.split("@")[0]
    username = base_username
    counter = 1
    while User.query.filter_by(username=username).first():
        username = f"{base_username}{counter}"
        counter += 1

    new_user = User(
        username=username,
        first_name=first_name,
        last_name=last_name,
        email=email,
        google_id=google_id,
        profile_image_path=picture,
    )
    db.session.add(new_user)
    db.session.commit()

    return new_user
</file>

<file path="backend/app/shared/models/__init__.py">
from app.auth.user_model import User  # noqa: F401
from app.trades.model import Trade  # noqa: F401
from app.trades.offer.model import Offer  # noqa: F401
from app.map.model import ResourceSpot  # noqa: F401
from app.shared.notification.model import Notification  # noqa: F401
from app.shared.rating.model import Rating  # noqa: F401
</file>

<file path="backend/app/__init__.py">

</file>

<file path="backend/docs/API.md">
# RosaCycle Backend API Reference

A complete reference for every endpoint currently implemented. Written for whoever wires the frontend up to this backend — read this instead of the Flask source.

## Base URL

Local dev: `http://localhost:5000`
Deployed: whatever Railway domain the project owner gives you (ask them — the CLI session that built this backend was deliberately unlinked from Railway so the deployed project could be handed off cleanly).

## Auth

All endpoints require a JWT **except**: `POST /auth/register`, `POST /auth/login`, `GET /health`, `GET /`, `GET /media/<path>`.

Flow: call `POST /auth/register` or `POST /auth/login` → both return `{"token": "...", "user": {...}}` → send that token on every subsequent request as:

```
Authorization: Bearer <token>
```

There is no refresh-token flow and no server-side session/blocklist — tokens are stateless JWTs with no built-in expiry handling beyond whatever `flask-jwt-extended`'s defaults are. `POST /auth/logout` is a no-op that just returns `{"success": true}` — actually discarding the token client-side (e.g. clearing local storage) is what "logs out" the user.

## Error format

Every error (validation, not-found, forbidden, conflict, or unexpected) returns:

```json
{ "error": "Human-readable message." }
```

with an appropriate HTTP status code:

| Status | Meaning | Example cause |
|---|---|---|
| 400 | Validation error | Missing required field |
| 401 | Auth failure | Missing/invalid JWT, wrong login credentials |
| 403 | Forbidden | Acting on a resource you don't own (e.g. editing someone else's trade) |
| 404 | Not found | Bad ID in a URL |
| 409 | Conflict | e.g. sending an offer on a trade that's already reserved, re-deciding an already-decided offer |
| 422 | Unprocessable | Reserved for stricter validation, currently used interchangeably with 400 in a couple of spots |
| 500 | Server error | Unexpected/unhandled exception |

## Request formats — read this before wiring forms

Most endpoints take a plain **JSON body** (`Content-Type: application/json`). Exactly **three** take **`multipart/form-data`** instead, because they accept an image file alongside text fields:

- `POST /trades` (create trade)
- `POST /trades/:id/offers` (send offer)
- `POST /resource-spots` (report resource spot)
- (also `PATCH /trades/:id`, `PATCH /users/me`, `POST /resource-spots/:id/photos` — anything that can attach/replace an image)

For those, send a `FormData` body with each field as a form field (not nested JSON) and the image under the key `image`. Don't set `Content-Type` manually for `FormData` — let the browser set the multipart boundary.

## Enum values — exact casing the API returns

These are lowercase (Python enum `.value`), not capitalized. Map to whatever display labels you want on the frontend — don't guess the casing.

- **Trade status**: `open` | `reserved` | `completed`
- **Offer status**: `pending` | `accepted` | `declined`
- **Resource Spot status**: `active` | `collected`
- **Trading-for type**: `specific` | `nothing` | `negotiating`
- **User role**: `user` | `admin`
- **Notification type**: `offer_received` | `offer_accepted` | `offer_declined`

## Images

Any `image` field in a response is either `null` or a full URL (already resolved against `PUBLIC_BASE_URL`) — e.g. `"https://.../media/trades/<uuid>.jpg"`. Just use it directly as an `<img src>`; don't try to construct the URL yourself.

---

## Auth

### `POST /auth/register`
Public. JSON body:
```json
{ "username": "luke", "firstName": "Luke", "lastName": "S", "email": "luke@example.com", "password": "password123" }
```
→ `201`
```json
{ "token": "...", "user": { "id": "...", "username": "luke", "firstName": "Luke", "lastName": "S", "email": "luke@example.com", "profileImage": null, "role": "user", "createdAt": "2026-07-28T12:01:44+00:00" } }
```
`409` if email or username is already taken.

### `POST /auth/login`
Public. JSON body: `{ "email": "...", "password": "..." }` → `200` same shape as register. `401` on wrong credentials.

### `POST /auth/logout`
Auth required. No body. → `200 { "success": true }`. Doesn't invalidate the token server-side.

---

## User

### `GET /users/me`
Auth required. → `200`, the current user (same shape as the register/login `user` object).

### `PATCH /users/me`
Auth required. **Multipart** form fields, all optional: `firstName`, `lastName`, `username`, `email`, `password`, `currentPassword` (required if changing `username`/`email`/`password`), file field `image` for a new profile photo.

Business rule: changing username/email/password/photo is rate-limited to once every `PROFILE_EDIT_COOLDOWN_DAYS` (default 7) — a second sensitive change within that window returns `409` with a "you can update again in N day(s)" message.

→ `200`, updated user object. `403` if `currentPassword` is missing/wrong when required.

---

## Trade

Trade object shape (returned by all trade endpoints):
```json
{
  "id": "...", "name": "Old Woods", "category": "Wood", "material": "Reclaimed lumber",
  "image": null, "location": "Balibago, Santa Rosa, Laguna", "pickupLocation": null,
  "posterName": "Luke", "posterId": "...", "weightKg": 4.5, "quantity": 1,
  "tradingFor": { "type": "negotiating", "value": null },
  "description": "...", "status": "open", "hasOffers": false, "offerAccepted": false,
  "createdAt": "2026-07-28T12:01:44+00:00"
}
```
Note: `hasOffers`/`offerAccepted` are computed from the trade's offers at read time — they're not stored fields, always trust what the API returns rather than caching/deriving them yourself.

### `GET /trades`
Auth required. Query params (both optional): `?category=Wood`, `?location=Santa+Rosa`. Returns all **other users'** open/reserved/completed trades (excludes your own) — this is the Browse view. → `200`, array of Trade.

### `GET /trades/mine`
Auth required. → `200`, array of your own trades (Track Trades / My Trades view).

### `GET /trades/:id`
Auth required. → `200`, single Trade. `404` if not found.

### `POST /trades`
Auth required. **Multipart.** Required fields: `itemName`, `category`, `material`, `locationText`, `quantity`, `tradingForType` (one of `specific`/`nothing`/`negotiating`). Optional: `description`, `weightKg`, `pickupLocationText`, `tradingForValue` (required in practice if `tradingForType` is `specific`), file `image`. → `201`, the created Trade.

### `PATCH /trades/:id`
Auth required, **owner only** (`403` otherwise). **Multipart.** Any of the same fields as create, all optional — only sent fields get updated. → `200`, updated Trade.

---

## Offer

Offer object shape:
```json
{
  "id": "...", "tradeId": "...", "tradeName": "Old Woods",
  "offererId": "...", "offererName": "Aero",
  "itemName": "Old Bond Paper", "category": "Paper", "material": "Clean sheets",
  "weightKg": 2.0, "description": "...", "image": null,
  "status": "pending", "createdAt": "...", "decidedAt": null
}
```

### `POST /trades/:id/offers`
Auth required. **Multipart.** Required: `itemName`, `category`, `material`. Optional: `weightKg`, `description`, file `image`.

Business rules enforced: you can't offer on your own trade (`403`); the trade must be `open` (`409` if already `reserved`/`completed` — **a trade can only have one active/pending offer at a time**, this is the core invariant). On success the trade flips to `reserved`. → `201`, the created Offer.

### `GET /trades/:id/offers`
Auth required. → `200`, array of Offers on that trade (there will only ever be at most one `pending` at a time, but past `declined` ones stay in history).

### `POST /offers/:id/accept`
Auth required, must be the **trade owner** (`403` otherwise). Offer must be `pending` (`409` if already decided). On success: offer → `accepted`, trade → `completed`, offerer gets an `offer_accepted` notification. → `200`, updated Offer.

### `POST /offers/:id/decline`
Same auth/ownership rules as accept. On success: offer → `declined`, **trade reopens to `open`** (so a new offer can be sent), offerer gets an `offer_declined` notification. → `200`, updated Offer.

### `GET /offers/mine`
Auth required. → `200`, array of offers **you sent** (on other people's trades).

### `GET /offers/received`
Auth required. → `200`, array of offers **received** on your own trades.

---

## Resource Spot

Resource Spot object shape:
```json
{
  "id": "...", "name": "Garbage Pile", "material": "Plastic",
  "weightKg": 6.5, "quantity": 12, "description": "...",
  "location": "Tagapo, Santa Rosa, Laguna", "permissionNote": "Public area, no permission needed.",
  "image": null, "status": "active", "reporterId": "...", "reporterName": "Luke",
  "createdAt": "...", "expiresAt": "..."
}
```
No lat/lng — location is free text only (MVP scope decision). Map UI needs to render pins some other way (e.g. geocode `location` client-side, or just list them) — this is a known frontend gap, not something the API provides.

### `GET /resource-spots`
Auth required. → `200`, array of currently **active and unexpired** spots only (expired/collected ones are filtered out server-side — no client-side filtering needed).

### `GET /resource-spots/:id`
Auth required. → `200`, single spot (includes expired/collected ones too, unlike the list endpoint). `404` if not found.

### `POST /resource-spots`
Auth required. **Multipart.** Required: `name`, `material`, `locationText`. Optional: `weightKg`, `quantity`, `description`, `permissionNote`, file `image`. → `201`, created spot. `expiresAt` is set automatically (server-configured number of days out).

### `POST /resource-spots/:id/photos`
Auth required — **any authenticated user**, not just the reporter (per spec: "community members may submit a new photo after collecting materials"). **Multipart**, required file `image`. `409` if the spot is already `collected`. → `200`, updated spot with the new photo.

### `POST /resource-spots/:id/collected`
Auth required — again any authenticated user, not owner-restricted. No body. `409` if already collected. → `200`, updated spot with `status: "collected"` (this is when it should disappear from a map view — check `GET /resource-spots` again, it'll be filtered out).

---

## Notification

Notification object shape:
```json
{
  "id": "...", "type": "offer_received", "title": "New offer received",
  "body": "You received an offer on \"Old Woods\".",
  "relatedTradeId": "...", "relatedOfferId": "...",
  "isRead": false, "createdAt": "..."
}
```

### `GET /notifications`
Auth required. → `200`, all your notifications, newest first.

### `GET /notifications/unread-count`
Auth required. → `200 { "count": 3 }` — for a bell-icon badge.

### `PATCH /notifications/:id/read`
Auth required, must be the recipient (`403` otherwise). No body. → `200`, updated notification with `isRead: true`.

---

## Rating

Only usable on **completed** trades, and only by the two actual parties (the trade owner and the offerer whose offer was accepted) rating each other — one rating per direction per trade.

### `POST /trades/:id/ratings`
Auth required. JSON body: `{ "score": 5, "comment": "Great trade!" }` (`score` 1–5 required, `comment` optional). `403` if the trade isn't completed, you're not a party to it, or there's no accepted offer to rate. `409` if you've already rated this trade. → `201`, created rating.

### `GET /users/:id/ratings`
Auth required. → `200 { "ratings": [...], "average": 4.5 }` — `average` is `null` if the user has no ratings yet.

---

## AI (Gemini photo assessment)

Stateless — nothing is persisted by these calls. Always returns `200`, never errors out to the client (falls back to an empty/zero-confidence suggestion if Gemini fails or `GEMINI_API_KEY` isn't set) — always let the user fall through to manual entry regardless of what comes back.

### `POST /ai/assess-trade-photo`
Auth required. **Multipart**, required file `image`. → `200`:
```json
{ "itemName": "", "category": "", "material": "Steel scrap", "weightKg": 4.5, "confidence": 87 }
```

### `POST /ai/assess-resource-spot-photo`
Auth required. **Multipart**, required file `image`. → `200`:
```json
{ "material": "Plastic", "weightKg": 4.0, "quantity": 3, "confidence": 82 }
```

---

## Misc

### `GET /health`
Public. → `200 { "status": "ok" }`.

### `GET /`
Public. → `200`, basic service info — useful for confirming the deployed URL is actually this API before you start integrating.

### `GET /media/<path:filename>`
Public. Serves uploaded images. You should never need to construct this URL manually — every `image` field in every response is already the full resolved URL.
</file>

<file path="backend/migrations/alembic.ini">
# A generic, single database configuration.

[alembic]
# template used to generate migration files
# file_template = %%(rev)s_%%(slug)s

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false


# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic,flask_migrate

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[logger_flask_migrate]
level = INFO
handlers =
qualname = flask_migrate

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
</file>

<file path="backend/migrations/env.py">
import logging
from logging.config import fileConfig

from flask import current_app

from alembic import context

# this is the Alembic Config object, which provides
# access to the values within the .ini file in use.
config = context.config

# Interpret the config file for Python logging.
# This line sets up loggers basically.
fileConfig(config.config_file_name)
logger = logging.getLogger('alembic.env')


def get_engine():
    try:
        # this works with Flask-SQLAlchemy<3 and Alchemical
        return current_app.extensions['migrate'].db.get_engine()
    except (TypeError, AttributeError):
        # this works with Flask-SQLAlchemy>=3
        return current_app.extensions['migrate'].db.engine


def get_engine_url():
    try:
        return get_engine().url.render_as_string(hide_password=False).replace(
            '%', '%%')
    except AttributeError:
        return str(get_engine().url).replace('%', '%%')


# add your model's MetaData object here
# for 'autogenerate' support
# from myapp import mymodel
# target_metadata = mymodel.Base.metadata
config.set_main_option('sqlalchemy.url', get_engine_url())
target_db = current_app.extensions['migrate'].db

# other values from the config, defined by the needs of env.py,
# can be acquired:
# my_important_option = config.get_main_option("my_important_option")
# ... etc.


def get_metadata():
    if hasattr(target_db, 'metadatas'):
        return target_db.metadatas[None]
    return target_db.metadata


def run_migrations_offline():
    """Run migrations in 'offline' mode.

    This configures the context with just a URL
    and not an Engine, though an Engine is acceptable
    here as well.  By skipping the Engine creation
    we don't even need a DBAPI to be available.

    Calls to context.execute() here emit the given string to the
    script output.

    """
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url, target_metadata=get_metadata(), literal_binds=True
    )

    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online():
    """Run migrations in 'online' mode.

    In this scenario we need to create an Engine
    and associate a connection with the context.

    """

    # this callback is used to prevent an auto-migration from being generated
    # when there are no changes to the schema
    # reference: http://alembic.zzzcomputing.com/en/latest/cookbook.html
    def process_revision_directives(context, revision, directives):
        if getattr(config.cmd_opts, 'autogenerate', False):
            script = directives[0]
            if script.upgrade_ops.is_empty():
                directives[:] = []
                logger.info('No changes in schema detected.')

    conf_args = current_app.extensions['migrate'].configure_args
    if conf_args.get("process_revision_directives") is None:
        conf_args["process_revision_directives"] = process_revision_directives

    connectable = get_engine()

    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=get_metadata(),
            **conf_args
        )

        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
</file>

<file path="backend/migrations/README">
Single-database configuration for Flask.
</file>

<file path="backend/migrations/script.py.mako">
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

"""
from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

# revision identifiers, used by Alembic.
revision = ${repr(up_revision)}
down_revision = ${repr(down_revision)}
branch_labels = ${repr(branch_labels)}
depends_on = ${repr(depends_on)}


def upgrade():
    ${upgrades if upgrades else "pass"}


def downgrade():
    ${downgrades if downgrades else "pass"}
</file>

<file path="backend/.gitignore">
venv/
__pycache__/
*.pyc
.env
instance/
*.db
</file>

<file path="backend/.python-version">
3.13
</file>

<file path="backend/Procfile">
release: flask db upgrade
web: gunicorn -w 1 -b 0.0.0.0:$PORT run:app
</file>

<file path="backend/seed.py">
"""Populates the database with demo data for local development/demos.

Usage: python seed.py
Safe to re-run: skips seeding if any users already exist.
"""
from dotenv import load_dotenv

load_dotenv()

from app.app import create_app  # noqa: E402
from app.database import db  # noqa: E402
from app.models.enums import ResourceSpotStatus, TradeStatus, TradingForType  # noqa: E402
from app.models.resource_spot import ResourceSpot  # noqa: E402
from app.models.trade import Trade  # noqa: E402
from app.models.user import User  # noqa: E402
from app.utils.auth import hash_password  # noqa: E402

DEMO_USERS = [
    {"username": "luke", "first_name": "Luke", "last_name": "S", "email": "luke@example.com"},
    {"username": "aero", "first_name": "Aero", "last_name": "T", "email": "aero@example.com"},
    {"username": "franz", "first_name": "Franz", "last_name": "M", "email": "franz@example.com"},
]
DEMO_PASSWORD = "password123"

DEMO_TRADES = [
    {
        "item_name": "Old Woods", "category": "Wood", "material": "Reclaimed lumber",
        "description": "Still good, open for swap: paper or old magazines.",
        "weight_kg": 4.5, "quantity": 1, "location_text": "Balibago, Santa Rosa, Laguna",
        "trading_for_type": TradingForType.SPECIFIC, "trading_for_value": "Paper or old magazines",
    },
    {
        "item_name": "Scrap Metal Pile", "category": "Metal", "material": "Assorted steel scraps",
        "description": "From a home renovation project.",
        "weight_kg": 6.2, "quantity": 1, "location_text": "Dita, Santa Rosa, Laguna",
        "trading_for_type": TradingForType.NEGOTIATING, "trading_for_value": None,
    },
    {
        "item_name": "Bulk Cardboard", "category": "Mixed", "material": "Flattened boxes",
        "description": "Dry and clean.",
        "weight_kg": 15.0, "quantity": 9, "location_text": "Pulong Santa Cruz, Santa Rosa, Laguna",
        "trading_for_type": TradingForType.NOTHING, "trading_for_value": None,
    },
]

DEMO_SPOTS = [
    {
        "name": "Garbage Pile", "material": "Plastic",
        "description": "Pile of plastic bottles and containers left near the sari-sari store.",
        "weight_kg": 6.5, "quantity": 12, "location_text": "Tagapo, Santa Rosa, Laguna",
        "permission_note": "Public area, no permission needed.",
    },
    {
        "name": "Overgrown Lot", "material": "Mixed",
        "description": "Vacant lot with scattered scrap metal and cardboard.",
        "weight_kg": 15.0, "quantity": 5, "location_text": "Market Area, Santa Rosa, Laguna",
        "permission_note": "Ask barangay caretaker first.",
    },
]


def seed():
    app = create_app()
    with app.app_context():
        if User.query.first() is not None:
            print("Database already has users — skipping seed.")
            return

        users = []
        for u in DEMO_USERS:
            user = User(password_hash=hash_password(DEMO_PASSWORD), **u)
            db.session.add(user)
            users.append(user)
        db.session.flush()

        for i, t in enumerate(DEMO_TRADES):
            trade = Trade(owner_id=users[i % len(users)].id, status=TradeStatus.OPEN, **t)
            db.session.add(trade)

        for i, s in enumerate(DEMO_SPOTS):
            spot = ResourceSpot(
                reporter_id=users[i % len(users)].id,
                status=ResourceSpotStatus.ACTIVE,
                expires_at=ResourceSpot.default_expiry(app.config["RESOURCE_SPOT_EXPIRY_DAYS"]),
                **s,
            )
            db.session.add(spot)

        db.session.commit()
        print(f"Seeded {len(users)} users, {len(DEMO_TRADES)} trades, {len(DEMO_SPOTS)} resource spots.")
        print(f"Demo login: any of {[u['email'] for u in DEMO_USERS]} / password: {DEMO_PASSWORD}")


if __name__ == "__main__":
    seed()
</file>

<file path="frontend/src/features/auth/screens/SignInScreen.jsx">
import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { GoogleLogin } from '@react-oauth/google';
import Logo from '@/shared/components/common/Logo';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import PasswordInput from '@/shared/components/common/PasswordInput';
import { loginUser, loginWithGoogle } from '@/shared/services/api';
import { useAuth } from '@/features/auth/AuthContext';

export default function SignInScreen() {
  const navigate = useNavigate();
  const { login } = useAuth();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const canSubmit = /\S+@\S+\.\S+/.test(email) && password.length > 0;

  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!canSubmit || loading) return;
    setLoading(true);
    setError('');
    try {
      const { user, token } = await loginUser({ email: email.trim(), password });
      login(user, token);
      navigate('/trades', { replace: true });
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setLoading(false);
    }
  };

  const handleGoogleSuccess = async (credentialResponse) => {
    if (loading) return;
    setLoading(true);
    setError('');
    try {
      const { user, token } = await loginWithGoogle(credentialResponse.credential);
      login(user, token);
      navigate('/trades', { replace: true });
    } catch (err) {
      setError(err.message || 'Google authentication failed. Please try again.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex flex-col justify-center px-6 py-10">
      <div className="flex justify-center mb-8">
        <Logo size="lg" iconOnly />
      </div>

      <h1 className="text-2xl font-bold text-gray-900 text-center">Welcome back</h1>
      <p className="text-sm text-gray-500 text-center mt-1 mb-8">Sign in to continue.</p>

      {/* Google Login Section */}
      <div className="flex flex-col items-center mb-6">
        <GoogleLogin
          onSuccess={handleGoogleSuccess}
          onError={() => setError('Google Sign-In was unsuccessful.')}
          useOneTap
        />
        
        <div className="flex items-center w-full mt-6 mb-2">
          <div className="flex-1 border-t border-gray-200"></div>
          <span className="px-3 text-sm text-gray-400">or continue with email</span>
          <div className="flex-1 border-t border-gray-200"></div>
        </div>
      </div>

      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label className="text-sm font-medium text-gray-700 mb-1 block">Email</label>
          <input
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="you@example.com"
            className="w-full px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none"
          />
        </div>

        <div>
          <label className="text-sm font-medium text-gray-700 mb-1 block">Password</label>
          <PasswordInput
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="••••••••"
          />
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton type="submit" disabled={!canSubmit || loading}>
          {loading ? 'Signing in...' : 'Sign In'}
        </PrimaryButton>
      </form>

      <p className="text-center text-sm text-gray-500 mt-6">
        Don&apos;t have an account?{' '}
        <Link to="/signup" className="text-brand-600 font-semibold">
          Sign up
        </Link>
      </p>
    </div>
  );
}
</file>

<file path="frontend/src/features/auth/screens/SignUpScreen.jsx">
import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { GoogleLogin } from '@react-oauth/google';
import Logo from '@/shared/components/common/Logo';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import PasswordInput from '@/shared/components/common/PasswordInput';
import { registerUser, loginWithGoogle } from '@/shared/services/api';
import { useAuth } from '@/features/auth/AuthContext';

const inputClass =
  'w-full px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none';
const labelClass = 'text-sm font-medium text-gray-700 mb-1 block';

export default function SignUpScreen() {
  const navigate = useNavigate();
  const { login } = useAuth();
  const [firstName, setFirstName] = useState('');
  const [lastName, setLastName] = useState('');
  const [username, setUsername] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const validationMessage = (() => {
    if (!firstName.trim() || !lastName.trim()) return 'Enter your first and last name.';
    if (username.trim().length <= 2) return 'Username must be at least 3 characters.';
    if (!/\S+@\S+\.\S+/.test(email)) return 'Enter a valid email address.';
    if (password.length < 6) return 'Password must be at least 6 characters.';
    if (password !== confirmPassword) return "Passwords don't match.";
    return '';
  })();
  const canSubmit = validationMessage === '';
  const touched = [firstName, lastName, username, email, password, confirmPassword].some(
    (v) => v.length > 0
  );

  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!canSubmit || loading) return;
    setLoading(true);
    setError('');
    try {
      const { user, token } = await registerUser({
        firstName: firstName.trim(),
        lastName: lastName.trim(),
        username: username.trim(),
        email: email.trim(),
        password,
      });
      login(user, token);
      navigate('/trades', { replace: true });
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setLoading(false);
    }
  };

  const handleGoogleSuccess = async (credentialResponse) => {
    if (loading) return;
    setLoading(true);
    setError('');
    try {
      const { user, token } = await loginWithGoogle(credentialResponse.credential);
      login(user, token);
      navigate('/trades', { replace: true });
    } catch (err) {
      setError(err.message || 'Google sign-up failed. Please try again.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex flex-col justify-center px-6 py-10">
      <div className="flex justify-center mb-8">
        <Logo size="lg" iconOnly />
      </div>

      <h1 className="text-2xl font-bold text-gray-900 text-center">Create your account</h1>
      <p className="text-sm text-gray-500 text-center mt-1 mb-8">
        Join RosaCycle and start trading recyclables.
      </p>

      {/* Google Sign Up Section */}
      <div className="flex flex-col items-center mb-6">
        <GoogleLogin
          onSuccess={handleGoogleSuccess}
          onError={() => setError('Google Sign-Up was unsuccessful.')}
          useOneTap
        />
        
        <div className="flex items-center w-full mt-6 mb-2">
          <div className="flex-1 border-t border-gray-200"></div>
          <span className="px-3 text-sm text-gray-400">or sign up with email</span>
          <div className="flex-1 border-t border-gray-200"></div>
        </div>
      </div>

      <form onSubmit={handleSubmit} className="space-y-4">
        <div className="grid grid-cols-2 gap-3">
          <div>
            <label className={labelClass}>First name</label>
            <input
              className={inputClass}
              value={firstName}
              onChange={(e) => setFirstName(e.target.value)}
              placeholder="Juan"
            />
          </div>
          <div>
            <label className={labelClass}>Last name</label>
            <input
              className={inputClass}
              value={lastName}
              onChange={(e) => setLastName(e.target.value)}
              placeholder="Dela Cruz"
            />
          </div>
        </div>

        <div>
          <label className={labelClass}>Username</label>
          <input
            className={inputClass}
            value={username}
            onChange={(e) => setUsername(e.target.value)}
            placeholder="juandelacruz"
          />
        </div>

        <div>
          <label className={labelClass}>Email</label>
          <input
            type="email"
            className={inputClass}
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="you@example.com"
          />
        </div>

        <div>
          <label className={labelClass}>Password</label>
          <PasswordInput
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="At least 6 characters"
          />
        </div>

        <div>
          <label className={labelClass}>Confirm password</label>
          <PasswordInput
            value={confirmPassword}
            onChange={(e) => setConfirmPassword(e.target.value)}
            placeholder="Re-enter your password"
          />
        </div>

        {touched && validationMessage && (
          <p className="text-xs text-gray-500">{validationMessage}</p>
        )}
        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton type="submit" disabled={!canSubmit || loading}>
          {loading ? 'Creating account...' : 'Sign Up'}
        </PrimaryButton>
      </form>

      <p className="text-center text-sm text-gray-500 mt-6">
        Already have an account?{' '}
        <Link to="/signin" className="text-brand-600 font-semibold">
          Sign in
        </Link>
      </p>
    </div>
  );
}
</file>

<file path="frontend/src/features/auth/AuthContext.jsx">
import { createContext, useContext, useEffect, useState } from 'react';
import { logoutUser, updateUserProfile } from '@/shared/services/api';
import { USER_STORAGE_KEY, TOKEN_STORAGE_KEY } from '@/shared/lib/storageKeys';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(() => {
    const raw = localStorage.getItem(USER_STORAGE_KEY);
    return raw ? JSON.parse(raw) : null;
  });
  const [token, setToken] = useState(() => localStorage.getItem(TOKEN_STORAGE_KEY));

  useEffect(() => {
    if (user) localStorage.setItem(USER_STORAGE_KEY, JSON.stringify(user));
    else localStorage.removeItem(USER_STORAGE_KEY);
  }, [user]);

  useEffect(() => {
    if (token) localStorage.setItem(TOKEN_STORAGE_KEY, token);
    else localStorage.removeItem(TOKEN_STORAGE_KEY);
  }, [token]);

  // The backend has no refresh-token flow — a 401 means the token is gone/expired.
  // api.js can't reach this context directly (plain functions, not hooks), so it
  // dispatches this event instead; we react to it here by force-signing-out.
  useEffect(() => {
    const handleUnauthorized = () => {
      setUser(null);
      setToken(null);
    };
    window.addEventListener('rosacycle:unauthorized', handleUnauthorized);
    return () => window.removeEventListener('rosacycle:unauthorized', handleUnauthorized);
  }, []);

  const login = (userData, authToken) => {
    setUser(userData);
    setToken(authToken);
  };

  const logout = async () => {
    await logoutUser();
    setUser(null);
    setToken(null);
  };

  const updateUser = async (updates, currentPassword) => {
    const updated = await updateUserProfile(updates, currentPassword);
    setUser(updated);
    return updated;
  };

  return (
    <AuthContext.Provider value={{ user, isAuthenticated: !!user && !!token, login, logout, updateUser }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error('useAuth must be used within AuthProvider');
  return ctx;
}
</file>

<file path="frontend/src/features/map/screens/MapScreen.jsx">
import { useEffect, useRef, useState } from 'react';
import { MapPin, ChevronDown } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import MaterialTag from '@/shared/components/common/MaterialTag';
import { fetchResourceSpots, markResourceSpotCollected, addResourceSpotPhoto } from '@/shared/services/api';
import { getDistanceKm } from '@/features/map/geo';

export default function MapScreen() {
  const [selectedId, setSelectedId] = useState(null);
  const [spots, setSpots] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [busyId, setBusyId] = useState(null);
  const fileInputRef = useRef(null);
  const photoTargetId = useRef(null);

  const loadSpots = () => {
    setLoading(true);
    setError('');
    fetchResourceSpots()
      .then(setSpots)
      .catch((err) => setError(err.message || 'Could not load resource spots.'))
      .finally(() => setLoading(false));
  };

  useEffect(loadSpots, []);

  const toggleSite = (id) => setSelectedId((prev) => (prev === id ? null : id));

  const handleMarkCollected = async (id) => {
    if (busyId) return;
    setBusyId(id);
    try {
      await markResourceSpotCollected(id);
      loadSpots();
    } catch (err) {
      setError(err.message || 'Could not mark this spot collected.');
    } finally {
      setBusyId(null);
    }
  };

  const handleAddPhotoClick = (id) => {
    photoTargetId.current = id;
    fileInputRef.current?.click();
  };

  const handlePhotoFileChange = async (e) => {
    const file = e.target.files?.[0];
    const id = photoTargetId.current;
    e.target.value = '';
    if (!file || !id) return;
    setBusyId(id);
    try {
      await addResourceSpotPhoto(id, file);
      loadSpots();
    } catch (err) {
      setError(err.message || 'Could not add photo.');
    } finally {
      setBusyId(null);
    }
  };

  return (
    <div className="pb-24">
      <Header />

      <input
        ref={fileInputRef}
        type="file"
        accept="image/*"
        onChange={handlePhotoFileChange}
        className="hidden"
      />

      {/* Map container placeholder — no real coordinates from the backend, pins
          are laid out by index instead of lat/long. */}
      <div className="relative w-full h-56 bg-gray-200 flex items-center justify-center">
        <p className="text-sm text-gray-400">Map view</p>
        {spots.map((site, i) => (
          <button
            key={site.id}
            onClick={() => toggleSite(site.id)}
            className="absolute"
            style={{ left: `${25 + i * 22}%`, top: `${35 + (i % 2) * 20}%` }}
          >
            <MapPin
              size={28}
              className={selectedId === site.id ? 'text-brand-700 fill-brand-200' : 'text-brand-600 fill-brand-100'}
            />
          </button>
        ))}
      </div>

      <div className="px-4 pt-4 space-y-3">
        <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide">Nearby reported sites</p>

        {loading && <p className="text-center text-sm text-gray-400 py-8">Loading...</p>}
        {!loading && error && <p className="text-center text-sm text-red-500 py-4">{error}</p>}
        {!loading && !error && spots.length === 0 && (
          <p className="text-center text-sm text-gray-400 py-8">No resource spots reported yet.</p>
        )}

        {!loading &&
          spots.map((site) => {
            const expanded = selectedId === site.id;
            const distanceKm = getDistanceKm(site.location);
            const isBusy = busyId === site.id;
            return (
              <div
                key={site.id}
                className={`bg-white border rounded-xl overflow-hidden ${
                  expanded ? 'border-brand-500' : 'border-gray-100'
                }`}
              >
                <button
                  onClick={() => toggleSite(site.id)}
                  className="w-full flex items-center justify-between px-4 py-3 text-left"
                >
                  <div>
                    <p className="font-semibold text-gray-900 text-sm">{site.name}</p>
                    {distanceKm != null && <p className="text-xs text-gray-400 mt-0.5">{distanceKm} km away</p>}
                  </div>
                  <div className="flex items-center gap-2">
                    <MaterialTag material={site.material} />
                    <ChevronDown
                      size={16}
                      className={`text-gray-400 transition-transform ${expanded ? 'rotate-180' : ''}`}
                    />
                  </div>
                </button>

                {expanded && (
                  <div className="px-4 pb-4 pt-3 border-t border-gray-100 space-y-2">
                    <div className="aspect-[4/3] rounded-lg bg-gray-100 border border-gray-200 overflow-hidden">
                      {site.image && (
                        <img src={site.image} alt={site.name} className="w-full h-full object-cover" />
                      )}
                    </div>
                    <div className="flex gap-6 text-sm pt-2">
                      {site.weightKg != null && (
                        <span><span className="font-semibold">{site.weightKg}kg</span> weight</span>
                      )}
                      {site.quantity != null && (
                        <span><span className="font-semibold">{site.quantity}</span> quantity</span>
                      )}
                    </div>
                    {site.location && <p className="text-sm text-gray-500">📍 {site.location}</p>}
                    {site.description && (
                      <p className="text-sm text-gray-600 italic">&ldquo;{site.description}&rdquo;</p>
                    )}
                    {site.permissionNote && (
                      <p className="text-xs text-gray-400">Access notes: {site.permissionNote}</p>
                    )}

                    <div className="flex gap-2 pt-2">
                      <button
                        onClick={() => handleAddPhotoClick(site.id)}
                        disabled={isBusy}
                        className="flex-1 py-2.5 rounded-lg bg-gray-100 text-gray-700 text-xs font-semibold disabled:opacity-50"
                      >
                        Add photo
                      </button>
                      <button
                        onClick={() => handleMarkCollected(site.id)}
                        disabled={isBusy}
                        className="flex-1 py-2.5 rounded-lg bg-brand-600 text-white text-xs font-semibold disabled:opacity-50"
                      >
                        {isBusy ? 'Working...' : 'Mark Collected'}
                      </button>
                    </div>
                  </div>
                )}
              </div>
            );
          })}
      </div>

      <BottomNav />
    </div>
  );
}
</file>

<file path="frontend/src/features/map/screens/ResourceSpotConfirmScreen.jsx">
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import { SPOT_MATERIAL_OPTIONS } from '@/shared/utils/constants';
import { reportResourceSpot } from '@/shared/services/api';
import { getPendingCapture, clearPendingCapture } from '@/shared/lib/pendingCapture';

const inputClass =
  'w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand-500';
const labelClass = 'text-xs font-semibold text-gray-500 mb-1.5 block';

export default function ResourceSpotConfirmScreen() {
  const navigate = useNavigate();
  const [name, setName] = useState('');
  const [material, setMaterial] = useState('');
  const [weightKg, setWeightKg] = useState('');
  const [quantity, setQuantity] = useState('');
  const [description, setDescription] = useState('');
  const [locationText, setLocationText] = useState('');
  const [permissionNote, setPermissionNote] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState('');

  const imageFile = getPendingCapture();
  const previewUrl = imageFile ? URL.createObjectURL(imageFile) : null;

  const canSubmit = name.trim().length > 0 && material && locationText.trim().length > 0;

  const handleSubmit = async () => {
    if (!canSubmit || submitting) return;
    setSubmitting(true);
    setError('');
    try {
      const saved = await reportResourceSpot(
        {
          name: name.trim(),
          material,
          weightKg: Number(weightKg) || undefined,
          quantity: Number(quantity) || undefined,
          description: description.trim(),
          locationText: locationText.trim(),
          permissionNote: permissionNote.trim(),
        },
        imageFile
      );
      clearPendingCapture();
      navigate('/camera/reported', { state: { spot: saved } });
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Report Spot" />

      <div className="aspect-[4/3] bg-gray-200 flex items-center justify-center text-gray-400 text-sm overflow-hidden">
        {previewUrl ? (
          <img src={previewUrl} alt="Captured spot" className="w-full h-full object-cover" />
        ) : (
          'No photo captured'
        )}
      </div>

      <div className="p-5 space-y-6">
        <div>
          <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
            AI-Detected (editable)
          </p>
          <div className="space-y-3">
            <div>
              <label className={labelClass}>Material spotted</label>
              <div className="grid grid-cols-3 gap-2">
                {SPOT_MATERIAL_OPTIONS.map((opt) => (
                  <button
                    key={opt}
                    type="button"
                    onClick={() => setMaterial(opt)}
                    className={`py-2 rounded-lg text-xs font-medium ${
                      material === opt ? 'bg-brand-600 text-white' : 'bg-gray-100 text-gray-600'
                    }`}
                  >
                    {opt}
                  </button>
                ))}
              </div>
            </div>
            <div>
              <label className={labelClass}>Estimated weight (kg)</label>
              <input
                type="number"
                className={inputClass}
                value={weightKg}
                onChange={(e) => setWeightKg(e.target.value)}
                placeholder="e.g. 4.0"
              />
            </div>
            <div>
              <label className={labelClass}>Estimated quantity</label>
              <input
                type="number"
                className={inputClass}
                value={quantity}
                onChange={(e) => setQuantity(e.target.value)}
                placeholder="e.g. 3"
              />
            </div>
          </div>
        </div>

        <div>
          <label className={labelClass}>Spot name</label>
          <input
            className={inputClass}
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="e.g. Overgrown Lot"
          />
        </div>

        <div>
          <label className={labelClass}>Description (optional)</label>
          <textarea
            className={`${inputClass} min-h-[90px] resize-none`}
            value={description}
            onChange={(e) => setDescription(e.target.value)}
            placeholder="What did you find here?"
          />
        </div>

        <div>
          <label className={labelClass}>Location</label>
          <input
            className={inputClass}
            value={locationText}
            onChange={(e) => setLocationText(e.target.value)}
            placeholder="e.g. Tagapo, Santa Rosa, Laguna"
          />
        </div>

        <div>
          <label className={labelClass}>Permission / access notes (optional)</label>
          <textarea
            className={`${inputClass} min-h-[70px] resize-none`}
            value={permissionNote}
            onChange={(e) => setPermissionNote(e.target.value)}
            placeholder="e.g. Public lot, no permission needed / Ask caretaker first"
          />
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton onClick={handleSubmit} disabled={!canSubmit || submitting}>
          {submitting ? 'Reporting...' : 'Report Spot'}
        </PrimaryButton>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/map/screens/ResourceSpotScreen.jsx">
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import CameraViewfinder from '@/features/scan/components/CameraViewfinder';
import { setPendingCapture } from '@/shared/lib/pendingCapture';

/**
 * Bottom-nav "Camera" tab. Captures a photo of a resource spot (a location
 * with recyclable/reusable material worth flagging) and reports it as a
 * pin on the Map — this never creates a Trade. Trade item scanning lives
 * at /trades/scan instead (see ScanScreen.jsx).
 */
export default function ResourceSpotScreen() {
  const navigate = useNavigate();
  const [previewUrl, setPreviewUrl] = useState(null);

  const handleFileSelected = (file) => {
    setPendingCapture(file);
    setPreviewUrl(URL.createObjectURL(file));
  };

  const goToConfirm = () => {
    navigate('/camera/confirm');
  };

  return (
    <div className="h-screen flex flex-col overflow-hidden">
      <Header title="Resource Spot" showBell={false} />
      <div className="flex-1 relative min-h-0 pb-16">
        <CameraViewfinder
          previewImage={previewUrl}
          onCapture={goToConfirm}
          onFileSelected={handleFileSelected}
          onFlip={() => {}}
        />
        {previewUrl && (
          <div className="absolute bottom-0 left-0 right-0 px-4 py-3 bg-white border-t border-gray-100">
            <p className="text-sm text-gray-500 text-center">Photo captured. Tap shutter again to continue</p>
          </div>
        )}
      </div>
      <BottomNav />
    </div>
  );
}
</file>

<file path="frontend/src/features/map/screens/SpotReportedScreen.jsx">
import { useLocation, useNavigate } from 'react-router-dom';
import ConfirmationScreen from '@/shared/components/common/ConfirmationScreen';

export default function SpotReportedScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const spot = location.state?.spot;

  return (
    <ConfirmationScreen
      heading="Spot Reported!"
      subtext="Thanks for helping the community find this resource. It'll now show up as a pin on the map."
      summaryCard={
        spot && (
          <div>
            <p className="font-bold text-gray-900">{spot.name}</p>
            <p className="text-sm text-gray-500 mt-1">
              {spot.material}
              {spot.weightKg != null && ` • ${spot.weightKg}kg`}
              {spot.quantity != null && ` • ${spot.quantity} qty`}
            </p>
            {spot.description && <p className="text-sm text-gray-500 mt-1">{spot.description}</p>}
            {spot.permissionNote && (
              <p className="text-xs text-gray-400 mt-2">Access notes: {spot.permissionNote}</p>
            )}
          </div>
        )
      }
      primaryAction={{ label: 'View on Map', onClick: () => navigate('/map') }}
      secondaryAction={{ label: 'Report Another Spot', onClick: () => navigate('/camera') }}
    />
  );
}
</file>

<file path="frontend/src/features/map/geo.js">
// Mock data scope: Santa Rosa, Laguna only. The mock current user is based in
// Sinalhan, so every other barangay's distance is computed relative to that —
// single source of truth instead of hand-picked distanceKm numbers that can
// drift out of sync with the location text (that mismatch was a real bug once
// already, see mockTrackTrades history).
//
// Distances are straight-line approximations for demo purposes, not routed.
const BARANGAY_DISTANCE_FROM_USER_KM = {
  Sinalhan: 0,
  Tagapo: 1.1,
  Ibaba: 1.3,
  'Market Area': 1.6,
  Kanluran: 1.8,
  Malusak: 2.0,
  Pooc: 2.1,
  Labas: 2.4,
  Aplaya: 2.7,
  Dila: 3.0,
  Dita: 3.2,
  Caingin: 3.5,
  'Santo Domingo': 3.8,
  Macabling: 4.0,
  Malitlit: 4.6,
  'Pulong Santa Cruz': 5.2,
  Balibago: 5.5,
  'Don Jose': 6.0,
};

export const SANTA_ROSA_BARANGAYS = Object.keys(BARANGAY_DISTANCE_FROM_USER_KM);

// `location` is a full string like "Balibago, Santa Rosa, Laguna" — match it
// against the known barangay names rather than requiring an exact key.
export function getDistanceKm(location) {
  if (!location) return null;
  const barangay = SANTA_ROSA_BARANGAYS.find((b) => location.includes(b));
  return barangay ? BARANGAY_DISTANCE_FROM_USER_KM[barangay] : null;
}
</file>

<file path="frontend/src/features/scan/components/AIDetectedForm.jsx">
/**
 * Editable form shown after AI scans a photo.
 *
 * context="posting" -> shows Caption + Location + Desired pickup location +
 *                       Trade-for fields (docs: "User details" step under
 *                       Posting a Trade). Caption is a listing description,
 *                       so it only applies here — you're creating a new
 *                       public listing.
 * context="bidding"  -> just the AI-detected item fields above (Category,
 *                       Material, weight, Item name) — no caption, no
 *                       location, no message field. This is an offer on
 *                       someone else's existing trade; the trade owner
 *                       reviews the item itself, nothing else to fill in.
 *
 * All AI-generated values are pre-filled but editable, per docs:
 * "AI scans the photo and outputs the required item details" -> "User can edit details"
 */
export default function AIDetectedForm({ context = 'posting', values, onChange }) {
  const set = (key) => (e) => onChange?.({ ...values, [key]: e.target.value });

  const inputClass =
    'w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand-500';
  const labelClass = 'text-xs font-semibold text-gray-500 mb-1.5 block';

  return (
    <div className="space-y-5">
      <div>
        <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
          AI-Detected (editable)
        </p>
        <div className="space-y-3">
          <div>
            <label className={labelClass}>Category</label>
            <input className={inputClass} value={values.category || ''} onChange={set('category')} placeholder="e.g. Metal" />
          </div>
          <div>
            <label className={labelClass}>Material</label>
            <input className={inputClass} value={values.material || ''} onChange={set('material')} placeholder="e.g. Steel scrap" />
          </div>
          <div>
            <label className={labelClass}>Estimated weight (kg)</label>
            <input
              type="number"
              className={inputClass}
              value={values.weightKg || ''}
              onChange={set('weightKg')}
              placeholder="e.g. 4.5"
            />
          </div>
          {values.confidence != null && (
            <p className="text-xs text-gray-400">AI confidence: {values.confidence}%</p>
          )}
        </div>
      </div>

      <div>
        <label className={labelClass}>Item name</label>
        <input className={inputClass} value={values.itemName || ''} onChange={set('itemName')} placeholder="e.g. Old Woods" />
      </div>

      {context === 'posting' && (
        <div className="border-t border-gray-100 pt-5 space-y-3">
          <div>
            <label className={labelClass}>Quantity</label>
            <input
              type="number"
              min="1"
              className={inputClass}
              value={values.quantity || ''}
              onChange={set('quantity')}
              placeholder="e.g. 1"
            />
          </div>
          <div>
            <label className={labelClass}>Caption</label>
            <textarea
              className={`${inputClass} min-h-[80px] resize-none`}
              value={values.description || ''}
              onChange={set('description')}
              placeholder="Describe the item's condition, e.g. &quot;Flattened boxes, dry and clean.&quot;"
            />
          </div>

          <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide">Location</p>
          <div>
            <label className={labelClass}>Current location</label>
            <input
              className={inputClass}
              value={values.currentLocation || ''}
              onChange={set('currentLocation')}
              placeholder="Auto-filled from GPS"
            />
          </div>
          <div>
            <label className={labelClass}>Desired pickup location</label>
            <input
              className={inputClass}
              value={values.pickupLocation || ''}
              onChange={set('pickupLocation')}
              placeholder="Where should traders pick this up?"
            />
          </div>
          <div>
            <label className={labelClass}>Trade for</label>
            <div className="grid grid-cols-3 gap-2 mb-2">
              {[
                { key: 'specific', label: 'Specific item' },
                { key: 'nothing', label: 'Nothing' },
                { key: 'negotiating', label: 'Negotiable' },
              ].map((opt) => (
                <button
                  key={opt.key}
                  type="button"
                  onClick={() => onChange?.({ ...values, tradingForType: opt.key })}
                  className={`py-2 rounded-lg text-xs font-medium ${
                    values.tradingForType === opt.key
                      ? 'bg-brand-600 text-white'
                      : 'bg-gray-100 text-gray-600'
                  }`}
                >
                  {opt.label}
                </button>
              ))}
            </div>
            {values.tradingForType === 'specific' && (
              <input
                className={inputClass}
                value={values.tradingForValue || ''}
                onChange={set('tradingForValue')}
                placeholder="e.g. Paper or old magazines"
              />
            )}
          </div>
        </div>
      )}
    </div>
  );
}
</file>

<file path="frontend/src/features/scan/components/CameraViewfinder.jsx">
import { useRef } from 'react';
import { Upload, Circle, RefreshCw } from 'lucide-react';

/**
 * Camera viewfinder shell. There's no live camera feed — capture is a plain
 * <input type="file" accept="image/*"> (mobile browsers show a native
 * camera-or-gallery chooser for this). `onCapture` is only called once a
 * photo has already been selected — it means "continue," not "take photo."
 * `boundingBox` is a placeholder prop: { x, y, width, height } in percent
 * (0-100), drawn as an overlay rectangle, for a future AI-detection box.
 */
export default function CameraViewfinder({ previewImage, boundingBox, onCapture, onFileSelected, onFlip }) {
  const fileInputRef = useRef(null);

  const openPicker = () => fileInputRef.current?.click();

  const handleFileChange = (e) => {
    const file = e.target.files?.[0];
    if (file) onFileSelected(file);
    e.target.value = ''; // allow re-selecting the same file again later
  };

  const handleShutterClick = () => {
    if (previewImage) onCapture();
    else openPicker();
  };

  return (
    <div className="relative w-full h-full bg-black overflow-hidden">
      <input
        ref={fileInputRef}
        type="file"
        accept="image/*"
        onChange={handleFileChange}
        className="hidden"
      />

      {previewImage ? (
        <img src={previewImage} alt="Camera preview" className="w-full h-full object-cover" />
      ) : (
        <div className="w-full h-full flex items-center justify-center text-gray-500 text-sm">
          Camera preview
        </div>
      )}

      {boundingBox && (
        <div
          className="absolute border-2 border-sky-400 rounded-md"
          style={{
            left: `${boundingBox.x}%`,
            top: `${boundingBox.y}%`,
            width: `${boundingBox.width}%`,
            height: `${boundingBox.height}%`,
          }}
        />
      )}

      <div className="absolute bottom-0 left-0 right-0 flex items-center justify-between px-8 py-6 bg-gradient-to-t from-black/60 to-transparent">
        <button onClick={openPicker} aria-label="Upload from gallery" className="text-white active:scale-90 transition-transform">
          <Upload size={26} />
        </button>
        <button onClick={handleShutterClick} aria-label="Capture" className="active:scale-90 transition-transform">
          <Circle size={64} className="text-white" strokeWidth={2.5} />
        </button>
        <button onClick={onFlip} aria-label="Flip camera" className="w-11 h-11 rounded-full bg-black/40 flex items-center justify-center text-white active:scale-90 transition-transform">
          <RefreshCw size={20} />
        </button>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/scan/screens/ScanConfirmScreen.jsx">
import { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import AIDetectedForm from '@/features/scan/components/AIDetectedForm';
import { createTrade, sendOffer } from '@/shared/services/api';
import { getPendingCapture, clearPendingCapture } from '@/shared/lib/pendingCapture';

const BLANK_VALUES = {
  category: '',
  material: '',
  weightKg: '',
  itemName: '',
  quantity: '',
  description: '',
  currentLocation: '',
  pickupLocation: '',
  tradingForType: 'negotiating',
  tradingForValue: '',
};

export default function ScanConfirmScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const context = location.state?.context || 'posting';
  const tradeId = location.state?.tradeId;

  const [values, setValues] = useState(BLANK_VALUES);
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState('');

  const imageFile = getPendingCapture();
  const previewUrl = imageFile ? URL.createObjectURL(imageFile) : null;

  const canSubmit =
    context === 'posting'
      ? Boolean(
          values.itemName?.trim() &&
            values.category?.trim() &&
            values.material?.trim() &&
            values.quantity &&
            values.currentLocation?.trim()
        )
      : Boolean(values.itemName?.trim() && values.category?.trim() && values.material?.trim());

  const handlePrimaryAction = async () => {
    if (!canSubmit || submitting) return;
    setSubmitting(true);
    setError('');
    try {
      if (context === 'posting') {
        const trade = await createTrade(
          {
            itemName: values.itemName,
            category: values.category,
            material: values.material,
            description: values.description,
            weightKg: values.weightKg,
            quantity: values.quantity,
            locationText: values.currentLocation,
            pickupLocationText: values.pickupLocation,
            tradingForType: values.tradingForType,
            tradingForValue: values.tradingForType === 'specific' ? values.tradingForValue : undefined,
          },
          imageFile
        );
        clearPendingCapture();
        navigate('/trades/scan/created', { state: { trade } });
      } else {
        const offer = await sendOffer(
          tradeId,
          {
            itemName: values.itemName,
            category: values.category,
            material: values.material,
            weightKg: values.weightKg,
            description: values.description,
          },
          imageFile
        );
        clearPendingCapture();
        navigate(`/trades/${tradeId}/offer-sent`, { state: { offer } });
      }
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Edit Details" />

      <div className="aspect-[4/3] bg-gray-200 flex items-center justify-center text-gray-400 text-sm overflow-hidden">
        {previewUrl ? (
          <img src={previewUrl} alt="Captured item" className="w-full h-full object-cover" />
        ) : (
          'No photo captured'
        )}
      </div>

      <div className="p-5 space-y-6">
        <AIDetectedForm context={context} values={values} onChange={setValues} />

        {error && <p className="text-sm text-red-500">{error}</p>}

        <div className="space-y-3">
          <PrimaryButton onClick={handlePrimaryAction} disabled={!canSubmit || submitting}>
            {submitting
              ? 'Submitting...'
              : context === 'posting'
                ? 'Create Trade'
                : 'Send Offer'}
          </PrimaryButton>
          <button onClick={() => navigate(-1)} className="w-full text-center text-sm text-gray-500 font-medium">
            Retake Photo
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/scan/screens/ScanScreen.jsx">
import { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import CameraViewfinder from '@/features/scan/components/CameraViewfinder';
import { setPendingCapture } from '@/shared/lib/pendingCapture';

/**
 * AI scan step for a Trade item — reached only from within the Trades flow
 * (post a new item, or send an offer on someone else's item). This is
 * distinct from ResourceSpotScreen (the bottom-nav Camera tab), which
 * reports a location, not an item, and never creates a Trade.
 */
export default function ScanScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const context = location.state?.context || 'posting'; // 'posting' | 'bidding'
  const tradeId = location.state?.tradeId;

  const [previewUrl, setPreviewUrl] = useState(null);

  const handleFileSelected = (file) => {
    setPendingCapture(file);
    setPreviewUrl(URL.createObjectURL(file));
  };

  const goToConfirm = () => {
    navigate('/trades/scan/confirm', { state: { context, tradeId } });
  };

  return (
    <div className="h-screen flex flex-col overflow-hidden">
      <Header title={context === 'posting' ? 'Post an Item' : 'Create Offer'} showBell={false} />
      <div className="flex-1 relative min-h-0 pb-16">
        <CameraViewfinder
          previewImage={previewUrl}
          onCapture={goToConfirm}
          onFileSelected={handleFileSelected}
          onFlip={() => {}}
        />
        {previewUrl && (
          <div className="absolute bottom-0 left-0 right-0 px-4 py-3 bg-white border-t border-gray-100">
            <p className="text-sm text-gray-500 text-center">Photo captured. Tap shutter again to continue</p>
          </div>
        )}
      </div>
      <BottomNav />
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/components/cards/TradeCard.jsx">
import MaterialTag from '@/shared/components/common/MaterialTag';
import StatusPill from '@/shared/components/common/StatusPill';
import { getTradeStatus } from '@/shared/utils/tradeFormat';

/**
 * Two visual variants:
 * - "browse" (default): used in the Browse tab list — shows distance + poster name
 * - "track": used in the My Trades tab — minimal glance info only
 *   (name, material, weight, status); full detail (quantity, description,
 *   trading-for) lives in TradeDetailScreen behind "View Details".
 */
export default function TradeCard({ trade, variant = 'browse', onClick }) {
  const { image, name, material, distanceKm, posterName, weightKg } = trade;

  return (
    <button
      onClick={onClick}
      className="w-full text-left bg-white rounded-2xl overflow-hidden shadow-sm border border-gray-200"
    >
      <div className="relative aspect-[4/3] bg-gray-100 border-b border-gray-200">
        {image && <img src={image} alt={name} className="w-full h-full object-cover" />}
        {material && (
          <div className="absolute top-3 right-3">
            <MaterialTag material={material} />
          </div>
        )}
      </div>

      <div className="p-4">
        <div className="flex items-start justify-between gap-2">
          <h3 className="font-bold text-gray-900">{name}</h3>
          {weightKg != null && (
            <span className="font-semibold text-gray-900 shrink-0">{weightKg}kg</span>
          )}
        </div>

        {variant === 'browse' ? (
          <div className="flex items-center justify-between mt-1">
            <p className="text-sm text-gray-500">
              {distanceKm != null && `${distanceKm}km`}
              {distanceKm != null && posterName && ' • '}
              {posterName}
            </p>
          </div>
        ) : (
          <div className="mt-2">
            <StatusPill status={getTradeStatus(trade)} />
          </div>
        )}
      </div>
    </button>
  );
}
</file>

<file path="frontend/src/features/trades/components/trade/EcoImpactBox.jsx">
import { Leaf } from 'lucide-react';

export default function EcoImpactBox({ weightKg }) {
  if (!weightKg) return null;
  // The message always mirrors the item's own weight exactly — no estimate
  // or multiplier — and simply doesn't render at all when there's no weight.
  const pollutionKg = Number(weightKg);

  return (
    <div className="flex items-start gap-3 bg-green-200 rounded-2xl p-4">
      <div className="shrink-0 w-11 h-11 rounded-full bg-green-100 flex items-center justify-center relative">
        <Leaf size={22} className="absolute text-green-300 rotate-[20deg] translate-x-1" />
        <Leaf size={19} className="relative text-green-600 -translate-x-0.5 translate-y-0.5" />
      </div>
      <div>
        <p className="font-semibold text-gray-900 text-sm">Eco-Impact</p>
        <p className="text-sm text-gray-800 mt-0.5">
          Trading this material prevents ~{pollutionKg}kg of pollution from new production.
        </p>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/components/trade/FilterChipRow.jsx">
export default function FilterChipRow({
  options = ['All Items', 'Wood', 'Metal', 'Plastic', 'Fabric', 'Paper', 'E-waste', 'Mixed'],
  active,
  onSelect,
}) {
  return (
    <div className="flex gap-2 overflow-x-auto no-scrollbar px-4 -mx-4 pb-1">
      {options.map((opt) => {
        const isActive = opt === active;
        return (
          <button
            key={opt}
            onClick={() => onSelect?.(opt)}
            className={`shrink-0 px-4 py-2 rounded-full text-sm font-medium whitespace-nowrap transition-colors ${
              isActive ? 'bg-brand-600 text-white' : 'bg-gray-100 text-gray-700'
            }`}
          >
            {opt}
          </button>
        );
      })}
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/components/trade/MessageThread.jsx">
import { useState } from 'react';
import { Send } from 'lucide-react';

/**
 * Simple message thread — UI shell only.
 * Real-time send/receive logic belongs to Integration Lead ("Future chat integration").
 * This component just needs `messages` (array of { id, text, fromMe }) and an onSend callback.
 */
export default function MessageThread({ messages = [], onSend }) {
  const [draft, setDraft] = useState('');

  const handleSend = () => {
    if (!draft.trim()) return;
    onSend?.(draft.trim());
    setDraft('');
  };

  return (
    <div className="flex flex-col h-full">
      <div className="flex-1 overflow-y-auto px-4 py-4 space-y-3">
        {messages.map((m) => (
          <div key={m.id} className={`flex ${m.fromMe ? 'justify-end' : 'justify-start'}`}>
            <div
              className={`max-w-[75%] px-4 py-2.5 rounded-2xl text-sm ${
                m.fromMe ? 'bg-brand-600 text-white rounded-br-sm' : 'bg-gray-100 text-gray-800 rounded-bl-sm'
              }`}
            >
              {m.text}
            </div>
          </div>
        ))}
        {messages.length === 0 && (
          <p className="text-center text-sm text-gray-400 mt-10">Say hello to start the conversation.</p>
        )}
      </div>

      <div className="flex items-center gap-2 px-4 py-3 border-t border-gray-100 bg-white sticky bottom-0">
        <input
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          onKeyDown={(e) => e.key === 'Enter' && handleSend()}
          placeholder="Message"
          className="flex-1 bg-gray-100 rounded-full px-4 py-2.5 text-sm outline-none"
        />
        <button
          onClick={handleSend}
          className="w-10 h-10 rounded-full bg-brand-600 text-white flex items-center justify-center shrink-0 active:scale-95 transition-transform"
          aria-label="Send"
        >
          <Send size={17} />
        </button>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/screens/EditListingScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import { fetchTradeById, updateTrade } from '@/shared/services/api';

const inputClass =
  'w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand-500';
const labelClass = 'text-xs font-semibold text-gray-500 mb-1.5 block';

const TRADE_FOR_OPTIONS = [
  { key: 'specific', label: 'Specific item' },
  { key: 'nothing', label: 'Nothing' },
  { key: 'negotiating', label: 'Negotiable' },
];

export default function EditListingScreen() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [trade, setTrade] = useState(null);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');

  const [name, setName] = useState('');
  const [description, setDescription] = useState('');
  const [weightKg, setWeightKg] = useState('');
  const [quantity, setQuantity] = useState('');
  const [tradingForType, setTradingForType] = useState('negotiating');
  const [tradingForValue, setTradingForValue] = useState('');

  useEffect(() => {
    fetchTradeById(id)
      .then((data) => {
        setTrade(data);
        setName(data.name || '');
        setDescription(data.description || '');
        setWeightKg(data.weightKg ?? '');
        setQuantity(data.quantity ?? '');
        setTradingForType(data.tradingFor?.type || 'negotiating');
        setTradingForValue(data.tradingFor?.value || '');
      })
      .catch(() => setTrade(null))
      .finally(() => setLoading(false));
  }, [id]);

  if (loading) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Edit Listing" />
        <p className="text-center text-sm text-gray-400 py-16">Loading...</p>
      </div>
    );
  }

  if (!trade) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Edit Listing" />
        <p className="text-center text-sm text-gray-400 py-16">Listing not found.</p>
      </div>
    );
  }

  const canSave = name.trim().length > 0 && Number(weightKg) > 0 && Number(quantity) > 0 && !saving;

  const handleSave = async () => {
    if (!canSave) return;
    setSaving(true);
    setError('');
    try {
      await updateTrade(trade.id, {
        itemName: name.trim(),
        description: description.trim(),
        weightKg: Number(weightKg),
        quantity: Number(quantity),
        tradingForType,
        tradingForValue: tradingForType === 'specific' ? tradingForValue.trim() : undefined,
      });
      navigate(`/trades/${trade.id}?owner=1`, { replace: true });
    } catch (err) {
      setError(err.message || 'Could not save changes.');
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Edit Listing" />

      <div className="p-5 space-y-5">
        <div>
          <label className={labelClass}>Item name</label>
          <input className={inputClass} value={name} onChange={(e) => setName(e.target.value)} />
        </div>

        <div>
          <label className={labelClass}>Caption</label>
          <textarea
            className={`${inputClass} min-h-[80px] resize-none`}
            value={description}
            onChange={(e) => setDescription(e.target.value)}
          />
        </div>

        <div className="grid grid-cols-2 gap-3">
          <div>
            <label className={labelClass}>Weight (kg)</label>
            <input
              type="number"
              className={inputClass}
              value={weightKg}
              onChange={(e) => setWeightKg(e.target.value)}
            />
          </div>
          <div>
            <label className={labelClass}>Quantity</label>
            <input
              type="number"
              className={inputClass}
              value={quantity}
              onChange={(e) => setQuantity(e.target.value)}
            />
          </div>
        </div>

        <div>
          <label className={labelClass}>Trade for</label>
          <div className="grid grid-cols-3 gap-2 mb-2">
            {TRADE_FOR_OPTIONS.map((opt) => (
              <button
                key={opt.key}
                type="button"
                onClick={() => setTradingForType(opt.key)}
                className={`py-2 rounded-lg text-xs font-medium ${
                  tradingForType === opt.key ? 'bg-brand-600 text-white' : 'bg-gray-100 text-gray-600'
                }`}
              >
                {opt.label}
              </button>
            ))}
          </div>
          {tradingForType === 'specific' && (
            <input
              className={inputClass}
              value={tradingForValue}
              onChange={(e) => setTradingForValue(e.target.value)}
              placeholder="e.g. Paper or old magazines"
            />
          )}
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton onClick={handleSave} disabled={!canSave}>
          {saving ? 'Saving...' : 'Save Changes'}
        </PrimaryButton>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/screens/MessageThreadScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import MessageThread from '@/features/trades/components/trade/MessageThread';
import { fetchTradeById } from '@/shared/services/api';

export default function MessageThreadScreen() {
  const navigate = useNavigate();
  const { id } = useParams();
  const [trade, setTrade] = useState(null);

  useEffect(() => {
    fetchTradeById(id).then(setTrade).catch(() => setTrade(null));
  }, [id]);

  // Placeholder local state — real-time sync is Integration Lead's job.
  const [messages, setMessages] = useState([
    { id: 1, text: 'Hi! Is this still available?', fromMe: false },
    { id: 2, text: 'Yes, still open for trade!', fromMe: true },
  ]);

  const handleSend = (text) => {
    setMessages((prev) => [...prev, { id: prev.length + 1, text, fromMe: true }]);
  };

  return (
    <div className="flex flex-col h-screen">
      <Header onBack={() => navigate(-1)} title={trade?.posterName || 'Conversation'} showBell={false} />
      <div className="flex-1 overflow-hidden">
        <MessageThread messages={messages} onSend={handleSend} />
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/screens/OfferSentScreen.jsx">
import { useEffect, useState } from 'react';
import { useLocation, useNavigate, useParams } from 'react-router-dom';
import ConfirmationScreen from '@/shared/components/common/ConfirmationScreen';
import StatusPill from '@/shared/components/common/StatusPill';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';
import { fetchTradeById } from '@/shared/services/api';

export default function OfferSentScreen() {
  const navigate = useNavigate();
  const { id } = useParams();
  const location = useLocation();
  const offer = location.state?.offer;
  const [trade, setTrade] = useState(null);

  useEffect(() => {
    fetchTradeById(id).then(setTrade).catch(() => setTrade(null));
  }, [id]);

  return (
    <ConfirmationScreen
      heading="Offer Sent!"
      subtext={`${trade?.posterName || 'The poster'} will be notified about your offer. You'll get an update here once they respond.`}
      summaryCard={
        <div>
          <p className="text-xs text-gray-400 font-semibold uppercase mb-3">Your Offer</p>
          <div className="flex items-center gap-3">
            <div className="w-16 h-16 rounded-xl bg-gray-100 border border-gray-200 shrink-0 overflow-hidden">
              {offer?.image && (
                <img src={offer.image} alt={offer.itemName} className="w-full h-full object-cover" />
              )}
            </div>
            <div className="min-w-0">
              <p className="font-bold text-gray-900 truncate">{offer?.itemName || offer?.material || 'Your item'}</p>
              {offer?.weightKg && <p className="text-sm text-gray-500 mt-0.5">{offer.weightKg}kg weight</p>}
            </div>
          </div>
          {offer?.description && (
            <p className="text-sm text-gray-500 italic mt-3">&ldquo;{offer.description}&rdquo;</p>
          )}
          {offer?.weightKg && (
            <div className="mt-3">
              <EcoImpactBox weightKg={Number(offer.weightKg)} />
            </div>
          )}
          <div className="border-t border-gray-100 mt-3 pt-3 flex items-center justify-between">
            <span className="text-sm text-gray-400">Status</span>
            <StatusPill status="Pending" />
          </div>
        </div>
      }
      primaryAction={{
        label: 'View Offer History',
        onClick: () => navigate('/trades?tab=offers'),
      }}
      secondaryAction={{
        label: 'Back to Browse',
        onClick: () => navigate('/trades?tab=browse'),
      }}
    />
  );
}
</file>

<file path="frontend/src/features/trades/screens/RateTraderScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import StarRating from '@/shared/components/common/StarRating';
import { fetchTradeById, submitRating } from '@/shared/services/api';

export default function RateTraderScreen() {
  const navigate = useNavigate();
  const { tradeId } = useParams();
  const [trade, setTrade] = useState(null);
  const [rating, setRating] = useState(0);
  const [comment, setComment] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState('');

  useEffect(() => {
    fetchTradeById(tradeId).then(setTrade).catch(() => setTrade(null));
  }, [tradeId]);

  const handleSubmit = async () => {
    if (rating === 0 || submitting) return;
    setSubmitting(true);
    setError('');
    try {
      await submitRating(tradeId, rating, comment.trim() || undefined);
      navigate('/trades');
    } catch (err) {
      setError(err.message || 'Could not submit rating.');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Rate Trader" showBell={false} />

      <div className="px-5 pt-6 space-y-6 text-center">
        <h1 className="text-xl font-bold text-gray-900">
          Rate your trade with {trade?.posterName || 'the trader'}
        </h1>

        <div className="bg-white border border-gray-100 rounded-2xl p-4 flex items-center gap-3 text-left">
          <div className="w-14 h-14 rounded-lg bg-gray-100 shrink-0" />
          <div>
            <p className="font-semibold text-gray-900">{trade?.name || 'Trade item'}</p>
            <p className="text-xs text-gray-400">Completed trade</p>
          </div>
        </div>

        <StarRating value={rating} onChange={setRating} />

        <textarea
          value={comment}
          onChange={(e) => setComment(e.target.value)}
          placeholder="Leave a note about this trade (optional)"
          className="w-full bg-gray-100 rounded-xl px-4 py-3 text-sm outline-none min-h-[90px] resize-none text-left"
        />

        {error && <p className="text-sm text-red-500 text-left">{error}</p>}

        <div className="space-y-3 pt-2">
          <PrimaryButton disabled={rating === 0 || submitting} onClick={handleSubmit}>
            {submitting ? 'Submitting...' : 'Submit Rating'}
          </PrimaryButton>
          <button onClick={() => navigate('/trades')} className="text-sm text-gray-500 font-medium">
            Skip for now
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/screens/TradeCreatedScreen.jsx">
import { useLocation, useNavigate } from 'react-router-dom';
import ConfirmationScreen from '@/shared/components/common/ConfirmationScreen';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';

const TRADING_FOR_LABELS = {
  specific: (tf) => tf.value || 'a specific item',
  nothing: () => 'Nothing (free item)',
  negotiating: () => 'Open for negotiating',
};

export default function TradeCreatedScreen() {
  const navigate = useNavigate();
  const location = useLocation();
  const trade = location.state?.trade;

  return (
    <ConfirmationScreen
      heading="Trade Created!"
      subtext="Your item is now live. Interested traders can view your item picture, details, and send you an offer."
      summaryCard={
        trade && (
          <div>
            <p className="font-bold text-gray-900">{trade.name || trade.material}</p>
            <p className="text-sm text-gray-500 mt-1">{trade.weightKg}kg • {trade.category}</p>
            <p className="text-sm text-gray-500 mt-1">
              Trade for:{' '}
              {(TRADING_FOR_LABELS[trade.tradingFor?.type] || TRADING_FOR_LABELS.negotiating)(trade.tradingFor || {})}
            </p>
            {trade.description && (
              <p className="text-sm text-gray-500 italic mt-2">&ldquo;{trade.description}&rdquo;</p>
            )}
            <div className="mt-3">
              <EcoImpactBox weightKg={Number(trade.weightKg)} />
            </div>
          </div>
        )
      }
      primaryAction={{
        label: 'View in My Trades',
        onClick: () => navigate('/trades?tab=mine'),
      }}
      secondaryAction={{
        label: 'Back to Browse',
        onClick: () => navigate('/trades?tab=browse'),
      }}
    />
  );
}
</file>

<file path="frontend/src/features/trades/screens/TradeDetailScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams, useSearchParams } from 'react-router-dom';
import { MessageCircle, MoreVertical } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import MaterialTag from '@/shared/components/common/MaterialTag';
import StatusPill from '@/shared/components/common/StatusPill';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';
import { formatTradingFor, getTradeStatus } from '@/shared/utils/tradeFormat';
import { fetchTradeById, fetchOffersForTrade, acceptOffer, declineOffer } from '@/shared/services/api';

export default function TradeDetailScreen() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  // TradesScreen links to My Trades items with ?owner=1 — derive isOwner from
  // that instead of a prop, since no route ever actually passed one in.
  const isOwner = searchParams.get('owner') === '1';

  const [trade, setTrade] = useState(null);
  const [offers, setOffers] = useState([]);
  const [loading, setLoading] = useState(true);
  const [notFound, setNotFound] = useState(false);
  const [error, setError] = useState('');
  const [decidingOfferId, setDecidingOfferId] = useState(null);

  const loadTrade = () => {
    setLoading(true);
    setNotFound(false);
    setError('');
    const requests = [fetchTradeById(id)];
    if (isOwner) requests.push(fetchOffersForTrade(id));

    Promise.all(requests)
      .then(([tradeData, offersData]) => {
        setTrade(tradeData);
        if (isOwner) setOffers(offersData || []);
      })
      .catch((err) => {
        if (err.message?.toLowerCase().includes('not found')) setNotFound(true);
        else setError(err.message || 'Could not load this trade.');
      })
      .finally(() => setLoading(false));
  };

  useEffect(() => {
    loadTrade();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [id, isOwner]);

  const decide = async (offerId, action) => {
    if (decidingOfferId) return;
    setDecidingOfferId(offerId);
    setError('');
    try {
      if (action === 'accept') await acceptOffer(offerId);
      else await declineOffer(offerId);
      loadTrade();
    } catch (err) {
      setError(err.message || 'Could not update this offer.');
    } finally {
      setDecidingOfferId(null);
    }
  };

  if (loading) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Items" />
        <p className="text-center text-sm text-gray-400 py-16">Loading...</p>
      </div>
    );
  }

  if (notFound || !trade) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Items" />
        <p className="text-center text-sm text-gray-400 py-16">This trade could not be found.</p>
      </div>
    );
  }

  const itemInfo = (
    <>
      <div className="h-44 bg-gray-100 border-b border-gray-200">
        {trade.image && <img src={trade.image} alt={trade.name} className="w-full h-full object-cover" />}
        <div className="absolute top-3 right-3">
          <MaterialTag material={trade.material} />
        </div>
      </div>

      <div className="p-5 space-y-5">
        <div>
          <div className="flex items-start justify-between gap-2">
            <h1 className="text-2xl font-bold text-gray-900">{trade.name}</h1>
            {isOwner && <StatusPill status={getTradeStatus(trade)} />}
          </div>
          <p className="text-sm text-gray-500 mt-1">
            Posted by {trade.posterName}
            {trade.distanceKm != null && ` • ${trade.distanceKm}km`}
          </p>
          {trade.location && <p className="text-sm text-gray-500 mt-0.5">📍 {trade.location}</p>}
        </div>

        <div className="flex items-center justify-between text-sm">
          <div className="flex gap-6">
            <span><span className="font-semibold">{trade.weightKg}kg</span> weight</span>
            <span><span className="font-semibold">{trade.quantity}</span> quantity</span>
          </div>
          {isOwner && (
            <button
              onClick={() => navigate(`/trades/${trade.id}/edit`)}
              aria-label="Edit Listing"
              className="p-1 -mr-1 text-gray-400 active:scale-95 transition-transform"
            >
              <MoreVertical size={20} />
            </button>
          )}
        </div>

        {trade.description && (
          <p className="text-sm text-gray-600 italic">&ldquo;{trade.description}&rdquo;</p>
        )}

        <EcoImpactBox weightKg={trade.weightKg} />

        <div>
          <p className="text-sm text-gray-400 mb-1">Trade for:</p>
          <p className="font-semibold text-gray-900">{formatTradingFor(trade.tradingFor)}</p>
        </div>

        {/* Per docs: "user accepts offer > a chat is started" — for the owner,
            each accepted offer below already has its own "Open Chat" button,
            so this generic shortcut is just for the non-owner side. */}
        {trade.offerAccepted && !isOwner && (
          <button
            onClick={() => navigate(`/trades/${trade.id}/messages`)}
            className="flex items-center gap-2 text-brand-600 font-semibold text-sm"
          >
            <MessageCircle size={18} />
            Message
          </button>
        )}

        {!isOwner && (
          <div className="space-y-3 pt-2">
            {trade.tradingFor?.type === 'nothing' ? (
              // Free item — nothing to negotiate, so skip the offer flow entirely
              // and go straight to messaging the poster to arrange pickup.
              <PrimaryButton onClick={() => navigate(`/trades/${trade.id}/messages`)}>Message</PrimaryButton>
            ) : trade.status !== 'open' ? (
              <p className="text-center text-sm text-gray-400 py-2">
                This trade is no longer accepting offers.
              </p>
            ) : (
              <PrimaryButton
                onClick={() => navigate('/trades/scan', { state: { context: 'bidding', tradeId: trade.id } })}
              >
                Create Offer
              </PrimaryButton>
            )}
          </div>
        )}
      </div>
    </>
  );

  // Non-owner: no Received Offers section, so the page just scrolls normally.
  if (!isOwner) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Items" />
        <div className="relative">{itemInfo}</div>
      </div>
    );
  }

  // Owner: item info stays fixed in place; only Received Offers scrolls.
  return (
    <div className="h-screen flex flex-col overflow-hidden">
      <Header onBack={() => navigate(-1)} title="Items" />
      <div className="shrink-0 relative">{itemInfo}</div>

      <div className="flex-1 overflow-y-auto min-h-0 border-t border-gray-100 px-5 py-5">
        <h2 className="font-bold text-gray-900 mb-3">Received Offers</h2>
        {error && <p className="text-sm text-red-500 mb-3">{error}</p>}
        {offers.length === 0 ? (
          <p className="text-center text-sm text-gray-400 py-8">No offers received yet.</p>
        ) : (
          <div className="space-y-3">
            {offers.map((offer) => (
              <div key={offer.id} className="bg-white border border-gray-200 rounded-2xl p-4 space-y-3">
                <div className="flex items-start justify-between gap-2">
                  <div>
                    <p className="font-bold text-gray-900">{offer.offererName}</p>
                    <p className="text-xs text-gray-400">{new Date(offer.createdAt).toLocaleDateString()}</p>
                  </div>
                  <StatusPill status={offer.status} />
                </div>

                <div className="flex items-center gap-3 bg-gray-50 rounded-xl px-3 py-2">
                  <div className="w-12 h-12 rounded-lg bg-gray-100 border border-gray-200 shrink-0 overflow-hidden">
                    {offer.image && (
                      <img src={offer.image} alt={offer.itemName} className="w-full h-full object-cover" />
                    )}
                  </div>
                  <div className="flex-1 min-w-0">
                    <p className="text-sm font-semibold text-gray-900 truncate">{offer.itemName}</p>
                    <p className="text-xs text-gray-500">{offer.weightKg}kg</p>
                  </div>
                  <MaterialTag material={offer.material} />
                </div>

                {offer.status === 'pending' && (
                  <div className="space-y-2 pt-1">
                    <PrimaryButton
                      onClick={() => decide(offer.id, 'accept')}
                      disabled={decidingOfferId === offer.id}
                    >
                      {decidingOfferId === offer.id ? 'Accepting...' : 'Accept Offer'}
                    </PrimaryButton>
                    <button
                      onClick={() => decide(offer.id, 'decline')}
                      disabled={decidingOfferId === offer.id}
                      className="w-full text-center text-sm text-gray-500 font-medium disabled:opacity-50"
                    >
                      Decline
                    </button>
                  </div>
                )}

                {offer.status === 'accepted' && (
                  <PrimaryButton onClick={() => navigate(`/trades/${trade.id}/messages`)}>
                    <span className="flex items-center justify-center gap-2">
                      <MessageCircle size={18} />
                      Open Chat
                    </span>
                  </PrimaryButton>
                )}
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/screens/TradesScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { ChevronRight, MessageCircle } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import BottomNav from '@/shared/components/layout/BottomNav';
import SearchBar from '@/shared/components/common/SearchBar';
import FloatingActionButton from '@/shared/components/common/FloatingActionButton';
import FilterChipRow from '@/features/trades/components/trade/FilterChipRow';
import TradeCard from '@/features/trades/components/cards/TradeCard';
import StatusPill from '@/shared/components/common/StatusPill';
import { fetchBrowseTrades, fetchMyTrades, fetchMyOffers } from '@/shared/services/api';

const SUB_TABS = [
  { key: 'browse', label: 'Browse' },
  { key: 'mine', label: 'My Trades' },
  { key: 'offers', label: 'Offer History' },
];

export default function TradesScreen() {
  const navigate = useNavigate();
  // Tab state lives in the URL (not local state) so the browser Back button
  // returns to the tab you were actually on, instead of always resetting to
  // Browse — switching tabs uses replace so it doesn't spam browser history.
  const [searchParams, setSearchParams] = useSearchParams();
  const rawTab = searchParams.get('tab');
  const subTab = rawTab === 'mine' || rawTab === 'offers' ? rawTab : 'browse';
  const [search, setSearch] = useState('');
  const [filter, setFilter] = useState('All Items');

  const [browseTrades, setBrowseTrades] = useState([]);
  const [myTrades, setMyTrades] = useState([]);
  const [offerHistory, setOfferHistory] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setError('');
    Promise.all([fetchBrowseTrades(), fetchMyTrades(), fetchMyOffers()])
      .then(([browse, mine, offers]) => {
        if (cancelled) return;
        setBrowseTrades(browse);
        setMyTrades(mine);
        setOfferHistory(offers);
      })
      .catch((err) => {
        if (!cancelled) setError(err.message || 'Could not load trades.');
      })
      .finally(() => {
        if (!cancelled) setLoading(false);
      });
    return () => {
      cancelled = true;
    };
  }, []);

  const setSubTab = (tab) => {
    const next = new URLSearchParams(searchParams);
    next.set('tab', tab);
    setSearchParams(next, { replace: true });
  };

  const goToCreateTrade = () => navigate('/trades/scan', { state: { context: 'posting' } });

  const filteredBrowse = browseTrades.filter((t) => {
    const matchesFilter = filter === 'All Items' || t.material === filter;
    const matchesSearch = t.name.toLowerCase().includes(search.toLowerCase());
    return matchesFilter && matchesSearch;
  });

  return (
    <div className="pb-40">
      <Header />

      {/* Segmented tabs: Browse (all available trades), My Trades (own posts), Offer History (offers you sent) */}
      <div className="px-4 pt-4">
        <div className="flex bg-gray-100 rounded-xl p-1">
          {SUB_TABS.map((t) => (
            <button
              key={t.key}
              onClick={() => setSubTab(t.key)}
              className={`flex-1 py-2 rounded-lg text-xs font-semibold transition-colors ${
                subTab === t.key ? 'bg-white text-gray-900 shadow-sm' : 'text-gray-500'
              }`}
            >
              {t.label}
            </button>
          ))}
        </div>
      </div>

      {loading && <p className="text-center text-sm text-gray-400 py-10">Loading...</p>}
      {!loading && error && <p className="text-center text-sm text-red-500 py-10">{error}</p>}

      {!loading && !error && subTab === 'browse' && (
        <div className="px-4 pt-4 space-y-4">
          <SearchBar value={search} onChange={setSearch} />
          <FilterChipRow active={filter} onSelect={setFilter} />

          <div className="flex items-center justify-between text-sm">
            <span className="text-gray-500">{filteredBrowse.length} items nearby</span>
            <button className="text-gray-500 font-medium">Sort: Nearest ▾</button>
          </div>

          <div className="space-y-4">
            {filteredBrowse.map((trade) => (
              <TradeCard key={trade.id} trade={trade} onClick={() => navigate(`/trades/${trade.id}`)} />
            ))}
            {filteredBrowse.length === 0 && (
              <p className="text-center text-sm text-gray-400 py-10">No items match your search.</p>
            )}
          </div>
        </div>
      )}

      {!loading && !error && subTab === 'mine' && (
        <div className="px-4 pt-4 space-y-4">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-xl font-bold text-gray-900">My Trades</h1>
              <p className="text-sm text-gray-500 mt-1 max-w-[280px]">
                Manage your active contributions and review your ecological trade history.
              </p>
            </div>
            <span className="shrink-0 px-3 py-1 rounded-full bg-brand-100 text-brand-700 text-xs font-semibold">
              {myTrades.length} Active
            </span>
          </div>

          {myTrades.length > 0 ? (
            <div className="space-y-4">
              {myTrades.map((trade) => (
                <div key={trade.id} className="space-y-2">
                  <TradeCard trade={trade} variant="track" onClick={() => navigate(`/trades/${trade.id}?owner=1`)} />
                  <button
                    onClick={() => navigate(`/trades/${trade.id}?owner=1`)}
                    className="w-full py-3 rounded-xl bg-gray-100 text-gray-700 font-semibold text-sm"
                  >
                    View Details
                  </button>
                </div>
              ))}
            </div>
          ) : (
            <div className="text-center py-16">
              <p className="text-gray-400 text-sm mb-4">No active trades yet.</p>
              <button onClick={goToCreateTrade} className="text-brand-600 font-semibold text-sm">
                Post your first item →
              </button>
            </div>
          )}
        </div>
      )}

      {!loading && !error && subTab === 'offers' && (
        <div className="px-4 pt-4 space-y-4">
          <div>
            <h1 className="text-xl font-bold text-gray-900">Offer History</h1>
            <p className="text-sm text-gray-500 mt-1">
              Tap an offer to view what you sent.{' '}
              {offerHistory.some((o) => o.status === 'accepted') && 'Accepted offers open the chat.'}
            </p>
          </div>

          {offerHistory.length > 0 ? (
            <div className="space-y-2">
              {offerHistory.map((offer) => {
                const accepted = offer.status === 'accepted';
                return (
                  <button
                    key={offer.id}
                    onClick={() => navigate(`/trades/${offer.tradeId}/my-offer`)}
                    className="w-full flex items-center justify-between bg-white border border-gray-100 rounded-xl px-4 py-3 active:scale-[0.98] transition-transform"
                  >
                    <span className="text-sm font-medium text-gray-700">{offer.tradeName}</span>
                    <span className="flex items-center gap-2">
                      {accepted ? (
                        <>
                          <span className="text-xs font-semibold text-brand-600">Accepted</span>
                          <MessageCircle size={16} className="text-brand-600" />
                        </>
                      ) : (
                        <>
                          <StatusPill status={offer.status} />
                          <ChevronRight size={16} className="text-gray-300" />
                        </>
                      )}
                    </span>
                  </button>
                );
              })}
            </div>
          ) : (
            <p className="text-center text-sm text-gray-400 py-10">You haven&apos;t sent any offers yet.</p>
          )}
        </div>
      )}

      <FloatingActionButton label="Post a trade" onClick={goToCreateTrade} />
      <BottomNav />
    </div>
  );
}
</file>

<file path="frontend/src/features/trades/screens/ViewOfferScreen.jsx">
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { MessageCircle } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import MaterialTag from '@/shared/components/common/MaterialTag';
import StatusPill from '@/shared/components/common/StatusPill';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import EcoImpactBox from '@/features/trades/components/trade/EcoImpactBox';
import { fetchTradeById, fetchMyOffers } from '@/shared/services/api';

/**
 * Read-only record of an offer you already sent — reached from Offer History.
 * Not the create-offer flow: you already offered, this just shows what and
 * lets you jump to the chat once it's been accepted.
 */
export default function ViewOfferScreen() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [trade, setTrade] = useState(null);
  const [offer, setOffer] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    Promise.all([fetchTradeById(id).catch(() => null), fetchMyOffers()])
      .then(([tradeData, offers]) => {
        setTrade(tradeData);
        setOffer(offers.find((o) => o.tradeId === id) || null);
      })
      .finally(() => setLoading(false));
  }, [id]);

  if (loading) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Your Offer" />
        <p className="text-center text-sm text-gray-400 py-16">Loading...</p>
      </div>
    );
  }

  if (!offer) {
    return (
      <div className="pb-10">
        <Header onBack={() => navigate(-1)} title="Your Offer" />
        <p className="text-center text-sm text-gray-400 py-16">Offer not found.</p>
      </div>
    );
  }

  const accepted = offer.status === 'accepted';

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Your Offer" />

      <div className="p-5 space-y-5">
        <div>
          <p className="text-xs text-gray-400 font-semibold uppercase mb-1">Offer on</p>
          <p className="font-bold text-gray-900">{offer.tradeName}</p>
          {trade?.posterName && <p className="text-sm text-gray-500 mt-0.5">Posted by {trade.posterName}</p>}
        </div>

        <div className="border-t border-gray-100 pt-5">
          <p className="text-xs text-gray-400 font-semibold uppercase mb-3">You offered</p>
          <div className="flex items-center gap-3">
            <div className="w-16 h-16 rounded-xl bg-gray-100 border border-gray-200 shrink-0 overflow-hidden">
              {offer.image && (
                <img src={offer.image} alt={offer.itemName} className="w-full h-full object-cover" />
              )}
            </div>
            <div className="min-w-0 flex-1">
              <p className="font-bold text-gray-900 truncate">{offer.itemName}</p>
              <div className="flex items-center gap-2 mt-1">
                {offer.weightKg && <span className="text-sm text-gray-500">{offer.weightKg}kg</span>}
                {offer.material && <MaterialTag material={offer.material} />}
              </div>
            </div>
          </div>
          {offer.description && (
            <p className="text-sm text-gray-500 italic mt-3">&ldquo;{offer.description}&rdquo;</p>
          )}
          {offer.weightKg && (
            <div className="mt-3">
              <EcoImpactBox weightKg={Number(offer.weightKg)} />
            </div>
          )}
        </div>

        <div className="border-t border-gray-100 pt-5 flex items-center justify-between">
          <span className="text-sm text-gray-400">Status</span>
          <StatusPill status={offer.status} />
        </div>

        {accepted ? (
          <PrimaryButton onClick={() => navigate(`/trades/${offer.tradeId}/messages`)}>
            <span className="flex items-center justify-center gap-2">
              <MessageCircle size={18} />
              Open Chat
            </span>
          </PrimaryButton>
        ) : (
          <p className="text-center text-sm text-gray-400">
            Waiting for {trade?.posterName || 'the poster'} to respond.
          </p>
        )}
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/shared/components/common/ConfirmationScreen.jsx">
import { Check } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';

/**
 * Shared layout used by: Trade Created, Offer Sent (and could be reused
 * for other "success" moments later, e.g. a future report-submitted screen).
 *
 * summaryCard: optional ReactNode rendered inside a bordered card
 * primaryAction: { label, onClick }
 * secondaryAction: { label, onClick }
 */
export default function ConfirmationScreen({
  heading,
  subtext,
  summaryCard,
  primaryAction,
  secondaryAction,
}) {
  return (
    <div className="pb-28">
      <Header showBell={false} />
      <div className="px-5 pt-10 flex flex-col items-center text-center">
        <div className="w-20 h-20 rounded-full bg-brand-100 flex items-center justify-center mb-5">
          <div className="w-14 h-14 rounded-full bg-brand-600 flex items-center justify-center">
            <Check size={28} className="text-white" strokeWidth={3} />
          </div>
        </div>
        <h1 className="text-2xl font-bold text-gray-900 mb-2">{heading}</h1>
        {subtext && <p className="text-sm text-gray-500 max-w-xs">{subtext}</p>}

        {summaryCard && (
          <div className="w-full mt-8 border border-gray-200 rounded-2xl p-4 text-left">
            {summaryCard}
          </div>
        )}

        <div className="w-full mt-8 space-y-3">
          {primaryAction && (
            <PrimaryButton onClick={primaryAction.onClick}>{primaryAction.label}</PrimaryButton>
          )}
          {secondaryAction && (
            <button onClick={secondaryAction.onClick} className="text-sm text-gray-500 font-medium">
              {secondaryAction.label}
            </button>
          )}
        </div>
      </div>
    </div>
  );
}
</file>

<file path="frontend/src/shared/components/common/FloatingActionButton.jsx">
import { Plus } from 'lucide-react';

export default function FloatingActionButton({ onClick, label = 'Add' }) {
  return (
    <button
      onClick={onClick}
      aria-label={label}
      className="fixed z-30 bottom-24 w-14 h-14 rounded-full bg-brand-600 text-white shadow-lg flex items-center justify-center active:scale-95 transition-transform hover:bg-brand-700"
      style={{ right: 'max(1.25rem, calc(50% - 240px + 1.25rem))' }}
    >
      <Plus size={26} />
    </button>
  );
}
</file>

<file path="frontend/src/shared/components/common/Logo.jsx">
import logoIcon from '@/assets/logo.svg';

/**
 * Brand lockup. `iconOnly` shows just the mark (used on auth screens);
 * default shows "RosaCycle" text + mark together (used in the app header).
 */
const SIZES = {
  sm: { icon: 26, text: 'text-lg' },
  md: { icon: 34, text: 'text-2xl' },
  lg: { icon: 64, text: 'text-4xl' },
};

export default function Logo({ size = 'md', iconOnly = false, className = '' }) {
  const { icon, text } = SIZES[size] || SIZES.md;

  if (iconOnly) {
    return <img src={logoIcon} alt="RosaCycle" width={icon} height={icon} className={className} />;
  }

  return (
    <div className={`flex items-center gap-1 ${className}`}>
      <span className={`font-extrabold tracking-tight ${text}`} style={{ color: '#245f28' }}>
        RosaCycle
      </span>
      <img src={logoIcon} alt="" width={icon} height={icon} className="shrink-0" />
    </div>
  );
}
</file>

<file path="frontend/src/shared/components/common/MaterialTag.jsx">
const COLORS = {
  wood: 'bg-amber-100 text-amber-800',
  metal: 'bg-slate-200 text-slate-700',
  plastic: 'bg-blue-100 text-blue-700',
  fabric: 'bg-pink-100 text-pink-700',
  paper: 'bg-yellow-100 text-yellow-800',
  'e-waste': 'bg-indigo-100 text-indigo-700',
  mixed: 'bg-brand-100 text-brand-700',
  organic: 'bg-brand-100 text-brand-700',
};

export default function MaterialTag({ material, className = '' }) {
  const key = (material || '').toLowerCase();
  const style = COLORS[key] || 'bg-gray-100 text-gray-700';
  return (
    <span className={`inline-block px-3 py-1 rounded-full text-xs font-medium capitalize ${style} ${className}`}>
      {material}
    </span>
  );
}
</file>

<file path="frontend/src/shared/components/common/PasswordInput.jsx">
import { useState } from 'react';
import { Eye, EyeOff } from 'lucide-react';

/**
 * Password field with a show/hide toggle. Hidden by default (closed eye).
 */
export default function PasswordInput({ value, onChange, placeholder, className = '' }) {
  const [visible, setVisible] = useState(false);

  return (
    <div className="relative">
      <input
        type={visible ? 'text' : 'password'}
        value={value}
        onChange={onChange}
        placeholder={placeholder}
        className={`w-full pr-11 px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none ${className}`}
      />
      <button
        type="button"
        onClick={() => setVisible((v) => !v)}
        aria-label={visible ? 'Hide password' : 'Show password'}
        className="absolute right-3.5 top-1/2 -translate-y-1/2 text-gray-400 active:scale-90 transition-transform"
      >
        {visible ? <Eye size={19} /> : <EyeOff size={19} />}
      </button>
    </div>
  );
}
</file>

<file path="frontend/src/shared/components/common/PrimaryButton.jsx">
export default function PrimaryButton({ children, onClick, disabled = false, type = 'button', className = '' }) {
  return (
    <button
      type={type}
      onClick={onClick}
      disabled={disabled}
      className={`w-full py-3.5 rounded-xl font-semibold text-white transition-colors active:scale-[0.98] ${
        disabled ? 'bg-gray-300 cursor-not-allowed' : 'bg-brand-600 hover:bg-brand-700'
      } ${className}`}
    >
      {children}
    </button>
  );
}
</file>

<file path="frontend/src/shared/components/common/SearchBar.jsx">
import { Search } from 'lucide-react';

export default function SearchBar({ value, onChange, placeholder = 'Search materials' }) {
  return (
    <div className="flex items-center gap-2 bg-gray-200/70 rounded-xl px-4 py-3">
      <Search size={18} className="text-gray-500 shrink-0" />
      <input
        type="text"
        value={value}
        onChange={(e) => onChange?.(e.target.value)}
        placeholder={placeholder}
        className="bg-transparent outline-none text-sm w-full placeholder:text-gray-500"
      />
    </div>
  );
}
</file>

<file path="frontend/src/shared/components/common/SecondaryButton.jsx">
export default function SecondaryButton({ children, onClick, className = '' }) {
  return (
    <button
      type="button"
      onClick={onClick}
      className={`w-full py-3.5 rounded-xl font-semibold text-gray-700 bg-gray-100 hover:bg-gray-200 transition-colors active:scale-[0.98] ${className}`}
    >
      {children}
    </button>
  );
}
</file>

<file path="frontend/src/shared/components/common/StarRating.jsx">
import { Star } from 'lucide-react';

export default function StarRating({ value = 0, onChange }) {
  return (
    <div className="flex items-center justify-center gap-2">
      {[1, 2, 3, 4, 5].map((n) => (
        <button
          key={n}
          type="button"
          onClick={() => onChange?.(n)}
          aria-label={`${n} star`}
          className="active:scale-90 transition-transform"
        >
          <Star
            size={36}
            className={n <= value ? 'fill-yellow-400 text-yellow-400' : 'text-gray-300'}
          />
        </button>
      ))}
    </div>
  );
}
</file>

<file path="frontend/src/shared/components/common/StatusPill.jsx">
const STYLES = {
  pending: 'bg-yellow-100 text-yellow-800',
  accepted: 'bg-brand-100 text-brand-700',
  declined: 'bg-red-100 text-red-700',
};

export default function StatusPill({ status }) {
  const key = (status || '').toLowerCase();
  return (
    <span className={`px-3 py-1 rounded-full text-xs font-semibold capitalize ${STYLES[key] || 'bg-gray-100 text-gray-700'}`}>
      {status}
    </span>
  );
}
</file>

<file path="frontend/src/shared/components/layout/BottomNav.jsx">
import { Recycle, Camera, MapPin } from 'lucide-react';
import { useNavigate, useLocation } from 'react-router-dom';

/**
 * FINAL structure per team decision (confirmed by Luke/Franz/Albert):
 * 3 tabs — Trades (Browse + My Trades as internal sub-tabs), Camera, Map.
 * Dashboard was dropped. "Rate Trader" remains a one-off screen reached only
 * after a completed trade — intentionally NOT in this bottom nav.
 */
const TABS = [
  { key: 'trades', label: 'Trades', icon: Recycle, path: '/trades' },
  { key: 'camera', label: 'Camera', icon: Camera, path: '/camera' },
  { key: 'map', label: 'Map', icon: MapPin, path: '/map' },
];

export default function BottomNav() {
  const navigate = useNavigate();
  const location = useLocation();

  return (
    <nav className="fixed bottom-0 left-1/2 -translate-x-1/2 w-full max-w-[480px] bg-white border-t border-gray-100 flex items-stretch z-30">
      {TABS.map(({ key, label, icon: Icon, path }) => {
        const isActive = location.pathname.startsWith(path);
        return (
          <button
            key={key}
            onClick={() => navigate(path)}
            className="flex-1 flex flex-col items-center justify-center gap-1 py-2.5"
          >
            <span
              className={`flex items-center justify-center rounded-full px-3 py-1 transition-colors ${
                isActive ? 'bg-brand-600 text-white' : 'text-gray-500'
              }`}
            >
              <Icon size={20} />
            </span>
            <span className={`text-[11px] ${isActive ? 'text-brand-600 font-semibold' : 'text-gray-500'}`}>
              {label}
            </span>
          </button>
        );
      })}
    </nav>
  );
}
</file>

<file path="frontend/src/shared/components/layout/Header.jsx">
import { ArrowLeft, Bell } from 'lucide-react';
import Logo from '@/shared/components/common/Logo';
import UserMenu from '@/shared/components/layout/UserMenu';

/**
 * Shared header used across all screens.
 * - Default: shows "RosaCycle" logo + notification bell + user avatar (rightmost)
 * - With `onBack`: shows a back arrow + optional title instead of the logo
 */
export default function Header({ onBack, title, showBell = true }) {
  return (
    <header className="flex items-center justify-between px-4 py-3 bg-white border-b border-gray-100 sticky top-0 z-20">
      <div className="flex items-center gap-2">
        {onBack ? (
          <button
            onClick={onBack}
            aria-label="Go back"
            className="p-1 -ml-1 text-gray-700 active:scale-95 transition-transform"
          >
            <ArrowLeft size={22} />
          </button>
        ) : null}
        {title ? <span className="font-semibold text-gray-900">{title}</span> : <Logo size="sm" />}
      </div>

      <div className="flex items-center gap-3">
        {showBell && (
          <button aria-label="Notifications" className="p-1 text-gray-700 active:scale-95 transition-transform">
            <Bell size={22} />
          </button>
        )}
        <UserMenu />
      </div>
    </header>
  );
}
</file>

<file path="frontend/src/shared/lib/pendingCapture.js">
// Plain in-memory singleton for passing a captured File between screens.
// Not router state: a File riding through navigate(path, { state }) is
// unusual, under-documented browser behavior — this sidesteps it entirely.
// Only one capture flow is ever active at a time, so a module-level
// singleton is enough.
let pendingFile = null;

export function setPendingCapture(file) {
  pendingFile = file;
}

export function getPendingCapture() {
  return pendingFile;
}

export function clearPendingCapture() {
  pendingFile = null;
}
</file>

<file path="frontend/src/shared/lib/storageKeys.js">
// Shared localStorage keys — used by both AuthContext (React state sync) and
// api.js (plain functions, can't reach React context) so they never drift.
export const USER_STORAGE_KEY = 'rosacycle_user';
export const TOKEN_STORAGE_KEY = 'rosacycle_token';
</file>

<file path="frontend/src/shared/services/api.js">
/**
 * API layer — talks to the real RosaCycle backend (see backend/docs/API.md
 * for the full reference). Screens should only import from here, never call
 * fetch() directly and never import from data/mock*.js.
 */
import { TOKEN_STORAGE_KEY } from '@/shared/lib/storageKeys';

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000';

function getToken() {
  return localStorage.getItem(TOKEN_STORAGE_KEY);
}

function buildFormData(fields = {}, imageFile) {
  const form = new FormData();
  Object.entries(fields).forEach(([key, value]) => {
    if (value === undefined || value === null || value === '') return;
    form.append(key, value);
  });
  if (imageFile) form.append('image', imageFile);
  return form;
}

async function apiFetch(path, { method = 'GET', json, form, query } = {}) {
  let url = `${API_BASE_URL}${path}`;
  if (query) {
    const qs = new URLSearchParams(
      Object.entries(query).filter(([, v]) => v !== undefined && v !== null && v !== '')
    ).toString();
    if (qs) url += `?${qs}`;
  }

  const headers = {};
  const token = getToken();
  if (token) headers['Authorization'] = `Bearer ${token}`;

  const init = { method, headers };
  if (form) {
    init.body = form; // browser sets multipart Content-Type + boundary itself
  } else if (json !== undefined) {
    headers['Content-Type'] = 'application/json';
    init.body = JSON.stringify(json);
  }

  const res = await fetch(url, init);

  if (res.status === 401) {
    window.dispatchEvent(new CustomEvent('rosacycle:unauthorized'));
  }

  if (!res.ok) {
    let message = `Request failed (${res.status}).`;
    try {
      const body = await res.json();
      if (body?.error) message = body.error;
    } catch {
      // non-JSON error body — fall back to the generic message
    }
    throw new Error(message);
  }

  if (res.status === 204) return null;
  return res.json();
}

// --- Auth ---------------------------------------------------------------

export async function registerUser({ firstName, lastName, username, email, password }) {
  return apiFetch('/auth/register', {
    method: 'POST',
    json: { firstName, lastName, username, email, password },
  });
}

export async function loginUser({ email, password }) {
  return apiFetch('/auth/login', { method: 'POST', json: { email, password } });
}
export async function loginWithGoogle(googleToken) {
  return apiFetch('/auth/google', { 
    method: 'POST', 
    json: { token: googleToken } 
  });
}

export async function logoutUser() {
  try {
    await apiFetch('/auth/logout', { method: 'POST' });
  } catch {
    // logout is a stateless no-op server-side — never block local sign-out on it
  }
  return true;
}

export async function fetchCurrentUser() {
  return apiFetch('/users/me');
}

export async function updateUserProfile(updates, currentPassword) {
  const form = buildFormData({
    firstName: updates.firstName,
    lastName: updates.lastName,
    username: updates.username,
    email: updates.email,
    password: updates.password,
    currentPassword,
  });
  if (updates.imageFile) form.set('image', updates.imageFile);
  return apiFetch('/users/me', { method: 'PATCH', form });
}

// --- Trades ---------------------------------------------------------------

export async function fetchBrowseTrades({ category, location } = {}) {
  return apiFetch('/trades', { query: { category, location } });
}

export async function fetchMyTrades() {
  return apiFetch('/trades/mine');
}

export async function fetchTradeById(id) {
  return apiFetch(`/trades/${id}`);
}

export async function createTrade(fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch('/trades', { method: 'POST', form });
}

export async function updateTrade(id, fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch(`/trades/${id}`, { method: 'PATCH', form });
}

// --- Offers ---------------------------------------------------------------

export async function sendOffer(tradeId, fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch(`/trades/${tradeId}/offers`, { method: 'POST', form });
}

export async function fetchOffersForTrade(tradeId) {
  return apiFetch(`/trades/${tradeId}/offers`);
}

export async function acceptOffer(offerId) {
  return apiFetch(`/offers/${offerId}/accept`, { method: 'POST' });
}

export async function declineOffer(offerId) {
  return apiFetch(`/offers/${offerId}/decline`, { method: 'POST' });
}

export async function fetchMyOffers() {
  return apiFetch('/offers/mine');
}

export async function fetchReceivedOffers() {
  return apiFetch('/offers/received');
}

// --- Resource Spots ---------------------------------------------------------------

export async function fetchResourceSpots() {
  return apiFetch('/resource-spots');
}

export async function fetchResourceSpotById(id) {
  return apiFetch(`/resource-spots/${id}`);
}

export async function reportResourceSpot(fields, imageFile) {
  const form = buildFormData(fields, imageFile);
  return apiFetch('/resource-spots', { method: 'POST', form });
}

export async function addResourceSpotPhoto(id, imageFile) {
  const form = buildFormData({}, imageFile);
  return apiFetch(`/resource-spots/${id}/photos`, { method: 'POST', form });
}

export async function markResourceSpotCollected(id) {
  return apiFetch(`/resource-spots/${id}/collected`, { method: 'POST' });
}

// --- Notifications ---------------------------------------------------------------

export async function fetchNotifications() {
  return apiFetch('/notifications');
}

export async function fetchUnreadNotificationCount() {
  return apiFetch('/notifications/unread-count');
}

export async function markNotificationRead(id) {
  return apiFetch(`/notifications/${id}/read`, { method: 'PATCH' });
}

// --- Ratings ---------------------------------------------------------------

export async function submitRating(tradeId, score, comment) {
  return apiFetch(`/trades/${tradeId}/ratings`, { method: 'POST', json: { score, comment } });
}

export async function fetchUserRatings(userId) {
  return apiFetch(`/users/${userId}/ratings`);
}
</file>

<file path="frontend/src/shared/utils/constants.js">
export const SPOT_MATERIAL_OPTIONS = [
  'Wood',
  'Metal',
  'Plastic',
  'Fabric',
  'Paper',
  'E-waste',
  'Mixed',
  'Organic',
];
</file>

<file path="frontend/src/shared/utils/tradeFormat.js">
// Human-readable label for the tradingFor field per doc spec (x / nothing / open for negotiating)
export function formatTradingFor(tradingFor) {
  if (!tradingFor) return 'Open to offers';
  switch (tradingFor.type) {
    case 'specific':
      return tradingFor.value;
    case 'nothing':
      return 'Nothing (free item)';
    case 'negotiating':
      return 'Open for negotiating';
    default:
      return 'Open to offers';
  }
}

// Derives a trade's lifecycle status: Pending (open, whether or not it has an
// offer yet awaiting your decision) -> Accepted (offer accepted, chat unlocked).
export function getTradeStatus(trade) {
  return trade.offerAccepted ? 'Accepted' : 'Pending';
}
</file>

<file path="frontend/src/main.jsx">
import React from 'react';
import ReactDOM from 'react-dom/client';
import { GoogleOAuthProvider } from '@react-oauth/google';
import App from './App';
import './index.css';

const GOOGLE_CLIENT_ID = import.meta.env.VITE_GOOGLE_CLIENT_ID;

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GoogleOAuthProvider clientId={GOOGLE_CLIENT_ID}>
      <App />
    </GoogleOAuthProvider>
  </React.StrictMode>
);
</file>

<file path="frontend/.env.example">
VITE_API_BASE_URL=http://localhost:5000
</file>

<file path="frontend/fix-imports.cjs">
const fs = require('fs');
const path = require('path');

const SRC = path.join(__dirname, 'src');

const MAP = {
  AuthContext: 'features/auth/AuthContext',
  SignInScreen: 'features/auth/screens/SignInScreen',
  SignUpScreen: 'features/auth/screens/SignUpScreen',
  EditProfileScreen: 'features/profile/screens/EditProfileScreen',
  TradesScreen: 'features/trades/screens/TradesScreen',
  TradeDetailScreen: 'features/trades/screens/TradeDetailScreen',
  EditListingScreen: 'features/trades/screens/EditListingScreen',
  ViewOfferScreen: 'features/trades/screens/ViewOfferScreen',
  TradeCreatedScreen: 'features/trades/screens/TradeCreatedScreen',
  OfferSentScreen: 'features/trades/screens/OfferSentScreen',
  MessageThreadScreen: 'features/trades/screens/MessageThreadScreen',
  RateTraderScreen: 'features/trades/screens/RateTraderScreen',
  TradeCard: 'features/trades/components/cards/TradeCard',
  EcoImpactBox: 'features/trades/components/trade/EcoImpactBox',
  FilterChipRow: 'features/trades/components/trade/FilterChipRow',
  MessageThread: 'features/trades/components/trade/MessageThread',
  ScanScreen: 'features/scan/screens/ScanScreen',
  ScanConfirmScreen: 'features/scan/screens/ScanConfirmScreen',
  AIDetectedForm: 'features/scan/components/AIDetectedForm',
  CameraViewfinder: 'features/scan/components/CameraViewfinder',
  MapScreen: 'features/map/screens/MapScreen',
  ResourceSpotScreen: 'features/map/screens/ResourceSpotScreen',
  ResourceSpotConfirmScreen: 'features/map/screens/ResourceSpotConfirmScreen',
  SpotReportedScreen: 'features/map/screens/SpotReportedScreen',
  geo: 'features/map/geo',
  Header: 'shared/components/layout/Header',
  BottomNav: 'shared/components/layout/BottomNav',
  UserMenu: 'shared/components/layout/UserMenu',
  ConfirmationScreen: 'shared/components/common/ConfirmationScreen',
  FloatingActionButton: 'shared/components/common/FloatingActionButton',
  Logo: 'shared/components/common/Logo',
  MaterialTag: 'shared/components/common/MaterialTag',
  PasswordInput: 'shared/components/common/PasswordInput',
  PrimaryButton: 'shared/components/common/PrimaryButton',
  SearchBar: 'shared/components/common/SearchBar',
  SecondaryButton: 'shared/components/common/SecondaryButton',
  StarRating: 'shared/components/common/StarRating',
  StatusPill: 'shared/components/common/StatusPill',
  api: 'shared/services/api',
  storageKeys: 'shared/lib/storageKeys',
  pendingCapture: 'shared/lib/pendingCapture',
  constants: 'shared/utils/constants',
  tradeFormat: 'shared/utils/tradeFormat',
  'logo.svg': 'assets/logo.svg',
};

function walk(dir, out = []) {
  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
    const full = path.join(dir, entry.name);
    if (entry.isDirectory()) walk(full, out);
    else if (/\.(jsx?|tsx?)$/.test(entry.name)) out.push(full);
  }
  return out;
}

const files = walk(SRC);
let totalChanges = 0;

for (const file of files) {
  let content = fs.readFileSync(file, 'utf8');
  let changed = false;

  content = content.replace(
    /from\s+['"](\.\.?\/[^'"]+)['"]/g,
    (match, importPath) => {
      const base = path.basename(importPath);
      const key = MAP[base] !== undefined ? base : base.replace(/\.(jsx?|tsx?)$/, '');
      if (MAP[key] !== undefined) {
        changed = true;
        totalChanges++;
        return `from '@/${MAP[key]}'`;
      }
      return match;
    }
  );

  if (changed) {
    fs.writeFileSync(file, content, 'utf8');
    console.log('Updated:', path.relative(SRC, file));
  }
}

console.log(`\nDone. ${totalChanges} import(s) rewritten across ${files.length} files.`);
</file>

<file path="frontend/package.json">
{
  "name": "rosacycle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.4",
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.23",
    "tailwindcss": "^3.4.19",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@react-oauth/google": "^0.13.5",
    "lucide-react": "^1.27.0",
    "react-router-dom": "^7.18.1"
  }
}
</file>

<file path="backend/app/auth/controller.py">
from flask import request
from flask_jwt_extended import create_access_token

from app.auth.schema import serialize_user
from app.auth.service import authenticate_user, register_user
from app.services.auth_service import google_login_or_register
from app.shared.utils.errors import ValidationError


def register():
    
    body = request.get_json(silent=True) or {}
    required = ("username", "firstName", "lastName", "email", "password")
    missing = [field for field in required if not body.get(field)]
    if missing:
        raise ValidationError(f"Missing required field(s): {', '.join(missing)}.")

    user = register_user(
        username=body["username"],
        first_name=body["firstName"],
        last_name=body["lastName"],
        email=body["email"],
        password=body["password"],
    )
    token = create_access_token(identity=user.id)
    return {"token": token, "user": serialize_user(user)}, 201


def login():
    body = request.get_json(silent=True) or {}
    if not body.get("email") or not body.get("password"):
        raise ValidationError("Email and password are required.")

    user = authenticate_user(email=body["email"], password=body["password"])
    token = create_access_token(identity=user.id)
    return {"token": token, "user": serialize_user(user)}, 200


def logout():
    return {"success": True}, 200

def google_login():
    body = request.get_json(silent=True) or {}
    token = body.get("token")  # Frontend sends Google ID Token here
    if not token:
        raise ValidationError("Google token is required.")

    user = google_login_or_register(token)
    access_token = create_access_token(identity=str(user.id))
    
    return {"token": access_token, "user": serialize_user(user)}, 200
</file>

<file path="backend/requirements.txt">
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Cors==5.0.0
Flask-Limiter==3.9.2
psycopg2-binary==2.9.10
python-dotenv==1.0.1
Pillow==11.0.0
google-generativeai==0.8.3
google-auth==2.38.0
gunicorn==23.0.0
</file>

<file path="backend/run.py">
from dotenv import load_dotenv

load_dotenv()

from app.app import create_app  # noqa: E402

app = create_app()

if __name__ == "__main__":
    app.run(debug=True)
</file>

<file path="frontend/src/shared/components/layout/UserMenu.jsx">
import { useState, useRef, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { LogOut } from 'lucide-react';
import { useAuth } from '@/features/auth/AuthContext';

export default function UserMenu() {
  const { user, logout } = useAuth();
  const navigate = useNavigate();
  const [open, setOpen] = useState(false);
  const menuRef = useRef(null);

  useEffect(() => {
    if (!open) return;
    const handleClickOutside = (e) => {
      if (menuRef.current && !menuRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener('mousedown', handleClickOutside);
    return () => document.removeEventListener('mousedown', handleClickOutside);
  }, [open]);

  const handleLogout = async () => {
    setOpen(false);
    await logout();
    navigate('/signin', { replace: true });
  };

  const handleEditProfile = () => {
    setOpen(false);
    navigate('/profile/edit');
  };

  const displayName = [user?.firstName, user?.lastName].filter(Boolean).join(' ') || user?.username;
  const initial = user?.firstName?.[0]?.toUpperCase() || user?.username?.[0]?.toUpperCase() || '?';

  return (
    <div className="relative" ref={menuRef}>
      <button
        onClick={() => setOpen((v) => !v)}
        aria-label="User profile"
        aria-expanded={open}
        className="w-8 h-8 rounded-full bg-brand-600 text-white text-sm font-semibold flex items-center justify-center active:scale-95 transition-transform"
      >
        {initial}
      </button>

      {open && (
        <div className="absolute right-0 top-10 w-48 bg-white rounded-xl shadow-lg border border-gray-100 py-2 z-30">
          <button
            onClick={handleEditProfile}
            className="w-full text-left px-4 py-2 border-b border-gray-100 hover:bg-gray-50"
          >
            <p className="text-sm font-semibold text-gray-900 truncate">{displayName || 'RosaCycle User'}</p>
            <p className="text-xs text-gray-500 truncate">{user?.email}</p>
          </button>
          <button
            onClick={handleLogout}
            className="w-full flex items-center gap-2 px-4 py-2.5 text-sm text-red-500 font-medium hover:bg-gray-50"
          >
            <LogOut size={16} />
            Log Out
          </button>
        </div>
      )}
    </div>
  );
}
</file>

<file path="frontend/src/App.jsx">
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { AuthProvider, useAuth } from '@/features/auth/AuthContext';

import SignInScreen from '@/features/auth/screens/SignInScreen';
import SignUpScreen from '@/features/auth/screens/SignUpScreen';
import EditProfileScreen from '@/features/profile/screens/EditProfileScreen';
import TradesScreen from '@/features/trades/screens/TradesScreen';
import TradeDetailScreen from '@/features/trades/screens/TradeDetailScreen';
import EditListingScreen from '@/features/trades/screens/EditListingScreen';
import ViewOfferScreen from '@/features/trades/screens/ViewOfferScreen';
import ScanScreen from '@/features/scan/screens/ScanScreen';
import ScanConfirmScreen from '@/features/scan/screens/ScanConfirmScreen';
import TradeCreatedScreen from '@/features/trades/screens/TradeCreatedScreen';
import OfferSentScreen from '@/features/trades/screens/OfferSentScreen';
import MessageThreadScreen from '@/features/trades/screens/MessageThreadScreen';
import ResourceSpotScreen from '@/features/map/screens/ResourceSpotScreen';
import ResourceSpotConfirmScreen from '@/features/map/screens/ResourceSpotConfirmScreen';
import SpotReportedScreen from '@/features/map/screens/SpotReportedScreen';
import MapScreen from '@/features/map/screens/MapScreen';
import RateTraderScreen from '@/features/trades/screens/RateTraderScreen';

// Gate for screens that require a signed-in user; bounces to /signin otherwise.
function RequireAuth({ children }) {
  const { isAuthenticated } = useAuth();
  return isAuthenticated ? children : <Navigate to="/signin" replace />;
}

/**
 * FINAL nav structure (confirmed by team): 3 bottom tabs.
 * - /trades  -> Trades (Browse + My Trades internal sub-tabs)
 * - /camera  -> Camera / Resource Spot report (pins the Map, NOT a Trade)
 * - /map     -> Map (view-only pinned resource spot reports)
 *
 * Item scanning for a Trade (post or offer) is a *separate* flow at
 * /trades/scan — only reached from within Trades, never from the bottom nav.
 * Rate Trader is intentionally NOT a tab — reached only after a completed trade.
 */
export default function App() {
  return (
    <AuthProvider>
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Navigate to="/trades" replace />} />

          {/* Auth (email + password) */}
          <Route path="/signin" element={<SignInScreen />} />
          <Route path="/signup" element={<SignUpScreen />} />

          <Route path="/profile/edit" element={<RequireAuth><EditProfileScreen /></RequireAuth>} />

          {/* Trades (Browse + My Trades) */}
          <Route path="/trades" element={<RequireAuth><TradesScreen /></RequireAuth>} />
          {/* AI item scan, for posting a new trade or sending an offer on an existing one */}
          <Route path="/trades/scan" element={<RequireAuth><ScanScreen /></RequireAuth>} />
          <Route path="/trades/scan/confirm" element={<RequireAuth><ScanConfirmScreen /></RequireAuth>} />
          <Route path="/trades/scan/created" element={<RequireAuth><TradeCreatedScreen /></RequireAuth>} />
          <Route path="/trades/:id" element={<RequireAuth><TradeDetailScreen /></RequireAuth>} />
          <Route path="/trades/:id/edit" element={<RequireAuth><EditListingScreen /></RequireAuth>} />
          <Route path="/trades/:id/my-offer" element={<RequireAuth><ViewOfferScreen /></RequireAuth>} />
          <Route path="/trades/:id/offer-sent" element={<RequireAuth><OfferSentScreen /></RequireAuth>} />
          <Route path="/trades/:id/messages" element={<RequireAuth><MessageThreadScreen /></RequireAuth>} />

          {/* Camera tab: report a Resource Spot, pins the Map — not a Trade */}
          <Route path="/camera" element={<RequireAuth><ResourceSpotScreen /></RequireAuth>} />
          <Route path="/camera/confirm" element={<RequireAuth><ResourceSpotConfirmScreen /></RequireAuth>} />
          <Route path="/camera/reported" element={<RequireAuth><SpotReportedScreen /></RequireAuth>} />

          {/* Map (view-only pinned resource spot reports) */}
          <Route path="/map" element={<RequireAuth><MapScreen /></RequireAuth>} />

          {/* Rate Trader (one-off, reached after a completed trade) */}
          <Route path="/rate/:tradeId" element={<RequireAuth><RateTraderScreen /></RequireAuth>} />

          <Route path="*" element={<Navigate to="/trades" replace />} />
        </Routes>
      </BrowserRouter>
    </AuthProvider>
  );
}
</file>

<file path="frontend/vite.config.js">
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
});
</file>

<file path="backend/migrations/versions/41b0a18a356f_initial_schema.py">
"""Initial schema

Revision ID: 41b0a18a356f
Revises: 
Create Date: 2026-07-28 18:14:10.120158

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = '41b0a18a356f'
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table('users',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('username', sa.String(length=50), nullable=False),
    sa.Column('first_name', sa.String(length=50), nullable=False),
    sa.Column('last_name', sa.String(length=50), nullable=False),
    sa.Column('email', sa.String(length=255), nullable=False),
    sa.Column('password_hash', sa.String(length=255), nullable=False),
    sa.Column('profile_image_path', sa.String(length=255), nullable=True),
    sa.Column('role', sa.Enum('USER', 'ADMIN', name='userrole'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('profile_updated_at', sa.DateTime(timezone=True), nullable=True),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('users', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_users_email'), ['email'], unique=True)
        batch_op.create_index(batch_op.f('ix_users_username'), ['username'], unique=True)

    op.create_table('resource_spots',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('reporter_id', sa.String(length=36), nullable=False),
    sa.Column('name', sa.String(length=120), nullable=False),
    sa.Column('material', sa.String(length=120), nullable=False),
    sa.Column('weight_kg', sa.Numeric(precision=8, scale=2), nullable=True),
    sa.Column('quantity', sa.Integer(), nullable=True),
    sa.Column('description', sa.Text(), nullable=True),
    sa.Column('location_text', sa.String(length=255), nullable=False),
    sa.Column('permission_note', sa.String(length=255), nullable=True),
    sa.Column('image_path', sa.String(length=255), nullable=True),
    sa.Column('status', sa.Enum('ACTIVE', 'COLLECTED', name='resourcespotstatus'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('expires_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('collected_at', sa.DateTime(timezone=True), nullable=True),
    sa.ForeignKeyConstraint(['reporter_id'], ['users.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('resource_spots', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_resource_spots_location_text'), ['location_text'], unique=False)
        batch_op.create_index(batch_op.f('ix_resource_spots_material'), ['material'], unique=False)
        batch_op.create_index(batch_op.f('ix_resource_spots_reporter_id'), ['reporter_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_resource_spots_status'), ['status'], unique=False)

    op.create_table('trades',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('owner_id', sa.String(length=36), nullable=False),
    sa.Column('item_name', sa.String(length=120), nullable=False),
    sa.Column('category', sa.String(length=50), nullable=False),
    sa.Column('material', sa.String(length=120), nullable=False),
    sa.Column('description', sa.Text(), nullable=True),
    sa.Column('weight_kg', sa.Numeric(precision=8, scale=2), nullable=True),
    sa.Column('quantity', sa.Integer(), nullable=False),
    sa.Column('location_text', sa.String(length=255), nullable=False),
    sa.Column('pickup_location_text', sa.String(length=255), nullable=True),
    sa.Column('trading_for_type', sa.Enum('SPECIFIC', 'NOTHING', 'NEGOTIATING', name='tradingfortype'), nullable=False),
    sa.Column('trading_for_value', sa.String(length=255), nullable=True),
    sa.Column('image_path', sa.String(length=255), nullable=True),
    sa.Column('status', sa.Enum('OPEN', 'RESERVED', 'COMPLETED', name='tradestatus'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('updated_at', sa.DateTime(timezone=True), nullable=False),
    sa.ForeignKeyConstraint(['owner_id'], ['users.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('trades', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_trades_category'), ['category'], unique=False)
        batch_op.create_index(batch_op.f('ix_trades_location_text'), ['location_text'], unique=False)
        batch_op.create_index(batch_op.f('ix_trades_owner_id'), ['owner_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_trades_status'), ['status'], unique=False)

    op.create_table('offers',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('trade_id', sa.String(length=36), nullable=False),
    sa.Column('offerer_id', sa.String(length=36), nullable=False),
    sa.Column('item_name', sa.String(length=120), nullable=False),
    sa.Column('category', sa.String(length=50), nullable=False),
    sa.Column('material', sa.String(length=120), nullable=False),
    sa.Column('weight_kg', sa.Numeric(precision=8, scale=2), nullable=True),
    sa.Column('description', sa.Text(), nullable=True),
    sa.Column('image_path', sa.String(length=255), nullable=True),
    sa.Column('status', sa.Enum('PENDING', 'ACCEPTED', 'DECLINED', name='offerstatus'), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('decided_at', sa.DateTime(timezone=True), nullable=True),
    sa.ForeignKeyConstraint(['offerer_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['trade_id'], ['trades.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('offers', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_offers_offerer_id'), ['offerer_id'], unique=False)
        batch_op.create_index('ix_offers_one_pending_per_trade', ['trade_id'], unique=True, postgresql_where=sa.text("status = 'PENDING'"), sqlite_where=sa.text("status = 'PENDING'"))
        batch_op.create_index(batch_op.f('ix_offers_status'), ['status'], unique=False)
        batch_op.create_index(batch_op.f('ix_offers_trade_id'), ['trade_id'], unique=False)

    op.create_table('ratings',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('trade_id', sa.String(length=36), nullable=False),
    sa.Column('rater_id', sa.String(length=36), nullable=False),
    sa.Column('rated_id', sa.String(length=36), nullable=False),
    sa.Column('score', sa.Integer(), nullable=False),
    sa.Column('comment', sa.String(length=500), nullable=True),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.CheckConstraint('score >= 1 AND score <= 5', name='ck_rating_score_range'),
    sa.ForeignKeyConstraint(['rated_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['rater_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['trade_id'], ['trades.id'], ),
    sa.PrimaryKeyConstraint('id'),
    sa.UniqueConstraint('trade_id', 'rater_id', name='uq_rating_trade_rater')
    )
    with op.batch_alter_table('ratings', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_ratings_rated_id'), ['rated_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_ratings_rater_id'), ['rater_id'], unique=False)
        batch_op.create_index(batch_op.f('ix_ratings_trade_id'), ['trade_id'], unique=False)

    op.create_table('notifications',
    sa.Column('id', sa.String(length=36), nullable=False),
    sa.Column('recipient_id', sa.String(length=36), nullable=False),
    sa.Column('type', sa.Enum('OFFER_RECEIVED', 'OFFER_ACCEPTED', 'OFFER_DECLINED', name='notificationtype'), nullable=False),
    sa.Column('title', sa.String(length=120), nullable=False),
    sa.Column('body', sa.String(length=255), nullable=False),
    sa.Column('related_trade_id', sa.String(length=36), nullable=True),
    sa.Column('related_offer_id', sa.String(length=36), nullable=True),
    sa.Column('is_read', sa.Boolean(), nullable=False),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.ForeignKeyConstraint(['recipient_id'], ['users.id'], ),
    sa.ForeignKeyConstraint(['related_offer_id'], ['offers.id'], ),
    sa.ForeignKeyConstraint(['related_trade_id'], ['trades.id'], ),
    sa.PrimaryKeyConstraint('id')
    )
    with op.batch_alter_table('notifications', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_notifications_is_read'), ['is_read'], unique=False)
        batch_op.create_index(batch_op.f('ix_notifications_recipient_id'), ['recipient_id'], unique=False)

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('notifications', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_notifications_recipient_id'))
        batch_op.drop_index(batch_op.f('ix_notifications_is_read'))

    op.drop_table('notifications')
    with op.batch_alter_table('ratings', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_ratings_trade_id'))
        batch_op.drop_index(batch_op.f('ix_ratings_rater_id'))
        batch_op.drop_index(batch_op.f('ix_ratings_rated_id'))

    op.drop_table('ratings')
    with op.batch_alter_table('offers', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_offers_trade_id'))
        batch_op.drop_index(batch_op.f('ix_offers_status'))
        batch_op.drop_index('ix_offers_one_pending_per_trade', postgresql_where=sa.text("status = 'PENDING'"), sqlite_where=sa.text("status = 'PENDING'"))
        batch_op.drop_index(batch_op.f('ix_offers_offerer_id'))

    op.drop_table('offers')
    with op.batch_alter_table('trades', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_trades_status'))
        batch_op.drop_index(batch_op.f('ix_trades_owner_id'))
        batch_op.drop_index(batch_op.f('ix_trades_location_text'))
        batch_op.drop_index(batch_op.f('ix_trades_category'))

    op.drop_table('trades')
    with op.batch_alter_table('resource_spots', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_resource_spots_status'))
        batch_op.drop_index(batch_op.f('ix_resource_spots_reporter_id'))
        batch_op.drop_index(batch_op.f('ix_resource_spots_material'))
        batch_op.drop_index(batch_op.f('ix_resource_spots_location_text'))

    op.drop_table('resource_spots')
    with op.batch_alter_table('users', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_users_username'))
        batch_op.drop_index(batch_op.f('ix_users_email'))

    op.drop_table('users')
    # ### end Alembic commands ###
</file>

<file path="frontend/src/features/profile/screens/EditProfileScreen.jsx">
import { useState, useRef } from 'react';
import { useNavigate } from 'react-router-dom';
import { LogOut, Camera, Star, X } from 'lucide-react';
import Header from '@/shared/components/layout/Header';
import PrimaryButton from '@/shared/components/common/PrimaryButton';
import PasswordInput from '@/shared/components/common/PasswordInput';
import { useAuth } from '@/features/auth/AuthContext';

const inputClass =
  'w-full px-4 py-3.5 rounded-xl border border-gray-200 focus:border-brand-500 focus:outline-none';
const labelClass = 'text-sm font-medium text-gray-700 mb-1 block';

export default function EditProfileScreen() {
  const navigate = useNavigate();
  const { user, updateUser, logout } = useAuth();
  const fileInputRef = useRef(null);

  const [firstName, setFirstName] = useState(user?.firstName || '');
  const [lastName, setLastName] = useState(user?.lastName || '');
  const [username, setUsername] = useState(user?.username || '');
  const email = user?.email || '';
  const [currentPassword, setCurrentPassword] = useState('');
  const [password, setPassword] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [imageFile, setImageFile] = useState(null);
  const [imagePreview, setImagePreview] = useState(user?.profileImage || null);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');

  // 'idle' | 'cooldown-warning' | 'confirm-password'
  const [step, setStep] = useState('idle');
  const [warningType, setWarningType] = useState(null); // 'info' | 'password'
  const [confirmPasswordInput, setConfirmPasswordInput] = useState('');
  const [confirmError, setConfirmError] = useState('');

  const changingPassword = currentPassword.length > 0 || password.length > 0 || confirmPassword.length > 0;

  const infoChanged =
    firstName.trim() !== (user?.firstName || '') ||
    lastName.trim() !== (user?.lastName || '') ||
    username.trim() !== (user?.username || '');

  const imageChanged = imageFile !== null;
  const hasChanges = infoChanged || imageChanged || changingPassword;

  const infoValid =
    firstName.trim().length > 0 &&
    lastName.trim().length > 0 &&
    username.trim().length > 2;

  const passwordValid =
    !changingPassword ||
    (currentPassword.length > 0 && password.length >= 6 && password === confirmPassword);

  const canSave = hasChanges && infoValid && passwordValid;

  const initial = firstName?.[0]?.toUpperCase() || username?.[0]?.toUpperCase() || '?';

  const handlePickImage = () => fileInputRef.current?.click();

  const handleImageChange = (e) => {
    const file = e.target.files?.[0];
    if (!file) return;
    setImageFile(file);
    setImagePreview(URL.createObjectURL(file));
  };

  const buildUpdates = () => {
    const updates = {
      firstName: firstName.trim(),
      lastName: lastName.trim(),
      username: username.trim(),
    };
    if (changingPassword) updates.password = password;
    if (imageFile) updates.imageFile = imageFile;
    return updates;
  };

  const submit = async (currentPasswordForRequest) => {
    setSaving(true);
    setError('');
    try {
      await updateUser(buildUpdates(), currentPasswordForRequest || undefined);
      navigate(-1);
    } catch (err) {
      setError(err.message || 'Something went wrong. Please try again.');
      setStep('idle');
    } finally {
      setSaving(false);
    }
  };

  const handleSave = async () => {
    if (!canSave || saving) return;

    if (changingPassword) {
      setWarningType('password');
      setStep('cooldown-warning');
      return;
    }

    if (infoChanged) {
      setWarningType('info');
      setStep('cooldown-warning');
      return;
    }

    // Image-only change: no cooldown, no password prompt.
    await submit();
  };

  const handleConfirmCooldown = async () => {
    if (warningType === 'password') {
      // Current password was already entered inline — proceed straight to save.
      setStep('idle');
      await submit(currentPassword);
      return;
    }
    setStep('confirm-password');
  };

  const handleConfirmPassword = async () => {
    if (!confirmPasswordInput) {
      setConfirmError('Please enter your current password.');
      return;
    }
    setConfirmError('');
    await submit(confirmPasswordInput);
    setConfirmPasswordInput('');
  };

  const closeModal = () => {
    setStep('idle');
    setWarningType(null);
    setConfirmPasswordInput('');
    setConfirmError('');
  };

  const handleLogout = async () => {
    await logout();
    navigate('/signin', { replace: true });
  };

  return (
    <div className="pb-10">
      <Header onBack={() => navigate(-1)} title="Edit Profile" />

      <div className="p-5 space-y-4">
        {/* Avatar */}
        <div className="flex flex-col items-center gap-2 py-2">
          <button
            onClick={handlePickImage}
            className="relative w-24 h-24 rounded-full active:scale-95 transition-transform"
            aria-label="Change profile photo"
          >
            {imagePreview ? (
              <img src={imagePreview} alt="Profile" className="w-24 h-24 rounded-full object-cover" />
            ) : (
              <div className="w-24 h-24 rounded-full bg-brand-600 text-white text-3xl font-semibold flex items-center justify-center">
                {initial}
              </div>
            )}
            <span className="absolute bottom-0 right-0 w-8 h-8 rounded-full bg-gray-900 text-white flex items-center justify-center border-2 border-white">
              <Camera size={15} />
            </span>
          </button>
          <input
            ref={fileInputRef}
            type="file"
            accept="image/*"
            onChange={handleImageChange}
            className="hidden"
          />

          {typeof user?.rating === 'number' && (
            <div className="flex items-center gap-1 text-sm text-gray-600 mt-1">
              <Star size={15} className="fill-yellow-400 text-yellow-400" />
              <span className="font-semibold">{user.rating.toFixed(1)}</span>
              <span className="text-gray-400">rating</span>
            </div>
          )}
        </div>

        <fieldset className="space-y-4">
          <div className="grid grid-cols-2 gap-3">
            <div>
              <label className={labelClass}>First name</label>
              <input className={inputClass} value={firstName} onChange={(e) => setFirstName(e.target.value)} />
            </div>
            <div>
              <label className={labelClass}>Last name</label>
              <input className={inputClass} value={lastName} onChange={(e) => setLastName(e.target.value)} />
            </div>
          </div>

          <div>
            <label className={labelClass}>Username</label>
            <input className={inputClass} value={username} onChange={(e) => setUsername(e.target.value)} />
          </div>

          <div>
            <label className={labelClass}>Email</label>
            <div className="w-full px-4 py-3.5 rounded-xl border border-gray-100 bg-gray-50 text-gray-500">
              {email}
            </div>
          </div>

          <div className="border-t border-gray-100 pt-4 space-y-4">
            <p className="text-sm font-semibold text-gray-900">Change Password</p>

            <div>
              <label className={labelClass}>Current password</label>
              <PasswordInput
                value={currentPassword}
                onChange={(e) => setCurrentPassword(e.target.value)}
                placeholder="Required to set a new password"
              />
            </div>

            <div>
              <label className={labelClass}>New password</label>
              <PasswordInput
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="At least 6 characters"
              />
            </div>

            <div>
              <label className={labelClass}>Confirm new password</label>
              <PasswordInput
                value={confirmPassword}
                onChange={(e) => setConfirmPassword(e.target.value)}
                placeholder="Re-enter your new password"
              />
              {confirmPassword.length > 0 && password !== confirmPassword && (
                <p className="text-xs text-red-500 mt-1">Passwords don&apos;t match.</p>
              )}
            </div>
          </div>
        </fieldset>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <PrimaryButton onClick={handleSave} disabled={!canSave || saving}>
          {saving ? 'Saving...' : 'Save Changes'}
        </PrimaryButton>

        <button
          onClick={handleLogout}
          className="w-full flex items-center justify-center gap-2 py-3.5 rounded-xl bg-red-500 text-white font-semibold active:scale-[0.98] transition-transform hover:bg-red-600"
        >
          <LogOut size={16} />
          Log Out
        </button>
      </div>

      {step === 'cooldown-warning' && (
        <div className="fixed inset-0 bg-black/40 flex items-end sm:items-center justify-center z-50 p-4">
          <div className="bg-white rounded-2xl w-full max-w-sm p-5 space-y-4">
            <div className="flex items-start justify-between">
              <h3 className="text-lg font-semibold text-gray-900">Heads up</h3>
              <button onClick={closeModal} className="text-gray-400 hover:text-gray-600">
                <X size={20} />
              </button>
            </div>
            <p className="text-sm text-gray-600">
              {warningType === 'password'
                ? "Once you save, you won't be able to change your password again for 7 days. Do you want to continue?"
                : "Once you save these changes, you won't be able to update your profile info again for 7 days. Do you want to continue?"}
            </p>
            <div className="flex gap-3">
              <button
                onClick={closeModal}
                className="flex-1 py-3 rounded-xl border border-gray-200 font-semibold text-gray-700"
              >
                Back
              </button>
              <button
                onClick={handleConfirmCooldown}
                disabled={saving}
                className="flex-1 py-3 rounded-xl bg-brand-600 text-white font-semibold disabled:opacity-50"
              >
                {saving ? 'Saving...' : 'Confirm'}
              </button>
            </div>
          </div>
        </div>
      )}

      {step === 'confirm-password' && (
        <div className="fixed inset-0 bg-black/40 flex items-end sm:items-center justify-center z-50 p-4">
          <div className="bg-white rounded-2xl w-full max-w-sm p-5 space-y-4">
            <div className="flex items-start justify-between">
              <h3 className="text-lg font-semibold text-gray-900">Confirm it&apos;s you</h3>
              <button onClick={closeModal} className="text-gray-400 hover:text-gray-600">
                <X size={20} />
              </button>
            </div>
            <div>
              <label className={labelClass}>Current password</label>
              <PasswordInput
                value={confirmPasswordInput}
                onChange={(e) => setConfirmPasswordInput(e.target.value)}
                placeholder="Enter your current password"
              />
              {confirmError && <p className="text-xs text-red-500 mt-1">{confirmError}</p>}
            </div>
            <div className="flex gap-3">
              <button
                onClick={closeModal}
                className="flex-1 py-3 rounded-xl border border-gray-200 font-semibold text-gray-700"
              >
                Back
              </button>
              <button
                onClick={handleConfirmPassword}
                disabled={saving}
                className="flex-1 py-3 rounded-xl bg-brand-600 text-white font-semibold disabled:opacity-50"
              >
                {saving ? 'Saving...' : 'Save'}
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
</file>

<file path="README.md">
# Development Progress

## Phase 1 – System Planning & Architecture

**Status:** Completed

During the initial phase of development, the project focused on establishing RosaCycle's overall vision, system architecture, and business requirements before any implementation began. The team defined the application's primary objective of promoting a circular economy through community-based trading and resource recovery while aligning the platform with Sustainable Development Goal 12 (Responsible Consumption and Production).

Major discussions during this phase centered on refining the trading workflow, defining the business rules for offers and trades, designing the Resource Spot reporting system, planning the user entity, and determining the overall backend architecture. The repository was also structured into separate frontend and backend applications within a single monorepo to simplify collaboration while maintaining clear separation of concerns. The guiding principle throughout this phase was to fully understand the business problem before writing code, ensuring that implementation decisions were driven by well-defined requirements rather than assumptions.

---

## Phase 2 – Deployment & Infrastructure

**Status:** Completed

The third phase established the project's cloud infrastructure and deployment pipeline using Railway. Independent services were configured for the frontend application, backend API, and managed PostgreSQL database, allowing each component to be deployed and maintained separately.

The backend was successfully connected to the Railway PostgreSQL service using environment variables, and production database migrations were executed to generate the application's database schema. The React frontend, built with Vite, was deployed as a separate Railway service and verified to be publicly accessible. By the end of this phase, both the frontend and backend were successfully deployed in the cloud, providing a stable staging environment for future frontend-backend integration, feature development, and collaborative testing.

---

## Phase 3 – Frontend–Backend Integration

**Status:** In Progress

The current phase focuses on integrating the React frontend with the Flask REST API while ensuring both applications communicate reliably in both local and cloud environments. Prior to feature integration, the deployment infrastructure was established by deploying the frontend, backend, and PostgreSQL database as separate Railway services. Environment variables, database connectivity, and production migrations were configured and verified to provide a stable development and testing environment.

With the infrastructure in place, development has shifted toward replacing frontend mock data with live API endpoints. Integration will be performed incrementally, beginning with the trading module, which serves as the application's core functionality. Each frontend feature will be connected to its corresponding backend endpoint, tested independently, and validated before proceeding to the next component. This phased approach minimizes integration issues while ensuring that both the client and server remain synchronized throughout development.

The immediate objectives of this phase include integrating trade browsing, trade details, trade creation, offer management, user authentication, and other supporting services, ultimately transitioning the application from isolated frontend and backend implementations into a fully connected system.
</file>

<file path="backend/app/app.py">
import os

from flask import Flask, current_app, send_from_directory
from flask_cors import CORS
from flask_jwt_extended import JWTManager

from app.config import Config
from app.shared.database import db, migrate
from app.shared.utils.errors import register_error_handlers
from app.shared.utils.rate_limit import limiter


def create_app(config_object=Config):
    app = Flask(__name__, instance_relative_config=True)
    app.config.from_object(config_object)

    db.init_app(app)
    migrate.init_app(app, db)

    with app.app_context():
        from app.shared import models  # noqa: F401  (registers models with SQLAlchemy metadata)
    JWTManager(app)
    CORS(app, origins=app.config["CORS_ORIGINS"], supports_credentials=True)
    limiter.init_app(app)

    register_error_handlers(app)
    _register_blueprints(app)

    os.makedirs(app.config["UPLOAD_ROOT"], exist_ok=True)

    @app.get("/")
    def index():
        return {
            "service": "RosaCycle API",
            "status": "ok",
            "docs": "backend/docs/API.md",
            "health": "/health",
        }

    @app.get("/health")
    def health():
        return {"status": "ok"}

    @app.get("/media/<path:filename>")
    def media(filename):
        return send_from_directory(current_app.config["UPLOAD_ROOT"], filename)

    return app


def _register_blueprints(app):
    from app.auth.routes import auth_bp
    from app.profile.routes import user_bp
    from app.trades.routes import trade_bp
    from app.trades.offer.routes import offer_bp
    from app.map.routes import resource_spot_bp
    from app.shared.notification.routes import notification_bp
    from app.shared.rating.routes import rating_bp
    from app.scan.routes import ai_bp

    app.register_blueprint(auth_bp)
    app.register_blueprint(user_bp)
    app.register_blueprint(trade_bp)
    app.register_blueprint(offer_bp)
    app.register_blueprint(resource_spot_bp)
    app.register_blueprint(notification_bp)
    app.register_blueprint(rating_bp)
    app.register_blueprint(ai_bp)
</file>

</files>
